# === 機密情報（CLAUDE.md §3.1）===
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*_rsa
service-account*.json
gcp-*.json
credentials*.json
secrets/

# === 個人情報 ===
# 会員リスト・実データを含むエクスポートはリポジトリに置かない
会員データ/
会員リスト*
members_*.csv
*_personal.csv
*_dump.sql
*.bak

# === Node / Next.js ===
node_modules/
.next/
out/
build/
dist/
coverage/
*.tsbuildinfo
.eslintcache
# next build/dev が毎回自動生成する型参照。Next 16 では .next/types を import するため
# コミットするとクリーン環境の tsc --noEmit が壊れる（create-next-app 標準も ignore）
next-env.d.ts

# === E2E（Playwright）===
playwright-report/
test-results/

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# === Supabase ===
.supabase/
supabase/.temp/

# === エディタ・OS ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# === Obsidian（このリポジトリはボルト内にあるため）===
.obsidian/
.trash/

# === ログ ===
*.log
npm-debug.log*
yarn-error.log*
