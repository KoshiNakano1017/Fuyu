# gitleaks が「検出したが秘密ではない」と確認済みの指摘を記録する（fingerprint 指定）。
#
# 書式: <commit>:<path>:<ruleID>:<line>
#
# ⚠️ ここへ足すのは「実際に秘密ではないと確認できたもの」だけである。
#    赤を消す目的で秘密を通すと、public リポジトリでは回復不能になる（CLAUDE.md §3）。
#    1行ごとに「なぜ秘密ではないか」を必ず書く。

# DKIM の**公開**鍵（DNS の TXT レコードとして誰でも引ける値）の例示。
# `メール送信基盤_Resend設定手順書.md` の「期待される nslookup 出力」に載っていた。
# 秘密ではないが高エントロピーのため generic-api-key で誤検知する。
# 現行版は実値を置かない書き方へ直したため、履歴上のこの1件だけを無視する。
e6103bf53d7e6c4bfc4649ad6089b09eeaf450ea:docs/operations/メール送信基盤_Resend設定手順書.md:generic-api-key:333
