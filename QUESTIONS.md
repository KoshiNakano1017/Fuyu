# QUESTIONS.md

浮遊街アプリの実装着手前に、プロジェクトオーナーの確認・決定が必要な事項一覧。
優先度順（実装のブロッカーになりやすい順）。自律実装ループ（毎晩の仕様整理タスク）が
参照する前提のドキュメントであり、ステータスが「回答済み」になるまでは、
関連する仕様を確定事項として自律実装に着手しないこと。

出典の詳細な要約は `docs/spec/CONSOLIDATED_DECISIONS.md` を参照。
ブロッカーがどの作業パッケージを止めているかの逆引きは `docs/spec/WBS_Phase1.md` §16 を参照。

## 現況（2026-09-13 時点）

| 区分                         |      件数 |
| -------------------------- | ------: |
| **総件数**                    | **56件** |
| ✅ 回答済み・クローズ                | **37件** |
| 🟡 部分回答（方針のみ／一部論点が未決）      |      4件 |
| ⛔ **論点消滅**（前提が変わり検討不要になった） |  **2件** |
| ⬜ 未回答                      | **13件** |

> [!important] 2026-09-11：ベクトル基盤を **Supabase pgvector へ一本化**（v13 v1.26.0 ／ 決定ログ §17）
> **2026-08-16 の「Firestore へ統合」を限定的に覆す、本プロジェクト最大の構成変更。**
> 反転できたのは、当時 pgvector を捨てた根拠が **無料枠 500MB 制約**であり、
> **2026-09-08 の有料プラン決定で失効していた**ため。
>
> | 影響 | 内容 |
> | --- | --- |
> | ⛔ **論点消滅2件** | **ナレッジ投入方式 A/B/C**（移送が無くなるためキュー不要）／**`find_nearest` 移行**（Firestore を検索基盤に使わない。**3〜4人日が不要化**） |
> | 🔄 **実装形が変更** | 2026-09-10 の PII ガードは**原則（チャネル判定）を維持**しつつ、**単一DB内の RLS／ビュー**として実装する形へ |
> | ➕ **新規作業** | `line-rag-bot` の検索基盤移行（約430行）／本番68件の移行／Cloud Run→Supabase の接続経路（Supavisor・静的IP） |

> [!success] **真のブロッカーは 2026-09-10 に全て解消した（3件 → 0件）**
> 実装着手を止めている仕様上のブロッカーは、現時点で**存在しない**。
>
> | ブロッカー | 決着 |
> | --- | --- |
> | 決済QR・入金QRトークンの保護強化 | ✅ **2026-09-10**：A案（256bit乱数／平文非保存／TTL／単回使用） |
> | **非同期ジョブの実行基盤選定** | ✅ **2026-09-10**：**Cloud Tasks ＋ Cloud Scheduler**（GCP 全面統一・`pg_cron` 不使用） |
> | **公開予約OTPの送信基盤** | ✅ **2026-09-10**：**Resend**（Gmail 外だし案は不採用） |
>
> ⚠️ **ただし「決まった」と「反映された」は別である。** 下記3点は**設計書側が未追随**:
> ① `システムアーキテクチャ.md` の非同期ジョブ3レーン案（`pg_cron` 前提のまま）
> ② `非機能要件詳細.md` §7-4 の受容済みリスク根拠（#61 の決定で前提が変わった）
> ③ `API設計.md` §2-2b のメール送信経路（Resend の配線先が2系統ある点）

> [!note] 2026-09-10 のオーナー決定4件
> ①**非同期ジョブ＝Cloud Tasks ＋ Cloud Scheduler**（`pg_cron` を捨て GCP へ一本化）。
> ②**公開予約OTP＝Resend**。**起票時の前提（Supabase 既定SMTP がブロック）が誤りだったことも判明**し、
> 送信経路が**自前実装とSupabase Authの2系統**に分かれることを記録した。
> ③**ナレッジ投入のPIIガード＝出力導線での channel ガード**（投入は止めず、LINE 経路で無条件除外）。
> ④**親方兼街人の表示＝街人番号**（提案していた「親方優先」は不採用）。

> [!note] 2026-09-08 のオーナー決定3件で、未決が2件減った
> ①**Supabase は有料プランで運用する** → 「無料枠で満たせない非機能要件の扱い」が解消し、**真のブロッカーは4件→3件**。
> ②**自律開発ループは「移行」として扱わず、既存の実装にタスクを流す** → 同日起票の論点をクローズ。
> ③**Phase 1 は 61パッケージ全部をリリースする** → 「何をリリースするのか」が確定し、**完了判定が可能になった**。

> [!note] 2026-09-05 に「ロール別レスポンスマスキング」が解決した
> A案（PII別テーブル分離）の採用により、**WBS `2-2`・`2-3`・`2-4`・`8-1`・`8-4`・`10-3` の6件が一斉にブロック解除**された。
> ただし同日に新しいブロッカーが2件起票されているため、**ブロッカーの総数は減っていない**。

---

## ✅ [2026-09-26] ダッシュボードの「Uii流通量（会計換算）」の定義（v13 §13 ／ WBS `13-1`）
- ステータス: **回答済み（2026-09-26 オーナー確定＝累計）**。全期間の会計額を Uii 換算した**累計**を出す。
  残高合計ではない（未使用の滞留量は別の話）。⚠️ 同じカードの並びは「今日の実数」なので、
  **カード上で累計であることを明示する**（v13 §9 #69 ／ 決定ログ §25-2）
- 旧ステータス: 未回答
## [2026-09-26] 宿泊券はどの夜に充当されるか（単価が夜ごとに違う場合）（v13 §5.2.4 ／ WBS `3-7`）
- ステータス: 未回答
- 優先度: 中（**`3-7` をブロックしない**。充当の受付・記録・表示は実装済みで、
  「差引後の金額をいくらと表示するか」だけが未確定である）
- 背景: v13 §5.2.4 は「会員は保有宿泊券を充当できる。**予約時点では消費せず**、チェックアウト時に消費する」と
  定めるが、**どの夜に充てるか**は書かれていない。宿泊券は「1泊」の券であって金額が紐づいていないため、
  夜ごとの単価が同じなら順序は結果に影響しないが、次の2つの場面では**同じ1枚でも支払額が変わる**：
  1. **料金改定をまたぐ滞在**（`accommodation_rates` は適用期間付き ／ v13 §5.4.2②）
  2. **滞在中に宿泊形態が変わる滞在**（v13 §5.6.9 ／ WBS `3-10`。泊単位でその夜の形態の単価を積む）
- 選択肢:
  - A. **高い夜から充てる**（利用者に有利。券1枚の価値が最大になる）
  - B. **早い夜から充てる**（実装と説明が単純。前半が安いと券の価値が下がる）
  - C. **運営が夜を選ぶ**（現場の判断に委ねる。操作が増え、選ばなかった場合の既定が別に要る）
- 推奨: **A**（理由: ①券は「宿泊1泊と引き換えられる権利」であり、安い夜へ自動的に充てられると
  利用者から見て価値が黙って減る。②「高い夜から」は現場でも説明しやすい。
  ③B との差が出るのは改定をまたぐ滞在だけで、件数は少ない）
- 現在の実装（2026-09-26 ／ `src/lib/lodging/reservation-intake.ts` の `quoteReservation()`）:
  **単価が一律の夜だけで構成される滞在のときに限って差引後の金額を表示**し、
  ばらついている場合は金額を出さずに「運営が確定します」と出す（`payableYen` が `null`）。
  **順序をここで決めていない** — 金額の仕様判断にあたるため（CLAUDE.md §7.0「必ず先に聞く」）。
- 関連: 消費そのものの計上先は [2026-09-26]「宿泊券の残高不足分をどこへ計上するか」（WBS `3-4` ／ Issue #187）と
  同じ実装点に効く。両方まとめて決めると `3-4` の実装が一度で終わる
- 関連ファイル: v13 §5.2.4、`src/lib/lodging/reservation-intake.ts`、
  `supabase/migrations/0042_check_ins_stay_ticket_application.sql`、`tests/reservation-intake.test.ts`

## [2026-09-26] 宿泊券の残高不足分をどこへ計上するか（v13 §5.5 L1096 ／ 決定ログ §23-2 ／ WBS `3-4` ／ Issue #187）
- ステータス: 未回答
- 優先度: 高（**WBS `3-4`（宿泊券管理・消費/付与ロジック）の実装をブロックする。** 宿泊券は金銭的価値を持つ権利であり、
  計上先を誤ると請求漏れが残高の裏に隠れる）
- 背景: Issue #187（WBS `3-4` 宿泊券管理・消費/付与ロジック）の起票時に、起票支援エージェントが同じ論点を
  A/B/C で提示したが（2026-09-26 07:23 のコメント）、**回答が無いまま `auto` が付与された**ため PM 段階で再提示する。
  決定ログ §23-2（宿泊券の消費は「どの滞在でも1枚＝1泊」）の「実装時の扱い ②」は
  **「足りる分だけ消費し、残りは通常の未会計（円）として扱う」**と定めており、
  同節は「**この2点が意図と違う場合は差し戻しが要る**」とも書いている。
  しかし **宿泊費を未会計として計上する器が Phase 1 の実装に存在しない**：
  1. v13 §5.5 L1096 は「未会計Uii ＝ 未精算伝票のUii ＋ **宿泊費等の請求項目のUii**」と定めている
  2. 一方 `orders`（`0019_orders_and_settlement.sql` L46-48）の `order_source` は
     **`cafe` / `shop` の2値のみ**で、宿泊費を載せる伝票が作れない
  3. 宿泊費の算定・表示は WBS `8-6`（宿泊履歴〔泊まった部屋〕の表示）・`3-9`（宿泊料金マスタ）・
     `3-10`（滞在中の宿泊形態・部屋・日程の変更）側に残作業として積まれたままである
  Issue #187 の本文は本件を**スコープ外**（＝下記 A）と宣言しているが、これは決定ログ §23-2 の文面と
  食い違うため、PM の側で確定扱いにはできない。
- 選択肢:
  - A. **`3-4` は `consume` の自動計上までとする。** 不足分は「宿泊券で賄えなかった泊数 ＝ 通常どおり
    宿泊費の請求対象として残る」と読み、宿泊券側では何もしない。未会計への計上は `8-6`／`3-9`／`3-10` が
    入る時点で一緒に作る（Issue #187 本文のスコープ外宣言はこの案に相当する）
  - B. **`3-4` で宿泊費の未会計計上まで作る**（`orders.order_source` へ `lodging` を足す等）。
    決定ログ §23-2 の文面に最も忠実だが、`3-9`・`3-10`・`8-6`・`8-2` へ波及し想定サイズが **M → L**
  - C. **退館時に「宿泊券が◯泊不足しています」と運営へ表示するだけ**にして記録は作らない
- 推奨: **A**（理由: ①`3-4` の本体である「チェックアウト時の自動消費」は A/B/C のどれでも同一の実装になり、
  A なら先へ進められる。②宿泊費の金額を決める `3-9`（宿泊料金マスタ）が未実装であり、B を採っても
  **計上すべき金額が確定しない**。③C は請求漏れの検知を人の記憶に依存させるため、A より弱い）
- ⚠️ **本件は高リスク（金額・宿泊券の計算）であり、推測で進めない**（CLAUDE.md §7.0「必ず先に聞く」）。
  A を採る場合も、「不足泊数がどこにも記録されない期間が `8-6`／`3-10` の実装まで続く」ことの
  受け入れがオーナー判断である。
- 関連ファイル: v13 §5.5 L1096、`docs/spec/CONSOLIDATED_DECISIONS.md` §23-2、
  `supabase/migrations/0019_orders_and_settlement.sql`、`src/app/staff/checkins/actions.ts`、
  `src/lib/lodging/stay-tickets.ts`、`docs/spec/WBS_Phase1.md` `3-4`・`3-9`・`8-6`、Issue #187

## [2026-09-26] ダッシュボードの「Uii流通量（会計換算）」の定義（v13 §13 ／ WBS `13-1`）
- ステータス: 未回答
- 優先度: 低（**`13-1`（「今日の浮遊街サマリー」ダッシュボード）の本体は完了済み。** このカード1枚が出ないだけで運用は回る）
- 経緯: モック `prototype_v15.html` ⑫今日のサマリーに「Uii流通量（会計換算）」の KPI カードがあるが、
  正本 v13 に**この指標の定義が無い**。実装できる数え方が複数あり、どれも意味が違う。
- 選びうる定義:
  | 案 | 数え方 | 何が読めるか |
  | --- | --- | --- |
  | **A（推奨）** | **本日の注文・宿泊の会計額を Uii 換算した合計** | 「今日その場でいくら動いたか」。他のカード（未会計・本日の滞在）と同じ「今日」の軸に乗る |
  | B | `eumo_grants` の給付済み Uii の累計 | 運営が配った総量。今日の動きではない |
  | C | 会員の Uii 残高の合計 | 未使用の滞留量。会計とは別の話 |
- 推奨: **A**。ダッシュボードは「今日の実数」を並べる画面であり（§13 ／ 実装の設計方針）、
  累計・残高は軸が違う。A であれば `sumUnsettled()` と同じ材料から出せて、
  **未会計カードとの数字の整合も読み手が確かめられる**。
- ⚠️ ただし「流通量」という語からは B・C も自然に読めるため、**指標名の意味をオーナーに確定させたい**。
  金額に関わる表示であり、推測で入れると「運営が見ている数字の意味が実装ごとに違う」状態を作る
  （CLAUDE.md §7.0 の「認可・金額・個人情報の**仕様判断**は必ず先に聞く」に該当）。
- 併記（こちらは論点にしない）:
  - モック ⑫ の「**AI相談利用率**」は `line-rag-bot` 側の利用ログを要する。アプリは同プロジェクトへ
    API 連携しない方針が維持されている（v13 §9 #31 ／ #56 で連携は **Phase 2 以降**）ため、**Phase 1 では出せない**。
  - モック ⑫ の「**🐝 IoT環境監視（蜂の巣キット）**」はモック自身が **Phase 3 予定**と明示している。

## [2026-09-25] WBS `1-6`（pgvector有効化・RAG基盤）は決定 A 反映後に何が残っているか（Issue #173 ／ `CONSOLIDATED_DECISIONS.md` §17-6）
- ステータス: 未回答
- 優先度: 高（**Issue #173（WBS `1-6` pgvector有効化・RAG基盤）のタスク定義を止めている。** `9-1`（アプリ内の横断セマンティック検索）は `1-6` 依存のため連鎖する）
- 背景: Issue #173 は `wbs-to-issue.yml` による自動起票で、**完了条件が雛形のプレースホルダのまま**である。
  加えて次の2点が判明した。
  - **同じ WBS `1-6` の Issue #89 は 2026-09-25 にクローズ済み**である。`WBS_Phase1.md` の `1-6` 行は
    「DB 層を閉じた（2026-09-25 ／ Issue #89 ／ 決定 A）」と記録しており、`0102_rag_search_scope_and_pii.sql`
    （`rag.search_knowledge()` ／ `rag.redact_contact_info()` ／ `rag.scan_for_index()`）と
    `tests/db/rag-search-scope.test.ts` は main に入っている。同行が挙げる残作業は
    **「投影（各マスタ → `knowledge_chunks`）の実装」1点のみで、それは `9-1` の範囲だと明記されている**
  - それでも `1-6` 行は実装進捗 **95%**・ステータス 🟨 のままであり、スイープが再度払い出した。
    **残る 5% が何を指すのかは、WBS・正本・決定ログのどこにも書かれていない**
- 根拠として引かれた節が実在しない: Issue 本文の「根拠となる仕様」は **`v13 §16`** だが、
  **正本 v13 は §9（検討・未決事項）までで §16 は存在しない**。`§16` は `WBS_Phase1.md` の
  「QUESTIONS.md ブロッカー逆引き表」の節番号であり、正本ではない。本パッケージの仕様上の根拠は
  **v13 §9 #31（v1.26.0 限定改訂）と `CONSOLIDATED_DECISIONS.md` §17** である（起票3条件①が不成立）
- なぜ導出で進めないか: 残作業の候補（投影・`line-rag-bot` の検索基盤移行・本番ナレッジ68件の移行）は
  いずれも**個人情報を含むテキストのベクトル化とその出力制御**に直結する（§17-3・§17-5）。
  完了条件を PM エージェントが導出すると、それが誰の承認も経ずに
  「どこまでマスキングすれば合格か」の受入基準になる（設計 §10.1.3 の条件3・高リスク時は停止）
- 選択肢:
  - **A. Issue #173 を重複としてクローズし、`WBS_Phase1.md` の `1-6` を 100%・✅ にする。**
    決定 A（2026-09-25）で `1-6` は DB 層に閉じると定めており、その DB 層は `0102` で実装・テスト済みである。
    投影は `9-1` の Issue で扱う
  - B. Issue #173 の範囲を **`line-rag-bot` の検索基盤移行（Firestore → Supabase ／ §17-6 #1・#3）**と定義する。
    ただし対象コードは別リポジトリであり、本リポジトリの自律ループでは実装できない
  - C. Issue #173 の範囲を **投影（各マスタ → `knowledge_chunks`）の実装**と定義し、`9-1` から `1-6` へ移す
- 推奨: **A**（理由: 決定 A は `1-6`＝DB 層・`9-1`＝その上のアプリ側と切り分けており、`0102` でその DB 層は
  受入テスト付きで閉じている。残 5% の実体が誰にも書けない状態で Issue を開け続けると、
  `9-1` の着手も「`1-6` 未完」を理由に止まり続ける。B は別リポジトリのため本ループの対象外、
  C は決定 A の切り分けを 1日で覆すことになる）
- 関連ファイル: `docs/spec/WBS_Phase1.md` `1-6`（L485）・`9-1`（L599）、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §17-5・§17-6、
  `supabase/migrations/0102_rag_search_scope_and_pii.sql`、`tests/db/rag-search-scope.test.ts`、
  QUESTIONS.md「[2026-09-20] WBS `1-6`（pgvector有効化・RAG基盤）の残作業の範囲と、`9-1`…との切れ目」、
  Issue #173、Issue #89（クローズ済み）

## ⚡ [2026-09-25] 募集人数の列名が派生文書どうしで食い違っている（`recruit_count` vs `required_people`）（v13 §5.3 note ／ WBS `5-2` ／ Issue #167）
- ステータス: 未回答（実装は実在する物理列で進行中・ブロッカーではない）
- 優先度: 低〜中（派生文書を放置すると、次に募集人数へ触る実装が存在しない列名で設計を読み、実装前に手戻りする）
- 背景: Issue #167（WBS `5-2` 受注申請・運営審査・実行指示）の段取り作成時に判明。
  完了条件 A8（募集人数に達したクエストへは受注申請できない）の実装対象を特定する過程で、
  **正本 v13 には列名の記載自体が無く**、§5.3 note が「1クエスト＝運営が指定した**募集人数の範囲で**受注可」と
  日本語でのみ規定していることを確認した。列名を定めているのは派生文書2本で、値が食い違う。
  - `docs/spec/detailed-design/DB物理設計.md` L129 ／ `supabase/migrations/0007_quests_schema.sql` L73: **`recruit_count`**
  - `docs/spec/データモデル図_ER_Diagram.md` L181・L604: **`required_people`「募集人数」**
- 採用した内容: 実在する物理列 **`recruit_count`**（`0007` L73・`0012` L78 で `authenticated` に GRANT 済み）で実装を進行。
  正本に列名の記述が無いため §1.1「矛盾時は正本が勝つ」で自動解決する種類ではないが、
  **DB に実在する列は1つだけ**であり、実装対象の特定には曖昧さが無い。
- 派生文書側の訂正要否: `docs/spec/データモデル図_ER_Diagram.md` の `required_people` を
  `recruit_count` へ揃えるか。オーナー判断待ち（`docs/spec/` 配下のため自律ループからは編集しない）。
- 関連ファイル: v13 §5.3 note（L844）、`docs/spec/detailed-design/DB物理設計.md`、
  `docs/spec/データモデル図_ER_Diagram.md`、`supabase/migrations/0007_quests_schema.sql`、Issue #167

## ⚡ [2026-09-25] 募集枠を占有する受注申請ステータスの定義（とくに `申請中` と `差戻し`）（v13 §5.3 note L844・§5.3 項目3 L838・§5.3.2・§6 L2307 ／ WBS `5-2` ／ Issue #167）
- ステータス: **未回答（ブロッカー。PR #171 を `auto:blocked` で停止した）**
- 優先度: **高**（`申請中` を枠に数えるか否かで、**運営が複数の申請者から選べるかどうかが決まる**。
  `recruit_count` の既定値は 1（`0007_quests_schema.sql` L73-74）なので、`申請中` を占有に数えると
  **最初の1人が申請した時点で他の誰も申請できない＝早い者勝ち**になる）
- 背景（`申請中` ／ PR #171 レビュー 5a の指摘）: v13 §5.3 項目3 L838「運営側が『いつ・どこで・何を
  任せるか』の指示を出して**承認（マッチング成立）**」・§6 権限マトリクス L2307「クエスト審査・
  実行指示出し（**申請中処理**）」は、**複数の `申請中` が並んでいて運営がそこから選ぶ**運用を前提に読める。
  一方 `0043_quest_recruit_capacity.sql` の現実装は `申請中` を占有として数えるため、
  既定の `recruit_count = 1` では審査の余地が生じない。どちらが仕様かは正本に明文が無い。
  **これは「誰が受注申請できるか」という振る舞いの決定であり、自律ループの権限外**（設計 §7.1）。
- 決めてほしいこと（`申請中`）: 募集枠は **(a) 申請の時点で埋まる（早い者勝ち）**か、
  **(b) 運営が `指示済み` にした時点で埋まる（`申請中` は何人でも並べる）**か。
  (b) を採る場合、**既存の受入テスト `募集1名のクエストへ2人目は受注申請できない`
  （`tests/db/quest-application-capacity.test.ts`）の期待値が仕様と食い違う**ため、
  テスト側の修正も伴う（受入テストの書き換えはループの権限外 ／ 設計 §0・§11.6）。
- 背景（`差戻し`）: Issue #167（WBS `5-2` 受注申請・運営審査・実行指示）の PR #171 レビューで判明。
  v13 §5.3 note L844 は「1クエスト＝運営が指定した**募集人数の範囲で**受注可」と定めるが、
  **どのステータスの受注申請が枠を占有するかは正本にも `CONSOLIDATED_DECISIONS.md` にも記述が無い。**
  `quest_applications.status` は `申請中` / `指示済み` / `承認` / `差戻し` / `完了` / `キャンセル` の6値
  （`0017_quest_applications_and_work_logs.sql` L54-56）。
- 採用した内容（暫定）: **`キャンセル` だけを枠から外し、`差戻し` を含む残り5値を占有として数える**
  （`0043_quest_recruit_capacity.sql` の `quest_application_occupies_slot()`）。根拠は2点。
  - v13 §5.3.2 の遷移図では `差戻し` の次は**再提出**であり、受注は成立したままである。
    `0017` L64-65 も「差戻し後の再提出は `work_logs` を積み直すのであって、申請行を増やさない」と書いている
  - `差戻し` を枠から外すと、`recruit_count = 1` のクエストで差戻し中の受注者がいる間に別人を受け入れ、
    しかも `uq_quest_app_per_member`（`0017` L80）により**元の受注者は再申請できない**
- 決めてほしいこと（`差戻し`）: `差戻し` 中の受注申請は募集枠を占有し続けてよいか（＝差戻し中は他の人を受け入れないでよいか）。
  「占有しない」を選ぶ場合は、元の受注者が再提出できる経路（`uq_quest_app_per_member` の扱い）も併せて判断が要る。
- 反映先: 回答が出たら `quest_application_occupies_slot()` 1本を直せばビュー・トリガーの両方が追随する。
- 関連ファイル: v13 §5.3 note（L844）・§5.3.2、`supabase/migrations/0043_quest_recruit_capacity.sql`、
  `supabase/migrations/0017_quest_applications_and_work_logs.sql`、Issue #167 ／ PR #171

## ✅ [2026-09-25] チェックイン QR が何を指し・誰が発行し・どう失効するかが正本に無い（v13 §5.2 ／ WBS `3-2` ／ Issue #156）
- ステータス: **回答済み（2026-09-25 ／ 選択肢 A を採用）**
- 決定: **QR 導線を `3-2` から外し、`3-2b`（★ チェックインQR基盤）として WBS へ独立させる**
  （決定ログ §24-1）。本人特定QRは精算QR・入金QRと同規模の基盤（発行・送付・期限・失効・単回使用）に
  なり、`3-2` の残作業と規模が釣り合わない。**画面タップによる状態切替は 2026-09-22 に実装済み**であり、
  QR を分離すれば `3-2` はほぼ完了として扱える。
  ★ **静的URL（案 C）は採らない** — 法定記録である宿泊者名簿の入口URLが推測・共有可能になる。
  ⚠️ `3-2b` に着手する前に**正本 v13 §5.2 へ節を足す**必要がある（QR が何を指すか・発行者・期限・失効）。
  これは仕様判断であり、自律ループの範囲外である（`WBS_Phase1.md` の `3-2b` 行に 🔴 として記録した）
- 優先度: 高（**Issue #156（WBS `3-2` チェックイン／チェックアウト操作（QR））のタスク定義を止めている。**
  QR は作業パッケージ名そのものであり、範囲に含めるか否かが決まらないと完了条件を書けない）
- 背景: 正本 v13 §5.2（L417）は「**チェックイン / チェックアウト操作: 現地QR読み取りまたは画面タップで
  状態切替**」としか定めておらず、**QR が何を指すか（滞在 `checkin_id` か・人か）・誰がいつ発行するか・
  有効期限と失効・単回使用**を定めた節が、正本にも `DB物理設計.md`・`API設計.md` にも無い。
  正本内で QR のトークン設計があるのは**精算QR**（§5.6.3-5 ／ `orders`）と**入金QR**
  （§5.10.4 ／ `membership_applications`）の2つだけで、いずれも `check_ins` を対象にしていない。
  画面タップによる状態切替は 2026-09-22 に実装済み（`/staff/checkins`・画面ID A1）であり、
  **残っているのは QR 導線だけ**である。
- 選択肢:
  - A. **QR 導線を `3-2` から外し、別の作業パッケージ（チェックインQR基盤）として `WBS_Phase1.md` へ起こす。**
    `3-2` は画面タップでの状態切替 ＋ 宿泊者名簿（§5.2.7）で完結させる
  - B. 精算QR・入金QRと**同一規格**（256bit 乱数／sha256 保存／TTL／失効フラグ／単回使用。
    [2026-08-28] の決定どおり）を `check_ins` へ流用する前提で `3-2` に含める。
    ただし「QR が滞在を指すのか人を指すのか」は正本に無いため、**v13 §5.2 への節追加を伴う**
  - C. QR を「滞在（`checkin_id`）を指す静的な URL」に留め、トークン・TTL・失効を持たせない
- 推奨: A（理由: ①本人特定QRは発行・ハッシュ保存・TTL・失効・単回使用が必要で、精算QR・入金QRと
  同規模の基盤になる。`3-2` の残作業（名簿の同伴者UI）と規模が釣り合わない。②B は正本の節追加＝
  仕様判断を伴い、ループの権限外である。③C は**法定記録（宿泊者名簿）の入口の URL が推測・共有可能**
  になる。staff 認証は残るため実害は限定的だが、個人情報の入口に推測可能な識別子を置く設計を
  ループの判断で確定させるべきではない）
- 関連ファイル: v13 §5.2（L417）・§5.2.7、`docs/spec/WBS_Phase1.md` `3-2`、Issue #156、
  QUESTIONS.md「[2026-08-28] 決済QR・入金QRトークンの保護強化（乱数要件・TTL・単回使用）」

## ✅ [2026-09-25] AIウェルカムメッセージの送信契機・宛先・配信チャネルを定めた節が §5 に無い（v13 §4 L264・§2 L73 ／ WBS `3-2` ／ Issue #156）
- ステータス: **回答済み（2026-09-25 ／ 選択肢 D を採用）**
- 決定: **AIウェルカムメッセージを Phase 1 の対象から外す**（決定ログ §24-2 ／ `WBS_Phase1.md` §15 へ移送）。
  §9 #31（本体はエンドユーザー向け AI チャット UI を持たない）と §9 #56（`line-rag-bot` との
  API 連携は Phase 2 以降）により、**Phase 1 にはアプリから AI メッセージを送り出す経路が定義されていない**。
  経路が無い機能を `3-2` に残すと完了条件が永久に満たされない。
  ★ 固定文面の表示（案 E）で代替しない — **AI でないものを AI 機能として記録しない**
- 優先度: 中（Issue #156 の範囲に含まれるかが決まらない。ただし QR 導線の論点と独立しており、
  こちらだけが未決でも状態切替・宿泊者名簿の実装は進められる）
- 背景: 正本 v13 は §4 Phase 1 スコープ（L264「ワンタップ/QRチェックイン・チェックアウト機能 ＆
  AIウェルカムメッセージ送信」）と §2（L73）に**項目名を挙げているだけ**で、送信契機・宛先・文面・
  配信チャネルを定めた §5 の節が無い。加えて配信先の候補が仕様上ふさがっている：
  ① **v13 §9 #31（浮遊街アプリ本体はエンドユーザー向けの AI チャット UI を持たない）**は
  2026-09-11 の限定改訂でも明示的に維持されている。② `line-rag-bot` との **API 連携は §9 #56 により
  Phase 2 以降**。つまり Phase 1 にはアプリから AI メッセージを送り出す経路が定義されていない。
- 選択肢:
  - D. **AIウェルカムメッセージを `3-2` から外す**（配信経路が Phase 1 に存在しないため）。
    §4 の項目自体の扱い（Phase 2 送りか、文言の撤回か）は正本の改訂を伴うのでオーナー判断
  - E. AI 生成を伴わない**固定文面のアプリ内表示**（チェックイン完了画面の案内）までを `3-2` に含める。
    小さく、配信経路の問題も起きないが、§4 の「AI」ウェルカムメッセージは満たさない
  - F. Phase 1 で LINE Messaging API 連携を実装する（§9 #56 の「Phase 2 以降」を前倒しする判断で、
    **正本の改訂を伴う**）
- 推奨: D（理由: ①Phase 1 に送出経路が無く、E・F はいずれも正本の改訂を伴う。②`3-2` は宿泊者名簿
  （法定記録）と初回来訪キャッシュバック（金額付与）を抱えており、経路未定の機能を同じパッケージに
  残すと、確定済みの部分まで一緒に止まる）
- 関連ファイル: v13 §4（L264）・§2（L73）・§9 #31・§9 #56、`docs/spec/WBS_Phase1.md` `3-2`、Issue #156

## 🔴 [2026-09-24] WBS `1-2a`（Vercel プロジェクト連携とデプロイ環境変数の登録）を誰が実行するか — ループには Vercel の資格情報が無い（`WBS_Phase1.md` §S-0d 手順② ／ 決定ログ §20-2 ／ Issue #148）
- ステータス: 未回答
- 優先度: 高（**Issue #148 が着手できない**。完了条件5件のうち4件が Vercel CLI の認証を前提にしており、
  残る1件も `docs/spec/` の編集を要するため、現状のループ構成では**1件も満たせない**）
- 背景: Issue #148（WBS `1-2a`）の計画フェーズで判明。**仕様は揃っている**
  （`CONSOLIDATED_DECISIONS.md` §20-2 と `WBS_Phase1.md` §S-0d 手順② は実在し、Issue の記述と一致する。
  2026-09-22 に計画フェーズを止めた `v13 §20` の誤記も 2026-09-24 に訂正済み）。
  止まっているのは**仕様ではなく実行手段**である。2026-09-24 に実機で確認した結果:
  - `vercel` CLI は PATH にも `node_modules/.bin/` にも無い
  - `auto-02-implement.yml` が実装エージェントへ渡すのは `AUTOMATION_PAT` と
    `CLAUDE_CODE_OAUTH_TOKEN` のみで、**Vercel の資格情報は渡らない**
  - `.github/workflows/` に Vercel へ deploy / env 操作をするワークフローは存在しない
  - 完了条件5つ目「`WBS_Phase1.md` §S-0d に明文化」は `docs/spec/**` の編集であり、
    `automation/settings/pm.json`・`code.json` の双方で拒否されている（設計 §8.2 の仕様書ガード）
  - `WBS_Phase1.md` §S-0d 自身が「**アクセストークンを持たないエージェントからは実行できない**」と明記し、
    本作業を **オーナー作業**として分類している
- 選択肢:
  - A. **オーナー作業として扱い、Issue #148 をループの対象外にする**（`auto` を外し、§S-0d 手順②の
    チェックリストとして残す。実行後にオーナーが完了条件の確認結果を Issue へ貼る）
  - B. **`VERCEL_TOKEN` を Repository Secret に登録し、ループへ渡す**（`auto-02` に env を追加し、
    エージェントが `vercel env add` まで実行する）
  - C. **Issue #148 を分割する** — 検証スクリプト（`scripts/` に `vercel env ls` の期待値チェックを置く）
    までをループが実装し、**実際の登録と実行はオーナーが行う**
- 推奨: A（理由: B は**プロジェクト全体の環境変数を読み書きできるトークンを無人ループへ渡す**ことになり、
  `SUPABASE_SERVICE_ROLE_KEY`・`RESEND_API_KEY` を含む本番シークレットが操作範囲に入る。
  CLAUDE.md §3.1・§6.3 が「読み出せない型で保持する意味がある」としているのと逆行する。
  C は成果物が中途半端で、結局オーナーのブラウザ作業が残る。
  §S-0d が既にオーナー作業として手順化しており、A は**運用実態に合わせるだけで新たな仕組みを増やさない**）
- 関連ファイル: `docs/spec/WBS_Phase1.md` §S-0d 手順②、`docs/spec/CONSOLIDATED_DECISIONS.md` §20-2、Issue #148

## 🔴 [2026-09-22] CI が赤い：受入テストの期待値が Postgres の表示規則と食い違っている（`tests/db/shopping-list-schema.test.ts` ／ WBS `5-8`）
- ステータス: **未回答・PR #149 のマージブロッカー**（CI が赤い PR はマージしない ／ `CLAUDE.md` §6.2）。
  **ループ側では直せない**（`tests/` は書き換え禁止 ／ 設計 §0・§11.6 ／ `automation/settings/code.json` の
  `Edit(tests/**)` 拒否）。**オーナーまたは対話セッションでの修正が要る**
- 優先度: 高（他の受入テスト 482 件は通っており、**落ちているのはこの1件だけ**）
- 症状: ジョブ「DBテスト（RLS・ガードトリガー）」の
  `クエストとの紐付けは品目側の1列（完了条件7）› quest_id が quests を参照している` が
  `Expected: "public.quests" ／ Received: "quests"` で落ちる。
- 原因: **実装ではなくテストの期待文字列**。`confrelid::regclass::text` は
  **`search_path` から見えるリレーションを非修飾で描画する**（CI の接続先は `public` が
  `search_path` に入っているため `quests` になる）。マイグレーションの定義自体は正しい
  — `0030_shopping_list_items.sql` L91 が `quest_id uuid REFERENCES public.quests (quest_id) ON DELETE SET NULL`。
  **完了条件7 が求める「品目側の1列でクエストを参照する」は実装済みで、
  同テスト内の「品目を参照する中間テーブルが1つも無い」も通っている。**
- 裏付け: 同じ書き方をする既存の受入テストは、いずれも**非修飾**を期待値にしている
  — `tests/db/lodging-register.test.ts` L337・L350 が `"members/n"`、
  `tests/db/members-schema.test.ts` L211 も同様（L120 の `"auth.users(id)"` は
  `auth` が `search_path` に無いため修飾されるのが正しい）。
- 必要な対応: `tests/db/shopping-list-schema.test.ts` L190 の期待値を
  **`"public.quests"` → `"quests"`** にする（固定したい条件は「参照先が `quests` であること」であり、
  完了条件そのものは変わらない）。
- ⚠️ ループが勝手に直してはならない理由: 期待値の書き換えは**受入基準の変更**に見えるため、
  「落ちたテストを通るように書き換える」経路をループに開くと 設計 §11.6 の commit-first が成立しなくなる。
  **本件は人の目で「期待値の誤りであって仕様の変更ではない」ことを確認したうえで直す。**
- 関連ファイル: `tests/db/shopping-list-schema.test.ts` L180-L191、
  `supabase/migrations/0030_shopping_list_items.sql` L91、Issue #147 ／ PR #149

## 🔴 [2026-09-22] 買い物リストの品目を「いつまで」編集できるか（v13 §5.12.1「編集・取下げ」 ／ WBS `5-8`）
- ステータス: **未回答・実装は据え置き**（PR #149 レビュー指摘 [review-spec] を受けて起票）。
  **誰が何をいつ変えられるかは認可の仕様判断であり、ループが決めてよい事項ではない**
  （設計 §0 ／ `CLAUDE.md` §7.0「必ず先に聞く」＝認可の仕様判断）
- 優先度: 高（振る舞いが変わる論点。ゲート3でオーナーの確定を得る）
- 論点: 正本 `v13 §5.12.1` L2207 は「**登録者本人と運営（管理者・コアメンバー）が編集・取下げできる**」と
  定めるだけで、**状態（ステータス）による制限を置いていない**。§6 の認可表 L2360
  「自分が登録した品目の編集・取下げ」も同様に状態を条件にしていない。
  一方、実装 `src/lib/shopping/status.ts` L121 の `EDITABLE_STATUSES` は編集可能な状態を
  `希望` / `買う` / `クエスト化済` に限定し、**`購入済` と `見送り` の編集を禁じている**。
  DB 側（`0030_shopping_list_items.sql` の `shopping_list_items_update_self` /
  `_update_staff`）は状態を問わず更新を許しており、**アプリ層だけが独自の禁止規則を持つ**状態にある。
- 実装がそう書かれている理由（＝提案の根拠。仕様ではない）: `購入済` と `見送り` はどちらも
  **結論が出た記録**であり、買った後に品名・数量を書き換えると購入記録が何に対するものだったか分からなくなる。
  見送った後に書き換えると、§5.12.2 で必須の見送り理由が別の品目の理由になる。
- 選択肢:
  - A. **状態条件を外す**（§5.12.1 の字面どおり「本人＋運営」だけで判定する）。
    `購入済` / `見送り` の品目も本人・運営が編集できるようになる＝**現在の画面の振る舞いが変わる**
  - B. **制限を残す**（現状の実装のまま）。その場合は §5.12.1 に「結論が出た状態（`購入済` / `見送り`）は
    編集不可。直すときは登録し直す」を明記し、正本を実装に追随させる
  - C. **運営だけは状態を問わず編集できる**（本人は結論前のみ）。記録の訂正権限を運営に残す折衷案
- 推奨: B（理由: A は §5.12.2 の「見送り理由は必須」と整合しない編集を許してしまう。
  ただし**どれを採るかは認可の仕様判断**であり、ループでは決めない）
- 関連ファイル: v13 §5.12.1 L2207・§6 L2360、`src/lib/shopping/status.ts` L112-L157、
  `supabase/migrations/0030_shopping_list_items.sql`、Issue #147 ／ PR #149

## ⚡ [2026-09-22] 立替実費の扱いが正本と派生文書で食い違っている（v13 §5.12.4）
- ステータス: 未回答（**正本優先で進行中・ブロッカーではない**）
- 優先度: 低〜中（実装対象は正本側の記述だけで一意に決まるため実害は無いが、
  派生文書に旧記述が残っていると、後続パッケージ `5-9`（買い物リストからの買い出しクエスト化 ＋ 立替実費の記録）で
  「記録のみなら作るのでは」と読み違える余地が残る）
- 背景: Issue #147（WBS `5-8`）の段取り作成時に判明。正本 `v13 §5.12.4`（L2248〜2271）は
  「**立替の記録項目｜持たない**（`advance_*` 相当のカラムを `shopping_list_items` に作らない）」だが、
  `docs/spec/detailed-design/画面設計.md` L124（C14 行）は「立替実費とレシート写真は**記録のみ**」、
  `docs/spec/CONSOLIDATED_DECISIONS.md` L1668（§21-1 の表）は「**立替実費は Phase 1 では記録のみ**」と残っている。
  同じ派生文書内に正本と一致する記述も併存する（`画面設計.md` L210「立替実費の入力欄は設けない」／
  決定ログ L1676 §21-2 ③「精算経路も立替の記録項目も作らない」／`DB物理設計.md` L2350「`advance_*` は不要」）。
  正本 L23・L2853 の v1.31.0 が「§5.12.4 を『記録のみ』から『精算も記録もしない』へ全面改訂した」と明記しており、
  食い違っているのは**派生文書に残った旧記述の文面のみ**である。
- 採用した内容: 正本 `v13 §5.12.4` の記述（`advance_*` を作らない）を採用して進行
  （`0030_shopping_list_items.sql` L274〜277 にも該当列は無く、Issue #147 の完了条件も同じ形で書かれている）
- 派生文書側の訂正要否: `docs/spec/detailed-design/画面設計.md` L124 と
  `docs/spec/CONSOLIDATED_DECISIONS.md` L1668 を正本に合わせて訂正するか。オーナー判断待ち
  （自律ループは `docs/spec/` を編集しない ／ 設計 §8.2。CLAUDE.md §7.0.1 のインタラクティブセッションで処理する想定）
- 関連ファイル: v13 §5.12.4、`docs/spec/detailed-design/画面設計.md` L124・L210、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §21-1・§21-2、Issue #147

## [2026-09-25] 本人向けカレンダー（A12）の置き場所が正本と実装で食い違う（v13 §5.2.5② ／ WBS `3-8`）
- ステータス: 未回答（**ブロッカーではない**。残枠算出・C10 側の作業は本論点に依存せず進む）
- 優先度: 中（ユーザーに見える導線が変わる。ゲート1でオーナーが判断できる粒度）
- 背景: Issue #169（WBS `3-8` 宿泊枠の残数算出＋カレンダー）のタスク定義時に判明。
  v13 §5.2.5② は本人向けカレンダーの画面を「**マイログ（A6）**」と書き、
  `docs/spec/detailed-design/画面設計.md` §2 も A12 を「**A6 マイログ内タブ**」と定義している。
  一方、実装は `src/app/reservations/page.tsx`（画面ID **A11 宿泊予約**）の中の
  「予定・履歴」セクションとして置かれており（2026-09-21）、A6 側には無い。
  さらに `画面設計.md` §2-4 L176 は A12 の実装状態を「予約日を単色で塗るのみ。
  **日付タップでの滞在詳細遷移と、予定／履歴／要確認の3色区別が未実装**」と記録している。
- 選択肢:
  - A. 正本どおり **A6 マイログ内のタブ**へ A12 を移し、`/reservations` 側の一覧は削る
  - B. 現状どおり **`/reservations` 内に置いたまま**とし、正本 §5.2.5② と `画面設計.md` §2 の
    「A6 内タブ」という記述を実装に合わせて改める
  - C. **両方から到達できる**ようにする（A6 にタブを置き、`/reservations` からはそこへリンクする）
- 推奨: A（理由: 正本が定める配置であり、A6 マイログは「本人の記録を集約する画面」という位置づけと整合する。
  B は正本の改訂を伴うため §7.0 の「`docs/spec/` の仕様変更」に当たり、自律ループの権限外になる）
- 関連ファイル: v13 §5.2.5②、`docs/spec/detailed-design/画面設計.md` §2 A12・§2-4 L176、
  `src/app/reservations/page.tsx`、Issue #169

## [2026-09-25] 宿泊券の手動減算で残高をマイナスにできてよいか（v13 §5.8.5 ／ WBS `10-4`）
- ステータス: 未回答（**ブロッカーではない**。推奨案で実装済みであり、覆す場合の差分も小さい）
- 優先度: 中（宿泊券は金銭価値を持つ権利であり、運用で詰まると現場が止まる）
- 背景: WBS `10-4`（宿泊日数手動調整ログ）の実装時に判明。v13 §5.8.5 は運営の手動増減を
  「出資追加、特例付与、**誤登録の訂正**、イベント招待枠等」と例示するが、**減算の下限を定めていない**。
  残高は取引の積み上げ（`stay_ticket_balance()`）であり、DB 側にも下限の制約は無い
  （`0016` は 0 泊と符号の矛盾だけを弾く）。
  **問題が起きるのは「誤登録の訂正」である。** 例: 初期付与を 7 泊と誤って入れ、うち 5 泊を
  既に消費している会員を 4 泊へ訂正したい場合、必要な減算は −3 だが残高は 2 しかない。
  マイナスを許さないと、この訂正は**入らない**。
- 選択肢:
  - A. **残高がマイナスになる減算を禁止する**（減らせるのは残っている泊数まで）
  - B. マイナスを許す（次の付与が先にマイナスの相殺へ充当される）
  - C. マイナスは許さないが、**訂正専用の取引種別**を足して「消費済みぶんの遡及訂正」を別建てで記録する
- 推奨: **A**（理由: 宿泊券は「泊まれる権利」であり、負の権利は存在しない。B を許すと、
  本人のマイページに「残り −2 泊」と出る状態が生まれ、次の付与が黙って相殺へ消える
  ＝ 付与した側と受け取る側で認識が食い違う。C は種別を増やすぶん監査は正確になるが、
  Phase 1 の運用件数（訂正は例外処理）に対して器が重い）
- 実装: **推奨 A で実装済み**（`decideStayTicketAdjustment()` の `would_go_negative`）。
  B へ覆す場合は同関数の1条件を外すだけで済み、DB の変更は不要。
  C を採る場合は `0016` の `tx_type` CHECK へ値を足す ALTER が要る。
- 影響: `10-4`（実装済み）。`3-4`（チェックアウト時の自動消費）は残高が 0 のときの扱いを
  別に持つため、本論点とは独立である。

## [2026-09-22] 買い物リストの `見送り` を登録者へ通知する手段（v13 §5.12.2 ／ WBS `5-8`）
- ステータス: 未回答（**ブロッカーではない**。見送り理由の保存と一覧上の可視化まででタスクは閉じられる）
- 優先度: 中（`5-8` の完了条件からは外してあるため、`5-8` の実装は止まらない）
- 背景: Issue #147（WBS `5-8` 買い物リスト（ほしいものリスト）の登録・一覧・ステータス管理）の
  タスク定義時に判明。v13 §5.12.2 は `見送り` について「**理由の入力を必須**とし、**登録者へ通知する**」と
  定めているが、**通知の実現手段が仕様から読み取れない**。`WBS_Phase1.md` に通知の作業パッケージが無く、
  `supabase/migrations/` にアプリ内通知テーブルも、`src/` に LINE 送信経路も存在しない
  （`LINE_CHANNEL_*` は `WBS_Phase1.md` L240 で「`1-4`・通知が要る段階で」とされたまま）。
- 選択肢:
  - A. 今回は**見送り理由の保存と一覧上での可視化まで**を完了条件とし、通知は対象外にする
    （通知経路が入る時点でまとめて実装し、派生タスクを `WBS_Phase1.md` へ追記する）
  - B. `見送り` 時に **LINE Messaging API で登録者へ送る**ところまで `5-8` に含める
    （`LINE_CHANNEL_*` の Secrets 投入と送信基盤の新設を伴い、規模が **S → M**）
  - C. **アプリ内通知テーブルを新設**して `5-8` に含める
    （クエスト承認・宿泊など他機能の通知方式を先に決める必要があり、`5-8` 単独では決められない）
- 推奨: A（理由: 通知基盤が未着手であり、`5-8` に取り込むと S 規模の想定を外れる。
  §5.12.2 が通知で防ごうとしているのは「同じ品目が翌週また登録される」ことであり、
  見送り理由が一覧で読める状態は**その目的を部分的に満たす**）
- 関連ファイル: v13 §5.12.2、Issue #147、`docs/spec/WBS_Phase1.md` L510

## ✅ [2026-09-22] 買い物リストの登録モーダルをダッシュボード（A2／C1）に置くか（v13 §5.12.1 ／ WBS `5-8`）
- ステータス: **A2 は実装済み・C1 のみ `13-1` へ送る**（2026-09-22 ／ PR #149 レビュー指摘 R21 で解消）
- ⚠️ **本項の当初の背景記述は事実誤認だった。** 「A2 ホームそのものが存在せず／未実装」と書いていたが、
  **A2 ホームは `src/app/page.tsx` として実装済み**である（同ファイルの冒頭コメントに画面ID A2 と明記。
  `tests/home-entry.test.ts` が入口としての受入条件を固定している）。
  参照した `detailed-design/画面設計.md` L175 の「🔴 設計のみ」は**記述が古い**。
  誤認のまま「A2 への設置は A2 新設時へ送る」を推奨し、v13 §5.12.1「入口」が要求する
  クイックアクションが無いまま `5-8` を完了扱いにしかけていた。
- 対応（2026-09-22 ／ PR #149）: **A2 ホームへ「🛒 ほしいものを登録」のクイックアクションを設置した**
  （`src/app/page.tsx`）。`/shopping` の `ShoppingRegisterForm` は当初からボタン＋`<dialog>` の
  自己完結した部品として作ってあり、ホームでもそのまま同じモーダル1枚を使い回せる。
  出し分けは一覧画面と同じ `canRegister()`（ゲストは登録不可・閲覧のみ／§9 #65②）。
- 残件: **C1 統合ダッシュボードへの設置は `13-1`（今日の浮遊街サマリー）側の作業**として残る。
  同じ `ShoppingRegisterForm` を1行置くだけで済む。
- 関連ファイル: v13 §5.12.1、`src/app/page.tsx`、`src/app/shopping/ShoppingRegisterForm.tsx`、
  `docs/spec/detailed-design/画面設計.md` A2・A13・C14（L175 の進捗記述が古い）、Issue #147 ／ PR #149

## 🔴 [2026-09-22] 買い物リスト（`/shopping`）への到達経路が平置きナビ項目になっている（v13 §5.9.1・§5.12.1 note・§5.9.5 ／ WBS `5-8`）
- ステータス: **ゲート3でオーナーの確定が要る**（本 Issue は risk=high。
  設計 §3.4.1 により高リスクでの仮決定は認められないため、⚡ 仮決定から格上げした
  ／ PR #149 レビュー指摘 ／ 2026-09-22）
- 優先度: 高（**マージ前に決める**。ただし実装は現状のまま据え置きで CI は緑）
- 背景: Issue #147（WBS `5-8` 買い物リスト（ほしいものリスト）の登録・一覧・ステータス管理）の
  PR #149 レビューで判明。実装は `src/lib/auth/navigation.ts` の表示マトリクスへ
  **`shoppingList`（「買い物リスト」／`/shopping`）の行を追加**し、ゲストを `limited` としている。
  しかし**正本はこの配置を定めていない**。
  - v13 §5.9.1 の表に**買い物リストの行は無い**
  - v13 §5.12.1 note は「**タブを増やさず**、登録はダッシュボード（A2／C1）のクイックアクション、
    運営の判断・クエスト化は C14（管理系画面）」と配置を名指ししている（13枚目のタブは §5.9.5 に抵触する）
  - 一方で v13 §5.9.5「**空振りの禁止**」は「権限があるのにメニューへ出ていない画面を作らない」と定める。
    §6 L2358「**買い物リストの閲覧**」は**ゲストを含む全ロールが 〇** であり、
    ナビ項目を外すと**閲覧の到達経路が無くなる**（登録のクイックアクションは登録専用のモーダルであって、
    一覧＝A13・C14 へは連れていかない）
- **ループ側はこの行を動かさない（`limited` のまま据え置く）。判断はゲート3でオーナーが行う。**
  - 理由1（受入テストと衝突する）: `tests/shopping-list-screen.test.ts` が完了条件14 として
    `canAccessPath("/shopping", "guest") === true` ／ 同 `"member"` を固定しており、
    `canAccessPath()` は `AREAS` を唯一の根拠にしている（`navigation.ts`）。
    **この行を外すと当該受入テストが落ちる。** 受入テストは仕様を実行可能にしたものであり、
    通すために書き換えることはできない（設計 §0・§11.6）
  - 理由2（振る舞いの変更＝仕様判断）: 行を外す／運営限定にすると、
    **いま街人・ゲストに見えている「買い物リスト」の導線が消える**。
    誰に何が見えるかを決めるのは仕様であってループではない（設計 §7.1）
  - 理由3: 同 受入テストの完了条件25 は「常時表示ナビは7項目まで」で §5.9.5 を固定しており、
    現状の member／guest は **ちょうど7項目**（`home`・`quests`・`shoppingList`・`cafeOrder`・
    `myPage`・`upload`・`stayReservation`）で上限内にある
- 選択肢（**ゲート3でオーナーが1つ選ぶ**）:
  - A. **現状を追認**し、ナビ項目を残す（v13 §5.9.1 の表へ「買い物リスト」の行と
    ゲスト `△（閲覧のみ）` を追記する。**正本の改訂が要る**）
  - B. ナビ項目を**外し**、到達はクイックアクションと C14 のみにする
    （§5.12.1 note に忠実。ただし **§6 L2358 のゲスト・街人の「閲覧 〇」が到達不能**になり、
    上記の受入テスト2件も同時に書き換えが要る＝完了条件そのものの変更）
  - C. ナビ項目を**運営（admin／core_member）限定**にする（B と同じく §6 L2358 と衝突する）
- 推奨: A（理由: §6 L2358 が閲覧を全ロールに開いている以上、到達経路は全ロールに要る。
  §5.12.1 note が禁じているのは「**13枚目のタブ**」であり、現状は7項目に収まっている。
  ただし **A を採る場合も §5.9.1 の表への追記が必須**である ― 表と実装の乖離を残さない）
- ⚠️ **ループ側で対応できない残件**: A〜C のいずれも `docs/spec/`（v13 §5.9.1 ／ `WBS_Phase1.md` `5-8` 行）
  の編集を伴うが、**自律ループは `docs/spec/` へ書き込めない**（設計 §8.2 の仕様書ガード ／
  `automation/settings/code.json`）。**オーナーまたは対話セッションでの追記が要る。**
- 関連ファイル: `src/lib/auth/navigation.ts` L96-103（**本 PR の差分ではない**。
  PR #130 で `main` へ入った既存コード）、v13 §5.9.1・§5.9.5・§6 L2358・§5.12.1 note、
  `tests/shopping-list-screen.test.ts`、`docs/spec/WBS_Phase1.md` L510、Issue #147 ／ PR #149

## ✅ [2026-09-22] WBS `5-8`（買い物リスト）を「✅ 完了・実装100%」と記録したが未達の残作業が同じ行に残っている（WBS `5-8` ／ `13-1`）
- ステータス: **回答済み（2026-09-26 ／ 推奨 A を採用）**。①の古い記述を削除し、②の残り（C1 統合ダッシュボードへのクイックアクション）を `13-1` の作業内容へ移管した。`13-1` 側で C1 の設置まで実装済み（`src/app/admin/page.tsx`）。`5-8` は完了のまま据え置く
- 旧ステータス: 未回答（**ブロッカーではない**。進捗記録の正確性の問題）
- 優先度: 中（完了扱いのまま放置すると、**残件②が以後どこからも拾われない**）
- 背景: PR #149 のレビュー指摘。`docs/spec/WBS_Phase1.md` L510 は `5-8` を
  「✅ 完了（#147 ／ 2026-09-22）・実装 100%」としながら、同じ行に残作業を2点抱えている。
  - ① **DB 受入テスト（16件）が未実行** — **記述が古い**。PR #149 で DB 受入テストを追加済み
  - ② **ダッシュボード（A2 ホーム／C1）からのクイックアクション**が `13-1` 待ち。
    これは v13 §5.12.1「入口」が名指しで要求し、`5-8` の作業内容欄にも明記された**スコープ内**の項目
    → **A2 側は 2026-09-22 に実装済み**（`src/app/page.tsx` ／ PR #149 レビュー指摘 R21）。
    **残るのは C1（`13-1` 今日の浮遊街サマリー）への設置のみ**
- 選択肢:
  - A. ②の**残り（C1 側）を `13-1` の作業内容へ移し**、`5-8` は完了のままにする
    （①の記述と「A2 待ち」の記述は削除する）
  - B. `5-8` を **🟨 のままに戻し、実装％を下げる**（②が `5-8` のスコープであることを維持する）
- 推奨: A（理由: A2 は `5-8` の中で実装され、残る C1 の実装主体は `13-1` 側の画面である。
  `5-8` を 🟨 に留めると `13-1` 完了まで依存が二重に見える）
- ⚠️ **ループ側で対応できない**: どちらの案も `docs/spec/WBS_Phase1.md` の編集を伴う。
  A は**他の行（`13-1`）の作業内容欄**まで触るため、設計 §10.1.5 導線5（`5-8` 行の
  実装％／総合％／ステータスのみ）の範囲外でもある。**オーナーまたは対話セッションでの対応が要る。**
- 関連ファイル: `docs/spec/WBS_Phase1.md` L510、v13 §5.12.1、Issue #147 ／ PR #149

## ✅ [2026-09-22] Vercel Preview がどの Supabase プロジェクトを指すか（WBS `1-2a` ／ 決定ログ §20-2）
- ステータス: **回答済み（2026-09-24 オーナー決定＝選択肢A「Preview は dev Supabase を指す」）**。
  決定はオーナーとの対話セッションで行われ、Issue #148 の完了条件4つ目に記載されている。
  ⚠️ **`CONSOLIDATED_DECISIONS.md` と `WBS_Phase1.md` §S-0d への反映は未了**
  （`docs/spec/` はループから編集できないため ／ 設計 §8.2）。Issue #148 の完了条件5つ目がこの反映にあたる
- 優先度: 高（**WBS `1-2a`（Vercel プロジェクト連携とデプロイ環境変数の登録）が着手できない**。
  Preview 環境の接続先が決まらないと、環境変数の登録内容そのものが決まらない）
- 背景: Issue #148（WBS `1-2a`）の起票時に判明。`WBS_Phase1.md` §S-0d 手順②-4 が
  「**Preview がどの Supabase を指すかを決めて明文化する**」を未決として残している。
  `SUPABASE_SERVICE_ROLE_KEY` は RLS も GRANT も迂回する（`src/lib/supabase/admin.ts`）ため、
  Preview に prod の値を入れると**実名370名の会員データに対してレビュー操作が走る**
  （CLAUDE.md §3.1・§6.2 が E2E について警告しているのと同じ事故）。
  なお **prod を指す選択肢は無い**ことは §S-0d が既に明記しており、論点は残り二択の切り分けである。
- 選択肢:
  - A. **Preview は dev Supabase を指す**（`1-1b` の dev プロジェクト。`NEXT_PUBLIC_*` と `SUPABASE_SERVICE_ROLE_KEY` の Preview 値を dev に揃える）
  - B. **Preview 専用の Supabase プロジェクトを新設して指す**（`1-1b` で作る E2E 専用プロジェクトと共用する）
  - C. **Preview デプロイを当面止め、Production のみ発行する**（`github.silent` のまま Preview を無効化）
- 推奨: A（理由: dev は `1-1c`（マイグレーション適用）・`1-1d`（テストアカウント投入）が
  2026-09-21 に完了済みで**そのまま動く**。B は未完了の `1-1b` に依存し `1-2a` の着手時期が遅れる。
  C は「オーナー／運営スタッフがブラウザから触れる環境が無い」という `1-2a` の起票理由そのものを残す）
- 関連ファイル: `docs/spec/WBS_Phase1.md` §S-0d 手順②、`docs/spec/CONSOLIDATED_DECISIONS.md` §20、Issue #148

## [2026-09-22] チェックアウト時に宿泊券を消費する条件（どの滞在が何泊を消費するか）（v13 §5.2.2・§5.8.5 ／ WBS 3-4）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-25・オーナー確定）：選択肢A ＝ どの滞在でも1枚＝1泊**
- 優先度: 中（**チェックイン／チェックアウト操作（`3-2`）は実装済みで動く**。止まっているのは「消費の自動計上」だけ）
- 背景: v13 は消費のタイミングを明確に定めている——**「宿泊券の消費（`stay_ticket_transactions` の
  `consume`）はチェックアウト時に発生する」**（§5.2.2 note）、**「予約時点では消費せず、
  チェックアウト時に消費する」**（§5.2.4 の表「宿泊券の充当」）。
  2026-09-22 にチェックアウト操作（`/staff/checkins`）を実装したが、**消費の計上は入れていない**。
  決まっていないのは「**どの滞在が**」「**何泊を**」消費するかである。
  1. **充当したかどうかを記録する場所が無い。** §5.2.4 は「会員は保有宿泊券を**充当できる**」と
     書いており、充当するかは選択に読める。しかし `check_ins`（`0014`・`0026`）に
     その選択を持つ列が無い。「会員の滞在は常に消費する」のか「充当を選んだ滞在だけ」なのかで
     実装が変わる
  2. **残高が足りないときの扱いが未定義。** `stay_ticket_balance()` は取引の合計であり、
     負の残高も表現できてしまう。不足分を未会計（円）へ回すのか、残高の範囲だけ消費するのか
  3. **ゲストの滞在の扱い**。宿泊券は会員の権利（§5.10.4）であり、ゲストには無い
- 選択肢:
  - A. **会員の滞在は残高のある限り自動で消費する**（泊数分。残高が足りなければ足りる分だけ）。
    充当の選択は持たない
  - B. **`check_ins` へ「宿泊券を充当する」列を足し**、選んだ滞在だけ消費する（§5.2.4 の
    「充当できる」を選択として実装する）
  - C. **Phase 1 は自動消費を行わず**、運営が顧客管理画面から `staff_adjust` で手動計上する
    （§5.8.5 の手動増減で運用する）
- 推奨: A（理由: ①宿泊券は「4泊分の滞在権利」（§5.10.4）であって割引クーポンではないため、
  泊まれば減るのが素直である。②B は列の追加に加えて、公開予約・アプリ内予約・現地受付の
  3経路すべてに選択を足すことになり、`3-5b`・`3-7`・`3-2` へ波及する。
  ③C は「残高は正しいが、いつ減ったか誰も分からない」状態を作る——手動計上を忘れた分だけ
  残高が実態より多いまま残り、**気づくのは次の予約で満室になったとき**である）
- ⚠️ **宿泊券は金銭的価値を持つ権利であり、消費の条件はオーナー判断が要る**（CLAUDE.md §7.0 の
  「必ず先に聞く」区分）。決まるまで `consume` の自動計上は実装しない。
- 実装済みの範囲（2026-09-22）: **残高と明細の表示**（マイページ ／ 顧客詳細）。
  読み取りだけで残高を動かさないため、どの選択肢に決まっても手戻りしない。
- 関連ファイル: v13 §5.2.2・§5.2.4・§5.8.5・§5.10.4、
  `supabase/migrations/0016_membership_plans_and_stay_tickets.sql`、
  `src/lib/lodging/stay-tickets.ts`、`src/app/staff/checkins/actions.ts`、
  `docs/spec/WBS_Phase1.md` `3-4`

### ✅ 決定（2026-09-25・オーナー確定）

**選択肢A を採用。「どの滞在でも1枚＝1泊」。** 滞在の種類（形態・部屋・予約経路）で消費枚数を変えず、
充当するかどうかの選択も持たない（`check_ins` への列追加は行わない）。

実装時の扱い（**A の但し書きをそのまま採る。異なる場合は差し戻しが要る**）:

1. **会員の滞在はチェックアウト時に泊数分を自動で `consume` する**（1泊＝1枚）
2. **残高が足りなければ足りる分だけ消費し、残りは通常の未会計（円）として扱う**
3. **ゲストの滞在は消費しない**（宿泊券は会員の権利 ／ v13 §5.10.4）

記録: `CONSOLIDATED_DECISIONS.md` §23-2。

---

## [2026-09-22] 精算QRをゲストが読み取る先（未ログインで金額を見せてよいか）（v13 §5.6.1④・§5.6.3-5）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-24・オーナー確定）**
- 優先度: 中（**WBS `7-3` の発行・失効・消し込みは実装済みでブロックされていない**。残るのは「読み取った先に何を出すか」だけ）
- 背景: v13 §5.6.1④ と §5.10.4 は「一括精算QRをモーダルで発行し、**ゲストのスマホで読み取ってもらう**」と定める。
  2026-09-22 に発行・失効・単回使用の基盤（256bit乱数 ／ ハッシュ保存 ／ TTL 24時間）を実装したが、
  **QR が指す先のページを作っていない**。作るとすれば「トークンを知っていれば誰でも開ける」ページになり、
  そこに**伝票の金額が未ログインで表示される**。これは認可の設計判断であり、推測で決めると
  v13 §5.9.3（DOM 非表示は認可ではない）の趣旨に反する。
  現在の実装は**発行したトークンを画面へ1度だけ表示する**ところまでで、精算の消し込みは運営の操作である。
- 選択肢:
  - A. **公開ページを作らない**（現状維持）。QR は eumo 側の決済経路へ渡す値として使い、
    アプリ側は「発行した・使った」の記録だけを持つ
  - B. **トークン限定の公開ページ**を作る。表示は「合計金額（Uii 主・円 副）と支払い方法の案内」だけに限り、
    明細・氏名・会員番号は出さない。TTL 24時間・単回使用はそのまま効く
  - C. **ログイン必須**の精算ページにする。本人だけが自分の伝票を開ける
- 推奨: B（理由: ①A は「ゲストのスマホで読み取ってもらう」という仕様の動線が成立しない。
  ②C は**ゲスト（アプリ未登録の来訪者）が払えない**——精算QRは §5.10.4 で街人登録前の人にも使う経路である。
  ③B の漏洩範囲は「合計金額」1点で、トークンは24時間・単回使用であり、
  伝票を編集すれば即時失効する（§5.6.3-5 ／ 実装済み）。明細を出さなければ、何を頼んだかは漏れない）
- ⚠️ 金額と未ログイン表示に関わるため、**オーナー判断が要る**（CLAUDE.md §7.0 の「必ず先に聞く」区分）。

### ✅ 回答（2026-09-24・オーナー確定）

**未ログインのゲストが精算QRを読み取って参照できるようにする。** 選択肢**B**（トークン限定の公開ページ。
表示は「合計金額（Uii主・円副）と支払い方法の案内」のみに限り、明細・氏名・会員番号は出さない。
TTL24時間・単回使用はそのまま効く）を採用する。実装はまだ着手していない
（`src/lib/billing/settlement-qr.ts`は発行・失効・単回使用の基盤のみで、QRが指す公開ページ自体は未作成）。

- 関連ファイル: v13 §5.6.1④・§5.6.3-5・§5.10.4、`src/lib/billing/settlement-qr.ts`、
  `src/app/admin/customers/[memberId]/actions.ts`、`supabase/migrations/0019_orders_and_settlement.sql`
## [2026-09-22] 朝会の「ナレッジ候補」の恒久的な保存先（v13 §5.7.4 ② ／ WBS `1-6`・`9-1`）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-24・オーナー確定）**
- 優先度: 中（**WBS `4-2` は迂回済みでブロックされていない**。`9-1`（横断セマンティック検索）の入力源に関わる）
- 背景: v13 §5.7.4 ② は、朝会の構造化と**同じ1回の呼び出し**でナレッジ候補も出させると定める
  （別呼び出しにすると非機能 P3 の60秒を圧迫するため）。実装もそのとおりに1回で受け取っている
  （`src/lib/morning-meetings/structure.ts`）。しかし**受け取ったナレッジ候補を入れる先が Phase 1 に無い**。
  クエスト候補は `0020` が足した `morning_meetings.extracted_quest_candidates` に入り、
  議事録サマリーは `summary_text` に入るが、ナレッジの正本は **`0100` の `knowledge_chunks`**（ベクトル基盤）であり、
  その投入経路は **WBS `1-6`（pgvector有効化・RAG基盤）・`9-1`** の担当で未完である。
- 迂回（2026-09-22 実装）: 捨てると再生成のたびにモデル呼び出しが必要になるため、
  **`summary_text` の末尾へ `■ナレッジ候補（未登録）` の節として残す**（人がそのまま読める形）。
  `formatKnowledgeCandidatesText()`。**ナレッジとしては検索対象にならない**状態である。
- 選択肢:
  - A. `1-6`／`9-1` の完成時に、`summary_text` 内の `■ナレッジ候補（未登録）` 節から
    `knowledge_chunks` へ一括移送する（移送スクリプトを `9-1` の範囲に含める）
  - B. `morning_meetings` に `extracted_knowledge_candidates jsonb` 列を足し、機械可読な形で貯める
    （`extracted_quest_candidates` と対称。移送時のパースが要らない）
  - C. 朝会からのナレッジ候補は Phase 1 では生成させない（プロンプトから外す）
- 推奨: B（理由: ①A はテキストからの逆パースになり、書式を1文字変えると移送が壊れる。
  ②C は v13 §5.7.4 ② が「同じ1回の呼び出しに相乗りさせる」と定めた趣旨——**後から別呼び出しで
  作り直すと60秒枠を圧迫する**——に反し、Phase 1 蓄積分が失われる。③B は `0020` と同じ形の列追加1本で済み、
  `9-1` 側は jsonb を読むだけになる）

### ✅ 回答（2026-09-24・オーナー確定）

**恒久的な保存先は `knowledge_chunks`（pgvector・`0100_rag_pgvector_knowledge_chunks.sql`）とする。**
保存経路は推奨どおり**選択肢B**（`morning_meetings.extracted_knowledge_candidates jsonb` 列を新設し、
機械可読な形でいったん貯める → `1-6`／`9-1` の完成時に `knowledge_chunks` へ移送する）を採用する。
選択肢Aのテキスト逆パースは避ける。

- 関連ファイル: v13 §5.7.4 ②、`src/lib/morning-meetings/structure.ts`、
  `supabase/migrations/0020_morning_meeting_structuring.sql`、`supabase/migrations/0100_rag_pgvector_knowledge_chunks.sql`、
  `docs/spec/WBS_Phase1.md` `4-2`・`1-6`・`9-1`

---

## [2026-09-22] 買い物リスト（v13 §5.12）のスコープ・ゲストの登録可否・立替実費の精算経路（v13 §9 #65）
- ステータス: ~~未回答~~ → ✅ **回答済み・クローズ（2026-09-22 オーナー確定）**
- 優先度: ~~高~~ → **解消**（`5-8`・`5-9` は 🟢 着手可能へ）
- 背景: 2026-09-22 オーナー指示「**買い物リスト追加、クエストにする。ダッシュボードからほしいものリストみたいに登録**」を受け、
  正本 v13 に **§5.12「買い物リスト（ほしいものリスト） ＆ 買い出しクエスト化」を新設**した（v1.30.0）。
  **機能要件は確定**（登録はモーダル1枚・品名のみ必須／`希望 → 買う → クエスト化済 → 購入済` ／ `見送り`／
  複数品目を選んで**買い出しクエストを1件**起案／完了報告は §5.3 の Before/After をレシート写真として流用）。
  `WBS_Phase1.md` にも **`5-8`・`5-9`** として起票済みで、**決まり次第そのまま着手できる状態**にある。
  未確定なのは次の3点のみ。
- 確認したいこと:
  - **① Phase の置き場所** — Phase 1 に押し込むか、Phase 2 の先頭に置くか。
    最小構成（テーブル1本＋一覧画面＋登録モーダル＋クエスト起案の接続）で **S規模1件**の増分。
    2026-09-08 のオーナー確定「Phase 1 は **61パッケージ全部**」は本件の起票前の数字であり、
    Phase 1 とするなら**分母が 63 になる**（＝全体進捗率が下がる）
  - **② ゲスト（`guest`）が「ほしいもの」を登録できるか** — Phase 1 の既定は**閲覧のみ・登録不可**としている。
    ゲストにも開放すると、運営が `買う` 判断で弾くとはいえ、**リストの母数と重複検知の負荷が増える**
  - **③ 立替実費（円）の精算経路** — 買い出しは他のクエストと違い、**実行者が自分の財布から先に円を払う**。
    Phase 1 は「**記録のみ**（立替額・レシート写真を残し、精算は現地の現金／Eumo 手渡し）」という**仮決定**を置いた。
    恒久仕様としては次のどちらか（またはアプリでは扱わない）を選ぶ必要がある
- 選択肢（③について）:
  - A. **§5.6.6 の `settlement_adjustments`（返金区分）へ相乗り** — 発生・残置・消込・90日滞留アラートの
    仕組みが**そのまま使える**。ただし同テーブルは現在「**伝票の遡及修正で生じた差額**」専用であり、
    伝票を持たない立替を入れると `伝票番号` が NULL になる行が混ざる
  - B. **§5.3.1 の `eumo_grants`（`grant_type = manual`）で返す** — 「誰に・いくら・何のために」を
    追跡する器として既に完成しており、**手動起票の経路も用意済み**。ただし**円の立替を Uii で返す**ことになり、
    実費と報酬の区別が会計上つかなくなる
  - C. **アプリでは精算しない（Phase 1 の仮決定をそのまま恒久仕様にする）** — 記録のみ
- 推奨: **①＝最小構成のみ Phase 1**（理由: 登録の受け皿が無いと、この機能の価値である「気づいた瞬間の登録」が
  発生せず、Phase 2 に入った時点で中身が空になる。§5.11 メディアを Phase 1 へ戻したときと同じ理屈）。
  **②＝Phase 1 は登録不可**（ゲストは滞在が短く、買い出しの需要は運営・街人側に集中する）。
  **③＝C（記録のみ）で Phase 1 を通し、Phase 2 で A へ寄せる**（理由: A は器としては正しいが、
  `settlement_adjustments` は**伝票前提の設計**であり、立替を入れるなら区分の追加と
  「伝票を持たない差額」の扱いを決め直す必要がある。残8日でやる作業ではない）
- 関連ファイル: v13 §5.12・§9 #65、`docs/spec/WBS_Phase1.md` §5（`5-8`・`5-9`）・§15、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §21、`docs/spec/detailed-design/画面設計.md`（A13・C14）

### ✅ 回答（2026-09-22・オーナー確定）

| # | 論点 | 回答 |
| --- | --- | --- |
| ① | Phase の置き場所 | **最小構成で実装する（Phase 1）**。WBS `5-8`・`5-9` を 🟢 着手可能へ。**分母は 61 → 63** |
| ② | ゲストの登録可否 | **登録不可・閲覧のみ**。登録できるのは**管理者・コアメンバー・街人**の3ロール（サーバサイドでも拒否する） |
| ③ | 立替実費の精算経路 | **精算は不要**。選択肢 **C（アプリでは扱わない）**。精算経路だけでなく**立替の記録項目（`advance_*`）も作らない** |

**③の反映で仕様が1段軽くなった。** 当初の仮決定は「記録のみ」だったが、確定は「**精算も記録もしない**」である。
証憑が要る場合は**クエスト完了報告（v13 §5.3 の After 写真）にレシートを添付**すれば足りるため、
買い物リスト側に金額の項目を持たない。`shopping_list_items` から `advance_payer_id` / `advance_amount_jpy` /
`receipt_media_id` / `advance_settled*` を削除した。

⚠️ **立替が常態化する運用に変わった場合は本項を再オープンすること。** そのとき必要になるのは
カラム追加ではなく**債務を扱う器の設計**である（v13 §5.12.4）。

- 反映先: v13 **v1.31.0**（§4・§5.12.1・§5.12.4 全面改訂・§5.12.5・§6・§7・§9 #65）、
  `docs/spec/WBS_Phase1.md`（`5-8`・`5-9`・§15・分母）、`docs/spec/CONSOLIDATED_DECISIONS.md` §21、
  `docs/spec/detailed-design/画面設計.md`（A13・C14）・`DB物理設計.md` §7-21

## [2026-09-21] 招待メール（経路B）のリンクを受ける導線がアプリ側に存在しない（v13 §5.2.6 ／ 決定ログ §20-1）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-22）**
- **回答（2026-09-22・オーナー決定）: A（招待もコード方式へ寄せる）を採用する。**
  - `admin.auth.admin.inviteUserByEmail()` を**やめる**。招待メールには**ログイン画面の案内だけ**を載せ、
    会員は**通常のメールOTP（6桁コード）**でログインする。`auth.users` 生成後、
    **招待台帳の宛先と一致することを確認したうえで** `members.auth_user_id` と結合する
  - `member_invitations` の台帳・有効期限・`active` への送信禁止・監査記録は**そのまま維持**
- ✅ **実装完了（2026-09-22 ／ WBS `2-1d`）**
  - `sendInvitation()` は「①台帳へ記録 → ②**ログイン画面の案内メール**（`src/lib/mail/resend.ts`）」の2段になった。
    本文に載る URL は**ログイン画面だけ**で、トークン付き URL もコードも載せない（受入テストで固定）
  - 続く結合は**ログイン側**で成立する：`requestLoginCode` が**招待台帳に未消費・期限内の行があるアドレスにだけ**
    `auth.users` の新規作成を許し（`shouldCreateUser`）、`bindAuthUserToMember()` が台帳の宛先と突合して結合する。
    判定は `findUsableInvitation()` に集約した（同じ条件を2箇所に書くと片方だけ緩む）
  - ⚠️ **`RESEND_API_KEY` が未設定のため実送信はまだできない**。未設定時は専用の理由を返し、
    **台帳の行は残す**（招待の窓は開いているので、会員が自分でログインを始めれば成立する）。
    鍵の登録は `1-1e` と同じオーナー作業
    （v13 §5.2.6 danger の4項目は**すべて台帳側で満たしており、リンクは要件ではない**）
  - **リンクが全経路から消え、Site URL / Redirect URLs の設定がどの経路にも影響しなくなる**
  - ⚠️ **正本の文言を改訂した**（「アカウント作成リンクを送る」→ 案内を送る ／ v13 §5.2.6・§9 #67）
  - 反映先: v13 §5.2.6・§9 #67、`CONSOLIDATED_DECISIONS.md` §22-1、`WBS_Phase1.md` **`2-1d`（新設）**
- 旧ステータス: 未回答（**ブロッカーではない**。ログイン経路は6桁コードで完結しており、招待だけが宙に浮く）
- 優先度: 中（`2-1b`（ログイン導線・セッション・ロールガード）は完了扱いだが、**経路Bの招待は実際には最後まで通らない**）
- 背景: 2026-09-21 の決定（§20-1）で、ログインメールからサインインリンクを外し6桁コードのみにした。
  ログイン経路はリダイレクトを伴わないため、これで Site URL / Redirect URLs への依存が消える。
  **しかし招待（経路B）は別経路である。** `src/lib/auth/invitations.ts` は
  `admin.auth.admin.inviteUserByEmail(email)` を呼んでおり、これは **Invite user テンプレート**という
  別テンプレートの**リンク方式**のメールを送る。リンクは Supabase の `/auth/v1/verify` を経由して
  **Site URL へ戻ってくる**が、**アプリ側に受け口が無い**:
  - `src/app/` に `auth/callback` 相当のルートが存在しない
  - `createBrowserSupabaseClient()`（`src/lib/supabase/client.ts`）は**定義されているだけで、どこからも呼ばれていない**。
    したがって URL ハッシュに載って戻ってくるトークンを拾う主体が居ない
  - つまり招待された街人は、リンクを踏んでも**トップページに着地するだけでログイン状態にならない**
- 選択肢:
  - A. **招待もコード方式へ寄せる** — `inviteUserByEmail` をやめ、招待メールは「ログイン画面のURLと、
    そこでメールアドレスを入れてもらう案内」だけにする。`member_invitations` の台帳・有効期限・
    `active` な会員へ送れない検査はそのまま活き、`bindAuthUserToMember` も現状のまま通る。
    **リンクが全経路から消え、Site URL / Redirect URLs の設定が完全に不要になる**
  - B. **受け口を作る** — `/auth/callback` を新設し、ブラウザクライアントでトークンを回収してセッションを張る。
    Site URL / Redirect URLs の登録が必要になり、環境ごと（dev / Preview / prod）に管理対象が増える
  - C. 招待は Phase 1 では提供せず、運営が口頭・LINE で案内して通常ログインさせる
- 推奨: A（理由: ①§20-1 の決定理由がそのまま招待にも当てはまる。メール本文に「踏めば即ログインできるURL」を
  残す限り、転送・誤送信・受信箱の漏洩が乗っ取り経路になる。v13 §5.2.6 の danger が要求する4項目
  （送信者の限定／誰が・いつ・どの会員へ／有効期限／`active` へは送れない）は**すべて `member_invitations` 側で
  満たしており、リンクそのものは要件ではない** ②B は新規ルート＋クライアントコンポーネント＋環境ごとの
  URL 管理が増え、残工期に対して割に合わない ③ただし v13 §5.2.6 は経路Bを
  「運営が個別にアカウント作成**リンク**を送る」と書いており、**A を採ると正本の文言の改訂が要る**）
- 関連ファイル: v13 §5.2.6、`src/lib/auth/invitations.ts` L86、`src/lib/supabase/client.ts`、
  `src/app/login/actions.ts`、`docs/spec/CONSOLIDATED_DECISIONS.md` §20-1、`docs/spec/WBS_Phase1.md` §S-0c

## [2026-09-20] 事前予約（朝スロット）に対応する商品が正本のカフェメニューに存在しない（v13 §5.4.1b ／ §9 #48）
- ステータス: 未回答（**ブロッカーではない**。朝の選択肢が0件のまま `3-5c` を実装できる）
- 優先度: 中（`3-5c`（カフェ事前予約注文）の朝スロットが空のままリリースされる）
- 背景: v13 §5.4.1b は事前予約の対象を「**朝ごはん／昼／夜のプレートごはん**」とし、
  `menu_items.is_pre_orderable = true` の商品を選択肢に出すと定めている。
  しかし正本のメニュー（`docs/spec/requirements/カフェメニュー.md` v1・31品）には
  **朝食に当たる商品が1件も無い**。昼（ワンプレートごはん 1,300円）と
  夜（季節のプレートごはん 2,100円）は実在する。
  モック `docs/design/prototype_v15.html` L3399 には「朝ごはん（和定食） 1,000円」があるが、
  **モックは価格の正本ではない**（`CLAUDE.md` §1.1）。
  そのため `0018_menu_items.sql` の初期32行では、**朝の事前予約対象を1件も立てていない**
  （単価を推測で作らない／`CLAUDE.md` §7）。
- 選択肢:
  - A. **「朝ごはん（和定食）1,000円」を正本メニューへ追加する**（モックの値を採用して確定させる）
  - B. 朝の事前予約は Phase 1 では提供しない（`meal_slot = 'breakfast'` を選択肢に出さない）
  - C. 商品名・単価をオーナーが別途指定する
- 推奨: A（理由: モックは公開予約ページの事前予約フローを朝・昼・夜の3スロットで実装しており、
  朝だけ選べない状態は §5.4.1b の「滞在全日程の朝・昼・夜」という要件と食い違う。
  価格はオーナー確認で確定させるのが筋だが、モックに実在する 1,000円 が最も確度が高い）
- 関連ファイル: v13 §5.4.1b、`docs/spec/requirements/カフェメニュー.md`、
  `supabase/migrations/0018_menu_items.sql` §④、`docs/design/prototype_v15.html` L3399

## [2026-09-20] 伝票の編集履歴ログ・精算グループ・手動調整行の物理設計が存在しない（v13 §5.6.4・§5.6.2・§7）
- ステータス: 未回答（**ブロッカーではない**。`6-1`・`8-3` は現行の列で動く）
- 優先度: 中（`7-2`（伝票管理）・`8-1`（顧客サマリー・注文履歴編集画面）の完了条件に影響する）
- 背景: 正本 v13 が要求する3つのエンティティに、`DB物理設計.md` の対応する DDL が無い。
  `0019_orders_and_settlement.sql` を書く際に判明した。
  1. **編集履歴ログ（`audit_log`）** — v13 §5.6.4「誰が・いつ・どの伝票の何を・なぜ」、
     §7 L2446（操作種別・変更前値・変更後値・編集理由）。
     現状は `order_items.edited_by` / `edit_reason` しか無く、**最後の1回の編集しか残らない**。
     §5.6.5-4「遡及修正は通常の編集ログとは別に**警告レベルで記録**」を満たせない
  2. **精算グループ（一括精算）** — v13 §7 L2445（精算グループID・対象伝票リスト・請求合計）。
     現状は精算QRの列が**伝票単位**にしか無く、「未精算の伝票を複数選んで1つのQRを発行する」
     （§5.6.1④ ／ 画面 C2）を表現できない
  3. **手動調整行** — v13 §5.6.2（まかない補助・割引）、§7 L2443。
     `settlement_adjustments` は**遡及修正の差額**であって別概念であり、
     §5.6.3-2「伝票合計（円）＝ 行小計の合計 ＋ **手動調整額**」が計算できない
- 独自に作らなかった理由: 監査ログの粒度・保存年限（v13 §5.11・非機能）を、
  注文管理の作業パッケージが単独で決めてしまうことになるため（`CLAUDE.md` §7）。
- 選択肢:
  - A. **3件をまとめて `DB物理設計.md` §3-2 へ追設計し、新しい作業パッケージとして WBS へ起票する**
  - B. 1件ずつ、それを必要とする作業パッケージ（`7-2`・`8-1`）の中で設計する
  - C. Phase 1 では見送り、編集履歴は `order_items` の最新1件のみで運用する
- 推奨: A（理由: 3件とも「伝票の周辺」という同じ文脈にあり、別々に設計すると
  監査ログの形が3通りに割れる。C は §5.6.5-4 が明示的に要求する警告記録を落とすため、
  会計の遡及修正が追えなくなる）
- 関連ファイル: v13 §5.6.2・§5.6.4・§5.6.5・§7 L2443-2446、
  `docs/spec/detailed-design/DB物理設計.md` §3-2、`supabase/migrations/0019_orders_and_settlement.sql` §⑦
- 📌 **2026-09-22 実装メモ（WBS `7-2`・`8-1` を本論点の回答を待たずに進めた範囲）**:
  - **実装した**: 明細の数量・単価の編集、明細行の取消（`0031_order_items_void.sql` ＝ 論理削除＋理由必須）、
    注文者の付替、伝票の取消、決済ステータスの手動切替、精算済み伝票の**差額の自動起票**
    （`settlement_adjustments` へ「未処理」）、編集時の**精算QRの自動失効**
  - **作らなかった（本論点の3件）**: 編集履歴ログ（`audit_log`）・精算グループ（一括精算）・手動調整行。
    推奨 A（3件まとめて追設計）の判断はオーナーのままである
  - ⚠️ **その結果の制約**: 編集理由は `order_items.edit_reason`（**最後の1回だけ**）に残る。
    付替・ステータス変更の理由も、専用ログが無いため**生きている明細の `edit_reason` へ書く**という
    暫定の形になっている。§5.6.5-4 が要求する「遡及修正を警告レベルで別に記録する」は**未達**である
  - ⚠️ 一括精算（複数伝票を選んで1つのQR）も未実装。QRは**伝票単位**でのみ発行できる
## ⚡ [2026-09-20] 仮決定で進行：WBS `18-1`（HTMLモック `prototype_v15.html` の維持・レビュー反映）の範囲（`CONSOLIDATED_DECISIONS.md` §14-2 ／ v13 §5.2.3・§5.3.1・§5.9.4・§5.10.8）

**何が決まっていないか**
Issue #99（WBS `18-1` HTMLモック `prototype_v15.html` の維持・レビュー反映）は完了条件が雛形のプレースホルダのままで、
「維持・レビュー反映」がどこまでを指すかが未確定。WBS `18-1` のステータス欄は **①v1.21.0 が未追随**と
**②`fetch`/`localStorage` が0件＝バックエンド結合が未着手**の2点を併記しており、①だけなら M 相当の文言・UI 追随、
②まで含むとモックがアプリ実装そのものに変わる。さらに Issue 本文の「根拠となる仕様」は **`v13 §14`** と書かれているが、
正本 v13 は §9 までしか存在しない（§14 は `CONSOLIDATED_DECISIONS.md` 側の節番号）。

**仮に採用した案**
`CONSOLIDATED_DECISIONS.md` §14-2「残作業」が名指しする4項目（§5.9.4 文言・§5.2.3 申告項目・チェックインバッジ・
Eumo給付一覧の種別/用途列）への追随に限定する。WBS `18-1` のステータス欄自身が「CONSOLIDATED §14-2 が明記」と
出典を示しており、推測ではなく既存の記録から範囲を引けるため。バックエンド結合は各機能パッケージ（`3-2`・`12-4` 等）の
担当であり `18-1` には含めない。

**併せて範囲から外したもの（理由）**
- **キャッシュバック金額の2プラン化表示**（40,000円＝10,000 uii ／ 30,000円＝5,000 uii ／ v13 §5.10.2・§9 #51）:
  §14-2 の4項目に含まれておらず、モックの現行表示は「5,000 コイン」単独。**金額付与の範囲**に踏み込むため
  範囲の切れ目はオーナー判断に委ねる（設計 §10.1.3）
- **ロール表示名の最終ラベル**: 未回答論点「[2026-09-01] 利用者向けロール表示名（呼称）の最終ラベル（v13 §9 #58）」に
  依存する。モックには v13 §5.9.4 の図が書いている**仮案**（システム管理者／システム運用者）を置き、
  モック内の改訂ログに「#58 未確定」と明記して確定扱いにしない

**覆す場合の手当て**
成果物は `docs/design/prototype_v15.html` 1ファイルの編集のみで、DB・API・本体コードに波及しない。
範囲が違っていた場合は当該コミットの revert（または該当ブロックの再編集）で戻せる。逆に範囲を広げる判断になった場合も、
バックエンド結合分を別 Issue として起こせばよい（`18-1` の成果は破棄されない）。

**状態**: ⚡ 仮決定で進行中（オーナー未確認 ／ Issue #99）

---

## [2026-09-20] WBS `10-4`（宿泊日数手動調整ログ）を操作できるロールが正本と派生文書で食い違う（v13 §5.8.5 ／ §7 宿泊日数調整ログ）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-25・オーナー確定）：選択肢B ＝ 正本 v13 §5.8.5 が正しい（`admin` ＋ `core_member`）。派生3文書を正本へ合わせて訂正済み**
- 優先度: 高（Issue #95 ／ WBS `10-4` をブロック。宿泊券は金銭価値を持つため誤った認可は取り消せない）
- 背景: Issue #95（WBS `10-4` 宿泊日数手動調整ログ）のタスク定義時に判明。正本 v13 §5.8.5 は「**管理者・コアメンバー**が顧客管理画面から宿泊日数・チケット数を加算／減算できる」と書き、`DB物理設計.md` §6 の RLS も `stay_ticket_transactions` の書き込みを `staff`（＝`is_staff()`＝`admin` または `core_member`／L1210）に与えている。一方 `API設計.md` L259 の `PATCH /api/members/{id}/stay-days` は **`admin` のみ**、`画面設計.md` C8（宿泊日数の手動増減）も **`admin`**、`WBS_Phase1.md` L340 の `10-4` も「管理者のみ操作可」で、**2対3に割れている**。Issue #95 の完了条件は雛形のプレースホルダのままであり、この論点を決めずに導出すると「誰が宿泊券を増やせるか」を機械が決めることになる。
- 選択肢:
  - A. `admin` のみに限定する（`API設計.md`・`画面設計.md` C8・WBS に合わせ、正本 v13 §5.8.5 と `DB物理設計.md` §6 を「`admin` のみ」へ改訂する）
  - B. `admin` ＋ `core_member` を許可する（正本 v13 §5.8.5 と RLS に合わせ、`API設計.md` L259・`画面設計.md` C8・WBS `10-4` の表記を「admin/core_member」へ訂正する）
  - C. `admin` ＋ `core_member` を許可したうえで、`core_member` には上限枚数・自拠点の会員のみ等の制約を課す（`QUESTIONS.md`「[2026-09-15] `core_member` の「自拠点のみ」が RLS で表現できず、正本より緩い認可が先に固定される」と同じ構図になるため、その決着と揃える必要がある）
- 推奨: B（理由: CLAUDE.md §1.1 で矛盾時は正本 v13 が勝つと定めており、正本 §5.8.5 と、認可の実体である `DB物理設計.md` §6 の RLS が一致しているため。A を採ると「RLS は core_member に許すが API は弾く」という二重基準が残り、PostgREST 直アクセスで迂回できる穴になる〔`quests` の `GRANT SELECT` で同型の事故が 2026-09-20 に起きている〕。ただし宿泊券は金銭価値を持つため、運用上コアメンバーに増減を任せない判断があるなら A を選び、正本側を改訂するのが正しい）
- 関連ファイル: v13 §5.8.5、v13 §7「★ 宿泊日数調整ログ」、`DB物理設計.md` §6（L1140・L1210・L2013）、`API設計.md` L259、`画面設計.md` C8、`WBS_Phase1.md` L340、Issue #95

### ✅ 決定（2026-09-25・オーナー確定）

**選択肢B を採用。「正本が正しい」** ＝ **`admin` ＋ `core_member`**（`is_staff()`）が操作できる。
正本 v13 §5.8.5 と `DB物理設計.md` §6 の RLS は変更せず、**食い違っていた派生文書3件を正本へ合わせて訂正した**:

| 訂正先 | 旧 | 新 |
| --- | --- | --- |
| `API設計.md`（`PATCH /api/members/{id}/stay-days`） | `admin` のみ | **`admin` / `core_member`** |
| `画面設計.md` C8（宿泊日数の手動増減） | `admin` | **`admin` / `core_member`** |
| `WBS_Phase1.md` `10-4` | 管理者のみ操作可 | **管理者・コアメンバーが操作可** |

⚠️ **`10-4` の依存**: 実体テーブル `stay_ticket_transactions` は `0016` で作成済み（`3-4` 側）。
調整ログの5種別（`初期入力／手動加算／手動減算／チェックアウト自動消費／街人登録付与`）は
§23-2 の自動消費と同じ明細に載るため、**`3-4` の消費実装と同じ作業でまとめて入れる**のが手戻りが少ない。

記録: `CONSOLIDATED_DECISIONS.md` §23-3。

> [!note] あわせて確認したい依存関係（本論点の回答とは独立・今回は質問しない）
> `10-4` の依存は `2-1`（ロール・アカウントステータス スキーマ設計／完了済み）とされているが、
> 調整ログの実体テーブル `stay_ticket_transactions` を作るのは **WBS `3-4`（宿泊券管理・消費/付与ロジック）**であり、
> 未着手（実装 0%）・マイグレーション未作成である。v13 §5.8.5 は残高を取引履歴の積み上げ（イベントソーシング）で
> 算出すると定めており、v13 §7 の調整ログは `初期入力／手動加算／手動減算／チェックアウト自動消費／街人登録付与` の
> 5種別を1つの明細に持つ。したがって `10-4` の実装は `3-4` に依存する見込みで、WBS の依存列の訂正が要る可能性がある。
## ⚡ [2026-09-20] 仮決定で進行：浮遊街コンシェルジュ（line-rag-bot）管理画面 URL の供給方法（v13 §5.9.5 ／ §9 #31）

**何が決まっていないか**
WBS `9-2`（line-rag-bot管理画面への導線（外部リンクのみ））が開く Streamlit 管理画面の URL が、
正本にも `.env.example` にも定義されていない。`.env.example` の `line-rag-bot 連携` 節にあるのは
Phase 2 用の `LINE_RAG_BOT_ENDPOINT`／`LINE_RAG_BOT_API_KEY`（いずれもコメントアウト）だけで、
Phase 1 の外部リンク先を指すキーが無い。

**仮に採用した案**
公開環境変数 `NEXT_PUBLIC_CONCIERGE_ADMIN_URL` を新設して供給する。URL は秘匿値ではなく
（Streamlit 側が自身のログイン機構で認証する／`画面設計.md` §4 B9）、環境ごとに値が変わるため。
**未設定時はボタンを描画しない**（v13 §5.9.5「空振りの禁止」。押しても何も起きない導線を出さない）。

**覆す場合の手当て**
参照箇所は導線ボタン1つと `.env.example` の1行のみ。サーバ側設定へ寄せ直す場合も
`NEXT_PUBLIC_` 接頭辞を外して Server Component 経由で渡す変更で済み、DB・API の形は変わらない。

**状態**: ⚡ 仮決定で進行中（オーナー未確認 ／ Issue #94）

---

## ⚡ [2026-09-20] 「ナレッジ登録」の行が正本内でフォームとも外部リンクとも読める（v13 §5.9.1・§5.7.5 ／ §9 #31）
- ステータス: 未回答（正本優先で進行中・ブロッカーではない）
- 優先度: 低〜中（派生文書を放置すると、後続の担当者が「アプリ側にフォームを作る」と読み違える）
- 背景: Issue #94（WBS `9-2` line-rag-bot管理画面への導線（外部リンクのみ））の段取り作成時に判明。
  調査メモが4点を記録した。①正本 v13 §5.9.1 の表は行名を「ナレッジ登録**フォーム**（§5.7）」のままにしており、
  無効化注記（同節 L1643–1645）が打たれているのは「AIコンシェルジュ（FAQチャット）」行だけである。一方
  §5.7.5 の後発注記（2026-08-16 再確定）は「ナレッジ登録画面自体も本体には実装せず、**Streamlit 管理画面への
  外部リンクのみ**」と述べる。②`docs/spec/detailed-design/画面設計.md` §2 の C5 行が対応WBS を `9-4`
  （未回答エスカレーション一覧の管理画面表示・➖ 不要化済み）としている（実際に外部リンクを担当するのは `9-2`）。
  ③`docs/spec/WBS_Phase1.md` §18-A が B9 を実装済みに数えているが、`画面設計.md` §2-4-2 が既に「誤り」と
  明記して 🔴 としている。④v13 §5.9.5 が「⚙️ 管理」へ束ねる対象として挙げる5項目に、ナレッジ登録の導線が
  列挙されていない（同節は「空振りの禁止」も定めている）。
- 採用した内容: **正本 v13 §5.7.5 の後発注記（2026-08-16）を採用して進行**（外部リンクのみを実装する）。
  ①は食い違うのが「その行の中身がフォームか外部リンクか」だけで、**表示ロール（`core_member`／`admin`）は
  §5.9.1 と §5.7.1 で一致**しており、認可の値域・RLS・判定キーに未確定部分は生じない。
  ④の「⚙️ 管理」への再編は WBS `2-6`（管理系メニューの到達性（ナビ情報設計）／ Issue #92）の範囲であり、
  本タスクでは `navigation.ts` の既存 `knowledgeForm` 行（`/staff/knowledge`・`STAFF_ONLY`）をそのまま使う。
- 派生文書側の訂正要否: ①§5.9.1 の行名を「ナレッジ登録（外部リンク）」へ改めるか、②`画面設計.md` §2 C5 の
  対応WBS を `9-4` → `9-2` へ直すか、③`WBS_Phase1.md` §18-A の B9 の計上を直すか。いずれもオーナー判断待ち
  （自律ループは `docs/spec/` を編集しない ／ 設計 §8.2・CLAUDE.md §7.0.1）。
- 関連ファイル: v13 §5.9.1、v13 §5.7.5、v13 §9 #31、`docs/spec/detailed-design/画面設計.md`、
  `docs/spec/WBS_Phase1.md`、Issue #94

---

## [2026-09-20] WBS `2-6`（管理系メニューの到達性）の範囲 — §5.9.1 の表に無い3画面の行を起こすか（v13 §5.9.1・§5.9.5 ／ §9 #54）
- ステータス: 未回答
- 優先度: 高（Issue #92 ／ WBS `2-6` をブロック。`2-6` は Tier A の未着手 11件のうちの1件）
- 背景: Issue #92（WBS `2-6` 管理系メニューの到達性（ナビ情報設計））は `wbs-to-issue.yml` の自動起票のため
  **完了条件がプレースホルダのまま**である。根拠節は実在し内容も一致する（v13 §5.9.1 の表示マトリクス、
  §5.9.5 の到達性要件＝ナビ7項目上限・「⚙️ 管理」へのグルーピング・横スクロール依存の禁止・入口の重複・空振りの禁止）。
  依存の WBS `2-3`（ロール別UI表示制御（フロント））は 2026-09-16 に完了しており、ナビ定義の置き場
  （`src/lib/auth/navigation.ts` の `AREAS`＝14領域）も既にある。現状の `RoleNav` は `visibleAreasFor()` の
  戻り値を**フラットに並べている**ため、`admin` では14項目が平置きになり §5.9.5 の「7項目まで」に違反している。
  ここまでは §5.9.5 の再編だけで解消できる。
  **止めた理由は、§5.9.5 が「⚙️ 管理」の中身として挙げる5項目のうち2つが §5.9.1 の表に行として存在しないこと。**
  ①**今日のサマリー** — §5.9.1 に行が無い（`画面設計.md` C1 `dashboard` として実在）。
  ②**顧客管理** — §5.9.1 では独立行ではなく「管理者PCダッシュボード（顧客管理・一括精算）」に内包され **admin のみ**。
  一方 `画面設計.md` C9（顧客管理画面（伝票編集・遡及修正））は **admin/core_member** としており、**両者が食い違う**。
  さらに ③**朝会・議事録**の行も §5.9.1 に無い（§6 権限マトリクス L2106 にはある）ことが、
  本ファイルの「[2026-09-13] WBS `4-1`（朝会テキスト投入UI）の完了条件と、`4-2` との範囲の切れ目」⑦に
  既に記録され、**`2-3`／`2-6` で §5.9.1 の表ごと行を起こす必要がある**とされている。
  `navigation.ts` の `AREAS` は `canAccessPath()` 経由で**サーバサイド認可の判断にも使われている**ため、
  行を足して表示ロールを決めることは §4.1 の「認可・ロール判定」にあたる。加えて §5.9.5 の「空振りの禁止」は
  ナビ定義と §5.9.1 の表を**同一のロール定義から生成する**ことを求めており、行の追加は
  **正本 v13 の改訂を伴う**が、自律ループは `docs/spec/` を編集できない（設計 §8.2 ／ CLAUDE.md §7.0.1）。
- 選択肢:
  - A. **`2-6` を「既存14領域の階層化のみ」に限定する。** 新規行は起こさず、各領域の表示ロールを1つも変えない。
    今日のサマリー・顧客管理は `adminDashboard`（`/admin`）配下の画面として扱い、朝会・議事録の行起こしは
    正本改訂を伴う別パッケージへ切り出す
  - B. **階層化 ＋ 新規3行（朝会・議事録／今日のサマリー／顧客管理）を追加する。** 表示ロールは
    朝会・議事録＝§6 権限マトリクスに合わせ、今日のサマリー・顧客管理＝**admin のみ**（現行 `adminDashboard` を踏襲）
  - C. **階層化 ＋ 新規3行を追加し、顧客管理は `画面設計.md` C9 に合わせて admin ＋ core_member とする。**
    §5.9.1 と C9 の食い違いを C9 側へ寄せて解消する
- 推奨: A（理由: §5.9.5 の主眼である「7項目上限」「横スクロール依存の廃止」「運営専用の『⚙️ 管理』へのグルーピング」は
  既存14領域の再編だけで達成でき、**認可の値域を1つも変えずに済む**。B・C は §5.9.1 の表へ行を足す＝正本 v13 の改訂を伴い、
  自律ループの編集範囲外である。また顧客管理のロールが admin か admin＋core_member かは §5.9.1 と C9 の食い違いであり、
  ナビ情報設計とは独立に解くべき論点で、`2-6` に巻き込むと範囲が認可設計まで広がる）
- 関連ファイル: v13 §5.9.1（L1623-1647）・§5.9.5（L1691-1707）・§9 #54（L2562）、
  `docs/spec/detailed-design/画面設計.md` C1・C9、`docs/spec/WBS_Phase1.md` L231（`2-6`）・L228（`2-3`）、
  `src/lib/auth/navigation.ts`、`src/components/nav/RoleNav.tsx`、`tests/role-navigation.test.ts`、Issue #92

---
## [2026-09-20] WBS `3-3`（予約キャンセル・ノーショー処理）の対象テーブル `check_ins` が未作成（v13 §5.2.2 ／ §9 #27）
- ステータス: 未回答
- 優先度: 高（Issue #93 ／ WBS `3-3`（予約キャンセル・ノーショー処理）のタスク定義を止めている。
  同じ `check_ins` 不在を理由に WBS `3-8`（宿泊枠の残数算出＋カレンダー）も止まっており、論点の構図が同一）
- 背景: v13 §5.2.2 は「予約の論理削除（`cancelled_at` ＋ `cancel_reason` ＋ 操作者ID）」と
  「紐づく `room_assignments` の終了による部屋の解放」を定めており、v13 §7 に追加列4本
  （`cancelled_at` / `cancel_reason_type` / `cancel_reason` / `cancelled_by`）が明記されている。
  しかし**キャンセル対象の「予約」の実体は `check_ins`**（`API設計.md` §2-2・§3-4 で
  `POST /api/reservations`・`POST /api/public/reservations` の対象テーブルが `check_ins`、
  `reservation_source` で入口を区別すると規定）であり、この `check_ins` が
  `supabase/migrations/` に存在しない（最新は `0101` まで。`0006_rooms_and_assignments.sql` の
  コメントに「`check_ins` は WBS `3-2`（チェックイン／チェックアウト操作）の成果物であり、まだ存在しない」と明記）。
  加えて `DB物理設計.md` §3-11 は `check_ins` を「Vault 側 `01_schema.sql` で実装済み」とし、
  同 §3-12 の注記では**列名を仮定していると自認**している。つまり
  「追加列4本を足す先のテーブル定義」がリポジトリ内に無く、列の現状も確定できない。
  一方 WBS `3-3` の依存欄は `3-1`（部屋台帳・部屋割当）のみで、`3-2` への依存が記載されていない。
  Issue #93 の完了条件・スコープ外はいずれも自動起票の雛形（プレースホルダ）のまま。
- 選択肢:
  - A. **`3-3` を保留し、先に `3-2`（チェックイン／チェックアウト操作）で `check_ins` を作る。**
    WBS `3-3` の依存を `3-1` → `3-1, 3-2` へ改め、Issue #93 は依存待ちとして据え置く
  - B. `3-3` のスコープを**顧客管理画面のキャンセルモーダル UI（モック実在）までに限定**し、
    DDL・API・RLS は `3-2` 完了後の別 Issue に分ける
  - C. `3-3` の中で `check_ins` の**最小スキーマも併せて作る**（`3-2` は操作UIのみを担当する形へ狭める）
- 推奨: A（理由: `0006_rooms_and_assignments.sql` のコメントが 2026-09-15 のオーナー決定として
  「`check_ins` を参照するものは `3-2` へ送る」という境界を明示的に引いており、C はその決定を覆す。
  B は UI だけが先に入り、論理削除の実体が無いまま「`3-3` 完了」と記録される状態を作る。
  また A は同一構図の `3-8` に対する推奨と一貫しており、`3-2` を1本解けば両方が動き出す）
- 関連ファイル: v13 §5.2.2（L422-441）、v13 §7「★ 予約キャンセル・ノーショーに伴う追加項目」（L2367-2371）、
  v13 §6 権限マトリクス（L2179）、`docs/spec/detailed-design/API設計.md` §2-2・§3-4、
  `docs/spec/detailed-design/DB物理設計.md` §3-11・§3-12、
  `supabase/migrations/0006_rooms_and_assignments.sql`、`docs/spec/WBS_Phase1.md` L241、Issue #93
- **追記（2026-09-25 ／ Issue #174 のタスク定義時）**: 本論点の**前提は解消している**。
  `supabase/migrations/0014_check_ins_and_accommodation_types.sql`（`check_ins` のスキーマ・制約・RLS・GRANT）が
  `check_ins` を作成し、キャンセル4列（`cancelled_at` / `cancel_reason_type` / `cancel_reason` / `cancelled_by`）、
  `chk_check_ins_cancel_complete`（理由なし取消を DDL で禁止）、UPDATE を staff に限る RLS、
  DELETE ポリシー無し（＝全拒否）まで含んでいる。推奨 A（先に `3-2` で `check_ins` を作る）の内容が
  事実として実現された状態であり、**本項目は WBS `3-3`（予約キャンセル・ノーショー処理）を
  もうブロックしない**。ステータスは選択肢の採否がオーナーの判断領域であるため書き換えていない。

## [2026-09-20] WBS `1-6`（pgvector有効化・RAG基盤）の残作業の範囲と、`9-1`（アプリ内の横断セマンティック検索）との切れ目（v13 §9 #31 ／ `CONSOLIDATED_DECISIONS.md` §17-6）
- ステータス: **回答済み（2026-09-25 ／ 選択肢 A を採用）**。`WBS_Phase1.md` の `1-6` 行に
  「DB 層を閉じた（2026-09-25 ／ Issue #89 ／ 決定 A）」として記録され、`0102_rag_search_scope_and_pii.sql`
  （`rag.search_knowledge()` ／ `rag.redact_contact_info()` ／ `rag.scan_for_index()`）と
  `tests/db/rag-search-scope.test.ts` で実装済み。Issue #89 はクローズ済み。
  ⚠️ ただし `1-6` は 95%・🟨 のまま残っており、**残 5% の実体が未定義**である
  （→ 本ファイル「[2026-09-25] WBS `1-6`…は決定 A 反映後に何が残っているか（Issue #173）」）
- 優先度: 高（Issue #89 ／ WBS `1-6` をブロック。`9-1` は `1-6` 依存のため連鎖する）
- 背景: Issue #89（WBS `1-6`）は `wbs-to-issue.yml` の自動起票のため**完了条件がプレースホルダのまま**である。
  基盤 DDL は 2026-09-20 に dev へ適用済み（`0100_rag_pgvector_knowledge_chunks.sql`＋`0013_vector_extension_schema.sql`＋
  `0101_revoke_service_role_excess_privileges.sql` ／ PR #82・PR #88）で、`WBS_Phase1.md` の実装進捗も **80%** である。
  残作業は `CONSOLIDATED_DECISIONS.md` §17-6 の **#5 検索時スコープ**（LINE 経路で個人紐付きチャンクを除外）と
  **#6 PII 検出の範囲拡張**（`rag.redact_known_names()` は現状**既知氏名のみ**）の2件だが、
  **`WBS_Phase1.md` の `1-6` 行と `9-1` 行が、同じ2件をどちらも自分の残作業として書いている**（L214・L317）。
  どちらのパッケージで作るかが決まらないと、Issue #89 の完了条件を書けない。
- なぜ導出で進めないか: 残作業2件は**個人情報の出力制御そのもの**であり（v13 §9 #31 改訂③・§17-5）、
  完了条件の書き方がそのまま「どこまで PII を検出すれば合格か」というセキュリティ境界の定義になる。
  PM エージェントの導出（`origin: "derived"`）は**誰の承認も経ずに受入基準になる**ため、高リスク範囲では行わない
  （`docs/自律開発ループ設計.md` §10.1.3 の条件3・高リスク時は停止）。
- 選択肢:
  - **A. `1-6` を DB 層に閉じる。** 検索時スコープ（LINE 経路の除外を RLS／ビュー／検索関数として DB 内に固定）と
    PII 検出の範囲拡張（`rag.redact_known_names()` を住所・電話・メール・金額・`member_notes` へ広げる）を
    **`1-6`＝Issue #89 の範囲**とし、`9-1` はその上で動くアプリ側の検索機能に限る
  - B. Issue #89 は **PII 検出の範囲拡張のみ**を担当し、検索時スコープは `9-1` へ送る
  - C. Issue #89 は基盤適用済み（`0100`・`0013`・`0101`・受入32件）をもって**完了としてクローズ**し、
    残作業2件はすべて `9-1` の Issue で扱う
- 推奨: **A**（理由: §16-3・§17-5 が「判定軸はチャネル」「単一DB内の RLS／ビューとして実装し、呼び出し側に委ねない」と
  定めており、`QUESTIONS.md` 2026-09-03 起票分の **R-b（フィルタの実装漏れ＝全開放）** を構造的に消すには
  **DB 層＝基盤パッケージ**に置くのが整合する。`9-1` の定義文は「クエスト起票・評価のための**内部検索**」であって
  アプリ機能側であり、ガードを `9-1` に置くと「フィルタ無しで検索する経路」を作れてしまう）
- ⚠️ 非ブロックの派生論点（本件の回答後に別途確定が必要）: **「危うい情報」の項目定義**。
  2026-09-03 起票分の叩き台（住所・電話・メール・金額・`member_notes`）はあるが、**確定していない**。
  A を採る場合、Issue #89 の完了条件はこの定義に直結する
- 関連ファイル: `docs/spec/WBS_Phase1.md` `1-6`（L214）・`9-1`（L317）・§19-2b、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §17-5・§17-6、`supabase/migrations/0100_rag_pgvector_knowledge_chunks.sql`、
  `tests/db/rag-pgvector.test.ts`、Issue #89
## [2026-09-20] WBS `4-2`（議事録の構造化 ＋ クエスト候補起案）の完了条件と、`4-3` との範囲の切れ目（v13 §5.1 ②③・§8 ／ §9 #63）
- ステータス: 未回答
- 優先度: 高（Issue #90 ／ WBS `4-2` をブロック。`4-3`（クエスト候補プレビュー・ワンタップ起案）は `4-2` に依存するため連鎖する）
- 背景: Issue #90 は `wbs-to-issue.yml` による自動起票で、**完了条件が雛形のプレースホルダのまま**である。
  Issue が挙げる根拠節は **v13 §8（非機能要件）のみ**で、§8 が `4-2` について定めているのは
  性能要件1行（「15分程度の朝会音声に対し、**録音終了から**文字起こし・議事録生成・クエスト抽出完了までを
  60秒以内（バックグラウンド処理）」）だけである。しかもこの起点は **2026-09-05 決定（§9 #63 ／ アプリは音声を扱わない）で
  「テキスト投入から60秒以内」へ要改訂とされたまま未反映**である（`WBS_Phase1.md` 4-2 の⚠️・`CONSOLIDATED_DECISIONS.md` 反映先欄）。
  機能面の実体は **§5.1 ②③**（構造化議事録の生成／**②と同一のAPI呼び出しで**クエスト候補JSONを同時出力）にあるが、
  §5.1 の本文もまた**録音前提のまま未改訂**である（§9 #63 が明記）。したがって**節番号だけでは `4-2` の範囲を確定できない**。
  ⚠️ **2026-09-19 の運用改訂（完了条件が空欄でも節番号が引けるなら導出して進む）を適用しても、本件は停止である。**
  導出すると **PII-A の `morning_meetings.transcript_text`（会話の全文＝個人の発言 ／ `DB物理設計.md` §6-6b #21）を
  外部AI（Gemini）へ送信する経路**と、**PII-A テーブルへの列追加（`extracted_quest_candidates`）**を
  PM が独断で確定することになり、設計 §10.1.3「導出すると高リスクの範囲（個人情報）に踏み込む場合は停止」に該当する。
- 選択肢:
  - A. `4-2` は、保存済み `transcript_text` を **1回の Gemini 呼び出し**へ投入し、
    **構造化議事録（`summary_text`）とクエスト候補JSON の両方**を生成して永続化するところまで
    （`extracted_quest_candidates` 列の追加 ＋ Cloud Tasks による非同期実行 ＋ 生成結果の保存 ＋ 失敗時の再試行）。
    候補のプレビュー・補正・クエストボードへの公開は `4-3`
  - B. `4-2` は**構造化議事録の生成（`summary_text` を埋める）まで**に限り、クエスト候補の抽出を `4-3` 側へ寄せる
  - C. `4-2` の範囲は A と同じだが、**非同期化を行わず** Server Action 内の同期呼び出しで完結させ、
    Cloud Tasks への載せ替えは後続の非機能改善として別パッケージにする
- 推奨: A（理由: ①v13 §5.1 ③が「②と**同一のAPI呼び出し**で、議事録と併せてクエスト候補JSONを同時出力」と
  明記しており、B は正本が1回と定める呼び出しを2パッケージへ割る。②非機能 §8 も
  「文字起こし・議事録生成・クエスト抽出完了まで」を**1つの60秒の枠**として、かつ**バックグラウンド処理**と
  定めているため、C（同期）は §8 の担保を空にする。③`4-3` は規模 S・内容も「抽出候補の補正・承認、
  クエストボードへの即時反映」であり、抽出本体を足すと規模が破綻する。④非同期ジョブ実行基盤は
  2026-09-10 に Cloud Tasks ＋ Cloud Scheduler で確定済み、Gemini のテキスト用クライアント基盤も
  `1-5` 完了済み（`src/lib/ai/gemini.ts` が実在）のため、A でも新たな未決は生じない）
- あわせて確定してほしい点（A を採る場合に `4-2` の完了条件へ直結する）:
  1. **PII-A テキストの外部送信の是非**: 2026-08-22／2026-09-05 決定は朝会の内容を Gemini へ渡す前提だが、
     テキスト化後の**氏名を含む本文をそのまま送ってよいか**（マスキング・匿名化の要否）は正本に記述が無い
  2. **性能要件 P3 の起点**: 「録音終了から」→「**テキスト投入から**60秒以内」で受入テストを書いてよいか（v13 §8 未改訂）
  3. **生成結果の保存先**: v13 §7「★ 朝会・議事録データ」は「抽出クエスト案JSONリスト」を定めるが、
     `supabase/migrations/0011_morning_meetings.sql` に該当列は無い。`4-2` で列を追加してよいか
- 申し送り（非ブロック・本件の回答後に実装側で処理する）:
  `detailed-design/API設計.md` §2-3 の `POST /api/morning-meetings` が「録音終了→音声アップロード起点」・
  **`admin` 限定**のままで、正本 v13 §9 #63・§6（コアメンバーも〇）と食い違う。
  CLAUDE.md §1.1 により正本が勝つ（Issue #23 の申し送りと同一の箇所）
- 関連ファイル: v13 §5.1 ②③・§7「★ 朝会・議事録データ」・§8・§9 #63、
  `docs/spec/WBS_Phase1.md` 4-1・4-2・4-3、`docs/spec/CONSOLIDATED_DECISIONS.md`（2026-09-05 決定群 ／ §19-1）、
  `docs/spec/detailed-design/DB物理設計.md` §3-4・§6-6b #21、`supabase/migrations/0011_morning_meetings.sql`、Issue #90
- 📌 **2026-09-22 実装メモ（本論点の回答を待たずに進めた範囲と、待っている範囲）**:
  オーナー指示「WBS 上進められる箇所はすべて実装し、高リスクの判断が必要な場合のみ判断をあおぐ」に従い、
  **取り消せる範囲だけ**を実装した（CLAUDE.md §7.0）。
  - **実装した**: 保存済み `transcript_text` を **1回の Gemini 呼び出し**へ投入し、構造化議事録・
    クエスト候補・ナレッジ候補を同時生成して `summary_text` ／ `extracted_quest_candidates` へ保存する
    Server Action（`src/app/admin/morning-meetings/structuring-actions.ts`）と、候補の補正・公開・却下
    （WBS `4-3`）。`0020` が用意済みの列へ書くだけであり、**スキーマ変更は伴わない**
  - **選択肢は C を採った（同期実行）**: 推奨 A（Cloud Tasks による非同期）は **GCP 側のキュー作成が
    オーナー作業として未了**（WBS `1-4` の残・`scripts/gcp/setup-media-infra.sh`）であり、待つと `4-3` まで
    連鎖して止まる。同期版は Server Action 1本であり、Cloud Tasks へ載せ替えるときに**呼び出し口ごと差し替わる**
    （載せ替えが手戻りにならない形で切ってある）。⚠️ **非機能 P3「60秒以内・バックグラウンド処理」の
    後半は未達**であり、これは `4-2` の完了条件として残る
  - ⛔ ~~待っている（オーナー判断）~~ → ✅ **回答済み（2026-09-24・オーナー確定）**: 上記「あわせて確定してほしい点」の
    **1（PII-A 本文の外部送信の是非・マスキングの要否）＝マスキング不要。現状の実装（本文をそのまま
    Gemini へ送る）を正式な恒久仕様として確定する。** 2（P3 の起点）・3（保存先＝`0020` で列追加済み）は
    従来どおり実装側で処理済み。**本論点はこれで全項目が解消し、`4-2` は完了として扱える**（Issue #90 の
    `auto:blocked` は解除してよい）。

### ✅ 回答（2026-09-24・オーナー確定）

朝会の議事録・クエスト候補・ナレッジ候補の抽出処理において、**個人の発言を含む本文（`morning_meetings.transcript_text`）をマスキングせず Gemini API へ送信することを正式に許可する。** 上記「実装メモ」で先行実装済みの挙動（マスキングなし）がそのまま恒久仕様になる。追加の送信前処理は不要。


## [2026-09-20] WBS `12-1`（街人登録モーダル）の完了条件と、`12-2` との範囲の切れ目（v13 §5.10.1〜§5.10.3・§5.10.6）
- ステータス: 未回答
- 優先度: 高（Issue #172 ／ WBS `12-1` をブロック。`12-2`・`12-3` は `12-1` に依存するため連鎖する）
- 🆕 2026-09-25 追記（Issue #172 の計画時に判明）: **実装は選択肢 B に相当する形で既に入っている。**
  Issue #87（同じ WBS `12-1`）は 2026-09-25 にクローズ済みで、`src/components/membership/RegistrationModal.tsx`
  ＋ `src/components/quests/QuestBoardWithRegistration.tsx`、`supabase/migrations/0037_membership_applications.sql`
  ＋ `0038_membership_approval.sql` が存在し、特典の数値は `membership_plans`（`is_current_signup_plan = true`）
  から引いている（`src/lib/membership/registration.ts`・`store.ts`）。つまり**申請確定・`membership_applications`
  の DDL/RLS まで `12-1` に入った**＝B であり、本項目の推奨 A（申請確定は `12-2` へ）とは異なる。
  一方 WBS `12-1` の行と Issue #172 本文は「**決定 A**」と記載しており、**採用案の呼び名が記録どうしで食い違う**
  （「Step 1 の入力を保存しない」という A の一部だけを指して A と呼んでいる可能性が高い）。
  さらに WBS `12-1` は 🟨・実装 90% のままであるため、`sweep.py` が**同じパッケージを Issue #172 として再払い出し**した。
  したがって決めていただきたいのは次の1点である。
  - A2. **`12-1` は完了として確定する** — Issue #172 を #87 の重複としてクローズし、WBS `12-1` を ✅／実装 100% へ
    更新、本項目を「回答済み（採用＝申請確定まで `12-1` に含める）」へ落とす（**推奨**）
  - B2. **残 10% の実体を先に定義する** — 未達点（例: モック `prototype_v15.html` の4ステップ差分への追随）を
    切り出して別 Issue にし、#172 はクローズする
  - C2. **Issue #172 を新規実装として進める** — 既に受入テスト31件が通っている認可・金額・個人情報の経路を
    作り直すことになるため、退行リスクに見合わない
  - 推奨: **A2**（理由: 上記の完了条件15項目は現行コードが満たしており、`12-1` を open に保つ実益が無い。
    設計 90% の残りは `prototype_v15.html` が4ステップ構成である差分＝`18-2` 画面ID対応表の担当と
    Issue #172 のスコープ外欄が明記しており、`12-1` 側の未達ではない）
- 背景: Issue #87 は `wbs-to-issue.yml` による自動起票で、**完了条件が雛形のプレースホルダのまま**である。
  根拠節は実在し内容も一致する（§5.10.1 の画面遷移、§5.10.2 Step 1 の入力欄＝ニックネーム／メールアドレス・
  電話番号／誕生年月、§5.10.3 Step 2 の再確認と申請確定、§5.10.6 の施錠カードのタップでモーダル起動）。
  ⚠️ **2026-09-19 の運用改訂（完了条件が空欄でも節番号が引けるなら導出して進む）を適用しても、本件は停止である。**
  Step 1 の入力欄は**個人情報の入出力**にあたり、Step 2 の「申請確定」は
  `membership_applications`（PII-B ／ `DB物理設計.md` §3-5・§6-1 #16）への **INSERT ＋ DBマイグレーション**を伴う。
  導出が高リスクの範囲（個人情報・DBマイグレーション）へ踏み込むため、**範囲の切れ目はオーナーの判断**に委ねる
  （設計 §3.4 判断順序1 ／ `5-1` の先例＝本ファイル「[2026-09-19] WBS `5-1`…」と同じ形）。
  なお `5-1`（Issue #53）で施錠表示・解放件数バナー・`opensRegistrationModal()`（施錠カードだけが
  モーダルを起動する述語）・サーバ側のゲスト受注申請拒否は**実装済み**であり、`12-1` に残るのは
  **モーダル本体**である。
- 選択肢:
  - A. `12-1` は**モーダル UI（Step 1・Step 2）まで**とする。入力値は画面状態に保持するだけで永続化せず、
    `[ 承認して申請する ]` の押下から先（申請レコード作成・二重申請防止・管理者への通知）は `12-2` に置く
  - B. `12-1` に**申請確定まで**含める（A ＋ `membership_applications` の DDL・RLS と `申込中` レコードの作成、
    §5.10.3 の二重申請防止）。`12-2` は決済手段の選択・入金確認・承認からとする
  - C. `12-1` を**入力欄を持たない告知モーダル**（特典の提示と「申請へ進む」導線のみ）に絞り、
    入力欄ごと `12-2` へ寄せる
- 推奨: A（理由: WBS の `12-2` が「**申請**〜決済〜承認フロー」を担い、依存に `7-3`（精算QR基盤）を持つ。
  申請レコードは QR トークン列（`qr_token_hash`）を同じテーブルに抱えるため、`7-3` と同じ変更で作るほうが
  スキーマの切り直しが起きない。`membership_applications` は**マイグレーション未作成**（`0011` まで存在せず）で、
  B は `12-1` を DB マイグレーション＋PII-B の RLS 設計に広げる。C は §5.10.2 が Step 1 を「インフォメーション
  **＆ 入力**」と定めており、正本の Step 1 を分割することになる）
- 補足（A を採る場合に残る小さな論点）: Step 1 の特典表示（宿泊券4枚・キャッシュバック 5,000 コイン）は
  `membership_plans` 参照・**直書き禁止**（`DB物理設計.md` §3 冒頭4・v13 §9 #15）だが、同テーブルも未作成である。
  A なら「マスタ未整備のため表示値の取得箇所を1モジュールに集約し、`12-3` でマスタ参照へ差し替える」形で
  逃がせる想定。これでよいかも併せてご確認いただきたい
- 関連ファイル: v13 §5.10.1・§5.10.2・§5.10.3・§5.10.6、`docs/spec/WBS_Phase1.md` 12-1・12-2・12-3、
  `docs/spec/detailed-design/DB物理設計.md` §3-5・§6-1 #16、Issue #87、Issue #53
## [2026-09-20] WBS `10-2`（名寄せロジック）の完了条件と、どの名寄せ導線までを含めるか（v13 §5.8.3 ／ §9 #2・#16）
- ステータス: 未回答
- 優先度: 高（Issue #86 ／ WBS `10-2` をブロック。`10-3`（再訪アラート）・`12-4`（初回キャッシュバック判定）が
  `10-2` に依存するため連鎖する）
- 背景: Issue #86 は `wbs-to-issue.yml` による自動起票で、**完了条件が雛形のプレースホルダのまま**である。
  根拠節は実在し内容も一致する（v13 §5.8.3「STEP 2：予約・初回アクセス時の自動名寄せ」＝照合キーは
  `contact_info`（メール／電話）を第1優先・次いで氏名＋誕生年月、一致時は宿泊チケット・Uii残高・XP・
  街人ステータスを引き継いで `account_status` を `active` へ、§9 #2・#16 ＝ 照合キーを持たない105名は
  事前整備せず次回チェックイン時に現地で電話番号入力）。
  **誤名寄せ防止の必須要件も §5.8.3 で確定済み**であり、この点は論点ではない（複数候補ヒット時は自動連携せず
  運営承認キューへ／初回紐付け時は招待コードまたはメール・SMSのワンタイム認証による本人確認を必須／
  成立・解除は監査ログに記録）。
  未確定なのは**どこまでを `10-2` で作るか**である。v13 は名寄せの起点を**3つ**持つ
  （①予約時点＝§5.2.3・§5.4.1 のメールOTP ／ ②アプリ初回アクセス・本登録時＝§5.2.6 ／
  ③チェックイン時の現地入力＝§5.8.3 の105名）。このうち**②の導線だけが実装済み**であり
  （WBS `2-1b` 完了 ／ #39）、①は `3-5b`（L・実装0%）、③は `3-2`（M・実装0%）にそれぞれ別パッケージとして存在する。
  名寄せの成立は**他人の宿泊チケット・Uii残高・XP の引き継ぎ**を意味し、`account_status` を `active` へ
  遷移させる**認可の起点**でもあるため、PM が完了条件を導出すると「範囲の切れ目」を誰の承認も経ずに
  固定してしまう（設計 §10.1.3）。
- 選択肢:
  - A. 照合基盤のみ（照合エンジン＝`member_identifiers` 第1優先 → `member_profiles_private` の氏名＋
    `birth_ym`、複数候補時の運営承認キュー、名寄せログ（§7「会員データインポート・名寄せログ」）の
    DDL ＋ RLS、`pre_registered → active` 遷移のサーバ関数。呼び出し側の導線は各パッケージで接続）
  - B. A ＋ ②アプリ初回アクセス導線（`2-1b` のメールOTP ログイン後に自動照合し、
    「〇〇さん、おかえりなさい！」と `[ このデータで連携して開始する ]` まで）
  - C. A ＋ 3導線すべて（①`/reserve` の予約時点照合・③チェックイン画面の「連絡先未登録」バッジと
    現地電話番号入力も本パッケージで実装）
- 推奨: B（理由: `2-1b` が完了済みで**メールOTP による本人確認が既に成立している唯一の導線**であり、
  §5.8.3 が必須とする「初回紐付け時の本人確認」を新規基盤なしで満たせる。A 単体では呼び出し側が
  存在しないため「〜が〜できる」の形の**検証可能な完了条件を書けない**。C は `3-5b`（Phase 1 最大の
  未実装パッケージ・規模 L）と `3-2`（M）を丸ごと飲み込み、規模 M の `10-2` が破綻する）
- ⚠️ 併記（本問とは別の論点・回答不要なら現状維持で可）: WBS `10-2` の依存は 2026-09-19 に
  `10-1` → `2-1` へ付け替えられているが、WBS 本文に「**この付け替えが妥当かはオーナー確認が要る**」と
  残っている。`10-1`（一括インポート）は §5.8.2 が 2026-09-19 に「実装しない」と決着しているため、
  付け替え自体は整合していると見ている。
- ⚠️ 正本側の追随漏れ（実装判断には影響しない）: §5.8.3 の誤名寄せ防止ブロック末尾に
  「連絡先を持たないゲストの同定方式は §9 #2 の未決事項として継続」と残っているが、
  **§9 #2 は 2026-08-14 に決定済み**（取り消し線付き）である。
- 関連ファイル: v13 §5.8.1・§5.8.3・§7（会員データインポート・名寄せログ）・§9 #2・#16、
  `docs/spec/WBS_Phase1.md` L316（`10-2`）・L203（`2-1b`）・L218（`3-2`）・L222（`3-5b`）、
  `docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.3・§6.3、Issue #86

## ✅ [2026-09-20] WBS `5-2`（受注申請・運営審査・実行指示）の完了条件と、`5-3`・`5-6` との範囲の切れ目（v13 §5.3-2・§5.3-3 ／ §6）
- ステータス: ✅ **回答済み・クローズ（2026-09-25 オーナー確定：B＝サーバ層 ＋ 画面まで）**
- 回答: **B**（推奨どおり）。`5-2` の範囲は「サーバ層 ＋ 画面（参加申請ボタン・指示出しモーダル）」で、
  `指示済み` で閉じる。完了報告（`5-3` 完了報告（Before/After写真・作業時間））・
  二段階承認（`5-6` クエスト承認の二段階化）は別パッケージのまま。権限は v13 §6 権限マトリクスどおり
  `admin` / `core_member` のみ（`role` で判定し `member_type` は使わない ／ CLAUDE.md §4.1）。
  回答の所在: Issue #84（WBS 5-2 受注申請・運営審査・実行指示）2026-09-25 のオーナーコメント。
  この回答に基づく完了条件は Issue #167（WBS 5-2 の残作業）の本文へ転記済み。
- 優先度: 高（Issue #84 ／ WBS `5-2` をブロック。`5-3`・`5-6` は `5-2` に依存するため連鎖する）
- 背景: Issue #84 は `wbs-to-issue.yml` による自動起票で、**完了条件が雛形のプレースホルダのまま**である。
  根拠節は実在し内容も一致する（v13 §5.3-2「受注申請 ＆ ステータス管理」＝`申請中` の発生・`execution_mode`・
  `required_certification` の安全ゲート、§5.3-3「運営による審査・指示出し」＝運営が日時・場所・内容を指示して
  マッチング成立、§5.3.2 の遷移 `申請中 → 指示済み`）。
  **権限は §6 権限マトリクス「クエスト審査・実行指示出し（申請中処理）」で `admin`／`core_member` のみ可と
  確定済み**であり、この点は論点ではない（`member_type` ではなく `role` で判定する／CLAUDE.md §4.1）。
  未確定なのは**どこまでを `5-2` で作るか**である。`5-2` は新規テーブル `quest_applications`
  （`0007_quests_schema.sql` L12 で「→ WBS 5-2」として意図的に未作成）とその RLS を伴い、
  **認可判定・DBマイグレーションに該当する高リスク**のため、PM が完了条件を導出すると
  「範囲の切れ目」を誰の承認も経ずに固定してしまう（設計 §10.1.3）。
- 選択肢:
  - A. サーバ層まで（`quest_applications` テーブル＋RLS、受注申請 API／Server Action、
    `required_certification` × `members.certifications` の安全ゲート、`申請中 → 指示済み` の指示出し API。画面は別 Issue）
  - B. A ＋ 画面（会員の「参加申請」ボタンと、運営の指示出しモーダル。`prototype` に実在）まで
  - C. A のうち安全ゲート（`required_certification` 照合）を別パッケージへ切り出し、
    `申請中` ステータスの管理と指示出しのみ
- 推奨: B（理由: WBS `5-2` の内容欄が「『申請中』ステータス管理、運営による審査・指示」で、
  ステータス欄に「モックに指示出しモーダル実在」と書かれており、**画面まで含めて初めて
  「会員が参加申請できる」「運営が指示を出して承認できる」を検証可能な完了条件にできる**。
  後続の `5-3`（完了報告）・`5-6`（承認の二段階化）が別パッケージとして明示的に切られているため、
  `指示済み` までで範囲が自然に閉じる。C は安全ゲートを後付けにすると
  **未資格者が受注申請できる状態が一時的に生まれる**ため推奨しない）
- 関連ファイル: v13 §5.3-2・§5.3-3・§5.3.2・§6、`docs/spec/WBS_Phase1.md` L246-251、
  `supabase/migrations/0007_quests_schema.sql` L12、Issue #84
## ✅ [2026-09-20] `media_assets` の列構成が正本 v13 §7 と `DB物理設計.md` §3-7 の DDL で食い違う（v13 §7 ／ §9 #57）
- ステータス: ✅ **回答済み・クローズ（2026-09-22 オーナー確定：C＝正本 §7 を全面採用）**
- 回答: **C**。正本 v13 §7「★ メディアアセット」の保持項目を全面的に採る。
  推奨していた B（不足列のうち4つだけを足す）は不採用。`DB物理設計.md` §3-7 の DDL に対し、
  次を加えた形で `supabase/migrations/0027_media_assets.sql` として実装済み（PR #119）。
  - `media_type` の値域を §3-7 の `('photo','video')` → **正本 §7 の `('image','video','pdf')`** へ
  - `content_type` / `file_size_bytes` / `usage_scene text[]` / `ai_tags text[]` を追加
  - 紐付けキー `knowledge_id` / `category_id` を追加（参照先マスタが未作成のため **FK は張らず値だけ持つ**。
    `0006` の `rooms.place_id` と同じ作法で、参照先を作る作業パッケージが ALTER を足す）
- 論点 ③（正本内部の自己矛盾）の扱い: §5.11.7「対応形式」は画像・動画のみだが、
  v13 L1274・L1315 は「写真・PDF の添付」「画像・動画・PDFは Cloud Storage へ格納」と述べている。
  決定 C は **`pdf` を持つ側へ寄せて**解消する。PDF の投入経路は §5.11.7 の全ロール向け
  アップロードUI（カメラロール／その場で撮影）ではなく**ナレッジ添付**であり、
  どの `content_type` を受け付けるかは経路ごとにアプリ側の許可リストが決める
  （`src/lib/media/upload-policy.ts`）。
- 派生文書への反映: `DB物理設計.md` §3-7 の DDL を本決定へ追随させた（CLAUDE.md §7.0.1）。
- 以下は起票時点の記録（経緯として保持）:
- ~~ステータス: 未回答~~
- 優先度: 高（Issue #85 ／ WBS `2-1c` をブロック。`2-1c` は `1-4`・`14-1`・`14-6` の依存元）
- 背景: Issue #85（WBS `2-1c` ＝ `media_assets` スキーマ ＋ RLS）の起票時に判明。WBS は
  「**仕様の追加は無い**（DDL は §3-7、RLS は §6 に定義済み）」として設計 100% としているが、
  正本 v13 §7「★ メディアアセット」（L2293）が挙げる保持項目と、`DB物理設計.md` §3-7（L444-472）の
  DDL の列構成が一致しない。差分は次の3点。
  - ① **`ai_tags[]` が DDL に無い。** §9 #57（2026-08-29 オーナー決定）と改訂履歴 v1.21.0 が
    「`media_assets` に `ai_tags[]` を追加（§5.11.3・§7）」と明記しているが、`DB物理設計.md` は
    全文に `ai_tags` の記述が無く、v1.21.0 の反映が派生文書側で漏れている。
  - ② **`contentType`・ファイルサイズ・`usage_scene[]`・`knowledge_id` / `category_id` が DDL に無い。**
  - ③ **ファイル種別の値域が違う。** 正本 §7 は `image / video / pdf` の3値、DDL は
    `CHECK (media_type IN ('photo','video'))` の2値。なお正本 §5.11.7 の「対応形式」は
    画像・動画のみ（JPEG/PNG/HEIC/WebP・MP4/MOV）であり、**pdf については正本内部で食い違っている**。
- なぜ止めたか: 本パッケージは **DBマイグレーション ＋ RLS（認可）＋ PII-B（`geo_location`・`ai_caption`）**
  で §4.1 の高リスクに該当する。設計 §3.3 は「正本 vs 派生文書の矛盾は正本優先で進む」を定めるが、
  **リスク判定が高の場合は対象外**（矛盾の事実自体を人間に見せる）と明記している。加えて正本 §7・
  `DB物理設計.md` §3-7 はいずれも「**後から列を足すと全件の再解析が必要になるため先に用意する**」と
  述べており、列の取りこぼしが後戻りコストに直結する。③は正本内部の自己矛盾のため §3.3 でも解決できない。
- 選択肢:
  - A. `DB物理設計.md` §3-7 の DDL をそのまま実装し、不足列は Phase 2 の ALTER で足す
  - B. §3-7 の DDL に、正本 §7・§9 #57 が挙げる不足列（`ai_tags text[]`・`content_type`・
    `file_size_bytes`・`usage_scene text[]`）を本パッケージで加える。`media_type` は §5.11.7 の
    対応形式に合わせて `('photo','video')` のままとし、**pdf は入れない**
  - C. 正本 §7 の記述を全面採用する（`media_type` に `pdf` を含む3値 ＋ 上記の不足列すべて ＋
    `knowledge_id` / `category_id`）
- 推奨: B（理由: `ai_tags[]` は §9 #57 の**オーナー決定そのもの**であり、派生文書の追随漏れとみなせる。
  `content_type`・`file_size_bytes` は §5.11.2 ①の署名付きURL発行時に検証する値で、孤児ファイル検出
  （§8 非機能）にも使うため先に持つ利得が大きい。一方 pdf は §5.11.7 の対応形式・§5.11.5 の上限制御と
  整合せず、正本内部の自己矛盾であるため広げない。`knowledge_id` / `category_id` はナレッジ基盤側の
  パッケージ（`1-4`・§5.7）と範囲が重なるため本パッケージでは張らない）
- 申し送り: Issue #85 の「完了条件」は `wbs-to-issue` の雛形プレースホルダのまま。本論点が決着すれば
  §3-7・§6 の PII-B テンプレートから導出可能（`origin: "derived"`）。本論点の決着のみで着手できる。
- 関連ファイル: v13 §7（L2293）・§5.11.7（L2054）・§6（L2149-2153）・§9 #57、
  `docs/spec/detailed-design/DB物理設計.md` §3-7（L444-472）・§6-1 #22（L1153）・§6-6（L1973・L2024）、
  `docs/spec/WBS_Phase1.md` L204、Issue #85
## ✅ [2026-09-20] ベクトル基盤（WBS `1-6`／`9-1`）を Phase 1 に入れるか、Phase 2 へ回すか
- ステータス: ✅ **回答済み・クローズ（2026-09-20 オーナー確定：Phase 1 に入れる）**
- 回答: **A（Phase 2 へ回す）は不採用。Phase 1 に含める。**
  同日 `0100_rag_pgvector_knowledge_chunks.sql`（pgvector 基盤）を **dev Supabase へ適用済み**（PR #82）。
  CI の退避ステップも撤去され、`tests/db/rag-pgvector.test.ts`（受入32件）で毎回検証される状態になった。
  これにより WBS の実装対象は **61件 → 63件**（`1-6`・`9-1` が加わる）。
  WBS `1-6` の 🔴 は解除し、残作業は `9-1` 側の**検索時スコープ**（LINE 経路の個人紐付き除外）と
  **PII 検出の範囲拡張**に絞られた（`CONSOLIDATED_DECISIONS.md` §17-6）。
- 以下は起票時点の記録（経緯として保持）:
- ~~ステータス: 未回答（残10日の工期配分に直結するため、9/30 を待てない）~~
- 優先度: 高
- 背景: 2026-09-11 のオーナー決定（`docs/spec/CONSOLIDATED_DECISIONS.md` §17 ／ v13 §9 #31 限定改訂・v1.26.0）で
  ベクトル基盤が Supabase pgvector へ反転し、**2026-08-16 に不要化していた WBS `1-6`（pgvector 有効化・RAG基盤）と
  `9-1`（アプリ内の横断セマンティック検索）が実質的に復活**した。しかし `WBS_Phase1.md` は §19-2b（工期影響 +5〜10人日）
  にしか反映しておらず、**§1・§9 の表は「不要化」のまま**残っていた（2026-09-20 に追随させた）。
  §19-2b 自身が「Phase 1 の 9/30 までに入れるのか、Phase 2 へ回すのかが未確定」と明記している。
- なぜ今きく必要があるか: **`1-6` は依存（`2-1`）が完了済みであり、WBS 上 🟡 のままだと
  30分ごとのスイープが自動で Issue を払い出す**（`automation/scripts/wbs_epic.py` の `classify()` は
  🟡 を `ready` と判定する）。残10日・Tier A が未着手（`3-2`・`6-1`〜`6-4`・`7-2`・`8-1` ほか）という状況で
  **スコープ未確定のパッケージへ実装枠が流れる**。~~暫定措置として WBS 上は 🔴 ブロック中にして払い出しを止めている。~~
  → **決着済みのため 🔴 は解除した**（2026-09-20）。
- 選択肢:
  - **A（推奨）: Phase 2 へ回す。** Phase 1 は「アプリはベクトル基盤を持たず、bot は Firestore のまま」で走る。
    §19-2b の +5〜10人日がまるごと 9/30 の外に出るため、**納期リスクを増やさない**。
    `0100_rag_pgvector_knowledge_chunks.sql` はリポジトリに置いたまま（CI の DB テストでは検証する）、
    dev/prod への適用は Phase 2 で行う。
  - B: Phase 1 に入れる。クエスト起票・評価でナレッジを使う機能が9月中に入るが、
    **Tier A（チェックイン・注文・会計）を圧迫する**。61パッケージ全部という 2026-09-08 のリリース定義とも別枠になる。
  - C: `1-6`（基盤＝DDL の適用まで）だけ Phase 1 に入れ、`9-1`（検索機能）は Phase 2 へ回す。
- ブロックするもの: ~~WBS `1-6`（🔴 でスイープの払い出しを停止中）・`9-1`（`1-6` 依存で連動）~~ → **解除済み**
- 関連ファイル: `docs/spec/CONSOLIDATED_DECISIONS.md` §17（とくに §17-6 の派生作業6件）、
  `docs/spec/WBS_Phase1.md` §19-2b・`1-6`・`9-1`、
  `supabase/migrations/0100_rag_pgvector_knowledge_chunks.sql`、`tests/db/rag-pgvector.test.ts`

## ✅ [2026-09-19] アップロード署名付きURLのファイルサイズ上限の具体値（v13 §5.11.5）
- ステータス: ✅ **回答済み・クローズ（2026-09-22 オーナー確定：A＝画像 20MB ／ 動画 200MB）**
- 回答: **A**（推奨どおり）。画像 20MB ／ 動画 200MB。
  値は `src/lib/media/upload-policy.ts` の `MAX_IMAGE_BYTES` / `MAX_VIDEO_BYTES` に持ち、
  署名付きURL発行時に `x-goog-content-length-range` として焼き込む。**強制はストレージ側**であり、
  クライアント側の検証には依存しない（v13 §5.11.5）。受入テストは
  `tests/media-upload-policy.test.ts` と `tests/media-signed-url.test.ts`（PR #119）。
- ⚠️ **PDF の上限は本決定に含まれない**（決定 A は画像・動画の2種のみを挙げている）。
  決定 C で `media_type` に `pdf` が入ったため、暫定で**画像と同じ 20MB** を当てている。
  PDF は §5.11.7 のアップロードUIではなくナレッジ添付の経路で入る書類であり、
  動画側（200MB）へ寄せる理由が無いため。運用で不足したら本ファイルへ別途起票すること。
- 正本への反映: v13 §5.11.5 の「ファイルサイズ上限」行に確定値を明記した（CLAUDE.md §7.0.1）。
- 以下は起票時点の記録（経緯として保持）:
- ~~ステータス: 未回答~~
- 優先度: 高（Issue #18 ／ WBS `1-4` の完了条件 B「`x-goog-content-length-range` で上限超過を
  ストレージ側で拒否する」が、数値が無いと受入テストへ変換できない）
- 背景: Issue #18（WBS `1-4`）本文の ⚠️2 として起票時から指摘され、
  「[2026-09-13] WBS `1-4` の完了条件と、`14-1` との範囲の切れ目」の**申し送り①**として
  「回答後に別途起票する」とされていたが、本日まで本ファイルへ起票されていなかった。
  v13 §5.11.5 は `x-goog-content-length-range` による**ストレージ側での上限強制**を定めるが、
  **上限の数値は正本にも詳細設計にも無い**。`非機能要件詳細.md` §1 の「50MB程度のファイルで…」は
  **性能の前提**であって上限の定義ではないため、上限値へ流用していない。
  本件は署名に焼き込むセキュリティ制御値であり、PM が推測で決めると誰の承認も経ずに固定される。
- 選択肢:
  - A. 画像 20MB ／ 動画 200MB（HEIC の原寸と 1〜2分の 4K 動画が通る水準）
  - B. 画像 10MB ／ 動画 100MB（無料枠〜低額運用を優先し、越える動画は再圧縮を求める）
  - C. 画像・動画とも一律 50MB（`非機能要件詳細.md` §1 の性能前提へ揃える）
- 推奨: A（理由: v13 §5.11.7 が対応形式に HEIC と MP4/MOV を含めており、スマートフォンで
  撮ったままの動画が拒否されると「撮る・貯める」（§5.11.3 の Phase 1 方針）が成立しない。
  ストレージ側のコストはライフサイクル（§5.11.4）と予算アラートで抑える設計になっている）
- 関連ファイル: v13 §5.11.5・§5.11.7、`docs/spec/basic-design/infra/非機能要件詳細.md` §1、Issue #18

## [2026-09-19] メディア用 GCS バケットを `line-rag-bot` と同一 GCP プロジェクトに置くか（v13 §5.11.2）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-24・オーナー決定）**
- 優先度: 中（Issue #18 ／ WBS `1-4` のバケット・サービスアカウント・予算アラートの作成先に影響する）
- 背景: Issue #18 本文の ⚠️4。`CONSOLIDATED_DECISIONS.md` §15-2 に
  「両システムが同一 GCP プロジェクトか」が**どの設計文書にも記録がない未決**として残っている。
  `1-4` では専用サービスアカウント（メディア用バケットのみ・監査ログ用バケットへの権限なし）を作るため、
  プロジェクトの分け方が IAM の設計に直結する。
- 選択肢:
  - A. 同一プロジェクトに置き、バケットとサービスアカウントで分離する
  - B. 浮遊街アプリ専用のプロジェクトを新設する
- 推奨: B（理由: `line-rag-bot` とは §9 #31 で「API 連携ゼロ」の境界を引いており、
  課金・予算アラート・IAM を跨がせない方が §5.11.2 の「権限を用途ごとに分ける」方針と一貫する）

### ✅ 回答（2026-09-24・オーナー確定）

**A（`line-rag-bot` と同一プロジェクト `gen-lang-client-0065941155` を使う）を採用する。** 新規プロジェクトは作らない。
- 理由: 新規プロジェクトの起票・請求先アカウントの紐付けの手間を避け、既存の認証・課金経路をそのまま使う
- 推奨 B（分離）の懸念点は、`scripts/gcp/setup-media-infra.sh` が**バケット単位のIAMバインディング**
  （`media-signer`＝`roles/storage.objectAdmin` を `fuyuugai-media-private` バケットにのみ付与、
  `media-finalize`＝`roles/storage.objectViewer` を同バケットにのみ付与。プロジェクト全体のロールは付けない）
  で満たしている。監査ログ用バケット（別途 GCS 保管予定）とは**サービスアカウントを分ける**運用を維持すること
- ✅ **実行済み（2026-09-24）**: `GCP_PROJECT_ID=gen-lang-client-0065941155` で
  `scripts/gcp/setup-media-infra.sh` を実行し、バケット `fuyuugai-media-private`（Tokyo region・
  Uniform bucket-level access・Public access prevention・ライフサイクル・CORS）・
  サービスアカウント2件・Cloud Tasks キュー `media-finalize`・予算アラート（月5,000円）を作成した。
  Vercel 側の `GCP_PROJECT_ID`／`GCS_BUCKET_PRIVATE`／`GCP_SERVICE_ACCOUNT_JSON` は既に Secret 型で登録済み
  （3日前）だったため追加設定は不要だった
- ⚠️ **実行時に `scripts/gcp/media-bucket-cors.json` のバグを発見・修正した**：
  `--cors-file` は配列そのものを要求するが、同ファイルは `lifecycle-file` と同じ形式
  （`{"_comment": ..., "cors": [...]}`）で包んでしまっており、`'str' object has no attribute 'items'`
  で毎回失敗していた。バケット・サービスアカウント・キューが「既に在る」状態だったのは、
  過去のセッションでこのバグに阻まれ CORS 設定だけが当たらないまま止まっていたためと推測される
- 残タスク: Cloud Function `media-object-finalize`（`functions/media-object-finalize/` にソースあり）の
  デプロイは本論点のスコープ外・未実施。`SUPABASE_SERVICE_ROLE_KEY` を Secret Manager へ登録してから
  `gcloud functions deploy` する必要がある（スクリプト末尾の案内コマンド参照）
- 関連ファイル: v13 §5.11.2、`docs/spec/CONSOLIDATED_DECISIONS.md` §15-2、Issue #18、
  `scripts/gcp/setup-media-infra.sh`、`scripts/gcp/media-bucket-cors.json`

## [2026-09-19] GCP 月額予算アラートの水準（監査ログの GCS 保管を追加した後の妥当性）（v13 §5.11.4）
- ステータス: 未回答（**ブロッカーではない**。金額はコンソール設定値であり、実装物に焼き込まれない）
- 優先度: 低（Issue #18 ／ WBS `1-4` の完了条件 D。「アラートが設定されていること」までは金額抜きで検証できる）
- 背景: Issue #18 本文の ⚠️5。v13 §5.11.4 の例示は月5,000円だが、
  `非機能要件詳細.md` §4 と `システムアーキテクチャ.md` が「監査ログの GCS 保管を追加したことで
  5,000円が妥当か再確認が必要」という ⚠️ を残している。
- 選択肢:
  - A. 例示どおり月5,000円で設定し、運用実績を見て見直す
  - B. 監査ログ分を上乗せした金額（例：月8,000円）で設定する
- 推奨: A（理由: 予算アラートは**通知であって遮断ではない**ため、低めに置いて実績で上げるほうが安全。
  超過時に止まらない GCP の課金特性は `QUESTIONS.md`「非同期ジョブの実行基盤」でも確認済み）
- 関連ファイル: v13 §5.11.4、`docs/spec/basic-design/infra/非機能要件詳細.md` §4、Issue #18

## ⚡ [2026-09-19] `quests` の RLS が、派生文書ではゲストに `guest_allowed=true` の行だけを見せる（v13 §5.10.6）
- ステータス: ✅ **回答済み（2026-09-20 オーナー）**
- 回答: **派生文書側（`DB物理設計.md`・`API設計.md`）を正本 v13 §5.10.6 に合わせて訂正した。**
  行は絞らない・列（報酬額・指示内容）だけを `v_quest_board` で絞る、という「採用した内容」のとおりで確定。
  あわせてオーナーから別途、**PostgREST 直アクセスでは列マスクが迂回できていた**（`quests` への
  `GRANT SELECT` が列を絞っていなかった）ことの指摘があり、`0012_quest_reward_column_lockdown.sql`
  で列単位GRANTへ是正した。**`core_only_reward` フラグも新設**（コア・管理者限定の詳細非開示）。
  詳細は `CONSOLIDATED_DECISIONS.md` §2「`quests` の PostgREST 直アクセスで報酬額・指示内容が
  露出していた穴を塞ぎ...」を参照。「`docs/spec/` は自律ループから編集しない」という本項の
  保留理由は、**今回はオーナーとの対話（Claudian）内での確定であり自律ループの編集ではない**ため該当しない。
- 以下は起票時点の記録（経緯として保持）:
- ~~ステータス: 未回答（正本優先で進行中・ブロッカーではない）~~
- 優先度: 低〜中（派生文書を放置すると、後続の `5-2`・`2-3` で同じ設計を読んだ実装が
  「ゲストには施錠クエストを見せない」方向へ戻り、解放件数バナー（`🔒 あと N 件…`）が成立しなくなる）
- 背景: Issue #53（WBS `5-1`）の段取り作成時に判明。正本 v13 §5.10.6（L1789）は
  `guest_allowed = false` を「**一覧には表示したうえで施錠表示**」と定めるが、
  `docs/spec/detailed-design/DB物理設計.md` L2036-2044 の `quests_select_guest` は
  `USING ( guest_allowed = true )` でゲストから**行ごと隠す**。同書 L1154 の RLS 適用表、
  および `docs/spec/detailed-design/API設計.md` L136（「`guest_allowed` でフィルタ」）も同じ向き。
- 採用した内容: 正本 v13 §5.10.6 の記述を採用して進行（`CLAUDE.md` §1.1「矛盾時は正本が勝つ」）。
  ゲストにも `guest_allowed = false` の**行は返し**、報酬額・指示内容・担当者情報の**列を返さない**
  （RLS は列を絞れないため、列の制御はビュー側で行う）。
- 派生文書側の訂正要否: `DB物理設計.md` L1154・L2036-2044 と `API設計.md` L136 を
  正本に合わせて訂正するか。オーナー判断待ち（`docs/spec/` は自律ループから編集しない）
- 関連ファイル: v13 §5.10.6、`docs/spec/detailed-design/DB物理設計.md`、
  `docs/spec/detailed-design/API設計.md`、Issue #53
## ⚡ [2026-09-19] Claude のテキスト呼び出しに `@anthropic-ai/sdk` と Claude Agent SDK のどちらを使うか（v13 §9 #45）
- ステータス: 未回答（仮決定で進行中・ブロッカーではない）
- 優先度: 低〜中（誤ると `1-5` のクライアント層1モジュールと依存1件を差し替えることになる。
  呼び出し側は共通インタフェース越しのため波及しない）
- 背景: Issue #55（WBS `1-5`）の段取り作成時に判明。2026-09-19 のオーナー決定 A は
  「Claude（**Agent SDK**）／Gemini のテキスト用クライアント基盤」という表記だが、調査で
  **Anthropic 公式が両者を別レイヤーと位置づけている**ことが確認された
  （Messages API のクライアント＝`@anthropic-ai/sdk` ／ Claude Agent SDK ＝エージェントループ・
  ツール実行・ランタイムを提供する上位レイヤー。出典: https://platform.claude.com/docs/en/cli-sdks-libraries/overview ）。
  `1-5` の成果物は「テキストプロンプトを渡して構造化された応答を1回で得る」だけで、
  エージェントループもツール実行も要らない。
  現状 `@anthropic-ai/claude-agent-sdk` は `devDependencies` にあり、利用箇所は
  `automation/orchestrator/lib/runAgent.mjs`（自律ループ専用）1ファイルのみ。
- 選択肢:
  - A. `@anthropic-ai/sdk`（Messages API）を `dependencies` に追加してアプリ本体のテキスト呼び出しに使う。
    Agent SDK は自律ループ専用の `devDependencies` のまま据え置く
  - B. `@anthropic-ai/claude-agent-sdk` を `dependencies` へ移し、アプリ本体もこれを使う
    （オーナー決定 A の字面どおり）
- 仮決定: **A を採用して進行する。** 理由: §9 #45 が「AI呼び出しは抽象化して差し替え可能にしておけばよい」と
  明記しており、`1-5` の完了条件も抽象化（呼び出し側が SDK へ直接依存しないこと）で担保される。
  `4-2` 以降でエージェントループが必要になった時点で B へ寄せても、差し替わるのはクライアント層1モジュールだけで済む。
- 可逆性: 高。共通インタフェース（`src/lib/ai/types.ts`）の実装を1つ差し替え、`package.json` の依存を1件入れ替えるだけ。
  呼び出し側のコードは変更不要。
- 関連ファイル: v13 §9 #45、`package.json`、`automation/README.md` L70、Issue #55

---

## [2026-09-19] WBS `10-1` 一括インポートパイプラインの完了条件が未記入（Issue #57）（v13 §5.8.2）
- ステータス: ✅ **回答済み（2026-09-19 オーナー決定）: A — パイプライン本体（サーバ側処理＋管理画面UI）に限定し、検証は自作フィクスチャCSVのみ。列マッピングを含み、v13 §5.8.2 の5手順を1つの業務フローとして扱う。この範囲内であれば完了条件を正本から導出してよい（`origin: "derived"` ＋ `basis` 必須）。範囲の外へ出る必要が生じた時点で再度停止する**
- 優先度: 高（Issue #57 のタスク定義を止めている。個人情報を一括書き込みする導線のため、
  合格線が決まらないまま受入テストを作ると、検証不足の PII 取込経路がそのまま残る）
- 背景: `wbs-to-issue.yml` が WBS `10-1` から生成した Issue #57 の「完了条件」「スコープ外」が
  雛形のプレースホルダのままで、起票前チェックの3つ目（完了条件が検証可能）も未チェック。
  根拠節 v13 §5.8.2 は実在し「アップロード → 列マッピング → 取込プレビューと検証 →
  `pre_registered` 生成 → 取込結果ログ」の5手順を定めているが、**どこまでを本パッケージの
  合格線とするか**が Issue 上にない。あわせて WBS `10-1` のステータス欄にある
  2026-09-05 決定（開発段階は全環境で自作フィクスチャのみ／実データ投入はセキュリティ試験合格後・
  本番環境のみ／本パッケージは工程の最終盤へ移す）により、起票時の想定から完了条件が動いている可能性がある。
- 選択肢:
  - A. パイプライン本体（サーバ側処理＋管理画面UI）に限定し、検証は自作フィクスチャCSVのみで行う。
    完了条件は v13 §5.8.2 の5手順をそのまま受入テストへ変換する。名寄せ（10-2）・再訪アラート（10-3）・
    宿泊日数調整ログ（10-4）・実データ投入はスコープ外
  - B. A のうち列マッピングUIを外し、固定フォーマット前提のサーバ側取込処理＋検証＋ログのみとする
    （列マッピング画面は別パッケージへ分割）
  - C. 2026-09-05 決定で工程最終盤へ移ったため、現時点では着手せず `auto` ラベルを外して後続工程まで寝かせる
- 推奨: A（理由: v13 §5.8.2 が5手順を1つの業務フローとして定めており、B は §5.8.2-2 の
  「固定フォーマットを前提としない」要件がどのパッケージでも満たされない宙吊り状態を生む。
  A は実データを一切使わないため 2026-09-05 決定と矛盾せず、C のように実装可能な部分まで止める必要もない）
- 関連ファイル: v13 §5.8.2（L1496-1502）、`docs/spec/WBS_Phase1.md` `10-1`、Issue #57
## ✅ [2026-09-19] WBS `5-1`（クエストボード）の完了条件と、ゲスト開放ゲートをどこまで含めるか（v13 §5.3・§5.10.6）
- ステータス: **回答済み（2026-09-19 オーナー決定：B を採用）**
- 回答: **B。`5-1` に、サーバ側のゲスト受注申請拒否（`guest_allowed=false` に対する API 拒否）まで含める。**
  表示だけ先行マージされる期間の穴を作らないため。あわせて「この範囲の中であれば、完了条件は正本 v13 の
  該当節から**導出して進めてよい**（`origin: "derived"` と `basis`（節番号）を必ず付け、Issue コメントで明示する）」
  「範囲の外へ出る必要が生じた場合は改めて止める」ことも指示された（Issue #53 のオーナーコメント）。
  → 導出した完了条件8項目は Issue #53 のタスク定義コメントに記載。`TASKS.md` へ TODO として追記済み。
- 優先度: 高（Issue #53 のタスク定義を止めていた。**2026-09-19 に解消**）
- 背景: Issue #53 は `wbs-to-issue.yml` による自動生成で、**「完了条件」「スコープ外」がテンプレートの
  プレースホルダのまま**である（起票前チェックの3つ目も未チェック）。
  ⚠️ **2026-09-19 の運用改訂（完了条件が空欄でも節番号が引けるなら導出して進む）を適用しても、本件は停止のままである。**
  本パッケージは `guest_allowed` による**ゲスト開放判定＝認可・ロール判定**を中核に含み、導出が
  高リスクの範囲（認可）に踏み込むため、**範囲の切れ目はオーナーの判断**に委ねる（設計 §3.4 判断順序1）。
  具体的には、v13 §5.10.6 が `guest_allowed=false` について「**サーバ側でもゲストの受注申請APIを拒否する
  （表示制御のみに依存しない）**」と定めており、この API 側のゲートを `5-1`（ボード表示）と
  `5-2`（受注申請）のどちらの完了条件に置くかが WBS からは読み取れない。
- 選択肢:
  - A. `5-1` は**表示まで**とする（手動登録＋朝会抽出クエストの統合一覧、`guest_allowed=false` の施錠表示・
    申請ボタン非活性）。サーバ側の受注申請拒否は `5-2` の完了条件に置く
  - B. `5-1` に**サーバ側のゲスト受注申請拒否まで**含める（A ＋ `guest_allowed=false` に対するゲストの
    受注申請 API が拒否されることをテストで担保する）
  - C. `5-1` を**表示名を含まない範囲**に絞る（A から他者向け表示名の描画を外し、別パッケージへ切り出す）
- 推奨: B（理由: §5.10.6 は施錠表示とサーバ側拒否を**1つの判定基準として**定めており、A では
  「一覧の施錠表示だけが入った状態」が `5-2` のマージまで残る。表示制御だけを先にマージすると、
  その間はゲストが API を直接叩いて受注申請できる。認可のゲートは表示と同じ変更に含めるのが安全）
- 関連する未回答項目: 本ファイル「[2026-09-05] ニックネーム未設定会員の他者向け表示」の**論点⑤
  （親方会員番号の採番方式が未設計）**。①④の決定で表示規則の骨格は定まったが、**街人番号を持たない
  純粋な親方衆の表示番号が採番されていない**ため、他者向け表示名の完了条件はこの論点に依存する。
  選択肢 C はこの依存を切り離す案である（新規論点としては起票せず、既存項目を参照する）
- 関連ファイル: v13 §5.3・§5.10.6、`docs/spec/WBS_Phase1.md` 5-1・5-2、Issue #53
## [2026-09-19] WBS `2-4`（個人情報収集・保持）に残っている作業範囲（v13 §5.2 ／ §7 宿泊者名簿）
- ステータス: ✅ 回答済み（2026-09-19 ／ **A を採用**）
- 回答: **A**（`2-4` ＝ `lodging_register_entries` の DDL ＋ RLS ＋ DBテストに限定。収集UIは `3-1`/`3-2` へ、
  プライバシーポリシー・利用規約の反映は別途）。あわせて、この範囲内であれば完了条件を正本から
  **導出して進めてよい**（`origin: "derived"` ＋ `basis` の明記が条件）とオーナーが許可。
  補足②の FK 問題も推奨どおり「`0006` の前例に倣い `checkin_id` の FK を `3-2` 側の `ALTER` で後付け」で確定。
  出典: Issue #58 オーナーコメント（2026-09-19）。範囲外へ出る必要が生じた場合は改めて停止する。
- 優先度: ~~高（Issue #58 のタスク定義を止めている）~~ → 解消
- 背景: Issue #58（WBS `2-4`）は `wbs-to-issue.yml` の自動生成 Issue で、
  **完了条件がテンプレートのプレースホルダのまま**（`〔「〜が〜できる」の形で、検証可能に書く〕`）であり、
  起票チェックの3つ目も未チェックのまま `auto` が付いている。
  さらに `2-4` の当初スコープの中核だった **`member_profiles_private`（氏名・カナ・住所・出身地・誕生年月）は
  `2-1`（#29）で DDL 済み・`2-2`（#35）で RLS と列単位 GRANT 済み**であり、
  **`2-4` として何が残っているのかが Issue からも WBS からも一意に読めない**。
  一方で `DB物理設計.md` §3-13 の **`lodging_register_entries`（宿泊者名簿・3年保存・匿名化対象外）は
  `supabase/migrations/` に存在しない**（`0001`〜`0006`・`0100` を確認）。
- 選択肢:
  - A. `2-4` ＝ **`lodging_register_entries` の DDL ＋ RLS ＋ テスト**に限定する
    （前泊地・後泊地を含む法定名簿の器を作るところまで。収集UIは `3-1`/`3-2` 側へ）
  - B. `2-4` ＝ **A ＋ 収集UI（氏名・カナ・住所・前泊地の入力と保存）**まで含める
  - C. `2-4` ＝ **B ＋ プライバシーポリシー・利用規約への反映**（文書作成を含む）まで含める
- 推奨: **A**（理由: WBS の規模は `S`、依存は `2-1` のみであり、B・C は `S` に収まらない。
  A は `2-1`／`2-2` と同じ「スキーマ＋RLS＋DBテスト」の型で完了条件を検証可能に書け、
  法定記録の器が先に無いと収集UIの保存先が決まらないため順序としても先行する。
  B・C は別パッケージとして切り出し、`3-1`/`3-2` および運用文書側に紐づけるのが素直）
- 補足（非ブロッカー）: カラムレベル暗号化の採否は WBS 上「Phase 1 不採用なら実装差分ゼロ」と整理済みのため、
  本件の判断には影響しない。
- 補足②（2026-09-19 追記 ／ A を採る場合に併せて要判断）: §3-13 の DDL は
  `checkin_id uuid REFERENCES check_ins(checkin_id)` を含むが、**`check_ins` テーブルは未作成**（WBS `3-2` の成果物）。
  `0006_rooms_and_assignments.sql` L74-80 に同じ待ち合わせが既に明記されている。
  A を採る場合、**`0006` の前例に倣い FK を `3-2` 側の `ALTER` で後付けする**か、`3-2` の完了を待つかの判断が要る。
  `3-2`（Issue #54）自体が未回答でブロック中のため、**待つ選択は `2-4` を無期限に止める**。推奨は前者。
- 補足③（2026-09-19 追記 ／ なぜ「導出して進む」改訂の対象外か）: 同日の改訂により、完了条件が雛形でも
  節番号が引けるなら導出して進むのが原則になったが、本件は明文の例外
  「**導出すると高リスク（個人情報）の範囲に踏み込む場合は停止。範囲の切れ目はオーナーの判断**」に該当する。
  未確定なのは完了条件の文言ではなく **`2-4` の範囲そのもの**（A/B/C）であり、
  範囲を機械側で決めると個人情報の収集項目と保存先が誰の承認も経ずに確定する。
- 関連ファイル: v13 §5.2、v13 §7「★ 宿泊者名簿（新規／§5.2・2026-09-05）」、`DB物理設計.md` §3-13、
  `supabase/migrations/0006_rooms_and_assignments.sql` L74-80、
  `docs/spec/WBS_Phase1.md` L205、Issue #58・#54

---
## [2026-09-19] WBS `3-2`（チェックイン／チェックアウト操作）の完了条件と、`12-4` との範囲の切れ目（v13 §5.10.8 ／ §5.8.4）
- ステータス: 未回答
- 優先度: 高（Issue #54 のタスク定義を止めている。範囲が決まらないと、金額付与（初回来訪
  キャッシュバック 10,000／5,000 uii）の自動起票を `3-2` に含めるかどうかが決まらない）
- 背景: `wbs-to-issue.yml` が WBS `3-2` から Issue #54 を生成したが、「完了条件」が雛形
  （`〔「〜が〜できる」の形で、検証可能に書く〕`）のまま未記入で、起票前チェックの3つ目も未チェックである。
  WBS `3-2` の概要は「状態切替」「AIウェルカムメッセージ」「3バッジ表示」の3つを一度に挙げているが、
  同じ WBS に **`12-4` 初回来訪キャッシュバックの判定・自動起票**（3バッジ表示＋自動起票）が別パッケージとして
  存在し、`12-4` の依存に `3-2` が入っている。`3-2` のステータス欄も「バッジ部分は 12-4 と一体」と記している。
  また `3-1`（完了・#31）は `check_ins` を参照する外部キーと会員向けポリシー2件を `3-2` へ送っている。
- 選択肢:
  - A. **`3-2` は「チェックイン／チェックアウトの状態切替」までとする。**
    `check_ins` の DDL・RLS（`3-1` から送られた外部キーと会員向けポリシー2件を含む）、QR 読み取りと
    画面タップによる状態遷移、チェックアウト時の状態確定まで。**3バッジ表示・キャッシュバックの自動起票は
    `12-4` に残し、AIウェルカムメッセージも別途切り出す**
  - B. A に加えて**3バッジ表示（街人／ゲスト・初回／再訪・未発行／依頼済み／受領済み）までを `3-2` に含める**。
    `eumo_grants` への自動起票のみ `12-4` に残す
  - C. §5.10.8 全体（3バッジ表示・自動起票・「要確認」表示）を `3-2` に一括で含め、`12-4` を廃止する
- 推奨: A（理由: ①WBS が `12-4` を独立パッケージとして持ち、その依存に `3-2` を置いている以上、
  バッジを `3-2` に取り込むと依存が循環する。②バッジ表示は `eumo_grants`（`5-7`）の存在を前提とするが、
  `5-7` は #59 未決でブロック中であり、`3-2` に取り込むと状態切替まで一緒に止まる。
  ③A の範囲なら金額計算・給付起票を一切含まないため、`3-2` 単体のリスク区分を下げられる）
- 関連ファイル: v13 §5.10.8・§5.8.4、`docs/spec/WBS_Phase1.md` `3-1`・`3-2`・`5-7`・`12-4`、Issue #54
- ⚠️ **追記（2026-09-20 オーナー）**: 本項の A/B/C 選択肢とは別に、**`3-2` の範囲には
  宿泊者名簿（`lodging_register_entries`）収集も含まれることが新たに確定した**
  （v13 §5.2.7 新設。`POST /api/checkins/{id}/lodging-register`）。これは「[2026-09-19] WBS `2-4`」が
  収集UIを `3-1`／`3-2` へ委ねていたにもかかわらず、`3-2` の概要にその記載が無かった空白を埋めるもの。
  A/B/C いずれを選んでも「チェックイン／チェックアウトの状態切替」の一部として`3-2`に残る
  （バッジ・キャッシュバックの取り扱いとは独立の論点のため）。`3-2` の概要欄・完了条件を書く際は
  本追記も反映すること。詳細: `CONSOLIDATED_DECISIONS.md` §7「チェックイン時の宿泊者名簿...収集経路を確定する」。
## ✅ [2026-09-19] WBS `1-5` の MCP Gateway は正本 §4 で Phase 3 に置かれている（v13 §4 ／ `システムアーキテクチャ.md` L1175）
- ステータス: ✅ **回答済み（2026-09-19 オーナー：A。前回の B は撤回）**
- 回答: **MCP Gateway を `1-5` から外し、Phase 3（v13 §4 FEL連携強化・マルチテナント化）の範囲とする。**
  Phase 1 の `1-5` は **「APIキー管理 ＋ Claude（Agent SDK）／Gemini のテキスト用クライアント基盤」**まで。
  音声マルチモーダルは 2026-09-05 決定（§9 #63）で失効済みのため含めない。
  この範囲であれば**正本から完了条件を導出してよい**（`origin: "derived"` ＋ `basis`）。
  WBS `1-5` 概要欄の追随（MCP Gateway・音声マルチモーダルの削除）は `docs/spec/` 配下のため**オーナー側で対応**する。
- 反映先: Issue #55 のタスク定義コメント（リスク区分: 高／ゲート1・2・3）、`TASKS.md`
- 以下は照会時点の記録（経緯として保持）:
- 優先度: 高（Issue #55 のタスク定義を止めている。1-5 は 4-2（議事録構造化・クエスト候補起案）の
  依存元だが、**下記の A を採れば 4-2 の依存分（Claude／Gemini クライアント基盤）は即着手できる**）
- 背景（本項の再照会分・2026-09-19）:
  オーナー回答は **B＝「APIキー管理 ＋ Claude（Agent SDK）／Gemini クライアント基盤 ＋ MCP Gateway」**
  だったが、**前回の照会時に提示できていなかった事実**が確認のうえ判明した。
  - **正本 v13 に `MCP` の記述は 0 件**（全文検索）。MCP Gateway を説明しているのは
    `docs/spec/basic-design/infra/システムアーキテクチャ.md` L1175-1177 のみで、その定義は
    「**分離された各テナント（浮遊街・FEL・将来の他コミュニティ）のデータやAIログを
    セキュアかつ疎結合にルーティングする中継層**」である。
  - 一方、**正本 v13 §4 は「FEL連携強化・マルチテナント化」を Phase 3 と定めている**（L380）。
    §2 の将来展望でも「FEL との共通データ連携および IoT 遠隔監視（Phase 3）」（L78）。
    つまり **MCP Gateway の用途そのものが、正本では Phase 3 に置かれている**。
  - 結果、MCP Gateway は **①受入基準を導出する材料が正本に無く、②正本 §4 の Phase 区分と衝突する**。
    前回 B を推奨した理由は「MCP は `1-5` 固有で他に引き取り手が無く、A にすると宙に浮く」だったが、
    **引き取り手は Phase 3（§4）に実在した**。推奨の前提が誤っていたため、ここで訂正する。
- 選択肢:
  - A. **MCP Gateway を `1-5` から外し、Phase 3（§4 FEL連携強化・マルチテナント化）へ送る。**
    Phase 1 の `1-5` は「APIキー管理 ＋ Claude（Agent SDK）／Gemini テキストクライアント基盤」までとする
  - B. MCP Gateway の**実体は作らず、将来 MCP へ差し替え可能な抽象化層（インターフェース分離）**までを
    `1-5` の成果物とする（`システムアーキテクチャ.md` のアーキテクチャ制約は満たすが、中継層は作らない）
  - C. 2026-09-19 回答どおり **MCP Gateway の実体を Phase 1 で構築する**
    （＝正本 §4 の Phase 3 → Phase 1 前倒しとなるため、**正本の改訂を伴う**）
- 推奨: **A**（理由: ①正本 §4 が FEL連携・マルチテナント化を Phase 3 と明示しており、
  MCP Gateway の定義（テナント間ルーティング）はそれそのもの。②正本に MCP の要件記述が 0 件のため、
  Phase 1 で作ると**受入基準を誰も検証できない**成果物になる。③A を採っても
  `4-2` が依存する Gemini／Claude クライアント基盤は `1-5` に残るため、**後続は止まらない**。
  ④Phase 1 は 9/30 リリース目標で残り 11 日であり、Phase 3 相当の中継層を高リスク3ゲートで
  通す余地が薄い。なお C を選ぶ場合は `docs/spec/` の改訂が要るが、**改訂はエージェント側では行わない**）
- 補足:
  - **A または B なら、残る2点（APIキー管理／Claude・Gemini クライアント基盤）の完了条件は
    正本から導出して即座に着手できる**（2026-09-19 のオーナー許可に従い `origin: "derived"` ＋ `basis` 付き）。
  - 本パッケージは **APIキー（シークレット）の取り扱い**と**外部API連携**を含むため、
    区分は**高リスク**（ゲート1・2・3／承認3回）で確定する。範囲の切れ目はオーナー判断に属する。
  - Issue 本文の「根拠となる仕様: `v13 §16`」は**実在しない参照**のままである。
    正本に節を新設するか、WBS の参照先表記を改めるかもオーナー判断（エージェントは `docs/spec/` を編集しない）。
- 関連ファイル: v13 §4（L78・L380）、`docs/spec/basic-design/infra/システムアーキテクチャ.md` L1175-1177、
  `docs/spec/WBS_Phase1.md` §1 L191、Issue #55

<details>
<summary>初回照会（2026-09-19 00:38 ／ オーナー回答「B」で決着済み。経緯として保持）</summary>

- ステータス: ✅ 回答済み（2026-09-19 オーナー：**B**。音声マルチモーダルのみ落とす）
- 優先度: 高（Issue #55 のタスク定義を止めている。1-5 は 4-2（議事録構造化・クエスト候補起案）の
  依存元であり、範囲が決まらないと 4-2 側の着手条件も確定しない）
- 背景: Issue #55（WBS `1-5` から自動生成）の起票3条件のうち2つが欠けている。
  ① **「根拠となる仕様」が `v13 §16` だが、正本 v13 に §16 は存在しない**（正本の大節は §9 まで）。
  実際に `§16` を持つのは `docs/spec/WBS_Phase1.md`（§16 QUESTIONS.md ブロッカー逆引き表）であり、
  これは「何を作るか」ではなく「何がブロックされているか」の表である。
  つまり本パッケージの仕様を定めた節が正本に存在しない。
  ② **完了条件が雛形（`〔「〜が〜できる」の形で、検証可能に書く〕`）のまま未記入**、スコープ外も未記入。
  さらに WBS `1-5` の作業内容欄は「APIキー管理、朝会音声のマルチモーダル入力、MCP Gateway」だが、
  **「朝会音声のマルチモーダル入力」は 2026-09-05 のスコープ変更で失効している**
  （`CONSOLIDATED_DECISIONS.md`：文字起こしは外部ソフトで実施済みとし、アプリはテキストを入力とする。
  WBS `4-2` も同日に「投入済みテキストから生成する」へ改訂済み）。記載3点のうち1点が既に無効である。
- 選択肢:
  - A. APIキー管理 ＋ Claude（Agent SDK）／Gemini の各クライアント基盤までとし、**MCP Gateway は別パッケージへ切り出す**
  - B. A ＋ MCP Gateway（WBS `1-5` の記述どおり3点構成。**失効した音声マルチモーダルのみを落とす**）
  - C. WBS の記述をそのまま維持する（朝会音声のマルチモーダル入力を含む3点）
- 推奨: B（理由: 2026-09-05 決定で失効したのは**音声マルチモーダルだけ**であり、
  WBS の記述を最小限の修正で活かせる。MCP Gateway は `1-5` 固有の成果物で他パッケージに引き取り手が無く、
  A にすると宙に浮く。Gemini は `4-2`（議事録構造化）が依然依存するため、
  **音声用ではなくテキスト用クライアント**として残す必要がある。C は失効済みの前提を実装することになる）
- 補足（回答時に併せて確定したい点 ／ 本論点に従属するため別項目にはしない）:
  - 上記が決まったうえで、**完了条件を「〜が〜できる」の形で Issue #55 に記入**していただきたい。
    完了条件が無いまま進めると受入テストが誤った期待値で固定される。
  - B を採った場合、規模 `M` に収まるかの再見積もりが要る可能性がある（MCP Gateway の設計進捗は WBS 上 60%）。
  - 正本 v13 に AI連携基盤の節が無いため、**`v13 §16` という参照自体をどう正すか**（正本に節を新設するか、
    WBS の参照先表記を改めるか）もオーナー判断。エージェント側では `docs/spec/` を編集しない。
- 関連ファイル: `docs/spec/WBS_Phase1.md` §1 L191・§16-1、`docs/spec/CONSOLIDATED_DECISIONS.md`（2026-09-05 スコープ変更）、Issue #55

</details>

## [2026-09-19] WBS `3-8` の残枠ビューは `check_ins` 未作成では組めない（v13 §5.2.5）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-20 オーナー指示）：選択肢 C を採用**
  — 「**WBS 上着手可能なタスクを実装。その際に DB が存在しない場合は実装すること**」という
  オーナーの直接指示により、`3-8` の中で `check_ins` を併せて作った。
  実体は `0014_check_ins_and_accommodation_types.sql`（`accommodation_types` 6形態 ＋ `check_ins`
  ＋ RLS ＋ GRANT ＋ `0006` が後続へ送っていた ALTER 2件の回収）と
  `0015_room_availability_view.sql`（`v_room_availability`）である。
  - 選択肢 A（`3-2` を先にやる）を採らなかったのは、**`3-2` 自体が別の未回答論点でブロック中**
    （本ファイル「[2026-09-19] WBS `3-2` … `12-4` との範囲の切れ目」）であり、
    A を選ぶと `3-3`・`3-4`・`3-8`・`6-1`・`8-1`・`8-6` の6件が無期限に止まるため。
  - `0006` の 2026-09-15 オーナー決定（「`check_ins` を参照するものは `3-2` へ送る」）とは
    **矛盾しない**。あの決定は `3-1` のスコープを DB 層に絞るためのものであり、
    `check_ins` を誰が作るかを永久に固定したものではない。`3-2` に残るのは
    **チェックイン操作 UI・QR・ウェルカムメッセージ・バッジ表示**である。
  - ⚠️ 副次的に判明した欠陥も是正した: `0006` の初期8行は **キャンプサイト=4 ／ 車中泊=2** で
    入っており、v13 §5.4.2 の確定表（**10 ／ 30**・合計66）と食い違っていた。
    そのままだと残枠ビューの分母が 32 になり、**予約できる枠を満室と表示していた**。
    `0014` が UPDATE で是正し、`tests/db/check-ins-and-availability.test.ts` が合計66を固定している。
- 優先度: 高（Issue #56 のタスク定義を止めている。`v_room_availability` は `3-5b` 公開予約ページ・
  `3-6` 宿泊予定カレンダー・`3-10` 滞在中変更の共通依存であり、ここが決まらないと §3 の後続が動かない）
- 背景: 2026-08-23 の改訂（v13 §9 #47）で残枠の算出元が `room_assignments` → **`check_ins`（宿泊形態単位）**
  へ変わったが、**WBS `3-8` の「依存」欄は `3-1` のまま**で追随していない。実装状況を確認したところ、
  `supabase/migrations/` は `0006_rooms_and_assignments.sql` までで、`check_ins` も
  `accommodation_types` も存在しない。`0006` には「`check_ins` は WBS `3-2` の成果物であり、まだ存在しない」
  と明記され、外部キーを後続の ALTER で足す手順までコメントで残されている（2026-09-15 オーナー決定）。
  DB物理設計 §3-12 のビュー定義は `check_ins` と `accommodation_types` の両方を参照するため、
  現状のままでは `CREATE VIEW` がエラーになる。Issue #56 の「完了条件」「スコープ外」も雛形のまま未記入で、
  起票前チェックの3つ目が未チェックである。
- 選択肢:
  - A. **`3-8` を保留し、先に `3-2`（`check_ins` スキーマ）を実装する。** WBS `3-8` の依存を
    `3-1` → `3-1, 3-2` へ改める。Issue #56 は依存待ちとして据え置く
  - B. `3-8` のスコープを **`accommodation_types` マスタ（6形態・`allocation_mode`）の DDL までに限定**し、
    `v_room_availability` ビュー本体と2系統のカレンダーは `3-2` 完了後の別 Issue に分ける
  - C. `3-8` の中で `check_ins` の**最小スキーマも併せて作る**（`3-2` はチェックイン／チェックアウトの
    操作UIのみを担当する形へ役割を狭める）
- 推奨: A（理由: `0006` のコメントが 2026-09-15 のオーナー決定として「`check_ins` を参照するものは `3-2` へ送る」
  という境界を明示的に引いており、C はその決定を覆すことになる。B は `accommodation_types` だけが先に入り、
  ビュー不在のまま「3-8 完了」と記録される状態を作るため、`3-5b`・`3-6` の依存判定が誤る）
- 補足（本論点とは別に、着手前に解消が要る）: WBS §16 の逆引き表は `3-8` を **#55（宿泊形態の旧名称が
  正本 §4・§5.2.4・§5.2.5・§7 に残存）の影響下**に置いている。マスタ初期値を投入する前に一括置換が必要。
- 関連ファイル: v13 §5.2.5（L614-641）、`docs/spec/detailed-design/DB物理設計.md` §3-12（L650-710）、
  `supabase/migrations/0006_rooms_and_assignments.sql`、`docs/spec/WBS_Phase1.md` L224、Issue #56

## [2026-09-19] WBS `1-4` の Issue が2本ある（#18 ＝ `auto:blocked` ／ #49 ＝ 新規）（v13 §5.11.2）
- ステータス: 未回答
- 優先度: 高（Issue #49 のタスク定義を止めている。どちらを正本 Issue とするかが決まらないと、
  #18 に積み上がった回答履歴（2026-09-14 の B 採用・2026-09-15 の C 採用）が #49 側で失われる）
- 背景: `wbs-to-issue.yml` が 2026-09-19 に WBS `1-4` から Issue #49 を生成したが、
  同じ `1-4` から生成された Issue #18 が既に存在し、`auto:blocked` のまま開いている。
  #49 の「完了条件」は雛形（`〔「〜が〜できる」の形で、検証可能に書く〕`）のまま未記入で、
  起票前チェックの3つ目も未チェックである。また #18 を止めている未回答論点
  「[2026-09-15] `media_assets` テーブル（DDL ＋ RLS）を作る作業パッケージが WBS 上に存在しない」は
  `1-4` 固有のため、Issue 番号が変わっても解消しない。
- 選択肢:
  - A. **#49 を重複としてクローズし、`1-4` は #18 で継続する。** あわせて `wbs-to-issue.yml` が
    同一 `wbs:` マーカーの既存 Issue を再生成しないようガードを入れる
  - B. #49 を正本とし、#18 をクローズする。#18 の回答履歴（B 採用・C 採用）を #49 本文へ転記し、
    完了条件・スコープ外を #49 に記入する
  - C. 両方を開いたまま、#49 を `media_assets` スキーマ専用の Issue として書き換える
    （上記未回答論点の選択肢 B に相当する新パッケージの受け皿にする）
- 推奨: A（理由: #18 には 2026-09-14・09-15 の**オーナー回答2件**と、2026-09-15 のゲート1差し戻しの
  経緯が蓄積している。B はこれを人手で転記する作業が発生し、転記漏れがそのまま仕様の取りこぼしになる。
  C は WBS の改訂を伴う判断（上記未回答論点）を Issue の付け替えで先取りすることになり、
  どちらの案が採られるか未確定のうちに実施できない）
- 状況（2026-09-19 14:30 確認・事実の記録／判断は加えていない）: **Issue #49 は既に CLOSED**（`NOT_PLANNED`、
  2026-09-19 00:28）であり、クローズ時のコメントは「スイーパーの重複起票です。同じ作業パッケージ `1-4` は **#18** で
  既に起票されています。… `1-4` の議論は #18 で続けてください」。重複判定のガードも
  `automation/scripts/sweep.py` に**実装済み**（`<!--wbs:X-->` マーカーに加え、タイトル `[auto] WBS <id>` からも
  既起票を検出する。L345-347）。**すなわち推奨 A の両方（#49 のクローズ ／ 再生成ガード）が実施済みで、
  オーナーの判断を待つ論点は残っていない**。次に本ファイルを触る者がクローズ扱いにしてよい。
- 関連ファイル: v13 §5.11.2（L1894-1963）、`docs/spec/WBS_Phase1.md` `1-4`、
  `.github/workflows/wbs-to-issue.yml`、`automation/scripts/sweep.py` L345-347、Issue #18・#49

## ✅ [2026-09-15] `media_assets` テーブル（DDL ＋ RLS）を作る作業パッケージが WBS 上に存在しない（v13 §5.11.2 ／ `DB物理設計.md` §3-7・§6）
- ステータス: ✅ **回答済み（2026-09-19 オーナー決定：B を採用）**
- 回答: **B**。`media_assets` は **Phase 1 で作る**。ただし `1-4` には含めず、
  **スキーマ ＋ RLS を独立パッケージ `2-1c` として新設**し、`1-4` の依存に追加する。
  `1-4` は GCS バケット・Edge Function・Cloud Functions 経路に専念する。
  DDL は `DB物理設計.md` §3-7、RLS は §6（L1960・L2011）に定義済みのため、仕様の追加は伴わない。
  オーナー補足：**「どの順番でも進むんだから、リスク高で起票しなくていい」** —
  推奨理由の末尾に付けた副作用（B では `1-4` の着手可能時期が後ろへずれる）は、**リスクとして起票しない**。
  作業パッケージは着手順に強い制約が無く、`1-4` が `2-1c` の完了を待つ形になっても計画上の問題にはならない。
  したがって「着手順の後退」を新たな論点として本ファイルへ起票することはしない。
- 反映先（2026-09-19 実施）: `docs/spec/WBS_Phase1.md` §2 に `2-1c` を新設／`1-4` の依存を `1-1` → `1-1, 2-1c` へ／
  `14-6`（`ai_tags[]` 列の先行用意）の依存を `14-1` → `2-1c` へ／`CONSOLIDATED_DECISIONS.md` §19-3／
  Issue #18・#49 の「スコープ外」1行目は**維持**（`media_assets` は `2-1c` の担当で確定）
- 優先度: 高（Issue #18 ／ WBS `1-4` のタスク定義を止めていた。**2026-09-19 に解消**。`1-4` の完了条件①「`media_assets` へ
  `pending` レコードを先行作成する」が、テーブルの担当が決まるまで受入テストに変換できなかった）
- 背景: Issue #18（WBS `1-4`）の計画フェーズで、2026-09-15 にゲート1で差し戻された論点。
  2026-09-13 起票の「`1-4` と `14-1` の範囲の切れ目」で **2026-09-14 にオーナーが B（①〜⑥の経路一式を `1-4` に含める）**
  を回答したことで、`media_assets` への `pending` 先行作成が `1-4` の完了条件に入った。
  しかし **同テーブルを作る作業パッケージが WBS 上に存在しない**（Issue #18 本文 ⚠️3 として起票時から指摘されていたが、
  `QUESTIONS.md` へは起票されないままだった）。
  - Issue #18 本文の「スコープ外」1行目は **`media_assets` の作成・列定義・RLS を明示的に除外**し、
    担当を「WBS `2-1` 系および `14-1`」としている
  - しかし `2-1`（PR #33 で完了）はロール・アカウントステータスのみで `media_assets` を含まず、
    `14-1` は **`1-4` に依存する側**、`14-6` は `ai_tags[]` カラムのみで `14-1` 依存である。
    `supabase/migrations/` に `media_assets` の DDL は存在しない（現在4本。いずれも会員系と pgvector）
  - DDL は `DB物理設計.md` §3-7（L444-480）に、RLS は §6（L1960・L2011）に**定義済み**であり、仕様の追加は伴わない。
    ただし同テーブルは §6 の PII 分類で **PII-B**（`geo_location`＝Exif 位置情報・`ai_caption`）である
  - 2026-09-15 のタスク定義は「`1-4` に含める」と解釈してゲート1へ出し、**差し戻された**（却下理由は未記録）
- 選択肢:
  - A. `1-4` に含める。Issue #18 本文の「スコープ外」1行目を撤回し、`DB物理設計.md` §3-7・§6 のとおり
    DDL ＋ RLS を `1-4` で作る
  - B. `1-4` からは外し、**`media_assets` のスキーマ ＋ RLS を独立パッケージとして新設**（`2-1c` 相当）、
    `1-4` の依存に追加する。`1-4` は GCS バケット・Edge Function・Cloud Functions 経路に専念する
  - C. `14-1` へ寄せ、`14-1` → `1-4` の依存の向きを反転させる
- 推奨: B（理由: ①Issue #18 本文が「スコープ外」と宣言した範囲を、PM の解釈で `1-4` へ引き戻すことになるのが A であり、
  実際にゲート1で差し戻されている。②WBS `1-4` の規模は **M** だが、A では PII-B テーブルの DDL ＋ RLS が加わり
  **L へ膨らむ**。③`media_assets` の RLS は「投稿者本人 ＋ staff は全件／一般会員・ゲストは公開のもののみ」という
  **認可の設計そのもの**（§6 L2011）であり、RLS 設計を担う `2-x` 系に置くほうが `2-2` との一貫性を保てる。
  ④C は WBS が定める依存の向き（`14-1` が `1-4` に依存）を反転させ、`5-3`・`14-2` の着手条件にも波及する。
  ただし B を採ると `1-4` の完了条件①が新パッケージの完了待ちになり、**`1-4` の着手可能時期が後ろへずれる**）
- 関連ファイル: v13 §5.11.2（L1894-1963）、`docs/spec/detailed-design/DB物理設計.md` §3-7（L444-480）・§6（L1960・L2011・L1153）、
  `docs/spec/WBS_Phase1.md` `1-4`・`14-1`・`14-6`、Issue #18
## ✅ [2026-09-15] `core_member` の「自拠点のみ」が RLS で表現できず、正本より緩い認可が先に固定される（v13 §6 ／ `DB物理設計.md` §6-9②）
- ステータス: ✅ **回答済み・クローズ（2026-09-19 オーナー決定：A を採用）**
- 回答: **A**。`core_member` は**全拠点を許可したまま**進める。
  オーナー補足：**「現状拠点は複数になる想定ではないので放念」**。
  よって B（`members` への `place_id` 追加）は行わず、C（正本 v13 §6 の「コアメンバーは自拠点」の撤回）も行わない
  ——正本の記述は**将来の複数拠点を見据えた記述としてそのまま残す**。
  本件は「**拠点を複数化すると決めた時点で再開する**」課題として本ファイルに記録として残すのみとし、
  それまで追加の調査・設計・起票は行わない（`3-1` 以降は現行の `is_staff()` テンプレートで進めてよい）。
  ⚠️ 再開時の影響範囲は、`3-1` を依存元とする7パッケージ（`3-2`・`3-3`・`3-5b`・`3-6`・`3-8`・`3-10`・`8-6`）と
  `rooms` / `room_assignments` の RLS である。
- 反映先（2026-09-19 実施）: `CONSOLIDATED_DECISIONS.md` §19-4／
  `docs/spec/detailed-design/DB物理設計.md` §6-9②（未解消の論点 → **受容済み**として記録）／`WBS_Phase1.md` 改訂履歴。
  **正本 v13 §6 は改訂しない**（将来の複数拠点を見据えた記述として残す）。
- 優先度: ~~中~~ → **放念（2026-09-19）**（Issue #31 ／ WBS `3-1` は本件をスコープ外と宣言して進行できる。ただし `3-1` は
  `3-2`・`3-3`・`3-5b`・`3-6`・`3-8`・`3-10`・`8-6` の7パッケージの依存元であり、ここで置いた
  「`core_member` は全拠点」のポリシーを後続がそのまま踏襲する）
- 背景: Issue #31（WBS `3-1` 部屋台帳・部屋割当）のタスク定義時に確認。
  `DB物理設計.md` §6-9② は「v13 §6 は顧客管理・部屋割当等を『コアメンバーは**自拠点**』と限定しているが、
  `members` に拠点を示す列が無い（`rooms.place_id` は存在する）。本節のポリシーは `core_member` に
  **全拠点**を許可しており、**v13 §6 より緩い**」と、自ら未解消の論点として明記している。
  `rooms` / `room_assignments` は `is_staff()`（＝`admin` または `core_member`）で書き込みを許す
  テンプレートに従うため、`3-1` の時点でこの緩い形が実装として固定される。
  拠点が1つしかない現状では実害が出ないが、拠点が増えた時点で遡及改修の対象が7パッケージに広がる。
- 選択肢:
  - A. 現状どおり `core_member` は全拠点を許可し、拠点が複数になる時点で `members` に拠点列を足して
    まとめて絞り込む（本件は「拠点が増えたら着手する」課題として記録に留める）
  - B. 先に `members` へ拠点列（`place_id`）を追加し、`3-1` の段階から
    `rooms.place_id` と突き合わせて `core_member` を自拠点に限定する
  - C. v13 §6 の「コアメンバーは自拠点」という限定自体を撤回し、正本を実装に合わせて改訂する
    （＝運用上、コアメンバーは全拠点を見てよいと決める）
- 推奨: A（理由: ①拠点が1つの現状では B の拠点列が常に同一値になり、
  絞り込みロジックが検証不能なまま7パッケージに入る。②C は正本の改訂であり仕様策定側の判断で、
  実装ループの範囲外（設計 §0）。③A を採っても、本項目が残る限り「緩い側に倒していること」は追跡できる。
  ただし**どの時点で拠点を複数化するか**は運営計画に依存するため、オーナー判断が要る）
- 関連ファイル: v13 §6、`docs/spec/detailed-design/DB物理設計.md` §6-9②・§6-7、Issue #31

## [2026-09-15] `member_role_changes` を `authenticated` へ `GRANT SELECT` するか（v13 §5.9.3 ／ `DB物理設計.md` §6-6②・§6-6b⑥）
- ステータス: 回答済み（2026-09-15）
- 回答: **A**。`DB物理設計.md` §6-6② の書き漏れとみなし、`GRANT SELECT ON member_role_changes TO authenticated`
  を追加する。行の絞り込みは `mrc_select_admin`（`is_admin()`）が担う。オーナー補足：B（`service_role` 経由に
  限定）は「`service_role` は RLS も GRANT も迂回するため必ず発火する関門はトリガーだけ」という §6-6b の
  設計思想に反し、読み取りのためだけに `service_role` 経路を増やすのは境界を薄くする。C は権限変更履歴を
  誰も読めないまま残し、`2-1` で `member_role_changes` を作った意味が失われる。
  **`is_admin()` で絞る点が要件**（`core_member` へ開くと運営内部の人事的情報が広がるため、
  v13 §8 の最小権限に沿って `admin` のみ）。
- 優先度: 高（Issue #35 ／ WBS `2-2` のタスク定義を止めていた。`admin` が `member_role_changes` を
  読めるかどうかで受入テストの期待値が「1行」と「0行」に反転するため、PM が推測で埋められない）
- 背景: Issue #35（WBS `2-2` Supabase RLSポリシー設計）のタスク定義時に判明。
  `DB物理設計.md` §6-6b⑥ は `CREATE POLICY mrc_select_admin ON member_role_changes FOR SELECT
  TO authenticated USING ( (SELECT public.is_admin()) );` を定義しており、**`authenticated` ロールの
  セッションから `admin` が読む**ことを前提にしている。一方、同じ §6 の GRANT 方針 §6-6② の
  `GRANT SELECT ... TO authenticated` の列挙に **`member_role_changes` は含まれていない**
  （`members` / `member_profiles_private` / `member_identifiers` ほか14件と、行を RLS で0件に絞る
  `member_notes` / `morning_meetings` / `work_log_reviews` の3件のみ）。
  §6-6 の冒頭は「**`GRANT` が無ければポリシーを書いても届かず、ポリシーが無ければ `GRANT` があっても0行になる**」と
  自ら述べているため、この2節は同時には成立しない。Issue #35 の完了条件
  「`admin` のセッションから `member_role_changes` を SELECT できる」は前者の読みに立っている。
  `role` 変更の監査記録（誰が誰を昇格・降格させたか）は §6-6b⑥ 自身が「運営の中でも限定情報」と
  位置づけており、`authenticated` への権限付与は認可に直接関わる判断であるため、
  `CLAUDE.md` §7「推測で実装しない」に従い停止した。
- 選択肢:
  - A. §6-6② の書き漏れとみなし、`GRANT SELECT ON member_role_changes TO authenticated` を追加する。
    行の絞り込みは `mrc_select_admin`（`is_admin()`）が担い、`admin` 以外は0行になる
  - B. GRANT は与えず、監査記録の閲覧は `service_role`（Edge Function / Server Actions）経由に限定する。
    §6-6b⑥ の `mrc_select_admin` は将来の閲覧UIに備えた先置きとして残し、
    Issue #35 の完了条件を「`authenticated` のどのロールからも0行（または権限エラー）」へ差し替える
  - C. `mrc_select_admin` も置かず `member_role_changes` を完全な全拒否（`service_role` 専用）とし、
    閲覧の設計は監査ログ閲覧画面の作業パッケージへ送る
- 推奨: A（理由: ①§6-6b⑥ が `TO authenticated` と明示的に書いており、B・C では書かれたポリシーが
  到達不能なコードとして残る。②A でも実際に読めるのは `is_admin()` が真のセッションだけで、
  `core_member`・一般会員・本人には0行が返る（§6-6b⑥ の「本人にも開かない」を満たす）。
  ③`anon` は §6-6① の `REVOKE ALL` で権限ゼロのままであり、公開面は広がらない。
  ただし**監査記録をクライアント経由で読ませてよいか**は運営判断であり、B を採る場合の実害も小さい）
- 関連ファイル: v13 §5.9.3、`docs/spec/detailed-design/DB物理設計.md` §6-6②・§6-6b⑤⑥、
  `supabase/migrations/0001_members_schema.sql`、Issue #35

## ⚡ [2026-09-14] 通貨表示の共通コンポーネント化が正本 v13 §5.5 では確定・`画面設計.md` §6 #8 では未決のまま（v13 §5.5・§9 #41）
- ステータス: 未回答（正本優先で進行中・ブロッカーではない）
- 優先度: 低（`Money.tsx` は既に実在するため実装は止まらない。放置すると、`画面設計.md` を先に読んだ
  実装者が「共通化の方針は未決」と誤認し、画面ごとに通貨表記を書く事故（v13 §5.5 が防ごうとしたもの）を招く）
- 背景: Issue #13 の段取り作成時に判明。調査メモ「矛盾」。
  正本 v13 §5.5（L906-954）・§9 #41（L2417）は「表示は**共通コンポーネント1箇所**に集約し、
  画面ごとに順序を書かない」を確定事項として記述しており、実体 `src/components/ui/Money.tsx` も既に存在する。
  一方 `docs/spec/detailed-design/画面設計.md` L502（§6 オーナー確認事項 #8）は
  「**通貨表示（Uii 主・円 副）の共通コンポーネント化** … 全画面に波及するため、**実装前に共通化の方針を
  決めておく必要がある**」を 2026-08-20 起票時のまま未解消で残している。
- 採用した内容: `CLAUDE.md` §1.1「矛盾時は正本が勝つ」により、**正本 v13 §5.5 を採用して進行**
  （`Money.tsx` 1箇所に集約する前提で Issue #13 を実装する）。
- 派生文書側の訂正要否: `画面設計.md` §6 #8 を「✅解消済み（2026-08-20、v13 §5.5 で共通コンポーネント
  1箇所に集約と確定）」の形で消し込むか。オーナー判断待ち。
  なお #8 の「全画面に波及する」点自体は未了である（`WBS_Phase1.md` `0-1` の「全画面をこれ経由に
  置き換える」は Issue #13 のスコープ外）。消し込む場合は、残作業が `0-1` にあることを併記する必要がある。
- 関連ファイル: v13 §5.5・§9 #41、`docs/spec/detailed-design/画面設計.md` §6 #8（L502）、
  `docs/spec/WBS_Phase1.md` `0-1`、Issue #13

## ⚡ [2026-09-14] `role` / `member_type` の値域が派生文書2件で正本 v13 §2 より狭いまま（v13 §2・§7）
- ステータス: 未回答（正本優先で進行中・ブロッカーではない）
- 優先度: 低〜中（放置すると `2-2` 以降の実装者が `custom` 無し4値・`街人`3値の記述を根拠にしうる）
- 背景: Issue #29（WBS `2-1`）の段取り作成時に判明。調査メモ「矛盾」表 #1・#2。
  正本 v13 §2（L133・L163-168）と §7（L2141-2142）は `role` を**5値**
  （`admin` / `core_member` / `member` / `guest` / `custom`）、`member_type` を**4値**
  （`親方` / `街人（コア）` / `街人（一般）` / `ゲスト`）と定める。一方
  `会員データモデル_ユーザーテーブル定義.md` §5.2 は `role` 4値（`custom` なし）・`member_type` 3値
  （`親方` / `街人` / `ゲスト`）のままであり、`DB物理設計.md` §6-9⑥⑦ は両件を「未決」として列挙したままである。
- 採用した内容: 2026-09-14 のオーナー回答（Issue #29 論点2＝5値 / 論点3＝4値）と `CLAUDE.md` §1.1
  「矛盾時は正本が勝つ」により、**正本 v13 §2 の値域を採用して進行**（`2-1` のマイグレーションで
  `role` は5値・`member_type` は4値の `CHECK` を書く）。
- 派生文書側の訂正要否: `会員データモデル_ユーザーテーブル定義.md` §5.2 の列表と
  `DB物理設計.md` §6-9⑥⑦ の未決リストを正本に合わせて訂正するか。オーナー判断待ち。
  なお §6-9⑥ の「`custom` の権限内容が未定義のため、ポリシーは暫定で `member` 相当として扱う」は
  オーナー回答も同じ扱いを認めているため、そのまま残してよい。
- 関連ファイル: v13 §2・§7、`docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.2、
  `docs/spec/detailed-design/DB物理設計.md` §6-9⑥⑦、Issue #29

## ⚡ [2026-09-14] `members.auth_user_id` の外部キーに `ON DELETE` 句の指定が無い（v13 §7）
- ステータス: 未回答（正本の記述どおりに実装して進行中・ブロッカーではない）
- 優先度: 低（`auth.users` の行を削除する運用が Phase 1 に無ければ実害は出ない）
- 背景: Issue #29（WBS `2-1`）の段取り作成時に判明。調査メモ「仕様に記載が無い点」。
  正本 v13 §7 は「`uuid` UNIQUE NULL / `REFERENCES auth.users(id)`」まで、
  `会員データモデル §5.2`・`DB物理設計 §2` も
  `ALTER TABLE members ADD COLUMN auth_user_id uuid UNIQUE REFERENCES auth.users(id);` までで、
  `ON DELETE` を書いていない（PostgreSQL の既定は `NO ACTION`）。
  `member_profiles_private.member_id` には `ON DELETE CASCADE`、`member_role_changes` の2 FK には
  `ON DELETE RESTRICT` が明記されているのと対照的である。
  `DB物理設計 §6-8④` は CI のテストユーザーを `supabase.auth.admin.createUser()` で毎回作るとしており、
  テストの後始末で `auth.users` の行を消す経路が生じうる（既定の `NO ACTION` では
  `members.auth_user_id` が残っていると削除が失敗する）。
- 採用した内容: **記述どおり `ON DELETE` 句を書かない**（＝ `NO ACTION`）。正本・派生文書とも
  明示していないものを段取りで補うのは仕様策定に当たるため（設計 §0）。
- 検討の要否: `auth.users` の削除時に `members.auth_user_id` を `NULL` に戻す
  （`ON DELETE SET NULL`）ほうが、`account_status = 'withdrawn'` かつ `auth_user_id` 任意という
  v13 §7 のアカウント状態表と整合するか。オーナー判断待ち。
  変更する場合は既存マイグレーションを書き換えず追加マイグレーションで行う（`CLAUDE.md` §4.5）。
- 関連ファイル: v13 §7、`docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.2、
  `docs/spec/detailed-design/DB物理設計.md` §2・§6-8④、Issue #29

## ⚡ [2026-09-14] `oyakata_member_no` が `DB物理設計.md` §6-6b① にだけ現れ、列定義がどこにも無い
- ステータス: ~~未回答~~ → ✅ **解消（2026-09-22）：`0029`（WBS `2-7`）で専用列として新設した**
- 優先度: ~~低~~ → **解消**
- **決着の内容**: `DB物理設計.md` §6-6b① の側（専用列がある前提）が正しかった。
  接頭辞方式（`legacy_member_no` に `OYA-`）を採れないのは、2026-09-10 決定が
  「**両方の番号を持つ会員**は街人番号で表示する」と定めており、正本 v13 §9 #26 により
  その会員（親方兼街人）が**1レコードに統合されている**ためである。
  1列では一方を捨てるしかなく、決定そのものが実装不能になる。
  採番方式は 2026-09-22 オーナー決定（§9 #62 ⑤）で「**一意であれば何でもよい**」と定まっており、
  実装は `OYA-` ＋3桁ゼロ埋め・起点45（1〜44 は親方衆44名の移行枠）を採った。
- 背景: Issue #29（WBS `2-1`）の段取り作成時に判明。調査メモ「派生文書どうしの食い違い」。
  `DB物理設計.md` §6-6b① の可否表は `oyakata_member_no` という列名を挙げるが、
  `会員データモデル §5.2a` の warning が「本リポジトリの設計文書に `oyakata_member_no` の列定義は1件もない」
  「専用列を持たせるのか接頭辞で足りるのかは未決」と自ら指摘している。正本 v13 §7 にも記述が無い
  （`grep` 済み）。同ノートは `legacy_member_no` に `OYA-` 接頭辞を付ける形で親方会員番号を実現している。
- 採用した内容: `2-1` の `members` に **`oyakata_member_no` 列を作らない**（`会員データモデル §5.2` の列表に無いため）。
- 派生文書側の訂正要否: `DB物理設計.md` §6-6b① の可否表から当該行を削除するか、
  逆に列として新設するか。`2-2`（列単位 GRANT）の着手前に決まっていればよい。オーナー判断待ち。
- 関連ファイル: `docs/spec/detailed-design/DB物理設計.md` §6-6b①、
  `docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.2・§5.2a、Issue #29

## [2026-09-14] ⚡ 出典表記に助詞を挟む形（`` `画面設計.md` に §2-4 ``）を `wbs_to_issue.py` が拾えない

- ステータス: 未回答（⚡ 非ブロック）
- 優先度: 低（起票の質に影響するが、ループを止めない）
- 背景: 2026-09-14 に `extract_spec_refs()` の出典判定を修正し、装飾（バッククォート・太字）と
  語尾「モデル」「定義」を扱えるようにした（WBS 全体で8件の誤判定を是正）。
  ただし `` `画面設計.md` に **§2-4 画面ID対応表** `` のように**助詞を挟む**形は今も拾えず、
  出典なし＝正本 v13 として扱われる。WBS `18-2` に実在する。
- なぜ直さなかったか: 助詞を許すと `CLAUDE.md に書いてある v13 §4` のような文まで
  誤って出典化する。誤検知を増やす修正は、**間違った根拠が正しい顔で載る**という
  元の不具合と同じ害をもたらすため、隣接形だけを対象にした。
- 実害: v13 に同じ番号の節が実在すると、誤った根拠がゲート1まで温存される
  （設計 §10.3 が「最頻の事故ポイント」と呼ぶ状態）。`18-2` の `§2` がこれに当たる。
- 論点:
  - ① WBS 側の表記を隣接形（`` `画面設計.md` §2-4 ``）へ統一するか
  - ② 抽出側で助詞（に・の・で・は）を限定的に許すか（誤検知とのトレードオフ）
  - ③ 出典候補を `docs/` 配下の実ファイル名と照合して確定するか（語尾リストの当てもの廃止）
- 関連ファイル: `automation/scripts/wbs_to_issue.py`（`SOURCE_BEFORE_RE`・`strip_source_markup`）、
  `automation/scripts/wbs_selftest.py` 検査6、`docs/spec/WBS_Phase1.md` `18-2`
- ブロックするもの: なし（`18-2` を起票する時点で根拠を目視確認すれば足りる）

## [2026-09-13] Issue #23（WBS `4-1`）の完了条件4・5を検証する土台が無い（`members`・`role`・RLS ヘルパーが未実装／v13 §5.9.3・§8）
- ステータス: ✅ **全面解決（2026-09-19）**。絞り込んだ論点（ログイン導線が無い状態で完了条件4・5 をどう満たすか）は
  **オーナーが推奨どおり A' を採用**（Issue #23 コメント ／ 2026-09-19）。`morning_meetings` の DDL・RLS・投入UI・保存処理までを
  `4-1` で実装し、**検証は DB レベルの RLS テスト**で行う。**UI 到達制御は `2-3` の担当**（完了条件4 の「投入画面に到達できず」は
  `2-3` へ送る）。**下記「併せて確認」の `2-2` との担当の切れ目も A' の範囲に含める**（`morning_meetings` のポリシーと GRANT を
  `4-1` が先に置き、マイグレーションに「このポリシーは `2-2` で見直す」と明記する）。なお前提として挙げていたログイン導線の不在も
  **`2-1b` の完了（#39 ／ 2026-09-16）で解消済み**（`src/middleware.ts`・`src/lib/auth/guard.ts`・`src/app/login` が main に実在）。
  ~~✅ 解決（2026-09-15）~~。DB 側は `2-1`（PR #33）で解消。残っていた「ログイン導線の不在」も、**根本原因が正本の空白（会員ログイン方式が未定義）**と判明し、v13 **§5.2.6**（メールOTP ／ §9 #64）で確定。実装担当として WBS **`2-1b`** を新設した。~~一部解決（DB 側のみ）／選択肢 A'・B'・C'~~ → **B' 相当（ログイン導線を独立パッケージ化）で決着**
  （下記「2026-09-15 の更新」を参照。絞り込み後の選択肢は A'／B'／C'）
- 優先度: 高（Issue #23 の実装着手がブロックされる。`4-2`・`4-3` は `4-1` に依存する）
- 背景: Issue #23 の段取り作成時に判明。調査メモ（Issue #23 コメント「関連する既存実装（リポジトリ内の現況）」）が
  次の事実を報告している。`supabase/migrations/` は `0100_rag_pgvector_knowledge_chunks.sql` の1本のみで、
  同ファイル自身が「前提となる `public.members` 等（WBS `2-1` の成果物）は未作成」と明記している。
  したがって **`public.members` / `public.current_member_role()` / `public.is_staff()` / `public.is_admin()` は
  リポジトリ上に存在しない**（定義は `DB物理設計.md` §6-3 に設計としてのみ存在）。
  `src/app/` にもログイン導線・`middleware.ts`・ロールガードは未実装である。
  一方、ゲート1で承認された Issue #23 の完了条件4「`member` / `guest` は投入画面に到達できず**サーバサイドでも拒否される**」・
  5「保存した議事録が**他の会員から読めないこと**が確認できる」は、`members.role` に基づくサーバサイド認可の
  実在を前提としている（v13 §8「認可の判定は `members.role` のみを根拠とする」）。
  また `DB物理設計.md` §3-4 の `morning_meetings.created_by` は `members(member_id)` への外部キーであり、
  **`members` が無い状態ではマイグレーションそのものが適用できない**。
  WBS `4-1` の依存欄は「なし」だが、これは 2026-09-05 のスコープ変更で `1-4`（署名付きURL基盤）が
  外れた際の記載であり、`2-1` との関係は検討されていない。`2-1` の実装進捗は 0% である。
- 選択肢:
  - A. `4-1` の依存に `2-1`（ロール・アカウントステータス スキーマ）を戻し、`2-1` の完了まで `4-1` を待つ
    （`WBS_Phase1.md` 4-1 の依存欄「なし」を訂正する）
  - B. `4-1` の中で `members` と `current_member_role()` / `is_staff()` を**最小限だけ**新設し、`4-1` を単独で完結させる
  - C. `4-1` では `morning_meetings` の DDL・RLS・投入UI・保存処理までを実装し、
    完了条件4・5 の**検証（認可テスト）だけ**を `2-1` 完了後の別タスクへ切り出す
- 推奨: A（理由: ①`morning_meetings.created_by` の外部キー先が存在せず、DDL が適用できない（`DB物理設計.md` §3-4）。
  ②B は `2-1` の確定設計（PII別テーブル分離・`role` 自己変更禁止トリガー §6-6b・`anon` への GRANT/REVOKE）と
  重複し、CLAUDE.md §4.5「既存マイグレーションを書き換えない」により後から正規版へ差し替えられない。
  ③C は認可（v13 §6・§5.9.3）と個人情報（`morning_meetings` は PII-A）に関わる完了条件を未検証のまま
  完了扱いにすることになり、v13 §8「クライアント側の制御のみに依存した実装を禁止する」の担保が空になる。
  なお `2-1` は 2026-09-13 に `1-1`（Supabase プロジェクト作成）が完了して**着手可能**になっている）
- 関連ファイル: v13 §5.9.3・§6・§8、`docs/spec/detailed-design/DB物理設計.md` §3-4・§6-3、
  `docs/spec/WBS_Phase1.md` 4-1・2-1、`supabase/migrations/0100_rag_pgvector_knowledge_chunks.sql`、Issue #23

### 2026-09-15 の更新（Issue #23 の再計画時に確認）

**解消した部分（DB 側）**: 上記の前提のうち「`members` / RLS ヘルパーが無い」はもう成り立たない。
推奨していた **A（`4-1` の依存に `2-1` を戻す）はオーナーが採用済み**（`WBS_Phase1.md` 4-1 依存欄
「~~なし~~ **2-1**」・2026-09-13 決定）で、**`2-1` は 2026-09-14 に完了した**（Issue #29 ／ PR #33）。
リポジトリ上に `supabase/migrations/0001_members_schema.sql`（`members`・`member_profiles_private`・
`member_role_changes`）・`0002_members_auth_helpers.sql`（`current_member_id()` / `current_member_role()` /
`is_staff()` / `is_admin()`）・`0003_members_guard_triggers.sql` が実在する。
`morning_meetings.created_by` の外部キー先も埋まったため、**DDL が適用できない問題は解消した**。
ヘルパ4本は **`SECURITY DEFINER` ＋ `SET search_path = ''`** で定義されており、
`2-2`（RLS ポリシー本体・GRANT/REVOKE）が未実装でも `role` の判定そのものは機能する。

**解消していない部分（アプリ側）**: 起票時の指摘のうち
「`src/app/` にログイン導線・`middleware.ts`・ロールガードが無い」は**今も事実**である。
`src/app/` は `page.tsx` / `layout.tsx` / `api` のみ、`middleware.ts` は不在、`getUser` の呼び出しは0件。
`src/lib/supabase/server.ts` 自身が「**セッション更新は middleware 側で行う前提**」と注記しているが、
その middleware が存在しない。`2-3`（ロール別UI表示制御・アクセス制限画面）は実装 0%、
かつ **WBS にログイン導線そのものを持つ作業パッケージが無い**。
このため Issue #23 の完了条件4「`member` / `guest` が投入画面に到達できず**サーバサイドでも拒否される**」と
完了条件5「他の会員から読めないこと**が確認できる**」について、**何をもって「合格」とするかが決められない**
（`admin` と `member` を名乗り分ける手段がリポジトリに無いため）。

**絞り込んだ論点**: `4-1` は、ログイン導線が無い状態で完了条件4・5 をどう満たし、どう検証するか。
- 選択肢:
  - A'. `4-1` では `morning_meetings` の DDL・RLS ポリシー（INSERT / SELECT ＝ `is_staff()`）・
    `GRANT SELECT, INSERT`・投入UI・保存処理までを実装する。完了条件4・5 の検証は
    **DB レベルの RLS テスト**（`request.jwt.claims` を差し替えて `admin` / `core_member` / `member` /
    `guest` / 未認証の5通りを流す）で行い、ログイン導線とUIの到達制御は `2-3` 側に残す
  - B'. ログイン導線（`middleware.ts` ＋ Supabase Auth セッション ＋ ロールガード）を新しい作業パッケージとして
    WBS に起こし、それが完了するまで `4-1` をさらに待たせる
  - C'. `4-1` の中で最小限のログイン導線・`middleware.ts`・ロールガードを併せて新設する
- 推奨: A'（理由: ①ヘルパ4本が `SECURITY DEFINER` のため、**ログインUIが無くても `role` による拒否は
  今日から実装・検証できる**。v13 §8 が要求するのは「クライアント側の制御のみに依存しないこと」であり、
  RLS による拒否が実証できれば担保は満たせる。②C' は `2-2`（`anon` 権限ゼロ・GRANT/REVOKE）と
  `2-3`（アクセス制限画面）の確定済み設計と重複し、CLAUDE.md §4.5「既存マイグレーションを書き換えない」に
  よって後から正規版へ差し替えられない。③B' は **WBS に存在しないパッケージ**を待つことになり、
  2026-09-13 に依存を `2-1` へ戻した判断と同じ足止めを繰り返す。
  なお A' を採る場合、完了条件4 の「投入画面に**到達できず**」の部分は**UI到達制御を `2-3` へ送る**ことを
  意味するため、Issue #23 の完了条件4 の文言もあわせて確定させたい）
- 併せて確認（`2-2` 未実装との関係）: `0001` は `ENABLE ROW LEVEL SECURITY` のみを置き、
  ポリシー本体と GRANT/REVOKE を `2-2` へ送っている（同ファイル L18・L163「ポリシー未定義＝全拒否」）。
  A' を採ると `4-1` が `morning_meetings` について**先に**ポリシーと GRANT を置くことになるため、
  `2-2` との担当の切れ目（テーブルごとに置くのか、`2-2` で一括するのか）も同時に決める必要がある。
  この点は上記「⚡ [2026-09-13] `morning_meetings` の派生文書3件が…」で指摘済みの
  **`GRANT INSERT` の記述がどこにも無い**問題と同じ箇所に当たる。

---

## ⚡ [2026-09-13] `morning_meetings` の派生文書3件が 2026-09-05 決定（§9 #63）に未追随（v13 §9 #63・§6）
- ステータス: 未回答（正本優先で進行中・ブロッカーではない）
- 優先度: 低〜中（派生文書を放置すると、`4-1`・`4-2` の実装者が録音前提・`admin` 限定の記述を根拠にしうる）
- 背景: Issue #23 の段取り作成時に判明。調査メモ「派生文書の記述（正本との食い違いを含む）」#5〜#7。
  ①`detailed-design/API設計.md` §2-3 `POST /api/morning-meetings` が「録音終了→音声アップロード起点
  （Gemini 連携をサーバ側で実行）」かつ認可 `admin` 限定。②`detailed-design/画面設計.md` C3「朝会・議事録モジュール」が
  対象ロール `admin`・録音・60秒SLA表示。③`basic-design/frontend/HTMLモック_v13.md` ②と
  `docs/design/prototype_v15.html` が録音（タイマー／サンプル音声読込）前提
  （`WBS_Phase1.md` 4-1 も「モック②は録音前提のため要改訂」と記載）。
- 採用した内容: 正本 v13 §9 #63（アプリは音声を扱わない／UIへのコピー＆ペースト／API連携・ファイルアップロードは行わない）と
  v13 §6（朝会＝管理者〇・コアメンバー〇）を採用して進行する。根拠は CLAUDE.md §1.1「矛盾時は正本が勝つ」。
  なお `DB物理設計.md` §3-4 の `audio_storage_path` と RLS ⑦ の `mm_insert_admin` については、
  2026-09-13 のゲート1で同じ扱い（正本優先）が既に確認されている（上記「[2026-09-13] WBS `4-1` …」の申し送り参照）。
- 派生文書側の訂正要否: `API設計.md` §2-3・`画面設計.md` C3・`HTMLモック_v13.md` ②・`prototype_v15.html` を
  正本に合わせて訂正するか。オーナー判断待ち（自律ループは `docs/spec/` を編集しない・設計 §0）。
- 併せて記録（矛盾ではなく派生文書側の欠落）: `DB物理設計.md` §6-6 は `morning_meetings` に `GRANT SELECT` のみを
  与えており、`GRANT INSERT` の記述がどこにもない。Supabase 公式ドキュメントは「grants と policies の二段で
  チェックされ、**grant が無ければポリシー評価の前に `42501` で落ちる**」と明記しているため、
  INSERT を許す実装には `GRANT INSERT` が必要になる（https://supabase.com/docs/guides/database/postgres/row-level-security ）。
- 追記（2026-09-19・Issue #55 の段取り作成時に発見 ／ **上の3件に入っていない新規の2件**）:
  ④`docs/spec/detailed-design/外部連携設計.md` L155-160「## 4. 朝会音声処理（Gemini直接連携）」が
  「統一メディア基盤へアップロード → **GCS URI を Gemini へ直接渡す**」のまま。
  ⑤`docs/spec/basic-design/infra/システムアーキテクチャ.md` L28「**朝会音声のみ Gemini API**」および
  L100・L827・L949 の図中ノード「Gemini API 朝会音声」。
  いずれも §9 #63（アプリは音声を扱わない／**ファイルアップロードは行わない**）と食い違う。
  **正本優先で進行する**（Issue #55 の `1-5` は音声・GCS URI を受け取る口を持たないクライアント層を作る）。
  訂正の要否は上と同じくオーナー判断待ち。あわせて `.env.example` の `GEMINI_API_KEY` の説明
  （「朝会音声のマルチモーダル処理用」）も未追随だが、こちらは `docs/spec/` 配下ではないため
  Issue #55 の変更対象に含めた。
- 追記（2026-09-19・Issue #23 の段取り作成時に発見 ／ **正本内部の記述差2件。いずれも非ブロック**）:
  ⑥`v13 §7`「★ 朝会・議事録データ」（L2166-2168）が保持項目に**「音声ファイルURL」を挙げたまま**である。
  §9 #63 の決定欄は未追随箇所として **§5.1 と §8 の P3 の2つだけ**を挙げており §7 は挙げていないため、
  形式上は正本内部の記述差にあたる。ただし ⒜§9 #63 は正本内の日付入りオーナー決定であり
  （CLAUDE.md §1.1）、⒝同じ論点（`DB物理設計.md` の `audio_storage_path`）について
  2026-09-13 のゲート1で正本優先が確認済み、⒞Issue #23 の完了条件3 が求める読み出し項目に
  音声ファイルURL は含まれていない——の3点から、**新たな未確定事項ではないと判断し止めていない**。
  `4-1` では音声関連の列を置かない。訂正の要否はオーナー判断待ち。
  ⑦`v13 §5.9.1` のタブ表（L1601-1615）に**朝会・議事録の行が無い**（§6 の権限マトリクス L2106 にはある）。
  §5.9.5 は「**空振りの禁止**＝権限があるのにメニューへ出ていない画面を作らない」「ナビ定義と §5.9.1 の表は
  同一のロール定義から生成する」と定めており、`src/lib/auth/navigation.ts` の `AreaKey` にも朝会の領域が無い。
  **`4-1` ではナビ項目を追加しない**（2026-09-19 オーナー決定 A' により UI 到達制御・ナビ導線は `2-3`／`2-6` の担当）。
  `4-1` 完了時点では投入画面へメニューから到達できない状態になるため、**`2-3`／`2-6` で §5.9.1 の表ごと
  朝会・議事録の行を起こす必要がある**ことをここに記録する。
- 関連ファイル: v13 §9 #63・§6・§7（L2166-2168）・§5.9.1（L1601-1615）・§5.9.5、
  `docs/spec/detailed-design/API設計.md` §2-3、
  `docs/spec/detailed-design/画面設計.md` C3、`docs/spec/basic-design/frontend/HTMLモック_v13.md`、
  `docs/design/prototype_v15.html`、`docs/spec/detailed-design/DB物理設計.md` §6-6、
  `docs/spec/detailed-design/外部連携設計.md` L155-160、
  `docs/spec/basic-design/infra/システムアーキテクチャ.md` L28・L100・L827・L949、
  `src/lib/auth/navigation.ts`、Issue #23、Issue #55

---

## [2026-09-13] WBS `4-1`（朝会テキスト投入UI）の完了条件と、`4-2` との範囲の切れ目（v13 §5.1・§9 #63）
- ステータス: 回答済み（2026-09-13）
- 優先度: ~~中（Issue #23 の実装着手がブロックされる。`4-2`・`4-3` は `4-1` に依存する）~~ → **解除（2026-09-13 の回答による）**
- 背景: Issue #23（WBS `4-1` から自動生成）の「完了条件」と「スコープ外」が雛形の
  `〔「〜が〜できる」の形で、検証可能に書く〕` のまま未記入で、起票前チェックの3つ目も未チェックである。
  2026-09-05 決定（v13 §9 #63）で「貼り付け時点で議事録として格納する」ことは確定しているが、
  **格納するテーブル・画面の保存操作までを `4-1` に含めるのか、投入UIだけで切るのか**が読み取れない。
  機械が解釈すると受入テストの合格条件を PM が決めることになり、ゲート1が空回りする。
  なお v13 §5.1 の本文は**録音前提のまま未改訂**であり（§9 #63 が明記）、節番号だけでは範囲を確定できない。
- 選択肢:
  - A. 投入UI ＋ 貼り付けたテキストを朝会・議事録レコードとして保存するところまでを `4-1` とする
    （構造化議事録の生成・クエスト候補抽出は `4-2`）
  - B. 投入UI（管理画面の入力フォーム・文字数等のバリデーション・保存API呼び出し）までを `4-1` とし、
    テーブル新設と永続化は `4-2` 側へ寄せる
  - C. 投入 ＋ 保存 ＋ 構造化議事録の生成までを `4-1` に含め、`4-2` はクエスト候補抽出のみとする
- 推奨: A（理由: v13 §9 #63 が「**貼り付け時点で議事録として格納する**」と明記しており、
  投入と格納を切り離すと「貼り付けたのに何も残らない」中間状態になる。
  v13 §7「朝会・議事録データ」に全文文字起こしテキストの保持が定義済みで、保存先も既に設計されている。
  想定サイズ S とも整合する）
- 関連ファイル: v13 §5.1・§7「★ 朝会・議事録データ」・§9 #63、`docs/spec/WBS_Phase1.md` 4-1・4-2、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §1（2026-09-05 決定）、Issue #23
- 回答（2026-09-13・プロジェクトオーナー ／ Issue #23 コメント）: **A で確定**。
  投入UI ＋ 貼り付けたテキストの**議事録レコードへの保存まで**を `4-1` に含める。
  構造化議事録の生成（`4-2`）とクエスト候補の抽出・起案（`4-2`・`4-3`）は `4-1` のスコープ外。
  根拠: v13 §9 #63「貼り付け時点で議事録として格納する」、保存先の項目は v13 §7「★ 朝会・議事録データ」に定義済み。
  この回答に基づき Issue #23 本文の「完了条件」「スコープ外」が記入された（各項目に根拠節を添付）。
- ⚠️ 実装時の申し送り（2026-09-13 PM）: `docs/spec/detailed-design/DB物理設計.md` の `morning_meetings` 定義が、
  正本 v13 の 2件の記述と食い違っている。**CLAUDE.md §1.1（矛盾時は正本 v13 が勝つ）に従い正本を採る**。
  ① `audio_storage_path text NOT NULL`（§3-4）は、v13 §9 #63「アプリは音声を扱わない」と矛盾する → 音声列を置かない
  ② RLS ⑦ の `mm_insert_admin`（INSERT を `is_admin()` 限定）は、v13 §6 権限マトリクス
  「朝会録音・議事録自動生成・クエスト自動抽出 ＝ 管理者〇／コアメンバー〇」と矛盾する → INSERT は `is_staff()` とする
  （SELECT は `is_staff()`、DELETE は全拒否のまま。**本人ポリシーは書かない** — DB物理設計 RLS ⑦ の注記どおり）。
  `DB物理設計.md` 側の追随改訂は仕様策定側の作業であり自律ループの対象外（設計 §0）。

---

## [2026-09-13] WBS `1-4`（署名付きURL基盤）の完了条件と、`14-1` との範囲の切れ目（v13 §5.11.2）
- ステータス: ✅ **回答済み（2026-09-14）: B — `1-4` は v13 §5.11.2 の①〜⑥の経路一式を含む**
  （署名付きURL発行の基盤 ＋ Object Finalize 起点の後処理・`pending`→`ready` 書き戻し・孤児ファイル日次検出）。
  `14-1` はアップロードUI導線と用途タグ入力に限る。AIタグ付け（§5.11.3）は Phase 2 のため範囲外。
  回答は Issue #18 のコメント（2026-09-14）で行われた。
  ⚠️ **本回答により `media_assets` への `pending` 先行作成が `1-4` に入ったが、同テーブルを作る
  パッケージが WBS 上に無い論点が残った** → 本ファイル冒頭「[2026-09-15] `media_assets` テーブル（DDL ＋ RLS）を
  作る作業パッケージが WBS 上に存在しない」へ分離して起票済み
- 優先度: 高（Issue #18 の実装着手がブロックされる。`5-3`・`14-1` も `1-4` に依存する）
- 背景: Issue #18（WBS `1-4` から自動生成）の「完了条件」が雛形の
  `〔「〜が〜できる」の形で、検証可能に書く〕` のまま未記入で、起票前チェックの3つ目も未チェックである。
  範囲を機械が解釈すると、v13 §5.11.2 の①〜⑥のどこまでを「完了」とみなすかを PM が決めることになり、
  ゲート1（オーナーの意図との照合）が空回りする。
  切れ目が曖昧なのは Cloud Functions の後処理（③④＝サムネイル生成・Exif抽出・`pending`→`ready` 書き戻し）と
  孤児ファイルの日次検出であり、これらは `1-4` の WBS 説明文（「GCSバケット・Cloud Functions・Supabase Edge Function」）にも、
  `14-1` メディアアップロード基盤（「Object Finalize 起点」・規模 S・依存 `1-4`）にも読める。
- 選択肢:
  - A. `1-4` は**署名付きURLの発行までの基盤**に限る（バケット・IAM 設定／Edge Function による PUT・GET の
    V4署名付きURL発行とロール判定／`media_assets` の `pending` 先行作成）。Cloud Functions の後処理と
    孤児ファイル掃除は `14-1` 側へ寄せる
  - B. `1-4` は **v13 §5.11.2 の①〜⑥の経路一式**（A の範囲＋ Object Finalize 起点の Cloud Functions 後処理・
    `pending`→`ready` 書き戻し・孤児ファイル日次検出）を含む。`14-1` はアップロードUI導線と用途タグ入力に限る。
    AIタグ付け（§5.11.3）は Phase 2 のため範囲外
  - C. `1-4` を分割する（`1-4a` バケット・IAM 設定／`1-4b` Edge Function の署名付きURL発行／
    `1-4c` Cloud Functions 後処理）。WBS の改訂を伴う
- 推奨: B（理由: `14-1` は規模 S で `1-4` に依存する位置づけであり、A を採ると S のパッケージへ
  Cloud Functions 一式が移って規模が破綻する。また `1-4` の WBS 説明文が Cloud Functions を明記している。
  非同期ジョブの実行基盤は 2026-09-10 に Cloud Tasks ＋ Cloud Scheduler で確定済みのため、B でも新たな未決は生じない）
- 追記（2026-09-13・PM）: **回答は本ファイルの A／B／C で行ってほしい。**
  Issue #18 本文の ⚠️1 が**同じ A/B/C の記号で別の内容**を割り当てており、1文字での回答が一意に定まらない。
  対応関係は次のとおり（Issue 本文 → 本ファイル）:
  - Issue ⚠️1 の A（Cloud Tasks 経由で関数が起動する「経路の疎通」まで。実処理は `14-1`）→ **本ファイルには対応する選択肢が無い**（A と B の中間）
  - Issue ⚠️1 の B（サムネイル生成・Exif抽出・書き戻しまで含む）→ 本ファイルの **B** にほぼ相当
  - Issue ⚠️1 の C（Cloud Functions は全て `14-1`）→ 本ファイルの **A** にほぼ相当

  なお A・B のいずれを採っても **`media_assets` の `pending` 先行作成**が `1-4` に入るため、
  同テーブルを作る作業パッケージ（現状 WBS 上で特定できない。Issue #18 ⚠️3）の担当を併せて指定する必要がある。
- 申し送り（本件の回答後に別途起票する。本件とは論点が異なるため束ねない）:
  ①ファイルサイズ上限の具体値（v13 §5.11.5 は `x-goog-content-length-range` での強制のみを定め、数値が無い／Issue #18 ⚠️2）
  ②GCP プロジェクトを `line-rag-bot` と共用するか（`CONSOLIDATED_DECISIONS.md` §15-2 の既存未決／⚠️4）
  ③予算アラートの水準（v13 §5.11.4 の例示は月5,000円だが、監査ログの GCS 保管追加後の妥当性が未確認／⚠️5）
- 関連ファイル: v13 §5.11.2、`docs/spec/WBS_Phase1.md` `1-4`・`14-1`、Issue #18

## [2026-09-14] 内部専用ナレッジ添付メディアの「公開範囲の継承」に、継承元が存在しない（v13 §5.11.5・§7）
- ステータス: ✅ **回答済み（2026-09-15）: C — Phase 1 の `1-4` から外し Phase 2 へ送る。閲覧用URLの TTL は 5分に一本化する**
- 優先度: 高（Issue #18 ／ WBS `1-4` を止めていた）
- 背景: Issue #18（WBS `1-4`）の段取り作成時に判明。正本 v13 §5.11.5「公開範囲の継承」は
  「内部専用ナレッジの添付ファイルは、ナレッジ本体の `target_role` を継承する」と定め、§7 は `media_assets` の
  紐付けキーに `knowledge_id` を挙げているが、**継承元が実在しない**。
  ①`DB物理設計.md` §3-7 の `media_assets` DDL に `knowledge_id` も `target_role` も無い
  （紐付けは `linked_quest_id` / `linked_work_log_id` / `place_id` のみ。`visibility` は `公開`/`運営のみ` の2値で別軸）、
  ②ナレッジ本体テーブルが Supabase に無い（§9 #31 の RAG 統合で `knowledge_items` は「実装対象ではない」と明記。
  現存する `knowledge_chunks` はチャンクであって本体ではない）、③メディア↔ナレッジのリンク定義がリポジトリ内に1件も無い。
- 選択肢:
  - A. `knowledge_id` で現存の `knowledge_chunks.target_role` を参照する
  - B. `target_role` を `media_assets` へスナップショット保持する
  - C. Phase 1 の `1-4` から外し Phase 2 へ送る。閲覧用URLの TTL は 5分に一本化する
- 採用: **C**（オーナー回答・2026-09-15）。理由: `WBS_Phase1.md` §9 の `9-3` が**ナレッジ登録・編集は
  line-rag-bot Streamlit 側で完結**としており、**アプリ側にナレッジへメディアを添付する導線が Phase 1 に存在しない**。
  継承元も呼び出し元も無いまま宙に浮く。A は Firestore 側（line-rag-bot）の値をアプリ側の認可判定に持ち込むことになり、
  §9 #31 が定めた「API連携ゼロ」の境界を跨ぐため不可。B は継承元が後から変わっても追随しない値を認可判定に使うことになる。
- ⚠️ **正本側の未追随**: v13 §5.11.2 ⑥ ほかに残る「内部専用ナレッジの添付は2分」の記述は、本決定と食い違う。
  **文言修正は仕様策定側の作業**であり、自律ループの対象外（設計 §0 ／ `CLAUDE.md` §1.1）。
- 関連ファイル: v13 §5.11.2（L1963）・§5.11.5、`docs/spec/detailed-design/DB物理設計.md` §3-7、
  `docs/spec/WBS_Phase1.md` `9-3`、Issue #18

## [2026-09-13] 「どちらもAで実行」の適用先と、Issue #4 の成果物（v13 §5.4.1）
- ステータス: ✅ **回答済み・クローズ（2026-09-13・起票と同日）**
- 優先度: ~~高~~ → 解消

> [!success] オーナー決定（2026-09-13・Issue #4）
> **選択肢 C。**「本 Issue のスコープを広げ、**注文完了画面・マイログの画面実装まで含める**。
> **進捗を WBS に反映すること**」
>
> | 影響 | 内容 |
> | --- | --- |
> | Issue #4 のリスク区分 | **低 → 高**（認可・個人情報・金額表示・DB マイグレーションに該当。ゲート1・2・3／承認3回） |
> | 実質サイズ | 想定 S → **M〜L**（WBS `6-1`・`8-4` はいずれも実装 0%） |
> | 追加の完了条件 | `docs/spec/WBS_Phase1.md` の `6-1` / `6-5` / `8-4` を実装後の実態へ更新する（**実装 PR 側で実施**。`docs/spec/` は PM の権限外） |
> | スコープ外は据え置き | Realtime 連携・状態遷移ロジック・更新権限・B1 カンバンは引き続き対象外 |
>
> 「[2026-09-03] 提供ステータス表示文言の仕様内不整合」の**論点2（適用画面の範囲）も本決定で A に確定**した
> （注文完了画面・マイログの両方に表示する）。論点1（`画面設計.md` の表記訂正）は未回答のまま残る。
- 背景: Issue #4（セルフ注文完了画面の提供ステータス文言の統一）で PM が停止し、
  オーナーから「**どちらもAで実行**」の回答を得た。ところが回答の宛先になり得る A が2系統ある。
  - (a) Issue コメントの主問（A/B/C・推奨A）＝「`1-2`／`6-1` 完了までブロックし、文言統一は `6-5` の一部として実施する」
  - (b) 本ファイル「[2026-09-03] 提供ステータス表示文言の仕様内不整合」の論点1・論点2（各 A/B の2択）
    ＝ 論点1-A「正本どおり『調理中』で確定」／論点2-A「注文完了画面・マイログの両方に表示する」

  「どちらも」は2件を指す語であり (b) に合致する一方、「実行」は着手を促す語で (a) の A（ブロック継続）と噛み合わない。
- 前提の変化: 停止時点で根拠にした「足場が無い」は**解消済み**。`1-2` Next.js雛形は 2026-09-05 に投入され
  （WBS L187・80%）、`src/lib`・`src/components` の配置規約が確定している。
  一方 `6-1`（セルフ注文）・`8-4`（マイログ）は **0%** で、表示先の画面は依然として存在しない。
- 確認したいこと: 上記を踏まえ、Issue #4 が今回何を成果物とするか
- 選択肢:
  - A. 「どちらもA」を (b) への回答として受け取り、本 Issue では**表示ラベルの単一情報源のみ**を実装する
    （`未提供 → 「調理中」`／`提供済み → 「提供済み」` の写像と単体テスト）。画面への適用は `6-1`／`8-4` に残す
  - B. (a) への回答として受け取り、本 Issue は `6-1` 完了まで `auto:blocked` のまま据え置き、文言統一は `6-5` で実施する
  - C. 本 Issue のスコープを広げ、注文完了画面・マイログの画面実装まで含める
- 推奨: A（理由: 停止時に B 案を避けた根拠＝「ディレクトリ構成をこのタスクの副産物として決めてしまう」が、
  `1-2` 投入で失効した。`src/components/ui/Money.tsx` と同じ表示層の共通化として先行実装でき、
  `6-1`／`8-4` が後から参照できる。C は想定サイズ S と Issue のスコープ外宣言に反する）
- 関連ファイル: v13 §5.4.1、Issue #4、`docs/spec/WBS_Phase1.md` L187・L256・L260、
  本ファイル「[2026-09-03] 提供ステータス表示文言の仕様内不整合」

---

## [2026-09-08] 自律開発ループの実行方式の確定と、移行の実施時期（決定ログと設計書でステータスが食い違っている）
- ステータス: **✅ 回答済み・クローズ（2026-09-08・起票と同日）**
- 優先度: ~~**高**~~ → 解消

### ✅ 回答（2026-09-08・プロジェクトオーナー）

> 「移行ってなにからなによ。**エージェント組み込みの自律ループができているんだから、そこに流すだけだろ**」

**「移行」という作業は存在しない。** 2026-09-07 の決定は**作り直しではなく方式の再定義**であり、
エージェント定義11本・`auto-01`〜`auto-03`・承認ゲートはすでに揃っている。**やることはタスクを流すことだけ。**
したがって本項目の論点②「移行を残22日のどこで実施するか」は**成立しない**。

**⚠️ ただし、起票時の調査で「流すだけ」を実際に阻んでいた不具合が2件見つかり、同日修正した:**

| # | 不具合 | 内容 | 状態 |
| --- | --- | --- | --- |
| 1 | **参照切れ7か所** | `auto-02`／`auto-03` が `automation/prompts/*.md` を指したままだった（09-07 に `automation/agents/` へ改名した際の追随漏れ）。**存在しないパスを読ませており、実行すれば必ず失敗する状態だった** | ✅ 修正済み |
| 2 | **frontmatter 未付与7本** | 09-05 作成の定義（`test-design`／`coding`／`review-quality`／`review-spec`／`review-privacy`／`fix`／`pm-report`）に `tools:`／`settings:` が無く、設計 §7.2 の「権限を設定で絞る」が効いていなかった | ✅ 付与済み |

現況: **`python automation/scripts/check_agents.py` は違反なし（11体すべて ok）**。

- ⚠️ **残る着手条件**（本項目のブロッカーではない）: 実装ループの試運転（設計 §9 フェーズ3 の 3-2／3-4）と、
  Environment **`gate-low-risk-objection`**（Wait timer 30分／Required reviewers なし）の作成
- ⚠️ **文書側の追随が必要**: `docs/自律開発ループ設計.md` §12.1 が `## 12. 未確定事項` 配下の
  「検討中」のままである。**「決定」側へ揃えること**
- ブロックするもの: WBS `0-D`。**アプリ実装（Tier A）と工数を食い合う**ため、順序の決定が必要

- 背景（2026-09-08 の情報統合で判明した食い違い）:
  1. **2026-09-05**: `auto-02-implement.yml`・`auto-03-review-merge.yml` と エージェント定義10本が
     **ワークフロー主導で完成**した（`automation/` 配下・実物を確認済み）。
  2. **2026-09-07**: 「**ワークフロー主導からエージェント主導へ移す**」決定が
     `CONSOLIDATED_DECISIONS.md` §1 に記録された。
  3. しかし反映先の `docs/自律開発ループ設計.md` §12.1 は **`## 12. 未確定事項` 配下**に置かれ、
     見出しも「エージェント方式への移行（**検討中** ／ 2026-09-07 追加）」となっている。
     → **同じ事項が、決定ログでは「決定」・設計書では「検討中」**という状態。

- ⚠️ **09-07 の決定理由の前提が、事実と食い違っている**:
  決定理由は「実装ループ（`auto-02`／`auto-03`）と残りプロンプト7本が**未着手**の、
  **いまが移行コストの最小点**」だった。しかし実際には **2026-09-05 に完成済み**であり、
  「未着手だから安い」という前提は成立していない。**移行するなら完成済みの資産を書き直すことになる。**
  （決定時は作業ツリーが 09-03 時点のまま更新されておらず、09-05 の成果が見えていなかったと思われる）

- 確認したいこと:
  1. **方式は最終的にどちらか。** エージェント主導へ移行するか、ワークフロー主導のまま進めるか
  2. 移行するとして、**残22日のどこで実施するか。** 移行作業はアプリ実装と工数を食い合う
  3. 「決定」と「検討中」の**どちらへステータスを揃えるか**（現状は文書間で矛盾している）

- 選択肢:
  - A. **09-07 の決定どおりエージェント主導へ移行する。** ②連携層を書き直す。
    ①エージェント定義11本と③承認ゲートは流用できるため、書き直しは連携層に限定される
  - B. **Phase 1 リリースまではワークフロー主導のまま運用し、移行は Phase 1 後へ回す。**
    残22日をアプリ実装に集中させる。09-07 の決定は「方針として保持・実施は延期」とする
  - C. **`auto-01`（計画ループ）のみ運用し、`auto-02`／`auto-03` は Phase 1 では使わない。**
    足場（`tests/` `src/` `supabase/`）は揃ったため技術的には動かせるが、試運転の工数を後回しにする

---

## [2026-09-08] 会員スキーマの 2026-09-05 決定が `DB物理設計.md` にしか反映されておらず、正本・ER図・会員データモデルが古いまま
- ステータス: ✅ **回答済み（2026-09-12 オーナー決定）**
- 優先度: ~~中（WBS `2-1` の着手前に解消が必要）~~ → **解消済み**
- ブロックするもの: ~~WBS `2-1`・`2-4`・`3-1`~~ → **ブロック解除**

> [!success] オーナー決定（2026-09-12）
> **① 反映の順序**：古い3文書の修正を **`2-1` の着手前に行う**（並行ではなく前提とする）。
> **② 食い違いの扱い**：**新しい方（`DB物理設計.md` 側 ＝ 2026-09-05 の会員スキーマ決定）を正とし、
> 古い文書を新しい内容へ書き換える。**
>
> ⚠️ **この決定は `CLAUDE.md` §1.1（正本 v13 が勝つ）を覆すものではない。**
> 「`DB物理設計.md` を正本に格上げする」のではなく、**v13 自体を最新の決定に追いつかせる**という意味である。
> 決定後も正本は v13 のままであり、食い違いが生じたら**v13 を直す**のが正しい対処になる。
>
> **反映実績（2026-09-12）**:
> - [[ER図_概念データモデル]]（2026-08-22 で停止していた最も古い文書）: PII の `member_profiles_private` 分離、`auth_user_id`、権限ガード、宿泊者名簿、`member_role_changes` を反映
> - [[会員データモデル_ユーザーテーブル定義]] v1.3.0: `full_name_normalized` 追加、`address` の役割是正、§5.8／§5.9 新設、`auth_user_id` 防御をトリガー併用へ訂正
> - [[データモデル図_ER_Diagram]] v1.2.0: 2テーブルを ER 図へ追加、前泊地・後泊地と `MEMBER_TYPES` の不整合を解消済みへ更新
> - 正本 v13 v1.27.0: §7 に宿泊者名簿・権限変更履歴を新設、§5.8.1 の物理名を §7 と整合、§8 の再実行安全性の担保手段を差し替え
> - [[DB物理設計]]: §6-1 一覧表と §6-7 の RLS 有効化から**漏れていた4テーブル**を追加（#32〜#35）

- 背景:
  `docs/role-lock-and-lodging-law` ブランチ（2026-09-06・**WIP コミット**）が `DB物理設計.md` へ +641行を追加し、
  次の3件を設計した（2026-09-08 に main へマージ済み）:
  - **§3-13 宿泊者名簿**（`lodging_register_entries`。旅館業法対応・3年保存優先・カスケード削除の対象外）
  - **§3-14 会員データ取込の二重取込防止**（正規化した本名 ＋ 誕生年月による照合）
  - **§6-6b 自己の `role` を誰も変更できない `BEFORE UPDATE` トリガー**

- ⚠️ **反映されていない先が明示的に残っている**:
  - コミットメッセージ自身が「**会員データモデル・ER図・v13 への反映**」を**未着手**と明記している
  - `CONSOLIDATED_DECISIONS.md` の 2026-09-05 決定（PII別テーブル分離・ニックネーム表示）も
    「テーブル定義・物理設計・ER図は**別エージェントが並行対応中**」と書かれたまま更新されていない
  - 結果、**`DB物理設計.md` だけが新しく、`会員データモデル_ユーザーテーブル定義.md`・
    `データモデル図_ER_Diagram.md`・正本 v13 §7 が古い**という状態になっている

- 確認したいこと:
  1. **反映を誰がいつ行うか。** `2-1` の着手前に必須か、並行でよいか
  2. **食い違いが見つかった場合、どちらを正とするか。** `CLAUDE.md` §1.1 では**正本 v13 が勝つ**が、
     内容としては `DB物理設計.md` の方が新しい。この逆転をどう扱うかの指針が必要
     （§3.3 の「低・中リスクの矛盾は正本優先で進行する」例外に該当するかの判断を含む）

---

## [2026-09-03] 浮遊街アプリのデータを浮遊街コンシェルジュのナレッジ空間へ投入する方式（v13 §9 #61）
- ステータス: ~~方針は回答済み／実装方式は未回答~~ → ✅ **全論点クローズ（2026-09-11）：単一ベクトル空間化により「投入」という行為自体が消滅**

> [!success] 2026-09-11 の決定で、本項の未決①③は論点ごと消滅した
> ベクトル基盤を **Supabase pgvector へ一本化**し、アプリとコンシェルジュが**同一のベクトル空間を共有**する
> 決定（v13 §9 #31 限定改訂・`CONSOLIDATED_DECISIONS.md` §17）により、次のとおり整理された。
>
> | 未決だった論点 | 2026-09-11 時点 |
> | --- | --- |
> | **① 投入方式（A/B/C）** | ⛔ **消滅**。単一空間のため**移送そのものが無い**（キュー不要） |
> | **② 危うい情報の機械的ガード** | ✅ **2026-09-10 に方式決定 → 09-11 に実装形が確定**：判定軸を**チャネル**に置く原則は維持し、**単一DB内の RLS／ビュー**として実装する |
> | **③ `find_nearest` 移行** | ⛔ **消滅**。Firestore を検索基盤として使わない（本ファイル別項参照） |
> | **④ 同一 GCP プロジェクトか** | ⛔ **重要度低下**。Firestore 直読みをしなくなるため。ただし **Cloud Run → Supabase の接続経路**が新たな論点（§17-6） |
>
> ⚠️ **残る実務課題**（論点ではなく作業）: 本番68件の移行／PII 検出の範囲拡張（現状は**既知氏名のみ**で
> 住所・電話・メール・金額・`member_notes` は未カバー）／`0100` の `source_type` に tools・recipes が無いこと。

<details><summary>以下、2026-09-03〜09-10 の検討内容（記録として保持）</summary>

- 優先度: 高（Phase 2 の中核。ただし**下記②のガードを決めずに着手すると個人情報が LINE へ露出する**）

> [!success] 2026-09-03 オーナー決定（方針）
> 1. **浮遊街アプリで集まったデータを、浮遊街コンシェルジュのベクトル空間のナレッジとして格納する。**
> 2. **LINE 側ロールの自己申告詐称は引き続き見逃す。** ただし
>    **個人情報など危うい情報は LINE 側で回答しないようにする。**
> 3. ベクトル基盤は **Firestore を継続**する（Chroma 等の専用 Vector DB へは移らない）。

### ⚠️ この決定が「受容済みリスク」の前提を変える（最重要）

`非機能要件詳細.md` §7-4 の受容済みリスクは、**LINE 詐称を見逃す根拠**を次のように書いている。

> LINEボット経由では**個人情報に到達できない**。RAG が返すのは `knowledge_{tenant}` の
> ナレッジチャンクのみで、**会員データを参照する経路が存在しない**（コード実査で確認済み）

**アプリのデータを投入すると、この「経路が存在しない」という根拠が成立しなくなる。**
オーナー決定②はこの点を正しく手当てするものだが、**従来の担保手段は「その種の情報をナレッジに
登録しない運用」という人手のルール**であった。自動投入パイプラインが入ると運用ルールでは守れないため、
**機械的なガードが新たに必要**になる。

### 確認したいこと

1. **投入方式（下記A〜Cのどれか）**
   - **A. 取込キュー方式（推奨）**：アプリは Firestore の**取込キュー用コレクション**へ
     「生テキスト＋メタデータ」だけを書く。埋め込み生成と `knowledge_*` への upsert は
     **既存の `KnowledgeIndexer`**（bot 側の定期ジョブ）が行う
   - B. アプリが `knowledge_*` へ直接書く（埋め込み生成もアプリ側で実装）
   - C. bot 側に取込用 REST エンドポイントを1本追加する
   - **A を推奨する理由**: `app/knowledge/indexer.py` の冒頭が
     「**The indexer is the only writer of generated chunks**」と宣言しており、`_sync()` は
     `desired` に無い generated chunk を**孤児として削除する**。B で直接書くと
     `reindex_all()` を1回走らせた時点で**消える**か、逆に「generated でない」印を付ければ
     **マスタ更新に永久に追随しない**（手動Q&A と同じ "never touched" 扱い）。
     また B は埋め込み生成ロジックを TypeScript 側へ**二重実装**することになり、
     旧 REST API 案が明示的に避けようとした構造（`docs/08` §9-5）を再現してしまう
2. ~~**「危うい情報を LINE 側で回答しない」を担保する機械的ガードの方式**~~
   → ✅ **回答済み（2026-09-10）：d. 出力導線での channel ガード**（下記）
   - a. **投入パイプライン側で弾く**（キュー投入時に、氏名・住所・電話・メール・金額・
     `member_notes` を含むレコードを機械的に拒否する）→ **不採用**（有用な知見も一緒に捨てるため）
   - b. 投入は許し `target_role` で出し分ける → **⚠️ 非推奨**。LINE 側ロールは自己申告であり
     詐称を見逃す決定と併用すると**ガードとして機能しない**
   - c. a＋b の併用（多層防御）
   - **どの項目を「危うい情報」と定義するか**の確定も必要（下表を叩き台とする）

### ✅ 回答（2026-09-10・プロジェクトオーナー）— 論点②

> 「アプリとしては学習するが LINE では表示させたくない。なので**学習ではなくアウトプットの導線で制御したい**」

**採用: d. 出力導線での channel ガード**（選択肢 a〜c のいずれでもない新規案）。

- **投入は止めない。** アプリ由来データは PII を含むものも含めてベクトル空間へ入れる（＝「学習はする」）
- **投入時に PII 検出を行い、チャンクへ `pii_flag` を立てる**（弾くのではなく印を付ける）
- **LINE Webhook 経路の検索を、`pii_flag = true` を無条件に除外するよう固定する**
- 管理画面に **①PII 検出チャンク一覧 ②LINE 経路での除外ログ**を出し、
  2026-09-03 の「LINE 側の経路で個人情報が入っていないか**見えるようにしたい**」を満たす

> [!important] ⚠️ ガードの判定軸は「ロール」ではなく「チャネル」であること（実装時の必須条件）
> 本決定は一見すると**非推奨とした選択肢 b に見えるが、別物である。**
> - **b（非推奨）**: 「**この利用者は誰か**」で出し分ける → LINE 側ロールは自己申告のため**詐称で破れる**
> - **d（採用）**: 「**この問い合わせはどの入口から来たか**」で出し分ける →
>   **利用者は自分が LINE から来た事実を詐称できない**ため、詐称を見逃す決定と併用しても破れない
>
> したがって実装は **`target_role` を見てはならない**。**LINE Webhook のハンドラという経路そのもの**に
> フィルタを固定すること。ここを取り違えると、非推奨として退けた b をそのまま実装することになる。

### ⚠️ この方式で新たに負うリスク（実装前に手当てが必要・3件）

出力側で止める方式は、投入側で弾く方式（選択肢 a）と比べて**PII 検出器の精度がそのまま防御力になる**。

| # | リスク | 手当て |
| --- | --- | --- |
| **R-a** | **検出漏れ＝即露出**。a なら「投入されなかった」で済むが、d では**検出を漏らした PII は即座に LINE から引ける状態**になる。検出器の再現率がセキュリティ境界そのものになる | `pii_flag` を**後から再計算できる設計**にし、**再スキャンジョブ**を回す。⑦監査ログ export と同じ **Cloud Scheduler**（2026-09-10 決定）に載せられる |
| **R-b** | **フィルタの実装漏れ＝全開放**。検索を呼ぶ箇所が複数あると、1か所でもフィルタを忘れた時点でガードが無効になる | フィルタを**単一のチョークポイント**（検索関数の内部）に置き、呼び出し側の指定に委ねない。**「フィルタ無しで検索する経路を作らない」**ことをテストで固定する（`CLAUDE.md` §4.4 の「個人情報」領域に該当＝**テスト必須**） |
| **R-c** | **現行実装ではフィルタがアプリコード側にしか置けない**。`firestore_knowledge_store.py:157` は全件 `stream()` して Python 側でコサイン計算しており、除外も Python 側の責務になる（R-b が起きやすい） | **`find_nearest` への移行（本ファイル別項・2026-09-03 起票）が先行条件になる。** `find_nearest` の事前フィルタは **equality** であり、`pii_flag == false` はまさに equality で表せる。移行すれば**DB 層で強制される事前フィルタ**になり、R-b が構造的に消える |

> [!warning] 依存関係が1本増えた
> **本決定により「`find_nearest` への移行時期」（未回答）の優先度が上がった。**
> 移行前に d を実装すると、ガードがアプリコード頼み（R-b・R-c）のまま本番に入る。
> **`find_nearest` 移行 → PII フィルタを事前フィルタとして実装**、の順序を推奨する。

**また `非機能要件詳細.md` §7-4 の受容済みリスクの根拠を差し替える必要がある。**
旧根拠「LINE 経由では会員データを参照する**経路が存在しない**」は本決定で成立しなくなる。
新根拠は「**LINE 経路の検索チョークポイントが `pii_flag` を無条件除外する**」であり、
**構造的な不到達から、実装とテストで担保する到達制御へ性質が変わった**ことを明記すること。
3. **投入対象データの範囲**（叩き台）

   | 判定 | データ | 根拠 |
   | --- | --- | --- |
   | ✅ 投入する | クエスト完了報告から抽出した失敗知見・コツ（→ `Recipe.failure_patterns`） | `IMPLEMENTATION_STATUS.md` 残タスク E2 で想定済み |
   | ✅ | 朝会議事録の決定事項・手順（**発言者名を落とす**） | 知見として有用 |
   | ✅ | カフェのレシピ・宿泊の作業手順 | RAG の本来用途 |
   | ⚠️ 要検討 | Uii/eumo の**制度説明・料金体系**（個別残高は除く） | 残タスク E3 |
   | ❌ 投入しない | 会員個人情報（氏名・カナ・住所・電話・メール・前泊地） | **S格付け**（§7-1） |
   | ❌ | 運営メモ `member_notes` | **S格付け** |
   | ❌ | 伝票・金額・個人の Uii 残高 | A格付け |
   | ❌ | 鍵・設備の扱い・原価 | 既存の登録禁止ルール |

4. **同一 GCP プロジェクトか**（Firestore 直読み／直書きの前提条件）
   アプリの `GCP_PROJECT_ID` と bot の `FIRESTORE_PROJECT_ID` が同一かは
   **どの設計文書にも記録がない**（既存の未決。#31 の注記でも「Phase 2 着手時に確認すれば足りる」とされている）
5. **チャンク数の見込み**。投入により 900（カテゴリルーティング着手ライン）・1000（体感が壊れるライン）を
   超える見込みなら、**下記 [2026-09-03] find_nearest 移行が先行タスクになる**

- 関連ファイル: `Projects/line-rag-bot/app/knowledge/indexer.py`、`app/knowledge/renderer.py`、
  `IMPLEMENTATION_STATUS.md`（既知の制限 #3・残タスク E2/E3）、`docs/08-浮遊街RAG詳細設計.md` §4-5・§9-5、
  `docs/spec/detailed-design/非機能要件詳細.md` §7-4、v13 §9 #31・#61

</details>

## [2026-09-03] ベクトル検索を全件スキャンから Firestore `find_nearest` へ移行する時期（前倒しの可否）
- ステータス: ~~未回答~~ → ⛔ **論点消滅（2026-09-11）：Firestore を検索基盤として使わなくなったため**

> [!success] 2026-09-11 のベクトル基盤一本化により、本項は検討不要になった
> ベクトル基盤を **Supabase pgvector へ一本化**する決定（v13 §9 #31 限定改訂・`CONSOLIDATED_DECISIONS.md` §17）により、
> **Firestore のベクトル検索そのものを使わなくなった**。したがって
> 「全件スキャンを `find_nearest` へ移行するか」という論点は**前提ごと消滅**する。
> **見積っていた 3〜4人日は不要**になり、代わりに `line-rag-bot` の検索基盤移行（約430行）が発生する。
> ⚠️ 本項が挙げていた R13（カテゴリ絞り込み）・R16（関連度の下限）は**消えていない**。
> pgvector 側で同等の手当て（事前フィルタ・距離しきい値）が必要である。

<details><summary>以下、2026-09-03 時点の検討内容（記録として保持）</summary>

- 優先度: 高（上記の投入タスクの**先行条件**。単独でも R13・R16 を同時に解消する）
- 背景: 現行の `firestore_knowledge_store.py:157` は
  `"Search by cosine similarity. Fetches all items and computes in-app."` のとおり
  **コレクション全件を `stream()` して Python 側で総当りコサイン計算**している。
  Firestore の**ネイティブ KNN 検索 `find_nearest` は使っていない**。
  既存方針は `IMPLEMENTATION_STATUS.md` 既知の制限 #1 の「**1000 到達で Firestore native への移行も併せて検討**」。
- **前倒しを提案する理由**:
  1. `find_nearest` は **距離しきい値（distance threshold）** を指定できる。
     これは別項で 2026-08-29 に決定済みの **`RAG_MIN_SIMILARITY=0.7`（R16）を DB 側で強制できる**ことを意味する
  2. **equality による事前フィルタ**が使えるため、カテゴリルーティング（R13）が
     アプリ側の分岐ロジックなしで実現できる。しかも `metadata["category_id"]` と
     `metadata["agent_type"]` は **`renderer.py:151-152` が既に書き込んでいる**
  3. 全件スキャンが無くなることで **R13 の危険ライン自体が後退し、カテゴリルーティングの緊急度が下がる**
  4. 上記のナレッジ投入をやるなら**どのみち先に踏む**
- **費用**（1000チャンク・1日100問の想定）:

  | | 現状（全件スキャン） | `find_nearest` |
  | --- | ---: | ---: |
  | 1問あたりの読み取り | 1,000 ドキュメント | KNNインデックス 10（100件で1読み取り）＋結果3件 ＝ **約13** |
  | 1問あたりの転送量 | 約 6MB | 約 18KB |
  | 月間読み取り（1日100問） | 約 300万 | 約 3.9万 |
  | 月額概算 | 約 **$1.1** | 約 **$0.014** |

  → **費用はむしろ下がる。論点にならない。** KNN ベクトルインデックス分のストレージは増えるが、
  768次元 × 1000件で数MB規模であり月額 $0.01 未満（誤差）。
- **工数見積り（`line-rag-bot` 側 ／ 合計 約3〜4人日・S〜M）**:

  | 作業 | 目安 |
  | --- | ---: |
  | `upsert_chunk()` を `Vector` 型で保存するよう変更 | 0.5日 |
  | `search()` を `find_nearest` へ書き換え | 0.5日 |
  | 既存チャンクのマイグレーション（**現在16件**・`reembed_all()` を流用可） | 0.5日 |
  | KNN コンポジットインデックス作成（gcloud） | 0.25日 |
  | `min_similarity` を `distance_threshold` へ移行＋テナント設定との整合 | 0.5日 |
  | テスト修正 | 1日 |

- ⚠️ **着手前に30分で検証すべきリスク**: **Firestore エミュレータが `find_nearest` をサポートするか未確認**。
  CI（`tests/test_firestore_knowledge_store.py`）はエミュレータ前提のため、未サポートならテスト戦略の見直しが要る
- ✅ 制約の確認（いずれも本件では問題なし）: Python/Node.js クライアントのみ対応（bot は Python）／
  最大2048次元（768で余裕）／最大1000件返却（`top_k=3`）／事前フィルタは equality
  （⚠️ 不等式での事前絞り込みは公式ドキュメント上は非対応。リリースノートには併用可の記述もあり**要確認**。
  カテゴリルーティングは equality で足りるため実害なし）
- 選択肢:
  - A. **前倒しして今すぐ着手**（推奨）。チャンク数と無関係に R13・R16 に効く
  - B. 既存方針どおり 900／1000 到達まで待つ
  - C. ナレッジ投入の着手が決まった時点で同時に行う
- 関連ファイル: `Projects/line-rag-bot/app/rag/firestore_knowledge_store.py:150-182`、
  `app/knowledge/indexer.py`、`IMPLEMENTATION_STATUS.md` 既知の制限 #1・#4

</details>

## [2026-09-03] ネットワーク層の防御（WAF／ファイアウォール）が設計に存在しない（非機能 F-9）
- ステータス: ~~未回答~~ → **✅ 回答済み（2026-09-05）**
- 優先度: ~~高（**会員370名の個人情報に対する防御が RLS 1枚のみ**という状態の是非）~~ → **回答により解消**（ただし下記「残る論点」を参照）
- 背景: 2026-09-03 のオーナー指摘（「FW見当たらない」）を受けて両リポジトリの設計文書を全文検索した結果、
  **`WAF` / `ファイアウォール` / `firewall` / `Cloud Armor` / `IP制限` / `ネットワーク制限` はいずれも 0 件**だった。
  存在する L7 防御は **① Cloudflare Turnstile（`/reserve` のみ・CAPTCHA 相当であって WAF ではない）**
  **② レート制限（OTP 発行のみ／F-7 で「他は未定義」と明記）** の2つだけである。
- **Supabase だけでは外部アクセスを遮断できない**（技術的事実として記録する）:
  1. Data API（PostgREST / Auth / Storage / Realtime）は `https://<ref>.supabase.co` として
     **常時インターネットに露出**する。ブラウザから直接叩かれる前提の設計
  2. Supabase の **Network Restrictions（IP許可リスト）は Postgres への直接接続にのみ適用**され、
     **PostgREST 等の HTTPS API には適用されない**（公式ドキュメント明記）
  3. `anon` キーはクライアント JS に埋め込まれる（`NEXT_PUBLIC_`）＝**公開情報**
  4. カスタムドメインを設定しても元の `.supabase.co` は**稼働し続け無効化できない**ため、
     前段に CDN/WAF を置いてもオリジンが露出したままになる
  → **インターネットと会員個人情報の間に立っているのは RLS 1枚のみ。F-6（RLSポリシーDDL未作成）が
    全ギャップ中で最も危険である理由がここにある。**
- ⚠️ **構造的な論点**: `API設計.md` §1 の原則①「単純CRUDは Supabase クライアントから RLS 越しに
  直接アクセスし、独自APIを作らない」により、**トラフィックの相当部分が設計上わざと Vercel を迂回する**。
  そのため Vercel WAF を導入しても**その経路には効かない**。「WAF を1つ入れれば済む」話にならない。
- 選択肢（`システムアーキテクチャ.md`「ネットワーク層の防御」節に詳細）:
  - **N-1. Vercel WAF を設定**（推奨・即実施可）。Vercel 経由の経路を守り F-7 の受け皿にもなる
  - **N-3. RLS の厳格化（F-6）を最優先タスク化**（**必須・代替なし**）
  - N-2. Supabase Network Restrictions（Postgres 直接接続のみ。効果は限定的だが設定は容易）
  - N-4. 直アクセスを廃止し全トラフィックを Server Actions 経由へ（Phase 2 以降。API設計 §1 の原則変更を伴う）
  - N-5. `line-rag-bot` 側：`allUsers` 公開の Cloud Run 前段に LB＋Cloud Armor（実稼働側のため優先度は高い）
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §2-6・§7-3（**F-9**・F-6・F-7）、
  `docs/spec/basic-design/infra/システムアーキテクチャ.md`「ネットワーク層の防御 → **★ 採用する構成**」、
  `docs/spec/detailed-design/API設計.md` **§1-1**、`docs/spec/CONSOLIDATED_DECISIONS.md` §15、
  `Knowledge/インフラ・セキュリティ/2026-09-02_浮遊街アプリ・line-rag-bot_インフラ構成とセキュリティ対応リスト.md`

### ✅ 回答（2026-09-05・プロジェクトオーナー）

**「FW を1枚貼る」構成は採らない。経路ごとに別の技術で塞ぐ「3前線」構成を採用する。**
前提プランは **Vercel Hobby（無料）／ Supabase 無料枠**。

| 前線 | 守る経路 | 使う技術 |
| --- | --- | --- |
| **前線1** | ブラウザ → **Vercel** → Supabase | **Vercel WAF**（全プラン無料の Managed DDoS 緩和・ボット対策・**Attack Challenge Mode**。Hobby の**カスタムルール3本**を配分） |
| **前線2**（本丸） | ブラウザ → **Supabase 直** | **`anon` 権限の削り込み（原則ゼロ）＋ RLS ＋ 正規JWT の入手経路の制限 ＋ Auth 標準レート制限 ＋ 使わない口を閉じる** |
| **前線3** | ブラウザ ⇄ **GCS** | **IAM ＋ バケット設定**（Uniform bucket-level access・Public access prevention `enforced`・署名付きURLのみ・用途別SA） |

**Vercel WAF カスタムルール3本の配分:**

| # | 対象 | アクション | 状態 |
| --- | --- | --- | --- |
| **R1** | `/api/public/*` | **IP単位のレート制限**（OTP乱発・予約スパム対策。**F-7 の受け皿**） | ⭐ 採用 |
| **R2** | 認証系パス（ログイン・パスワードリセット） | レート制限 | ⚠️ **条件付き**（下記） |
| **R3** | 管理系パス（`/admin/*` 等） | **日本国外からのアクセスをブロック** | ⭐ 採用 |

- **N-1 を採用、N-3（RLS厳格化）は引き続き必須、N-2 は不採用**（Pro 機能かつ Postgres 直接接続にしか効かないため）。
  N-4 は Phase 2 以降、N-5 は `line-rag-bot` 側の課題として据え置き
- **Pro へ移行するとカスタムルールは 40本**まで増える
- **⚠️ 最重要**: **どの経路を通っても最後に守っているのは RLS である。Vercel WAF は Vercel を通る経路にしか効かないため
  RLS の代替にはならない。** よって **F-6（RLSポリシーDDL未作成）の危険度は本回答によって下がらない**

### ⚠️ 残る論点（本回答では確定していない・オーナー判断が要る）

- **① 未ログインの `/reserve` が残枠を Realtime で購読する設計なのか。**
  v13 §5.2.5 は残枠の表示箇所に **`/reserve`（未ログイン）**を挙げつつ、更新手段を「**Supabase Realtime で即時反映**」と
  書いている。また `システムアーキテクチャ.md` の公開予約フロー図にも `DB->>W: …（Realtime）`（W ＝ `/reserve`）がある。
  **字義どおりなら `anon` に Realtime/SELECT 権限が必要になり、「`anon` 権限ゼロ」が崩れる。**
  設計意図は「運営側カレンダーへの反映」であり `/reserve` は `GET /api/public/availability` で足りる、というのが
  現時点の解釈だが**明記が無い**。**ここが確定しないと `anon` 権限ゼロの可否が決まらない**
- **② Vercel WAF の R2 枠の使い道。** `supabase-js` の `signInWithPassword()` は
  **`https://<ref>.supabase.co/auth/v1/*` へ直接発行され Vercel を通らない**ため、
  **Vercel WAF でログイン総当たりを止めることはできない**（総当たり対策の本体は前線2の Supabase Auth 標準レート制限）。
  R2 の枠は **(a) 認証を Server Action 経由へ寄せて R2 を有効化する**（`API設計.md` §1 の直アクセス原則に例外を1つ作る）／
  **(b) `/admin/*` のレート制限へ振り替える**／**(c) 予備として温存する** のいずれかを選ぶ必要がある
- **③ R3（国外ブロック）の運用**: **運営スタッフの海外渡航中に管理画面へ入れなくなる**副作用がある。
  緊急解除の手順を運用手順へ入れること（一般会員向け画面は国外ブロックしない）

---

## [2026-09-05] 公開予約OTPの送信基盤（Supabase 既定SMTP の 2通/時 上限）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-10）：Resend を採用**
- 優先度: ~~**高**~~ → **解消**（WBS 3-5b・3-5c のブロックが外れた）

### ⚠️ まず訂正：起票時の前提が誤っていた（2026-09-10 の設計実査で判明）

**「Supabase 既定SMTP の 2通/時 が公開予約をブロックする」という本項の前提は、半分しか当たっていない。**
実装を追うと、メール送信経路は**2系統に分かれている**。

| 経路 | 実装 | 既定SMTP 2通/時 の影響 |
| --- | --- | --- |
| **公開予約のOTP** | **自前実装**。`POST /api/public/reservations/otp` ＋ `reservation_otps` テーブル（`code_hash` 保存）、サーバ側 `service_role` で処理（`API設計.md` §2-2b） | **かからない**（Supabase Auth／GoTrue を経由しないため） |
| **会員のサインアップ・パスワードリセット** | **Supabase Auth**（メール / Google / LINE OAuth。`システムアーキテクチャ.md` L185） | **かかる** |

したがって公開予約OTPの実態は「Supabase のメール上限」ではなく、
**アプリサーバから送るメール送信手段そのものが未選定**という問題だった。
**結論（Resend が要る）は変わらないが、設定箇所が変わる** —
**Supabase ダッシュボードのカスタムSMTP設定だけでは公開予約は動かない。**
アプリ側に送信SDK（Resend HTTP API）の配線が別途必要である。

### ✅ 回答（2026-09-10）— 論点①②③

- **① プロバイダ: Resend を採用。** 上記2系統の**両方**に使う
  （Supabase Auth へは SMTP 認証情報を設定／アプリ側へは HTTP API を配線）
- **② 無料枠: 月3,000通・かつ 1日100通**、検証ドメイン1つ、ログ保持30日
  （出典: [Resend New Free Tier](https://resend.com/blog/new-free-tier)。**2026-09-10 時点。設定時に実機確認すること**）
  - ⚠️ **効くのは月次ではなく日次キャップの100通**。平常運転（収容66名）なら収まるが、
    **370名の会員移行で一斉に招待メールを出すと超過する**（往復で約740通＝無料枠では8日かかる）。
    WBS 10-1/10-2 が想定する「初回チェックイン時の現地入力で段階的に吸収」を守る限り問題にならないが、
    **一括招待を行う判断をした時点で有料化が必要になる**
  - ⚠️ 超過時は Pro **$20/月**。**課金対象は現時点で Supabase 有料プラン（2026-09-08 決定）＋ GCP Blaze の2つ**であり、
    Resend が有料化すると**3つ目**になる。起票時（2026-09-05）の「Supabase と Vercel は無料枠」という
    前提は**既に Supabase 側が撤回済み**である点に注意（本ファイル「[2026-09-05] 無料枠運用で満たせない非機能要件の扱い」）。
    `非機能要件詳細.md` §4 の予算管理に含めること
- **③ 送信ドメイン: `fuyuugai.com`**（既存。`OLD/2026-08-22_..._OLD.md` に `support@fuyuugai.com` の記載あり）に
  SPF / DKIM を設定し、`no-reply@fuyuugai.com` で送出する。**DNS 設定の担当者は未定（要指名）**

### 検討の記録：Gmail への外だしを不採用とした理由（2026-09-10）

オーナーより「Gmail と連携してそちらに実行を外だしできないか」という案があり、3通りに解釈して検討したが、
**いずれも成立しない**と判断した。

| 案 | 判定 | 理由 |
| --- | :---: | --- |
| 無料 Gmail を SMTP に使う | ❌ | 500通/日だが **From が `@gmail.com` に固定**され、`fuyuugai.com` の SPF/DKIM を張れない。予約確認・本人確認メールがスパム判定されやすい。App Password は2段階認証必須。**配信ログ・バウンス追跡ができない** |
| Google Workspace SMTP relay | △ | 2,000通/日（relay 10,000通/日）と枠は十分。ただし [Supabase 公式](https://supabase.com/docs/guides/troubleshooting/using-google-smtp-with-supabase-custom-smtp-ZZzU4Y) が「**送信元と SMTP ユーザーは Workspace 管理者アドレスであること**」と明記しており `no-reply@` の役割アドレスで出しにくい。Workspace 未契約なら結局課金。**同ドキュメント自身が「解決しなければ Resend / SendGrid / Mailgun / SES を検討」と誘導している** |
| GAS 等で送信処理ごと外だし | ❌ | **Supabase Auth のメールは Auth 内部で送るため外だし不可**（SMTP を差し替えることしかできない）。予約OTPは自前実装のため技術的には可能だが、GAS の無料クォータは実質100通/日で **Resend と変わらない**。加えて配信の可観測性とレート制限を自前で作ることになり、利点が無い |

**要するに、Gmail を挟んでも送信可能数は増えず、独自ドメインでの到達性と配信ログを失う。**

### 優先度（回答前の記録）
- 優先度（当時）: **高**（公開予約ページ `/reserve` の OTP 発行が実運用に耐えないため）
- 背景: ネットワーク層防御（F-9）の設計中に判明した。**Supabase の既定の組み込みSMTP は認証メールが 2通/時**に
  制限されている（無料枠・既定設定）。`API設計.md` §2-2b は OTP 発行を「同一メール24時間3件」で制限する設計だが、
  **その手前でプラットフォーム側の上限に当たる**ため、公開予約ページが実質的に機能しない。
  カスタムSMTP（Resend / SendGrid 等）を設定すると既定 30件/時となり、
  ダッシュボードの Authentication > Rate Limits で調整可能になる。
  **→ 「コストは全面的に無料枠で開始する」決定の下でも、カスタムSMTP の用意は事実上の必須要件になる。**
- 未回答の論点:
  - ① **採用するSMTPプロバイダ**（Resend / SendGrid / その他）
  - ② そのプロバイダの**無料枠で1日あたり何通まで送れるか**。**費用が発生するか**
    （発生する場合、「Supabase と Vercel は無料枠」という前提に**3つ目の課金要素**が加わる）
  - ③ 送信ドメインの設定（SPF / DKIM）を誰が行うか
- ⚠️ **要確認**: 上記の「2通/時」「30件/時」は 2026-09-05 時点の調査に基づく。**設定時に実機で確認すること。**
  あわせて、**設定したレート制限が期待どおり適用されない**旨の公開Issue報告（supabase/auth #2333 等）があるため、
  設定後に実測すること
- 関連ファイル: `docs/spec/detailed-design/API設計.md` §2-2b、
  `docs/spec/detailed-design/非機能要件詳細.md` §2-6・§7-3（**F-7**）、
  `docs/spec/basic-design/infra/システムアーキテクチャ.md`「ネットワーク層の防御 → ★ 採用する構成 → 前線2」
- ブロックするもの: 公開予約ページの実装（WBS 3-5 系）、F-7 の値確定

---

## [2026-09-05] 監査ログ A6 の「AI対話履歴」をどこから GCS へ書き出すか
- ステータス: 未回答
- 優先度: 中
- 背景: 監査ログの GCS 保管を設計する中で判明した。**非機能 A6 は保存対象に「AI対話履歴」を明記している**が、
  `API設計.md` §2-6 のとおり**浮遊街アプリ本体はアプリ内AIチャットUIを持たず、当該領域のエンドポイントはゼロ件**である。
  **AI との対話は `line-rag-bot`（LINE Webhook・Firestore）側で発生する。**
  したがって **A6 を満たすにはエクスポート元が2系統になる**:
  ① 浮遊街アプリ（Supabase PostgreSQL）／② `line-rag-bot`（Firestore）。
  **②の経路は現時点でどの設計文書にも存在しない。**
- 未回答の論点:
  - ① **②の経路を Phase 1 のスコープに含めるか**（含めない場合、A6 は「AI対話履歴について未達」であることを明示する必要がある）
  - ② 含める場合の**実装責務**（`line-rag-bot` 側の設計変更になるため、同リポジトリの作業になる）
  - ③ **両システムが同一 GCP プロジェクトか**（`CONSOLIDATED_DECISIONS.md` §15-2 の既存未決と同じ前提条件に乗る）
- ⚠️ **前提として守ること**: 🚫 **監査ログと AI対話履歴を `line-rag-bot` のナレッジ空間へ投入してはならない**
  （v13 §9 #61 の個人情報ガードが未決。**GCS への保管**と**ナレッジ空間への投入**はまったく別の行為である。混同しないこと）
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §3 **A6**・§7-3（**F-5**）、
  `docs/spec/basic-design/infra/システムアーキテクチャ.md`「監査ログの保管構成」、
  `docs/spec/detailed-design/API設計.md` §2-6・**§1-2**、`docs/spec/CONSOLIDATED_DECISIONS.md` §1・§15

---

## [2026-09-05] 朝会文字起こしテキストの投入経路
- ステータス: 回答済み（2026-09-05）
- 優先度: 中（~~WBS 4-2 の入出力設計をブロックする~~ → 回答によりブロック解除）
- 背景: 2026-09-05 オーナー指示により、朝会の文字起こしは外部ソフトで実施済みと判明し、アプリ側のスコープは
  「文字起こしテキスト → 構造化議事録 ＋ クエスト候補起案」へ変更された（`docs/spec/CONSOLIDATED_DECISIONS.md` §1 の
  2026-09-05 決定を参照）。テキストの受け取り方が決まらないと、WBS 4-2 の入出力設計に着手できない。
- 関連ファイル: `docs/spec/CONSOLIDATED_DECISIONS.md` §1、`docs/spec/WBS_Phase1.md` 4-1・4-2、
  `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.1・§8
- 未回答の論点:
  - ① 使用している外部ソフト名（API連携の可否・出力形式の確認のため）
  - ② テキストの投入方法（管理画面への貼り付け／ファイルアップロード／API）
  - ③ 話者ラベル・タイムスタンプの有無
- ~~ブロックするもの: WBS 4-2 の入出力設計~~ → **解除（2026-09-05 の回答による）**
- 回答（2026-09-05・プロジェクトオーナー）: 投入方法は**「浮遊街アプリのUI画面へコピー＆ペースト」で確定**（論点②）。
  **貼り付けられた時点で議事録として格納**し、格納したテキストは**学習データおよびクエスト定義の入力**として使用する。
  **API連携・ファイルアップロードは行わない。**
  - 論点①（外部ソフト名）: **API 連携を行わないため、実装ブロッカーではなくなった**。
    アプリは貼り付けられたテキストのみを受け取るため、どのソフトで文字起こししたかは実装に影響しない
  - 論点③（話者ラベル・タイムスタンプの有無）: 貼り付けテキストに含まれるかは未確定のまま残るが、
    **実装時にどちらでも動く設計にする**（含まれていれば構造化の手がかりとして使い、無ければ本文のみで処理する）方針とする
  - 反映先: `docs/spec/CONSOLIDATED_DECISIONS.md` §1「朝会の入力をテキストへ変更し、投入方法を…確定する」（2026-09-05 決定）

---

## [2026-09-05] 無料枠運用で満たせない非機能要件の扱い
- ステータス: ~~未回答~~ → **✅ 回答済み（2026-09-08）** → ⚠️ **実施時期を後ろ倒し（2026-09-25 オーナー確定）**
- 優先度: ~~中~~ → 解消

> [!warning] 2026-09-25：有料化は**リリース直前**に行う（CONSOLIDATED_DECISIONS §23-1）
> **決定そのものは維持されるが、適用が済んでいないため下記リスク3点は現時点でも実在する。**
> 無料枠で確認できる作業（SMTP・メールテンプレート・レート制限・マイグレーション適用・通し確認）を
> 先に消化し、prod プロジェクト作成（`1-1b`）と Pro 化はリリース当日に実施する。
> その間の暫定対処と境界条件（**実名370名の投入・公開URLの配布を行わない**）は
> `docs/operations/リリース前環境設定_オーナー作業手順書.md` §4・§5 にある。

### ✅ 回答（2026-09-08・プロジェクトオーナー）

**Supabase は有料プランで運用する。** 2026-09-05 の「全面無料枠で開始する」のうち
**Supabase 部分を撤回**する（**Vercel Hobby は据え置き**）。

これにより、無料枠を選んだ結果として受容していたリスク3点が**いずれも解消**する:

| 受容していたリスク | 状態 |
| --- | --- |
| 非機能 A4「日次バックアップ・DB保持期間30日」が無料枠では満たせない | ✅ 解消 |
| 1週間の非アクティブで自動停止し、dev 停止時に CI の E2E が落ちる | ✅ 解消 |
| DB容量 500MB ／ プロジェクト数上限2 | ✅ 解消 |

⚠️ **GCP は従来どおり従量課金（Blaze）**であり、本回答の対象外。
⚠️ `detailed-design/非機能要件詳細.md` A4・P6 の記載は**未反映**（別途改訂が必要）。

- 影響: `WBS_Phase1.md` §16-1 の**真のブロッカーから外れる**（4件 → 3件）。`1-1` の前提も有料プランへ変更
- 背景: 2026-09-05 の「コストは全面的に無料枠で開始する」決定（`docs/spec/CONSOLIDATED_DECISIONS.md` §1）により、
  非機能 A4「日次自動バックアップ、DB保持期間30日」が **Supabase 無料枠では満たせない**ことが確定した
  （無料枠に日次自動バックアップが無く、**Pro（$25/月）でも保持期間は7日**であるため、30日保持は Pro でも未達）。
  要件と実態の乖離を放置すると、非機能要件の確定と WBS 1-1（環境構築）が宙に浮く。
- 関連ファイル: `docs/spec/CONSOLIDATED_DECISIONS.md` §1（2026-09-05 コスト決定・受容リスク1）、
  `docs/spec/detailed-design/非機能要件詳細.md` A4、`docs/spec/WBS_Phase1.md` 1-1
- 未回答の論点:
  - ① **A4 の要件自体を改訂するか**（頻度・保持期間を実現可能な水準へ下げる）、
    **リスク受容として記録するか**（要件は据え置き、未達であることを明示して受容する）
  - ② 代替として **`pg_dump` を GCS へ日次エクスポートする運用を Phase 1 に含めるか**。
    監査ログの日次エクスポートと**同じジョブレーンに相乗り可能**だが、
    **非同期ジョブ実行基盤の未決に依存する**（本ファイル「[2026-08-28] 非同期ジョブの実行基盤（Cloud Functions / pg_cron / Cloud Scheduler）の選定」）
  - ③ **リリース後に prod を Pro（$25/月）へ上げる判断時期**（収益化の時点か、実データ投入の時点か）
- ブロックするもの: 非機能要件の確定、WBS 1-1

---

## [2026-09-05] ニックネーム未設定会員の他者向け表示
- ステータス: ~~未回答~~ → ~~**🟡 一部回答（2026-09-05）**~~ → ~~**🟡 一部回答（2026-09-10）**~~ → ✅ **回答済み（2026-09-22・全論点決着）**
- **回答（2026-09-22・オーナー決定）**
  - ② **`nickname` は必須化する。** 本登録時の必須入力とする。既存370名には**次回ログイン時に設定を求め**、設定されるまでの間だけ会員番号で表示する
  - ③ 既存会員への設定依頼は上記のとおり**次回ログイン時の導線**で行う（別途の一斉通知は行わない）
  - ⑤ **親方会員番号の採番方式は「一意であれば何でもよい」。** 桁数・起点・接頭辞は実装側の裁量とする。条件は `legacy_member_no` の `10xxx`／`20xxx` と**識別できること**だけ
  - 🚫 `full_name` へのフォールバック禁止は**維持**
- ✅ **実装完了（2026-09-22 ／ WBS `2-7` ／ `0029_oyakata_member_no_and_nickname.sql`）**
  - ⑤の裁量の使い方: **専用列 `members.oyakata_member_no`**（`OYA-` ＋3桁・採番の起点45＝1〜44 は移行枠）。
    接頭辞1列方式を採れない理由は本ファイル「[2026-09-14] `oyakata_member_no` が …」の決着を参照
  - ②の必須化: **本体はアプリ層**（入力検査 `src/lib/members/nickname.ts` ／ 設定画面 `/nickname` ／ 本登録フォームは WBS `12-1`）。
    移行370名には**ログイン直後の `/nickname` で設定を求める**（決定どおり強制はせず「あとで設定する」を残す）。
    DB 側は空白だけの値を拒否する CHECK 1本まで（`imported_from` を「アプリ経由で作られたか」の代理にする CHECK は、
    サーバ側で表示名を持たない会員行を作る正当な経路を巻き添えで落とすため採らない）
  - ★ 仕様に明文が無く禁止側で実装した1点: `街人#`／`親方#`／`ゲスト#` で始まるニックネームを拒否する
    （`display_name` が未設定時に会員番号へ落ちるため、名乗れると他人の会員番号を自分の表示名に掲げられる）
  - **ブロックしていた `WBS_Phase1.md` `5-1`・`2-3` を解除**し、実装は **`2-7`（新設）**が担当する
  - 反映先: v13 §9 #62、`CONSOLIDATED_DECISIONS.md` §22-4、`WBS_Phase1.md` `2-7`・`2-3`・`5-1`
- 旧ステータス: 🟡 一部回答（2026-09-10）（①代替表示＝会員番号・④親方兼街人＝**街人番号**が確定し**表示規則は完成**。残るのは②`nickname` 必須化の可否・③既存会員への設定依頼運用・⑤親方会員番号の採番方式）
- 優先度: 中
- 背景: 2026-09-05 の **A案（PII別テーブル分離）**の決定（本ファイル「[2026-08-28] ロール別レスポンスマスキングの設計方針」の回答、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §3）により、**他者向けの表示は `nickname` を使う**方針とした。
  しかし**移行370名は `nickname` が未設定**である（`会員データモデル_ユーザーテーブル定義.md` §5.2
  「実データに列なし。アプリ側で本人が設定」）。表示名の代替規則が決まらないと、クエストボード等の
  他者が並ぶ画面で**表示するものが無い**状態になる。
- 関連ファイル: `docs/spec/CONSOLIDATED_DECISIONS.md` §3（2026-09-05 A案決定）、
  `docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.2、
  `docs/spec/detailed-design/非機能要件詳細.md` §2-4（保護方式・データ最小化）、`docs/spec/WBS_Phase1.md` 5-1・2-3
- 🚫 **前提（決定済み・議論の対象外）**: **`full_name` へのフォールバックは情報漏えいになるため禁止**する。
  A案は「`members` に個人情報を残さない」ことで保護を成立させており、フォールバックはその前提を壊す。
- 未回答の論点:
  - ~~① **未設定時の代替表示をどうするか**。`街人#<legacy_member_no>` のような**非個人情報な識別子**を想定しているが、
    **親方衆44名は `legacy_member_no` が NULL** のため、この規則だけでは埋まらず**別途の規則が必要**になる~~
    → **✅ 回答済み（2026-09-05・下記）**
  - ② **本登録時に `nickname` を必須入力とするか**（必須化すれば新規会員側は解決するが、移行370名は残る）
  - ③ **既存会員への設定依頼の運用**（誰が・いつ・どの経路で依頼するか。LINE 通知か、次回チェックイン時の対面か）
- ブロックするもの: WBS **5-1**（クエストボード）・**2-3**（ロール別UI表示制御）
  （**① の回答により表示規則の骨格は決まったが、下記④が未回答のため完全な解除には至っていない**）

### ✅ 回答（2026-09-05・プロジェクトオーナー） — 論点①

- **ニックネーム未設定の場合は「会員番号」を表示する**
- **親方は「親方会員番号」として一意の番号を持ち、一般会員（街人）の会員番号とは切り離して管理する。**
  **別系統の採番であり、番号空間を共有しない**（これにより「親方衆44名は `legacy_member_no` が NULL」という
  論点①の障害が解消する）
- 🚫 **`full_name` へのフォールバックは引き続き禁止**（情報漏えいにあたるため。上記「前提」のとおり）
- 反映先: `docs/spec/CONSOLIDATED_DECISIONS.md` §3（2026-09-05 決定）、
  `docs/spec/detailed-design/非機能要件詳細.md` §2-4

### ⚠️ 新たに残った未回答の論点

- ~~**④ 親方兼街人の会員に、どちらの番号を表示するか（オーナー判断待ち）**~~
  → ✅ **回答済み（2026-09-10）：街人番号で表示する**
  - v13 **§9 #26** により、**中野裕士氏は「親方兼街人」として1レコードに統合されている**
    （`member_type='親方'` かつ街びと側の会員権を保持。移行対象は 371件 → 370名）。
    この会員は**親方会員番号と街人の会員番号の両方を持ちうる**ため、**表示規則が一意に定まらない**
  - ~~**★提案: 親方を優先する**（コミュニティ内での立場としてより特徴的であり、現場での呼ばれ方に近いと考えられるため）~~
    → **⚠️ 提案は採用されなかった。オーナー決定は「街人番号」**（2026-09-10）
  - **✅ 決定（2026-09-10・プロジェクトオーナー）: 親方兼街人は「街人番号」で表示する。**
    - 実装規則: **両方の番号を持つ会員は、他者向け表示において常に街人番号を用いる**。
      `member_type='親方'` であることは表示番号の選択に影響しない
    - この規則により表示は一意に定まり、**将来同種の会員が増えても実装が分岐しない**
    - 補足: 2026-09-05 決定の「親方は別系統の採番を持つ」は維持される（番号空間は分離したまま）。
      本決定が定めるのは**両方を持つ場合にどちらを表示するか**であって、採番の統合ではない
  - ⚠️ **ロール表示との関係に注意**: 本決定は**他者向けの「表示名（番号）」**の規則である。
    権限判定は従来どおり `role` のみで行い（v13 §2）、`member_type` を認可に使わない原則は変わらない
- **⑤ 親方会員番号の採番方式（未設計）**: 桁数・採番の起点・既存 `legacy_member_no`
  （`10xxx` 237件／`20xxx` 90件）との識別方法が未定
  - ⚠️ **スキーマへの反映（カラム追加）は本項では行っていない**。会員データモデルは別途作業中のため、
    **本項は決定の記録に留める**

---

## [2026-09-03] 提供ステータス表示文言の仕様内不整合（v13 §5.4.1 #39）
- ステータス: 🟡 **部分回答（2026-09-13）— 論点2は決着、論点1は未回答**
- 優先度: 低（残る論点1は派生文書の表記訂正のみ。実装は `CLAUDE.md` §1.1 により正本「調理中」で進行できる）

> [!success] 論点2（適用画面の範囲）＝ **A に確定**（2026-09-13・オーナー／Issue #4）
> 「本 Issue のスコープを広げ、**注文完了画面・マイログの画面実装まで含める**」との回答により、
> **両画面に提供ステータスを表示する**ことが確定した。`8-4` マイログのスコープに含める。
>
> **論点1（`画面設計.md` L319 の「未提供」表記を正本の「調理中」へ訂正するか）は未回答のまま。**
> 実装側は正本が勝つため止まらないが、派生文書の不整合は残っている（訂正には `docs/spec/` の編集が必要）。
- 背景: 自律開発ループの試運転（Issue #4「セルフ注文完了画面の提供ステータス文言を仕様どおりに統一する」）で、
  調査エージェントが正本と派生文書の不一致を発見した。実装前に確定しておかないと、コーディング時に
  どちらの表記を採用するかがエージェント任せになる。
- 確認したいこと:
  1. **本人向け表示語の不一致**: 正本 v13 §5.4.1「本人への表示」行は「**調理中**／提供済み」と定めるが、
     `docs/spec/detailed-design/画面設計.md` L319 は同じ本人向け表示を「**未提供**／提供済み」と書いている
     （DB保存値は `未提供`/`提供済み` で確定・`docs/spec/detailed-design/DB物理設計.md` L207–208。
     矛盾しているのは**表示語**の側）。`CLAUDE.md` §1.1 により矛盾時は正本が勝つため実装上は
     「調理中」が優先されるはずだが、画面設計.md 側を明示的に訂正するかどうかは未確認
  2. **適用画面の範囲**: §5.4.1「本人への表示」行は「セルフ注文後の画面」とのみ書くが、
     同表の「リアルタイム反映」行は「客側のスマホ（**マイログ・注文完了画面**）」の両方を挙げている。
     `HTMLモック_v13.md` L202 も両画面への表示を示唆する。マイログ画面にも同じ提供ステータス文言を
     表示するのか、注文完了画面のみか
- 選択肢（1について）:
  - A. 正本どおり「調理中」で確定し、画面設計.md L319 を訂正する
  - B. 画面設計.md の「未提供」（DB値そのまま）で統一し、正本 §5.4.1 側を訂正する
- 選択肢（2について）:
  - A. 注文完了画面・マイログの両方に表示する
  - B. 注文完了画面のみ（マイログは注文履歴の一覧のみで、個別の提供ステータスは表示しない）
- 関連ファイル: v13 §5.4.1・§9 #39、`docs/spec/detailed-design/画面設計.md` L319、
  `docs/spec/detailed-design/DB物理設計.md` L207–208、`docs/spec/basic-design/frontend/HTMLモック_v13.md` L200–202

> [!note] 2026-09-13：宛先の照会が決着した
> 「どちらもAで実行」の宛先が一意に定まらなかったため再照会したところ、オーナーは
> 「[2026-09-13] 「どちらもAで実行」の適用先と、Issue #4 の成果物」の**選択肢 C**（Issue #4 のスコープを
> 注文完了画面・マイログの画面実装まで広げる）を選んだ。これにより**論点2は A に確定**した（上記 success 参照）。
> **論点1は明示的な回答を得ていないため、未回答のまま据え置く**（`CLAUDE.md` §7）。

---

## [2026-09-01] EUMO発行記録（金額・用途・名前）の取得元と、既発行分の突合基準（v13 §9 #59）
- ステータス: ~~未回答~~ → ~~🟡 部分回答（2026-09-22）~~ → ✅ **全面回答済み（2026-09-22）：論点1＝個別手動起票、論点2＝§5.10.8③の「要確認」フォールバックへ一本化**
- 優先度: ~~高~~ → **解消**（`5-7`・`12-4` のブロック解除）
- 背景: 8/29最終レビューで、EUMO発行を「発行依頼→発行済み未受領→受領済み」で管理し「金額と用途と名前も控えたい」
  という指示を受けた（v1.21.0 §5.3.1 拡張で反映済み）。ただしレビュー中に「**どこから取得しようか？**」という
  論点が残った。アプリ起点の給付（クエスト報酬・初回来訪／街人登録キャッシュバック）は承認・チェックインから
  自動で金額・用途・氏名が埋まるため、未確定なのは**過去分**である。

> [!success] 論点1（過去発行分の取り込み方式）＝ **「手動起票」で確定**（2026-09-22 オーナー指示）
> システム導入前（ゆうきさんのメール運用時代）の発行分は、**特定の転記元データベースから自動一括インポートしない**。
> `eumo_grants.source = 'manual'`（既存スキーマにあるフラグ。`grant_type = 'manual'` と併用）で、
> 運営（`admin`／`core_member`）が判明したケースを都度・個別に手動入力する運用とする。
> これにより「転記元はメール送信履歴かExcel台帳かeumo側履歴か」という選定自体が不要になる
> （どの記録を見て入力するかは運営の裁量に委ね、システムは入力手段だけを用意する）。

> [!success] 論点2（既存街人の「受取済み」判定列）＝ **「要確認フォールバックへ一本化」で確定**（2026-09-22 オーナー確認）
> 街人リストの特定の列（コイン残高／コインキャッシュバック額／利用済コイン等）を機械的に読んで
> 受取済み／未受取を**一括判定する仕組みは作らない**。根拠：
> 1. 過去分は論点1により「運営が個別に手動で `eumo_grants` を起票する」運用になる。
> 2. §5.10.8③ には元々「**判定が疑わしい会員は自動起票せず、チェックイン画面に『要確認』表示を出して
>    運営判断に回す**」という退避設計が存在する（`CONSOLIDATED_DECISIONS.md` §14「初回来訪キャッシュバックを
>    チェックイン連動で自動起票する」決定に含まれる）。
> 3. **既存370名のうちシステムに `eumo_grants` の起票が無い会員が来訪した場合、自動発行はせず
>    一律「要確認」を出し、運営が個別に「この人は過去に受け取り済みか」を判断する**運用に一本化する。
>    街人リストの列を根拠にした一括判定ロジックは**作らない**（退避策ではなく正式な運用として採用）。
- 関連ファイル: v13 §5.3.1・§5.10.8・§7（Eumo給付・会員プランマスタ）・§9 #59、`0023_eumo_grants.sql`、
  [[Projects/fuyuugai-app/docs/spec/2026-08-29レビュー_指摘内容とv1.21.0対応まとめ.md|2026-08-29レビュー_指摘内容とv1.21.0対応まとめ]]

## [2026-09-01] 利用者向けロール表示名（呼称）の最終ラベル（v13 §9 #58）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-22）**
- **回答（2026-09-22・オーナー決定）: 改称しない。**`admin`＝**管理者** ／ `core_member`＝**コアメンバー** のまま据え置く（選択肢 A・B・C のいずれでもない）
  - 理由: Phase 1 で `member` / `guest` がロール名を目にするのは**アクセス制限画面（v13 §5.9.4）の1箇所だけ**であり、そこは既に
    「**この画面は運営業務専用です**」という機能を表す文へ改めてある（v1.21.0）。**上下関係を想起させていた主因はこの一文であって呼称そのものではない**ため、据え置いても 8/29 の指摘は解消している
  - ⚠️ **ただし表示名は1箇所の変換表を経由する構造にする。** いま内部呼称と表示名が一致していても、変換が無ければ **Phase 2 で改称する判断になったときに全画面を触ることになる**
  - **`WBS_Phase1.md` `2-5` の 🔴 ブロックを解除**し、作業は「変換表の設置」のみに縮小した
  - 反映先: v13 §2・§5.9.4・§9 #58・クライアント確認事項表、`CONSOLIDATED_DECISIONS.md` §22-2、`WBS_Phase1.md` `2-5`
- 優先度: 中（内部実装はブロックしない。モック・通知文言への展開が止まるのみ）
- 背景: 8/29最終レビューで「一般ユーザーに管理者・コアメンバーという呼称が見えると、誰が中枢かという上下関係・格差を
  感じさせる」という指摘を受け、v1.21.0 で §2 に利用者向け表示名を新設し、§5.9.4 のアクセス制限画面を
  「~~管理者・コアメンバー専用~~ → **運営業務専用**」へ改めた。ただしレビュー発言には
  「**システム管理者**という形式にする」「管理者ではなく**システム運用者**にするなど」の両案があり、
  `admin` / `core_member` への割当が確定していない。
- 選択肢:
  - A. 仮案どおり：`admin`＝**システム管理者**／`core_member`＝**システム運用者**（v1.21.0 の記載）
  - B. 両方とも「システム運用者」に統一（一般ユーザー向けにはロールの区別自体を見せない）
  - C. 別案（例：運営チーム／サポート担当 等）
- 関連ファイル: v13 §2（利用者向け表示名）・§5.9.4・§9 #58

## [2026-09-01] LINE／Discord チャット履歴の吸い上げスコープ（v13 §9 #60）
- ステータス: 未回答
- 優先度: 中（Phase 2 以降。9/10 リリースはブロックしない）
- 背景: 8/29最終レビューで、#42（Phase 2・画像/動画の自動収集）を**チャット履歴（テキスト）へ拡張**する構想が出た。
  課題認識は「普段の会話の中でタスクの企画・アイデアが流れてしまう（リアクションや議論の活性化は確認できる）」。
  打ち手案として Firestore への格納 → 構造化・ビジュアライズ → クエスト案の自動生成 →
  リアクションによる関心度可視化 → **災害版**への転用まで言及があった。
  ただしオーナー自身が「**チャット履歴を吸い上げて Firestore に格納するだけでいいかな**」と検討中の言い方だった。
- 確認したいこと:
  1. **着手スコープ**：A. まず「格納のみ」（後から構造化を足す）／B. 格納＋クエスト案生成まで／C. 可視化・災害版まで一気に
  2. **収集の同意**：対象チャンネルの範囲と、参加者への告知・同意の取り方（#42 の「個人情報の混入防止」と同根）
  3. **実装分担**：格納・構造化は `line-rag-bot`（Firestore）側、クエスト案の受け口は浮遊街アプリ側、でよいか
- 関連ファイル: v13 §4 Phase 2（#42 拡張）・§9 #60、`Projects/line-rag-bot/`（Firestore 基盤）

---

## [2026-08-29] 写真からのAI判定（収穫可否・設備点検・献立提案）を浮遊街コンシェルジュ（LINE）へ実装する
- ステータス: **回答済み（2026-08-29 オーナー決定・3回の回答で確定）** ／ ただし**下記「残る未決」3件は未回答**。
  未決が残るため、**本件の実装には着手しない**（`CLAUDE.md` §7）
- 優先度: 高（実装リポジトリが `line-rag-bot` に確定。既存クライアントとの相乗りがあるため慎重な投入が要る）

### ✅ 最終決定（2026-08-29・A案）

1. **写真は LINE（浮遊街コンシェルジュ）で受け取り、その場で Claude が即応**する。
2. **画像は保存しない**（GCS・`media_assets` へ入れない。メモリ上で Claude へ渡し、応答後に破棄）。
3. **ナレッジ未登録時は Claude が一般知識で回答**し、**蓄積後は Firestore のナレッジを参照**する。
4. **実装リポジトリは `line-rag-bot`**。`feature_flags` でテナント単位ON/OFF（既存クライアント無影響）。
5. **浮遊街アプリ側の変更は不要**（`media_assets` は従来設計のまま）。

> [!warning] ~~C案（`media_assets` と連携）~~ は同日中に撤回
> 一度は C案（アプリのメディアアップロードと連携）で確定し4文書へ反映したが、**実装先が浮遊街アプリへ移ることで
> 「コンシェルジュの写真対応」という当初要望と乖離する**ことが判明したため撤回した
> （畑・設備の前でスマホからLINEへ送る導線が失われる）。`CLAUDE.md` §2.4 に従い、
> 各文書に取り消し線で記録を残している。正本 v13 §9 #56 ／ 改訂履歴 **v1.19.0 → v1.20.0**。

**A案／C案の選択とは独立した、同日の別決定2件（維持）**
- **API連携は「一切実装しない」→「Phase 2 以降実施」へ変更**。
  ⚠️ ただし**起票理由（写真AI判定のナレッジ参照）が A案化により失われた**。決定自体は維持するが**再確認したい**。
- **メディアAI解析（用途タグ適合度・キャプション案／Phase 2）のエンジンを Gemini → Claude へ変更**（朝会音声は Gemini のまま）。

✅ 反映済み：v13 §9 #56・#31注記・改訂履歴 v1.19.0/v1.20.0、`外部連携設計.md` §2-2・§2-3、
`DB物理設計.md` §3-7・§8 #8、`システムアーキテクチャ.md`、`.env.example`、`CONSOLIDATED_DECISIONS.md` §9・§10。

> [!note] 以下「対象ユースケース」以降は C案 検討時に書いた記録
> 業務ドメインの対応づけと既存資産の分析は A案でもそのまま有効。
> ただし「本決定が覆す既存の確定事項」表の a・b・c と「実装に必要になる変更」は **C案前提**のため、
> A案では **b・c のみ有効／a と実装変更は不要**（アプリ側を触らないため）。

### 対象ユースケース（3件）
| # | 入力 | 期待する出力 | 業務ドメイン | RAG構造化タイプ | 誤答時の被害 |
| --- | --- | --- | --- | --- | :---: |
| 1 | 野菜の写真 | 収穫可能かの判定 | ⑤農業・食循環 | **判断型**（`Case`） | 中 |
| 2 | 設備の写真 | 清掃方法／修繕要否の判断 | ②建物管理・メンテナンス | レシピ型＋**規程型** | **大（安全事故）** |
| 3 | キッチンの食材写真 | 献立の提案 | ④飲食・カフェ運営 | レシピ型 | 小 |

出典: `docs/spec/requirements/業務カテゴリ体系とAIエージェント設計.md` §2。
判断型の受け皿 `Case`（`{状況}+{アクション}={結果}`）は `line-rag-bot/app/knowledge/models.py:350` に
**実装済み**で、docstring が例として「**農作業の見極め**」を挙げている。#1 は既存構造にそのまま乗る。

### 決定3（ナレッジ有無での出し分け）は、LINE側に既に同型の実装がある
浮遊街コンシェルジュ側には既に次の3点があり、**同じ三段構えをアプリ側へ移植する形**になる。
- `RAGService.has_knowledge(rag_collection) -> bool`（`app/rag/rag_service.py:40-53`）
- `FeatureFlags.general_answer`（`app/tenant/models.py`）＝「ナレッジ抜きの一般知識＋出典で答えるか」のテナント単位フラグ
- `LLMService.generate_general_answer()`（`app/llm/llm_service.py:97-198`）

本決定により、既存の未決 **A2「ナレッジ未整備期（初期データ投入前）の扱い」は実質決着**した
（＝Claude の一般知識でフォールバックする）。ただし **A3「フォールバック時の `system_prompt` に免責を含めるか」は未決のまま**であり、
ユースケース#2（修繕要否）と直結するため下記「残る未決」③で扱う。

### ⚠️ 本決定が覆す／改訂を要する既存の確定事項

| # | 既存の確定事項 | 出典 | 本決定との関係 |
| --- | --- | --- | --- |
| a | **アプリ本体はエンドユーザー向けAIチャットUIを持たない** | v13 §9 **#31**（2026-08-16） | **衝突しない**と整理する。本件は**チャットUIではなく `media_assets` のAI解析結果表示**であり、既に設計済みの `ai_caption` / `ai_purpose_score` と同じ枠内。ただし「解析結果に自然文の回答が含まれる」ため、**#31 の射程に入らないことを正本へ明記**する必要がある |
| b | **line-rag-bot とのAPI連携は一切実装しない（読み取り専用も不採用）** | `.env.example`、`外部連携設計.md` §2-2、v13 §9 #31再確定 | ✅ **解決済み（2026-08-29 追加回答）**：**「Phase 2 以降で実施」へ変更**。4文書へ取り消し線で反映済み。ただし参照方式（REST API か **Firestore 直読み**か）は下記②で未決 |
| c | **メディアAI解析は Gemini（朝会音声と同一基盤）** | `DB物理設計.md` §3-7、`システムアーキテクチャ.md` | ✅ **解決済み（2026-08-29 追加回答）**：**Claude へ変更**。Gemini は朝会音声に残す。3文書へ反映済み |

### 実装に必要になる変更（判明分・見積り前）
- `media_assets` に **判定結果を格納する列が無い**。`ai_caption`（キャプション案）・`ai_purpose_score`
  （用途タグ適合度）はいずれも**Instagram素材選び用**であり、「収穫可能か」の回答は入らない。
  新列（例: `ai_analysis_kind` / `ai_answer` / `ai_answer_sources`）の追加が要る
- `ck_media_purpose_tags_required`（用途タグ1つ以上必須）に対し、本用途のタグ体系が未定義
- `ai_processing_status` の CHECK に `'ready'` が無いのに、`システムアーキテクチャ.md` L118・L241 は
  「pending → **ready**」と書いている（**既存の不整合**。本件で §3-7 を触る際に併せて解消する）
- **非同期のため「撮ってすぐ返事」が成立しない**。朝会60秒SLA原則を踏襲する場合の体感設計が未定

### 残る未決（オーナー確認が必要・4件）
- ~~**① Phase 判定**~~ → ✅ **解決済み（2026-08-29）**：**Phase 2**。
  懸念していた「9/10 の押し出し」は**発生しない**。理由は、`media_assets` が既に
  **「Phase 1＝アップロードのみ／Phase 2＝検索・AI推薦」の2段構成で確定済み**であり
  （`画面設計.md` A10、`DB物理設計.md` §7表「Phase2: メディアの検索・AI推薦」、v13 §5.11.3）、
  `ai_*` 系カラムは **Phase 1 では値を入れない**と明記されているため。
  写真AI判定はこの **Phase 2 側にそのまま属する**。オーナー追加回答「API連携は Phase 2 以降」とも整合する。
- ~~**② GCPプロジェクトの同一性**~~ → **格下げ（2026-08-29 A案化）**：A案では**浮遊街アプリからナレッジを参照しない**ため、
  実装ブロッカーではなくなった。Phase 2 で API連携に着手する時点で確認すれば足りる。
- **② `RAG_MIN_SIMILARITY=0.0` の保留解除（A案化により昇格）**: 別項で**保留中**の「関連度の下限が無い」問題は、
  **写真だと明確に危険度が上がる**。テキストなら利用者は的外れな回答に気づけるが、**写真は「見て判断してくれた」ように
  見えるため、無関係チャンクを根拠にした誤答が権威づけされる**。収穫可否・修繕要否では実害に直結する。
  写真対応の投入前に、下限値の導入（推奨初期値 0.7 前後）を決める必要がある。
- **③ ユースケース#2（修繕要否）の責任範囲**: AIに「**修繕が必要／不要**」と**判定させる**か、
  「症状の言語化＋**判断は親方衆へ**」の**情報提供に留める**か。
  既存設計は道具の `safety_level: high` について「手順が確定するまで公開しない」という慎重側の思想を採っている
  （`line-rag-bot/app/knowledge/drafter.py:194`）。同じ基準を写真判定へ適用するかの判断。**A3（免責文言）と一体で決める。**
- **④ コスト上限・レート制限**: AI呼び出しのコスト見積り・上限は**全設計書に存在しない**
  （`非機能要件詳細.md` §4 はストレージ予算アラートのみ、同 §7 F-7 は「レート制限がOTP発行以外で未定義」）。
  写真は1枚あたりのトークンが重く、枚数制限が無いと青天井になる。

- 関連ファイル: `docs/spec/detailed-design/DB物理設計.md` §3-7、`docs/spec/detailed-design/API設計.md` §2-10、
  `docs/spec/detailed-design/画面設計.md` A10、`docs/spec/basic-design/infra/システムアーキテクチャ.md`（フロー2・非同期ジョブ3レーン）、
  `docs/spec/requirements/業務カテゴリ体系とAIエージェント設計.md` §2、`.env.example`、
  `docs/spec/detailed-design/外部連携設計.md` §2-2、v13 §5.11.2・§5.11.7・§9 #31・#32

---

## [2026-08-26] キャッシュバック 5,000 の単位は「円」か「コイン（Uii）」か
- ステータス: ✅ **回答済み（2026-09-01 オーナー確定 ／ 出典は 2026-08-29 最終レビュー）** — ~~未回答~~
- 優先度: ~~最高~~ → **解消**（`membership_plans` の初期値が確定し、街人登録の実装ブロックが外れた）

> [!success] 決定（2026-09-01）：単位は**コイン（uii）**・金額は**年会費プラン別の2段階**
> 1. **単位はコイン（uii）が正**（選択肢A）。8/29 最終レビューの発言が一貫して uii 建て
>    （「5,000 uii のプレゼント」「40,000円で払った街人は 10,000 uii になる」）だったことを追認した。
> 2. **金額は2段階**：**年会費 40,000円プラン ＝ 10,000 uii ／ 30,000円プラン（現行の街人登録・§5.10.2）＝ 5,000 uii**。
>    `membership_plans` を2プラン化。判別キーは街人リストの「**年会費額**」列（列の存在は確認済み）。
> 3. 反映先：v13 **v1.21.0**（§5.10.2・**§5.10.8 新設**・§7 `membership_plans` 明文化・§9 #51 決着）、
>    [[Projects/fuyuugai-app/docs/spec/2026-08-29レビュー_指摘内容とv1.21.0対応まとめ.md|2026-08-29レビュー_指摘内容とv1.21.0対応まとめ]]、`CONSOLIDATED_DECISIONS.md` §14。
> 4. ⚠️ 派生の要確認：**既存街人の「受け取り済み」判定基準・過去発行分の転記元は #59**（上記 [2026-09-01] 起票分）。
- 背景: 8/25レビューで「**街人登録画面でCB5000円ではわかりにくいので、キャッシュバックと略さずに**」という
  指摘を受けた。**表記**（「CB」を使わない）は正本 v1.18.0 §5.10.2 で反映済み。
  しかし指摘の言い方は「CB5000**円**」であり、正本の確定値（§9 #15・2026-08-14）は **コイン 5,000**（＝5,000 Uii ／ #18）である。
  Uii は `floor(単価 × 0.8)` の換算のため **5,000 Uii ≒ 6,250円相当**であり、両者は同じ金額ではない。
  **付与額が 1.25 倍ずれる**ため、表記の統一とは別に値そのものの確認が必要。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.10.2・§9 #51・#15・#18、
  `docs/spec/2026-08-25レビュー_指摘内容とv1.18.0対応まとめ.md`
- 選択肢:
  - A. **コイン 5,000（＝5,000 Uii）が正**。#15 の確定値どおり。UI 表記は `キャッシュバック 5,000 コイン（＝5,000 Uii）`
  - B. **5,000円が正**。#15 の確定値を訂正し、`membership_plans` の付与額を改める（実データとの突合が別途必要）

## [2026-08-26] 物販（Phase 2）のリンク先ECは既存か、これから作るのか
- ステータス: 未回答
- 優先度: 高（Phase 2 の見積りが変わる。新規なら EC 構築は本アプリの工数外の別タスクになる）
- 背景: 8/25レビューで「**物販（Phase2）についても円決済を可能に。ECにリンク飛ばすように**」という指示を受け、
  正本 v1.18.0 §5.4.3 で「**外部ECサイトへリンクで送客し、アプリ内に円の決済機構を作らない**」と確定した。
  しかし**リンク先のECが既に存在するのかが未確認**である。既存なら `menu_items.ec_url` に登録するだけで
  Phase 2 の実装はほぼゼロだが、新規構築ならEC側の立ち上げが別途必要になる。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.4.3・§9 #53・#4
- 選択肢:
  - A. 既存のEC（BASE / STORES / Shopify 等）がある → サービス名とストアURLを教えてほしい
  - B. これから作る → EC構築の担当者・時期を別タスクとして立てる必要がある
  - C. 併せて確認したい点：①アプリのSOLDOUTとECの在庫を同期するか ②対象商品の点数

## [2026-08-26] ~~9/10~~ **9月中**のリリースにクレジットカード決済が無くても運用できるか
- ステータス: ~~未回答~~ → ✅ **回答済み・クローズ（2026-09-25・オーナー確定）：選択肢A ＝ カード決済なしでリリース運用する**（精算QR・現金・Uii QR の3経路。カードは Phase 2 据え置き ／ CONSOLIDATED_DECISIONS §23-4） ／ ⚠️ **2026-09-03 の納期改訂で前提が変化（下記）**
- 優先度: ~~高~~ → **中**（納期が伸びたことで「間に合わない」という制約自体が緩んだため）
- 背景: 8/25レビューで「街人登録の際にクレジットカード決済の方が便利」という要望を受けたが、
  **カード決済は Phase 2**とした（正本 v1.18.0 §5.10.7・§9 #52 ／ 2026-08-25 オーナー判断）。
  理由は①**加盟店審査に通常2〜4週間**かかり ~~2026-09-10~~ のリリース目標（§4）に対して審査落ち時の代替時間が残らない
  ②アプリが決済トランザクションを持つ側に回ると §5.6.5（遡及修正）・§5.6.6（差額繰越）が前提から書き換わる
  ③§8 が求めるテスト（認可・金額計算）が増え、§4 の納期前提「テスト工数を圧縮しない」と衝突する、の3点。
  Phase 1 では代わりに**現金導線**と**Uii（eumo）QR**を追加し、「カードが無い人」の課題自体は解いている。
- ⚠️ **2026-09-03 の納期改訂による前提の変化**:
  - 納期が **9/10 → 9月中（〜9/30）** へ再改訂された（v13 v1.22.0）。
  - **理由①（加盟店審査が間に合わない）が部分的に崩れる**。9/03 に申請を開始すれば、
    審査期間の**下限2週間なら 9/17 頃**に完了し、9/30 のリリースには間に合う計算になる。
  - ただし**上限4週間なら 10/01 で間に合わない**ため、審査落ち・遅延時の代替時間は依然ほぼ無い。
  - **理由②③（決済トランザクションを持つことによる設計影響・テスト工数）は納期と無関係に残る。**
    こちらの方が本質的な論点であり、納期が伸びただけでは解消しない。
  - → **現時点では Phase 2 のまま据え置き**。ただし「Phase 1 へ戻すか」を改めて判断する余地が生まれた。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §4（v1.22.0）・§5.10.7・§9 #52・#8・#24
- 選択肢:
  - **A. Phase 1 は3経路（精算QR・現金・Uii QR）で運用できる。カードは Phase 2 でよい**（現行方針の追認）
  - B. カードが無いと運用が回らない → **9月中の納期なら加盟店審査が下限で間に合う可能性がある**ため、
    決済代行の選定・申請を即時開始する（理由②③の設計影響を受け入れる前提）
  - C. 判断を保留し、9/19 の中間チェックポイント（コア業務の疎通確認）時点で再検討する

## [2026-08-26] 宿泊形態の旧名称が正本に残存している（v1.16.0 の改称に未追随）
- ステータス: ~~未回答~~ → ✅ **解消（2026-09-21）：選択肢A（単純な表記の追随漏れ）を採用し一括置換**
- 優先度: ~~中~~ → **解消**（WBS `3-9`・`3-8` の当該ブロック要因を除去）
- 背景: v1.16.0（2026-08-22）で **ゲストハウス→ドミトリー**、**テント→キャンプサイト**へ改称し、
  **コテージを2棟→3棟**へ修正したが、正本の以下に**旧名称のままの記述が残っている**（2026-08-26 の棚卸しで検出）。
  - §4 Phase 1 スコープ（2箇所）：「ゲストハウス残り◯／アースバッグ残り◯」「6種の宿泊形態（コテージA/B・…・ゲストハウス・サロン）」
  - §5.2.4：「宿泊形態の選択 … **既定値は「ゲストハウス」**」
  - §5.2.5：「ゲストハウス残り◯／アースバッグ残り◯」
  - §7 宿泊料金マスタ：形態の列挙が旧名称
  #41（通貨表記の指示が10日間反映されないまま残っていた件）と**同型の取りこぼし**。
  v1.18.0 は 8/25 レビューの反映に範囲を限定したため（`CLAUDE.md` §5.2「1コミット1論点」）**未修正**。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §4・§5.2.4・§5.2.5・§7・§9 #55・§5.4.2（確定表）
- 選択肢:
  - A. 単純な表記の追随漏れ。オーナー確認後、別改訂（v1.18.1 相当）で一括置換してよい
  - B. 旧名称のまま運用している画面・帳票があるため、置換前に現場と突き合わせたい

### ✅ 対応（2026-09-21・Claudianセッション）
選択肢Aで一括置換（値の変更なし・表記のみ）。
- 正本 v13：§4（2箇所）・§5.2.4・§5.2.5・§5.6.9 の例文・§7 の宿泊料金マスタ定義を §5.4.2 確定表の名称へ置換。改訂履歴に v1.29.1 を追加、§9 #55 を解消済みへ更新。
- `DB物理設計.md` §3-10：`accommodation_rates.room_type` の CHECK（旧7値の直書き）を `accommodation_types(room_type)` への外部キーへ差し替え（実装済み `0014` の英字6値と結合できない不整合を解消）。
- 反映していないもの: `CONSOLIDATED_DECISIONS.md`・`画面設計.md`・`HTMLモック_v13.md`・`UI_UX設計指針.md`・`データモデル図_ER_Diagram.md` に残る旧名称は本対応の範囲外（別途確認要）。

## [2026-08-28] 非同期ジョブの実行基盤（Cloud Functions / pg_cron / Cloud Scheduler）の選定
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-10）：Cloud Tasks ＋ Cloud Scheduler（GCP 全面統一・pg_cron 不使用）**
- 優先度: ~~高~~ → **解消**（WBS 1-1, 1-4, 1-5, 3-5b, 4-2, 5-7, 8-3, 14-1 のブロックが外れた）

### ✅ 回答（2026-09-10・プロジェクトオーナー）

> 「非同期ジョブは GCP のキューイングサービスを使用」→ 確認の結果 **Cloud Tasks ＋ Cloud Scheduler** を採用。

**⚠️ 確認の過程で判明した重要な点: 「キュー」だけでは7本中5本を担えない。**
キュー（Cloud Tasks / Pub/Sub）は**順番待ちの仕組みであって定時起動の仕組みではない**ため、
起動契機で2群に分かれる。

| ジョブ | 起動契機 | 採用基盤 |
| --- | --- | --- |
| ① メディアAI解析・サムネイル生成 | GCS イベント | **Cloud Tasks** → Cloud Functions |
| ② 朝会音声の議事録生成 | GCS イベント | **Cloud Tasks** → Cloud Functions |
| ③ `reservation_otps` 期限切れ削除 | 定時 | **Cloud Scheduler** → Cloud Functions |
| ④ `eumo_grants` 14日滞留検知 | 定時 | **Cloud Scheduler** → Cloud Functions |
| ⑤ `settlement_adjustments` 90日滞留 | 定時 | **Cloud Scheduler** → Cloud Functions |
| ⑥ 孤児ファイル日次検出 | 定時 | **Cloud Scheduler** → Cloud Functions |
| ⑦ 監査ログ GCS 日次エクスポート（2026-09-05 追加） | 定時 | **Cloud Scheduler** → Cloud Functions |

- **`pg_cron` は使わない。** 当初の3レーン案は③④⑤を pg_cron に置いていたが、
  **基盤を GCP へ一本化する**判断により Cloud Scheduler へ寄せた。
  監視・IAM・課金・障害時の追跡経路が1系統になり、監査ログの GCS 保管（2026-09-05 決定）とも同じ基盤に乗る。
- Pub/Sub ではなく Cloud Tasks を採る理由: 現行7本は**すべて 1イベント→1処理**でファンアウトが無く、
  Pub/Sub の配信保証設計（重複配信・順序）を持ち込む利点が無い。

### ⚠️ この決定から派生する新規論点（要対応・未起票分）

**pg_cron を捨てたことで、③④⑤は「DB内で完結する処理」ではなくなった。**
Cloud Functions から Supabase PostgreSQL へ**ネットワーク越しに接続する**必要が生じる。

1. **接続資格情報の置き場所**: Cloud Functions 側に DB 接続情報が要る（GCP Secret Manager 想定）。
   `service_role` キーを GCP 側へ置くことになるため、`CLAUDE.md` §3.2 の「サーバサイドでのみ使用」の
   適用範囲に GCP を明記する必要がある
2. **⚠️ Supabase Network Restrictions（IP許可リスト）との衝突**: ネットワーク層防御（F-9）の
   選択肢 N-2 は「Postgres 直接接続を IP 許可リストで絞る」だが、**Cloud Functions の外向き IP は既定で動的**である。
   N-2 を採るなら **VPC コネクタ ＋ Cloud NAT で静的 IP を確保する**必要があり、構成が1段増える。
   **N-2 を採らないなら、Postgres 直接接続は許可リスト無しのまま残る**
3. **コスト**: `非機能要件詳細.md` §4 のとおり **GCP は Blaze 従量課金**であり、
   Supabase・Vercel の無料枠と違って**超過時に止まらない**。定時ジョブ5本の常時稼働分は予算アラートの対象に含める

- 関連ファイル: `docs/spec/basic-design/infra/システムアーキテクチャ.md`（非同期ジョブ一覧と実行基盤）、
  `docs/spec/detailed-design/DB物理設計.md` §3-2・§3-6・§3-7・§3-8、`docs/spec/detailed-design/非機能要件詳細.md` §2-2・§4・§7-3（F-9）

### 選択肢（回答前の記録）
- 優先度（当時）: 高（複数機能の実装方式に跨る基盤選定。各設計書は「非同期」「定期ジョブ」とだけ書き、実行基盤が未指定だった）
- 背景: バックエンド設計の横断整理（2026-08-28、`システムアーキテクチャ.md`「バックエンドの処理方式とデータフロー」新設）の
  過程で、以下のジョブがいずれも実行基盤未定と判明した。①メディアAI解析・サムネイル生成（DB物理設計.md §3-7）
  ②朝会音声の議事録生成（API設計.md §2-3）③`reservation_otps` 期限切れ削除（DB物理設計.md §3-6）
  ④`eumo_grants` 14日滞留検知（同 §3-8）⑤`settlement_adjustments` 90日滞留（同 §3-2）⑥孤児ファイル日次検出（非機能要件詳細.md §2-2）。
  システムアーキテクチャ.md に3レーン案（GCSイベント起点の重処理＝Cloud Functions／DB内で完結する掃除＝pg_cron／
  DBとGCSをまたぐ日次バッチ＝Cloud Scheduler→Cloud Functions）を**提案**として記載済み。承認までは確定扱いしない。
- 関連ファイル: `docs/spec/basic-design/infra/システムアーキテクチャ.md`（非同期ジョブ一覧と実行基盤）、
  `docs/spec/detailed-design/DB物理設計.md` §3-2・§3-6・§3-7・§3-8、`docs/spec/detailed-design/非機能要件詳細.md` §2-2
- 選択肢:
  - A. 提案どおり3レーン構成で確定（推奨。v13 §5.11.2 の既存経路と部品が重なり、新規インフラが増えない）
  - B. Supabase側（Edge Functions＋pg_cron）へ寄せる（GCP側の部品を減らしたい場合。ただしObject Finalize起点の書き戻しにはCloud Functionsが必須のため、完全には寄らない）
  - C. 個別に判断したいジョブがある（指定してほしい）

## [2026-08-28] 決済QR・入金QRトークンの保護強化（乱数要件・TTL・単回使用）
- ステータス: ~~未回答~~ → ✅ **回答済み（2026-09-10）：A案を採用**
- **回答（2026-09-10 ／ オーナーが判断を Claude へ委任）**
  - **A案を採用する。** ただし提案（§2-7）より一段強い形で確定した:
    ① トークン本体 = `base64url(gen_random_bytes(32))` ＝ **256bit**（提案の「128bit以上」を満たす）
    ② **平文をDBに保存しない**（`sha256` の16進のみ保持）。発行APIのレスポンスで**1度だけ**返し、
       再表示はせず、必要なら**再発行**して旧トークンを失効させる（`reservation_otps.code_hash` と同じ思想）
    ③ **TTL**: 精算QR **24時間** ／ 入金QR **7日**（いずれも既定値。運用で調整可）
    ④ **単回使用**: `settlement_qr_consumed_at` / `qr_consumed_at` を精算完了・承認確定で確定し、以降は無効
    ⑤ 検証は「**ハッシュ一致 ∧ 未消費 ∧ 未期限切れ**」の3条件を同時に満たすこと
  - **A案を選んだ決め手は「いま決めればスキーマ変更にならない」こと。** 本論点が「後付けはマイグレーション
    修正が必要」とされてきた前提は、`supabase/migrations/` が `.gitkeep` のみでテーブルが1つも
    作られていない現時点では**まだ発生していない**。初版DDLとして書けるため後付けコストはゼロであり、
    C案（リスク受容）が持つ「後で直せる」という含みは、**いまが最も安いタイミングである**ことを見落としている。
  - **B案（TTLのみ）を採らない理由**: 乱数強度が未指定のままではTTLを付けても推測可能なトークンは
    推測可能なまま。TTLは「盗まれた後の窓を狭める」対策であって「盗まれない」対策ではない。
  - **補強材料**: HTMLモック `prototype_v15.html` は既に「精算QR自動失効」まで実装している（WBS 18-1）。
    C案を選ぶとモックと設計が食い違い、§18-A の突合課題が1件増える。
  - 反映先: `非機能要件詳細.md` §2-7・§7-1・§7-3 F-1 ／ `DB物理設計.md` §3-2・§3-5・RLS突合表#16 ／
    `API設計.md` §2-5 ／ `CONSOLIDATED_DECISIONS.md` §1 ／ `WBS_Phase1.md` §16-1・7-3・12-2
  - ⚠️ **残る確認事項**: TTLの2値（24時間 / 7日）は運用実態を見ていない既定値である。
    入金QRの7日は「銀行振込を挟む可能性」を仮定した値であり、申請フローのSLAが確定したら見直すこと。
- 優先度: 高（決済のなりすましに直結し、DB物理設計の変更を伴うため実装前に確定したい）
- 背景: 脅威モデリング（非機能要件詳細.md §7 F-1、2026-08-28新設）で、`orders.settlement_qr_token` が
  平文text保存かつ**有効期限カラムを持たない**ことを確認した。トークンを知る者は清算導線へ到達できるため、
  推測可能な値や無期限トークンはなりすまし清算のリスクになる。`membership_applications.qr_token` には
  `qr_expires_at` があるが、乱数の強度要件が未指定。非機能要件詳細.md §2-7 に、①暗号学的乱数128bit以上
  ②TTL付与 ③清算完了／承認時の失効（単回使用）を**提案**として記載済み。
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §2-7・§7 F-1、`docs/spec/detailed-design/DB物理設計.md` §3-2・§3-5
- 選択肢:
  - A. 提案どおり採用（推奨）。DB物理設計.md へ `settlement_qr_token` のTTLカラム追加と乱数要件を追記する
  - B. 精算QRは店内の対面利用が前提のため、TTLのみ（例: 発行から24時間）の簡易対応に留める
  - C. Phase 1 は現設計のまま（リスク受容として非機能要件詳細.md §7-4 へ記録する）

## [2026-08-28] ロール別レスポンスマスキングの設計方針
- ステータス: 回答済み（2026-09-05）
- 優先度: 高（個人情報漏えいに直結。API実装前に方針確定が必要）
- 背景: 非機能要件詳細.md §5 #2（2026-08-16発見）で「レスポンスに含めるフィールドをロールで出し分ける設計が未着手」
  と指摘され、TASKS.md（詳細設計5）でも「オーナー確認事項として整理した」とされていたが、**本ファイルへの起票が漏れて
  追跡から外れていた**ため正式に起票する。v13 §8 は「氏名・連絡先・運営メモは一般会員・ゲスト向けAPIレスポンスに
  含めない」ことを要求している。脅威モデリング（非機能要件詳細.md §7 F-2）に、全エンドポイントへ
  ロール別レスポンスDTO（一般向けはニックネーム等の最小フィールド）を定義する案を記載済み。
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §2-4・§7 F-2、`docs/spec/detailed-design/API設計.md`、
  `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §8
- 選択肢:
  - A. ロール別DTO方式で確定（推奨）。API設計.md の主要エンドポイントへロール別レスポンス例を追記する改訂を行う
  - B. RLSの列レベル制御（ロール別ビューの分離）を正とし、API層は素通しにする
  - C. A＋B併用（API層DTOを正としつつ、S格付けテーブルのみビュー分離も併用する）
- 2026-09-05 オーナー回答（一部）: 保護対象スコープは**個人情報（S格付け）カラムのみ**で確定。
  方式はコスト比較の結果を受けて確定予定（AI提案: `API設計.md` §1 原則①によりクライアント→Supabase の
  直経路があるためアプリ層DTO単独では守れず、S格付けテーブルはDB側ビュー分離＋サーバ経由エンドポイントに
  薄いDTOを載せる**C案の限定形**を推奨）
- 2026-09-05 追加の論点提起（オーナー）と AI 回答: オーナーより
  **「RLSだけで足りるのでは／CRUD は Vercel を通るのでは」**という論点提起があった。AI回答の要旨:
  - (a) `supabase-js` の直アクセスは**ブラウザから `*.supabase.co` へ直接発行される**ため **Vercel を経由せず**、
    Vercel 上で動く DTO は実行されない
  - (b) **RLS は行単位の制御**であり、**許可した行は全列が返る**。したがって
    「他人の行を一部だけ見せたい」画面では**列制御が別途必要**になる
  - (c) ただし**個人情報を別テーブルへ分離すれば行制御だけで完結する**
  - 上記を踏まえ3案を提示: **A. PII別テーブル分離** / B. 直アクセスは自分の行のみとし、他者が絡む画面はサーバ経由 /
    C. ビュー分離
  - **A案を推奨**する。理由: ダミーデータ方針（`docs/spec/CONSOLIDATED_DECISIONS.md` §3・2026-09-05 決定）により
    **スキーマ変更コストが現時点で最小**であるため
  - ~~**オーナーの選択待ち**（本項のステータスは「一部回答・方式確定待ち」のまま）~~
    → **2026-09-05 オーナーが A案を選択し、本項は回答済みとなった**（下記「回答」を参照）
- **回答（2026-09-05・オーナー決定）**: **A案（PII別テーブル分離）を採用する。列マスキングは不採用。行単位RLSのみで保護する。**
  - `members` から個人情報カラム（`full_name`・`full_name_kana`・`address`・`hometown`・`birth_ym`）を
    **`member_profiles_private`** へ分離し、**`members` には個人情報を残さない**
  - これにより**列単位のマスキングは不要**となり、**行単位の RLS だけで保護が完結**する。
    一般会員は、個人情報を持つテーブルについては**自分の行のみ**参照できる（それ以外のテーブルは個人情報を持たない）
  - 他者を表示する画面（クエストボード等）では **`nickname`** を用いる。
    **未設定時に `full_name` へフォールバックしてはならない**（フォールバックは情報漏えいにあたる）
  - 選定理由: (a) `member_identifiers` でメールを分離済みという既存方針の延長である
    (b) ダミーデータ方針によりスキーマ変更コストが現時点で最小である
    (c) 「RLS対象は個人情報データのみ」というオーナー指示が「RLS を作り込むのは分離先テーブルのみ」という運用可能な形になる
  - 不採用: **B案**（直アクセスを自分の行のみに限定し、他者が絡む画面はサーバ経由）＝一覧画面ごとにエンドポイントが必要で実装量が増える／
    **C案**（ビュー分離）＝実質的な列マスキングであり管理対象が増える
  - あわせて **Supabase への直接アクセス（`API設計.md` §1 原則①）は Phase 1 で維持**することを決定した
    （A案により直アクセス経路でも行単位RLSだけで個人情報が守れるため。詳細は `docs/spec/CONSOLIDATED_DECISIONS.md` §1）。
    ただし **RLS は経路を問わず必須**である（サーバ経由にしても Data API はインターネットに露出し続けるため）
  - ✅ **本回答によりブロックが解除される作業パッケージ（`docs/spec/WBS_Phase1.md`）**:
    **`2-2`**（Supabase RLSポリシー設計）・**`2-3`**（ロール別UI表示制御）・**`2-4`**（個人情報収集・保持）・
    **`8-1`**（顧客サマリー・注文履歴編集画面）・**`8-4`**（マイログ）・**`10-3`**（再訪アラート）の**6件が解除される**
  - 反映先: `docs/spec/CONSOLIDATED_DECISIONS.md` §3（決定を記録）、
    `docs/spec/detailed-design/非機能要件詳細.md` §2-4「保護方式」・§5 #2・§7-3 F-2/F-6（反映済み）、
    `basic-design/backend/会員データモデル_ユーザーテーブル定義.md`・`detailed-design/DB物理設計.md` §6・
    `basic-design/backend/データモデル図_ER_Diagram.md`・v13 §7（**別エージェントが並行対応中**）
  - ⚠️ **派生した新規論点**: 移行370名は `nickname` 未設定のため、他者向け表示の代替規則が未確定。
    本ファイル「[2026-09-05] ニックネーム未設定会員の他者向け表示」へ起票した

## [2026-08-28] 個人情報カラムのカラムレベル暗号化の採否
- ステータス: 未回答
- 優先度: 中（Phase 1 の実装方式に影響するが、「不採用」の提案であり判断自体は軽い）
- 背景: 暗号化設計の整理（非機能要件詳細.md §2-7、2026-08-28新設）で、宿泊法対応により実名・住所を保持する
  `members` 等へのカラムレベル暗号化（Supabase Vault / pgsodium）の要否を検討し、**Phase 1 不採用を提案**した。
  理由: ①`contact_info` は名寄せの第1優先キーでユニーク制約が必要（v13 §5.8.3）なため、検索可能なまま暗号化するには
  決定的暗号化が必要になり、実装・鍵運用の複雑度が保護効果に見合わない
  ②保存時暗号化＋RLS＋ロール別マスキング＋バケット非公開で、脅威モデル上の主要な漏えい経路は塞がる。
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §2-7・§7
- 選択肢:
  - A. Phase 1 不採用で確定（推奨）。Phase 2 で Supabase Vault / pgsodium を再評価する
  - B. S格付けカラムのうち検索キーでないもの（住所等）のみ暗号化する
- 2026-09-05 オーナーよりコスト比較の質問。回答: 暗号化は3方式中最高コスト（鍵管理に加え、`contact_info` の
  名寄せユニーク制約と衝突し決定的暗号化が必要になる）。A案（Phase 1 不採用）を推奨、確定待ち

## [2026-08-28] 監査ログ「10年以上保存」の10年経過後の扱い（アーカイブ方針）
- ステータス: 未回答
- 優先度: 低（Phase 1 実装のブロッカーではないが、法務・運用判断の可能性があるため方向だけ早めに確認したい）
- 背景: 非機能要件詳細.md §3 A6 は監査ログの10年以上保存を要求しており、取引明細方式のテーブル設計で保存自体は
  満たしやすい。一方、10年経過後の扱い（アーカイブDB分離・削除可否）が未設計であることが §5 #3（2026-08-16発見）で
  指摘され、TASKS.md では「オーナー確認事項として整理した」とされていたが、**本ファイルへの起票が漏れていた**ため
  正式に起票する。
- 関連ファイル: `docs/spec/detailed-design/非機能要件詳細.md` §3・§5 #3・§7 F-5
- 選択肢:
  - A. 10年経過後もアーカイブとして無期限保持する（削除しない）
  - B. 10年経過後にコールドストレージへ移し、以後の削除はオーナー判断とする
  - C. 法務確認が必要（宿泊台帳の保存義務等との関係を確認してから決める）

---

## [2026-08-22] RAG統合の未決事項15件（孤児ノートからの移管）
- ステータス: **未回答** — オーナー確認待ち
- 経緯: `docs/spec/2026-08-22_line-rag-bot_浮遊街統合設計の考慮事項.md` は
  **どこからもリンクされない孤児ノート**（`CLAUDE.md` §1.2 違反）であり、かつ撤回済みの
  「アプリ内ナレッジ登録UI」記述を含んでいたため **OLD へ退避**した。
  ただし同ノートが抱えていた**未決事項を消さない**ため、ここへ移管する。
- 原本: [[Projects/fuyuugai-app/docs/spec/OLD/2026-08-22_line-rag-bot_浮遊街統合設計の考慮事項_OLD.md|OLD/2026-08-22_line-rag-bot_浮遊街統合設計の考慮事項_OLD]]

### A. アーキテクチャ判断が必要（7件）

| # | 論点 | 補足 |
| --- | --- | --- |
| A1 | `has_knowledge` フラグはテナント単位か、カテゴリ単位か | カテゴリ単位にすると R13 のカテゴリ絞り込みと接続できる |
| A2 | ナレッジ未整備期（初期データ投入前）の有効期限をどう扱うか | 初期投入は完了済みのため**優先度低下**。要再評価 |
| A3 | フォールバック時の `system_prompt` に免責を含めるか | R16（関連度の下限なし）を Phase 1 で許容した以上、**関連性が高い** |
| A4 | 高リスク道具検出時の `system_prompt` 追加ロジックの詳細 | 安全注意の自動付加の具体仕様 |
| A5 | モデル変更時のインデックス切り替え戦略（blue/green vs alias） | 埋め込みモデル変更＝全件再埋め込みのため影響大 |
| A6 | Embedding API quota 超過時の対応方針 | |
| A7 | 確信度スコアをユーザーに見せるか、の判断基準 | R16 の緩和策と併せて判断すべき |

### B. 実装確認が必要（3件）※2件は解決済み

| # | 論点 | 状態 |
| --- | --- | --- |
| ~~B1~~ | ~~ロール定義の正本は v13 §2 か~~ | ✅ **解決済み**（2026-08-22 オーナー回答 R4。正本は v13 §2、`line-rag-bot` は追従側） |
| ~~B2~~ | ~~`target_role` の値セットの確定~~ | ✅ **解決済み**（アプリ5値・LINE2値の非対称で確定。正本 v13 §2） |
| B3 | `DuplicateCheck` の TTL 管理実装（Redis vs Firestore TTL） | 未確認 |
| B4 | エスカレーション通知先「LINEグループ」と「メール」の仕分け基準 | 未確認 |
| B5 | 回帰テスト設定（同一質問 → 同一スコアを保証するか） | 未確認。CI 整備済み（R2）なので着手可能 |

### C. 会議で決定待ち（3件）

| # | 論点 | 補足 |
| --- | --- | --- |
| C1 | Phase 1 初期データ投入の完了目標日 | 初期投入自体は完了済み（16件）。**専用テナント `fuyugai` への分離**が残件（R5） |
| C2 | 初期メッセージ UI パターンの最終決定（案A/B/C） | 自己申告2区分方式の具体UI。原本 §「初期メッセージと UI/UX 設計」参照 |
| C3 | エスカレーション時のチケット化スコープ（Phase 1 / Phase 2） | |

> [!note] 移管にあたって行った整理
> 原本の15件のうち **B1・B2 は 2026-08-22 のオーナー回答で既に解決済み**だったため、
> 取り消し線で解決済みと明示した（`CLAUDE.md` §2.4）。残る **13件が未決**。
> なお原本にあった「FURPS+ フレームワークの適用」「決定記録テンプレート」は
> **提案段階の検討メモ**であり未決事項ではないため移管していない（原本を参照）。

---

## [2026-08-22] line-rag-bot のテナント機能を廃止し、浮遊街オリジナルとする
- ステータス: **回答済み（2026-08-22）** — 方針は「**コードは残し、浮遊街固定で運用**」に確定
- 回答（オーナー回答 R5 および追加確認）:
  1. **`line-rag-bot` は浮遊街オリジナルのプロダクトとして扱う。** 汎用マルチテナント製品としては展開しない。
  2. **ただしテナント機能のコードは削除しない。** `tenant_id` を浮遊街に固定して運用し、
     **新規テナントを増やさない運用ルール**とする。
- なぜ削除ではなく固定運用にしたか（2026-08-22 の実査に基づく）:
  - 設計文書に **既存クライアントが実在する記述**が複数ある
    （「`v1.1.0` をデプロイした瞬間、既存クライアントも同じコードで動く」`docs/08-浮遊街RAG詳細設計.md` §2-3）。
  - テナント機能を削除すると、**同じ Cloud Run / Firestore で稼働中の既存クライアントの LINE ボットが停止する**。
  - コードを残せば**工期への影響がゼロ**であり、Phase 1（9/10）を圧迫しない。
- この決定で変わること／変わらないこと:

  | 項目 | 扱い |
  | --- | --- |
  | `app/tenant/`・`feature_flags`・`knowledge_{tenant_id}` | **そのまま残す**（削除しない） |
  | 新規テナントの追加 | **行わない**（運用ルールで禁止） |
  | プロダクトとしての位置づけ | **浮遊街オリジナル**。他クライアントへの横展開は想定しない |
  | 既存クライアント | **現状のまま稼働継続**。停止させない |
  | リリース時の回帰確認 | **従来どおり必要**（共有デプロイのため。リスク R5 は消えない） |

- ⚠️ **注意**: 「浮遊街オリジナルとする」は*プロダクトの位置づけ*の話であり、
  *実行環境が浮遊街専用になった*わけではない。**既存クライアントとの相乗りは続く**ため、
  `docs/08-浮遊街RAG詳細設計.md` §2-3 の「既存テナント（`feature_flags` 未設定）の挙動が
  1ミリも変わらないこと」というリリースゲート要件は**引き続き有効**。
- ⚠️ **未完了の関連タスク**: `IMPLEMENTATION_STATUS.md` 残タスク **A3「Firestore テナント設定（fuyugai）の作成」が未完了**。
  現在、浮遊街の初期ナレッジ16件は **`tenants/default`**（既存クライアント側と同じ既定テナント）に入っている。
  固定運用にするとしても、**浮遊街専用テナントの分離は行うべき**（ナレッジの混在を防ぐため）。
- 関連ファイル: `Projects/line-rag-bot/docs/06-マルチテナント設計.md`、`docs/08-浮遊街RAG詳細設計.md` §2-3・§9-4、
  `app/tenant/tenant_service.py`、`IMPLEMENTATION_STATUS.md` A3

---

## [2026-08-22] ベクトル検索：カテゴリ事前選択＋エージェント経路による絞り込み（R13 の対応方針）
- ステータス: **回答済み（2026-08-22）— 着手トリガをチャンク数 900 に確定**
- オーナー提案: **UI でユーザーに最初にカテゴリを選ばせ、そのカテゴリに沿ったエージェントが
  カテゴリに沿った経路のデータのみを検索する。見つからなければ、類似度の近いエージェントが
  対応するカテゴリから検索する。**
- 評価: **性能課題（R13）の解消としては有効**。ただし**単独では不十分**であり、下記の条件付き。
  - ✅ **有効な理由**: Firestore 側で `where(category == X)` を効かせれば、
    **読み取り件数・転送量がカテゴリの件数まで落ちる**。全件スキャンの主コスト（転送量）が
    カテゴリ数に応じて 1/N になるため、1000超でもレイテンシが伸びにくい。
  - ✅ **既存設計と整合する**: 本リポジトリには既に **`agent_routing`（4系統分岐）** の設計と
    **業務カテゴリマスタ**が存在する（`IMPLEMENTATION_STATUS.md` 既知の制限 #4、
    コミット `1d2c9cb`）。現在は「データ保存のみ・ルーティング無効」の状態であり、
    **本提案はこの機能を有効化することとほぼ同義**。ゼロからの新規開発ではない。
  - ⚠️ **注意点1（設計文書が既に警告している）**: 「**ナレッジ件数が少ないうちは
    絞り込みが精度を下げる方向に働く**」（§8-3）。現在のチャンク数は16件規模であり、
    **今カテゴリ分割すると1カテゴリ数件になって当たらなくなる**。
    有効化は「チャンクが増えてから feature flag で切り替える」という既存方針が妥当。
  - ⚠️ **注意点2（R16 保留との相互作用）**: `RAG_MIN_SIMILARITY=0.0` を保留したまま
    カテゴリ絞り込みを入れると、**ユーザーがカテゴリを選び間違えた場合に、
    そのカテゴリ内の無関係チャンクを確信をもって返す**。誤答が「それらしく」なるため悪化しうる。
  - ⚠️ **注意点3（UX）**: 質問のたびにカテゴリ選択を挟むと、LINE の「話しかければ答える」体験が落ちる。
    フォールバック（類似カテゴリへの拡張）の設計次第では、結局複数カテゴリを走査して
    **絞り込みの効果が消える**点にも注意。
- **確定した進め方（2026-08-22）**:
  1. **Phase 1 では実装しない。** 現在のチャンク数（16件規模）では性能問題が発生せず、
     絞り込みは精度を下げるだけになる。
  2. **チャンク数 900 に達した時点で実装に着手する**（＝本提案の3段構成をそのまま実装）。
     900 は「絞り込みが精度を落とさなくなる母数」であり、かつ 1000（体感が壊れるライン）への助走でもある。
  3. 着手時に、**カテゴリ絞り込みと Firestore ネイティブ検索（`find_nearest`）を比較**する。
     両者は排他ではなく、**併用すると最も効く**（カテゴリで母集団を絞り、その中を KNN で引く）。
  4. 同時に **`RAG_MIN_SIMILARITY` の下限導入（保留中の論点）を再検討**する。
     下限が無いままカテゴリで絞ると、選び間違えたカテゴリ内の無関係チャンクを確信をもって返すため。
- ✅ **忘れない仕組みを実装済み（2026-08-22）**:
  `app/admin/widgets.py` の閾値を 1000 → **900** へ変更し、管理画面が
  **900 で警告（「カテゴリルーティングの実装に着手してください」）／1000 でエラー（優先タスク化）** を表示する。
  閾値の意味は `tests/test_chunk_threshold.py` で固定（片方だけ変更されるのを防ぐ）。
  **担当者がチャンク数を数えて気づく必要はない。**
- 関連ファイル: `Projects/line-rag-bot/app/rag/firestore_knowledge_store.py:149`、
  `app/knowledge/categories.py`、`docs/08-浮遊街RAG詳細設計.md` §8-3・§9-1

---

## [2026-08-22] Phase 1 リリース目標（~~9/10~~ → **9月中**へ再改訂・2026-09-03）
- ステータス: **回答済み（2026-08-22 → 2026-09-03 再改訂）** ／ **正本 v13 §4 は v1.22.0 で反映済み**
- 回答（初回・2026-08-22 R1）: ~~**Phase 1 リリースは 9/10 目標**~~
- **回答（再改訂・2026-09-03 オーナー確定）: Phase 1 リリースは「2026年9月中（〜2026-09-30）」目標**。
  **スコープは据え置き**（2026-08-16 の選択肢A を継続。絞り込みは行わない）。

### ⚠️ この再改訂は「エスカレーション条件の発火」への回答である（重要な記録）
2026-08-22 に設定した前提が崩れる条件のうち、**「8/31 時点で主要機能が動いていない場合は
スコープ再交渉（経営判断）へエスカレーションすること」が実際に発火していた**。

- 2026-09-03 時点で `package.json` は存在せず、**アプリケーションコードは 0 行**
- 実装対象は **61パッケージ**（`WBS_Phase1.md` 2026-09-03 全面改訂後の集計）
- **本再改訂は、そのエスカレーションに対する経営判断の結果**。「条件が発火したのに何も起きなかった」のではなく、
  **納期を動かす側で決着させた**ことをここに記録する
- 残日数: 7日 → **27日**（2026-09-03 → 09-30）

### 新しい中間チェックポイント（2026-09-03 設定）
旧条件（8/31）は発火・対応済みのため、27日の工期に対する条件を再設定した。

- **9/12 時点で `package.json` と Supabase 環境が存在しない場合** — 着手自体が遅れている
- **9/19 時点でコア業務（チェックイン・注文・会計）が通しで動いていない場合** — 再エスカレーション
- Claude による実装は**レビューとテストの工数を圧縮しない**。`CLAUDE.md` §4.4 が必ずテストを求める
  3領域（認可・金額計算・個人情報）は生成量が増えるほどレビュー負荷が増える

### ⚠️ 依然として未解決の論点

1. ~~**9月中に「何を」リリースするのかの定義が無い。**~~
   → ✅ **回答済み（2026-09-08 オーナー確定）：Phase 1 は 61パッケージ全部をリリースする。**
   部分リリースは行わない。`WBS_Phase1.md` §19-3 の **Tier A/B/C は「着手順序」であって
   スコープの仕分けではない**ことが確定した（2026-08-16 選択肢A「スコープ据え置き」の継続）。
   **これにより完了判定が可能になった**（61パッケージ全件の完了＝リリース可）。
   ⚠️ ただし 2026-09-08 時点で**着手5件・完了1件・残56件が0%**であり、
   **残22日で56パッケージ**となる。スコープを絞らない決定は**工期の逼迫を解消しない**。
2. **実装着手前に潰すべきブロッカーは3件**（`WBS_Phase1.md` §16-1）:
   ~~ロール別レスポンスマスキング~~（✅ 2026-09-05 解決・A案）／非同期ジョブ実行基盤／QRトークン保護強化／
   **公開予約OTPの送信基盤**（2026-09-05 起票）。
   ~~無料枠で満たせない非機能要件~~（✅ 2026-09-08 解決・Supabase 有料プラン）。
   いずれも**後から差し替えると手戻りが確定する**性質のもの。
3. 2026-08-24〜09-01 の約2週間分の成果（v13 v1.18.0〜v1.21.0・モック・運用マニュアル）が
   **未コミットで滞留**しており、public リポジトリ上でリンク切れになっていた（2026-09-03 に対応）。

### ⚠️ 副次的影響: クレジットカード決済の前提が変わった
「[2026-08-26] 9/10 リリースにクレジットカード決済が無くても運用できるか」の項も参照。
カードを Phase 2 とした根拠は「加盟店審査 2〜4週間が **9/10** に間に合わない」だったが、
**9/30 までなら審査期間の下限（2週間）は収まる**。Phase 1 復帰の可否に再検討の余地がある。
**現時点では Phase 2 のまま据え置き**、判断はオーナーマター。

- 関連ファイル: **正本 v13 §4（v1.22.0 で改訂済み）**、`WBS_Phase1.md` §S-1・§19、
  `CONSOLIDATED_DECISIONS.md` §1、`docs/オンボーディング資料_新規メンバー向け概要.md` §8 R1（**未反映**）

---

## [2026-08-22] テスト基盤：CI（ESLint / Pytest / E2E）をコミット時に実行する
- ステータス: **回答済み（2026-08-22）** ／ CI 設定は反映済み・**実装着手待ち**
- 回答: **CI（ESLint・Pytest・E2Eテスト）を Git コミット時に走らせる**（オーナー回答 R2）。
- 反映内容（2026-08-22 実施）:
  | リポジトリ | 変更 |
  | --- | --- |
  | `fuyuugai-app` | `.github/workflows/ci.yml` の push トリガを `main` 限定 → **全ブランチ**へ拡大。**E2E（Playwright）ジョブを新設** |
  | `line-rag-bot` | **CI が存在しなかった**ため `.github/workflows/ci.yml` を**新規作成**。gitleaks ＋ **Pytest（Firestore エミュレータ起動込み）** |
  | `CLAUDE.md` | §6.2 の表を実態へ更新。E2E の Secrets 分離（本番 Supabase を指さない）を明記 |
- 設計上の判断（オーナー回答の意図を保ったうえでの解釈）:
  1. 「コミット時」を **push 時**と解釈した。§5.1 が `main` への直接 push を禁じているため、
     `branches: [main]` のままでは feature ブランチのコミットが PR 作成まで一度も検証されないため。
  2. **ローカル `git commit` での E2E 実行はしない。** Playwright はブラウザ起動を伴い数分かかり、
     コミットのたびに走ると開発が止まる。§6.1／§6.2 の二段構え（手元は軽量・CI は重量）を維持した。
  3. `Pytest` は `fuyuugai-app`（TypeScript）には存在しない。**`line-rag-bot` 側の CI として実装**した。
  4. E2E・Playwright は `playwright.config.*` が存在するまで**自動スキップ**する（実装未着手で CI を赤くしない）。
- ⚠️ 残作業: **WBS 1-3 のブロックは解除されたが、E2E の中身はまだ無い。**
  `CLAUDE.md` §4.4 が必ずテストを要求する3領域（**認可・金額計算・個人情報**）の
  E2E シナリオ設計が必要。特に「街人が他人の会計を閲覧できないこと」は単体テストでは検証できない。
- 関連ファイル: `.github/workflows/ci.yml`（両リポジトリ）、`.githooks/pre-commit`、`CLAUDE.md` §6、`WBS_Phase1.md` 1-3

---

## [2026-08-22] LINEユーザーのロールを2区分に簡約し、target_role を4値→2値へ統合する
- ステータス: **回答済み（2026-08-22）** ／ **実装は未着手・影響調査が必要**
- 回答（オーナー回答 R6 および追加確認）:
  1. **LINEユーザーごとのロール分けはしない。** 運営が `user_roles` に手作業で登録する運用を**廃止**する。
  2. 代わりに**初期メッセージで自己申告**させ、「**運営・待ち人**」か「**ゲスト**」かで分岐する。
  3. `target_role` は **4値（guest / member / core_member / admin）→ 2値（guest / member）へ統合**する。
- これにより解消するもの: リスク **R6（ロール手動運用が追いつかず街人がナレッジに到達できない）**。
  未登録ユーザーが常に `guest` へフォールバックする問題が、自己申告により解消する。
- **詐称リスクの扱い（2026-08-22 オーナー判断）: Phase 1 では見逃す。**
  - 根拠: **LINE ボット経由で個人情報にはアクセスできない**ため、詐称されても実害が限定的である。
  - ✅ **この根拠は妥当**（2026-08-22 コード実査で確認）:
    - RAG が返すのは `knowledge_{tenant}` のナレッジチャンクのみで、**会員データを参照する経路が存在しない**
      （浮遊街アプリとの API 連携がゼロのため／§9 #31）
    - 会話履歴（`conversation`）・エスカレーション（`escalations`）には `user_id` と質問文が保存されるが、
      閲覧経路は **認証必須の Streamlit 管理画面のみ**（2026-08-16 に未認証公開を修正済み）。
      LINE 側からは読み出せない
  - ⚠️ **ただし「個人情報でない機微情報」は残る**。詐称した第三者が到達しうるのは、
    運営向けナレッジに含まれる**運営ノウハウ・鍵や設備の扱い・原価や単価**といった情報である。
    Phase 1 で許容するなら、**この種の情報をナレッジに登録しない運用**を併せて徹底すること
    （`docs/08-浮遊街RAG詳細設計.md` §8-7 と同じ「登録時に載せない」方式）。
  - **Phase 2 での再検討事項**として残す（合言葉・招待コード照合の導入）。
- ⚠️ **実装前になお決める必要がある論点**:
  1. **登録済みナレッジの再タグ付け。** 現在 `core_member` / `admin` でタグ付けされたナレッジを
     どちらへ寄せるか（`member` へ降格＝露出増／`admin` 相当を残す）の棚卸しが必要。
  2. **テナント機能の扱い**は R5 の決定（コード残置・浮遊街固定運用）に従う。
     `app/roles.py` を2値化する際も、`feature_flags` による切替は**残したまま**にすること。
- **アプリ5値 / LINE2値 の非対称（2026-08-22 確定）: そのままでよい。**
  - アプリ側5値（`admin`/`core_member`/`member`/`guest`/`custom`）は**画面認可**に使う。
  - LINE 側2値（`member`/`guest`）は**ナレッジの公開範囲の出し分けのみ**に使う。
  - **用途が異なるため揃える必要がない**（権限マトリクスの再設計は不要）。
  - マッピング: `admin`/`core_member`/`member`/`custom` → **`member`**、`guest` → **`guest`**。
    `custom` は LINE 側に対応値を作らず `member` 扱い。
  - ✅ 正本 v13 §2 に「アプリ5値 / LINE2値 の非対称」節として**反映済み（v1.16.0）**。
- 関連ファイル: `Projects/line-rag-bot/app/roles.py`、`app/line/user_roles.py`、
  正本 v13 §2・§6（権限マトリクス）、`docs/08-浮遊街RAG詳細設計.md` §8

---

## [2026-08-22] 宿泊の収容枠（満室判定の基準値）と部屋タイプの確定
- ステータス: **回答済み（2026-08-22、2回の回答で確定）**
- 回答（オーナー回答 R14 および追加確認）:
  1. **貸切日は想定しない。** 正本 §5.2.3 が挙げる貸切日（8/7・8/26・9/21〜23等）の
     予約不可日制御は**実装不要**。
  2. **満室の場合は UI 上から使用不可にする**（残枠0の宿泊形態を選択不可にする）。
  3. **「ゲストハウス」＝「ドミトリー」**（名称を統一。別施設ではない）。
  4. **「サロン」も宿泊施設として定員2名で開放する**（従来は宿泊枠外の扱い）。
  5. 確定した収容枠は以下のとおり。

     | 宿泊形態 | 棟数・区画 | 1棟あたり定員 | 最大収容 |
     | --- | ---: | ---: | ---: |
     | ドミトリー（旧「ゲストハウス」） | — | — | **16** |
     | コテージ | 3棟 | 2名 | **6** |
     | キャンプサイト（旧「テント」） | — | — | **10** |
     | 車中泊 | — | — | **30** |
     | アースバッグ | 1棟 | 2名 | **2** |
     | サロン | 1室 | 2名 | **2** |
     | **合計** | | | **66** |

  6. 部屋タイプは**6種**（ドミトリー／コテージ／キャンプサイト／車中泊／アースバッグ／サロン）。
     正本の旧6種（コテージA/B・アースバッグ・テント・車中泊・ゲストハウス・サロン）から
     **名称体系を上記へ改める**。
- **コテージの棟数（2026-08-22 確定）: 3棟**（A／B／C）。正本 §5.4.2 の料金マスタを**2棟→3棟へ改訂済み**（v1.16.0）。
- ⚠️ **残作業**: Googleフォームの選択肢（現行4種）を上記6種へ揃える改修が別途必要（§9 #30）。
- ✅ **正本へ反映済み（v1.16.0 / 2026-08-22）**: §5.4.2 に「宿泊形態と収容枠の確定」表を新設。
- 実装への反映方針: 上記の数値は `rooms`（部屋台帳）の `capacity` として保持し、
  **コードに直書きしない**。残枠は §5.2.5 のとおり `room_assignments` から**都度算出**する
  （集計カラムを持たない ＝ リスク R8 の再発防止）。
- 関連ファイル: 正本 v13 §5.2.1・§5.2.3・§5.2.5・§5.4.2、`docs/spec/データモデル図_ER_Diagram.md`

---

## [2026-08-22] 仕様書リポジトリへのアプリケーションコード混入（`docs/spec/` 配下の React 参照実装）
- ステータス: **クローズ（2026-08-22）— 保留のまま据え置き**
- 回答: **そのまま保留でクローズする**（オーナー回答 R7）。
  `docs/spec/ナレッジ登録UIコンポーネント_KnowledgeRegistrationForm.md` に含まれる
  560行の React 参照実装は、**現状のまま（仕様書内のコードブロックとして）残す**。
  「`docs/spec/` に実装コードを置く運用の是非」は**論点として決着させず、保留で確定**する。
- 意味するところ:
  - この論点を理由に**コミットを止めない**。`LOOP_LOG.md` で保留にしていた差分は通常どおり扱ってよい。
  - 「アプリケーションコードは一切存在しない」という表現は、**ビルド対象のコードが無い**という意味であり、
    仕様書内の参照実装コードブロックはこれに抵触しないものとして扱う。
  - 今後、**新たに**参照実装を書きたくなった場合は §1.2（新規ドキュメントを作らない原則）に従い、
    置き場所をオーナーに確認する（本クローズは既存分の追認であり、新規追加の許可ではない）。
- リスク R7 は**クローズ**。リスクマトリクスから除外する。
- 関連ファイル: `LOOP_LOG.md`、`docs/spec/ナレッジ登録UIコンポーネント_KnowledgeRegistrationForm.md`

---

## [2026-08-22] RAG の関連度下限（RAG_MIN_SIMILARITY）が 0.0 で、範囲外の質問にも無関係チャンクで回答する
- ステータス: ✅ **回答済み・保留解除（2026-08-29 オーナー決定）** — ~~保留（2026-08-22）~~
- 優先度: **高**（写真対応の前提条件。`line-rag-bot` の残タスク F0）

> [!success] 2026-08-29 オーナー決定：**下限値の設定を標準にする**
> 1. **`RAG_MIN_SIMILARITY` の既定を `0.0` → `0.7` へ引き上げ、「下限ありが標準」とする。**
>    値の根拠は本項の実測値：「東京の明日の天気」に対し**類似度 ≈0.65 の無関係チャンクがヒット**した。
>    **0.65 を弾ける最小ラインが 0.7** である（0.5 や 0.55 では**この実測ケースを止められない**）。
> 2. **下限未満だった場合の挙動は、既存の分岐にそのまま委ねる**（新規実装は不要）：
>    - `escalation` フラグON → **エスカレーション**（運営へ）
>    - OFF → **`general_answer` により Claude が一般知識で回答**
>      ＝ 2026-08-29 の「ナレッジが無ければ Claude が回答する」という指示と一致する
> 3. **テナント単位の設定は既に実装済み**（`tenants/{id}.rag_settings.min_similarity`、
>    `app/tenant/models.py: resolve_rag_params()`、管理画面 `app/admin/tabs/line_account.py`）。新規開発は不要。
>
> ⚠️ **実施順序の制約（重要）**
> - env 既定を 0.7 に上げると **既存クライアントの挙動も変わる**（`docs/08` §2-3 のリリースゲート
>   「既存テナントの挙動が1ミリも変わらないこと」に抵触）。したがって
>   **先に既存テナントへ `min_similarity: 0.0` を明示設定してピン留めしてから** env を変更する。
> - さらにその前に **A3（`fuyugai` テナントの作成とナレッジ移設）が必要**。
>   現在、浮遊街の初期ナレッジ16件は **`tenants/default`（既存クライアントと同じ既定テナント）**にあり、
>   テナントを分けない限り**浮遊街だけに閾値を効かせることができない**。
>
> ⚠️ **エスカレーション閾値との整合**
> `ESCALATION_THRESHOLD` の既定は **0.55** であり、`min_similarity=0.7` を入れると
> **フィルタを通過したパッセージは必ず 0.55 を超える**ため、エスカレーション判定は
> 「該当パッセージが0件のとき」しか発火しなくなる。**両者を 0.7 に揃える**ことを推奨する
> （揃えない場合、0.55〜0.7 の帯域が意味を持たなくなる）。実測での見直しは残タスク C1 と併せて行う。
- ⚠️ **保留中に残る影響**（受け入れているリスクの明示）:
  - 範囲外の質問にも**出典付きでもっともらしい誤回答**が出続ける
  - R13 のカテゴリ事前選択を導入すると、**ユーザーがカテゴリを選び間違えた場合に
    そのカテゴリ内の無関係チャンクを確信をもって返す**ため、本件との組み合わせで誤答が目立ちやすくなる
  - 緩和策（コード変更なし・運用のみ）: 回答文に「見当違いなときは『わからない』と送ってください」等の
    定型フッタを添え、エスカレーションへ誘導する運用でカバーする
- 背景: 2026-08-22 の本番実査で、初期ナレッジ16件の投入は**完了済み**と確認された
  （`has_knowledge=true` となり「ナレッジ0件 → 素の Gemini が回答」の経路は塞がれた）。
  しかし `RAG_MIN_SIMILARITY=0.0`（`app/config.py:140`）のため**関連度の下限が無く**、
  範囲外の質問でも `top_k=3` 件が必ずヒットし、それを根拠に回答してしまう。
  - 実測: 「東京の明日の天気」に対し、類似度 ≈0.65 のクエスト・ドリンクのチャンクが該当した。
- なぜ効くか: 出典表示付きで**もっともらしい誤回答**が出る。
  「無関係なナレッジを根拠として提示する」ため、ハルシネーション対策として入れた出典表示が**逆に信頼性を偽装**する。
  エスカレーション閾値（0.55・暫定値）とは別レイヤーの問題。
- ~~確認したい点~~ → ✅ **全3点とも 2026-08-29 に決着**（上記）:
  1. ~~下限値を導入するか／初期値~~ → **導入する。`0.7`**（実測 0.65 の誤ヒットを弾ける最小ライン）
  2. ~~下限未満の挙動~~ → **既存分岐に委ねる**（escalation ON＝エスカレーション／OFF＝Claude一般知識）
  3. ~~テナント単位で設定可能にするか~~ → **既に実装済み**。ただし **A3（テナント分離）が未完了**のため、
     現状では浮遊街だけに効かせられない点に注意
- 関連ファイル: `Projects/line-rag-bot/app/config.py:140`、`app/rag/firestore_knowledge_store.py:149`、
  `app/llm/llm_service.py:60`、`docs/08-浮遊街RAG詳細設計.md` §8-2

---

## [2026-08-22] Phase 2 で初回来訪者もアプリ内予約へ移行する方針（正本 §5.2.4 の「Googleフォームは廃止しない」と競合）
- ステータス: ✅ **回答済み・クローズ（2026-08-23）**
- 優先度: ~~中（Phase 2 スコープ）~~ → **高（Phase 1 へ前倒し）**

> [!success] 2026-08-23 オーナー決定：**選択肢A を採用し、Phase 1 で実施する**
> **Webブラウザだけで完結する公開予約ページ `/reserve` を用意し、Googleフォームを廃止する。**
> 下記「確認したい点」4件はいずれも決着した。
>
> 1. **アプリ未インストールの初回客** → **ログイン不要の公開ページ**で受ける。アプリのインストールは要求しない。
>    §5.2.4 が懸念した「予約経路の消滅」は起こらないため、同節の「Googleフォームは廃止しない」は**撤回**した（正本で取り消し線保存）。
> 2. **宿泊法必須項目の収集タイミング** → **§9 #30-② のまま「チェックイン時」**。予約フォームを重くすると初回客が離脱するため。
> 3. **Googleフォーム廃止の判断基準** → 段階的移行はせず、**公式サイトの Reservation ボタンのリンク差し替えをもって切替**。
>    フォームは削除せず「回答を受け付けない」設定にし、過去回答は台帳として保全する。
> 4. **貸切日・予約不可日の制御** → **実装しない**。現行フォームの注記（8/7・8/26・9/21〜23）は
>    **形骸化しており貸切運用の実態がない**ことをオーナーが確認（2026-08-23）。§5.4.2 の既定を維持する。
>
> 反映先: 正本 v1.17.0（§4・§5.2.3・§5.2.4・§5.2.5・§5.4.1b・§5.4.2③・§9 #46〜#49）、
> `detailed-design/外部連携設計.md` §5、`API設計.md`、`DB物理設計.md`、`WBS_Phase1.md`、`CONSOLIDATED_DECISIONS.md` §7
>
> ⚠️ **残課題**: 公式サイト「Reservation」ボタンのリンク差し替え（CMS種別・管理者が未確認）。
- 決定済みの方針（2026-08-22、オーナー）: **Phase 2 で「初回来訪者もアプリで予約を完結」させる。**
  Googleフォームは移行期間のみ併存させ、最終的に入口を1本化する。
- 背景: 正本 v13 §5.2.4 には現在、以下の**明示的な反対方針**が記載されている。

  > **Googleフォームは廃止しない** — 初回来訪者（アカウントを持たない人）の入口は、引き続きGoogleフォームである。
  > アプリ内予約はあくまで**リピーター向けの近道**として追加するものであり（中略）
  > 「アプリからしか予約できない」状態にすると、アプリを入れていない初回客の予約経路が消滅する。
  > 入口は2本のまま維持し、**下流（予約データの正本＝浮遊街アプリ）だけを1本に揃える**のが本設計の意図である。

  今回の方針はこの記述を Phase 2 で覆すものであり、`CLAUDE.md` §2.4（決定を覆すとき）に従い、
  **元の行を消さず取り消し線で上書きし、撤回の根拠を併記する**改訂が必要。
- 確認したい点（正本改訂の前に決めるべき論点）:
  1. **アプリ未インストールの初回客をどう受けるか。** §5.2.4 が挙げた「予約経路が消滅する」懸念への回答が必要。
     Webブラウザだけで完結する予約ページ（アカウント作成前でも入力可）を用意するのか、
     それとも初回客にはアプリインストールを必須とするのか。
  2. **宿泊法必須項目（住所・前泊地・後泊地/行先）の収集タイミング。** 予約時に取るか、チェックイン時に取るか。
     §5.2.3 のオープン論点③が未決のまま残っており、本件と一体で決める必要がある。
  3. **Googleフォーム廃止の判断基準。** アプリ経由率が何％を超えたら停止するか。
     §5.2.4 で導入済みの `check_ins.reservation_source`（`google_form` / `in_app` / `staff_manual`）で計測可能。
  4. **貸切日・予約不可日の制御。** 現行フォームでは注記のみでシステム制御されていない（§5.2.3）。
     アプリ内予約へ一本化する際は、残枠算出（§5.2.5）と合わせて予約不可日の制御が必要になる。
- 補足（誤解の整理）: 「Googleフォームと連携してアプリDBへ自動保存」する部分は**新規要件ではなく、既に Phase 1 の確定仕様**
  （§5.2.3 TO-BE②：フォーム送信をトリガーにアプリAPIへ送信し `pre_registered` の空枠を自動生成／§9 #3 イベント駆動）。
  ただし §5.2.3 の AS-IS 課題に「§4 の要件が**実運用上は未達**」と明記されているとおり、仕様はあるが実装されていない。
  Phase 2 の新規性は「**初回来訪者もアプリで完結**」の部分のみ、と整理した。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md`（§5.2.3・§5.2.4・§5.2.5・§9 #3・#36）、
  `docs/オンボーディング資料_新規メンバー向け概要.md`（§4 Phase 2）
- 選択肢:
  - A. Webブラウザ完結の予約ページを用意し、初回客もアプリDBへ直接取り込む（フォーム廃止可能）
  - B. 初回客はアプリインストール必須とする（導線の摩擦が増えるが実装は最小）
  - C. Phase 2 でも入口2本を維持し、フォームの自動連携精度を上げるに留める（§5.2.4 を維持）

---

## [2026-08-22] line-rag-bot の位置づけ（リポジトリ独立・プロダクト統合）をどこに正本として記録するか
- ステータス: **回答済み（2026-08-22）**
- 回答（オーナー回答 R4・R5 および統合可否の確認）:
  1. **統合しない方針を維持する。** 浮遊街アプリは **Supabase の認証基盤**を使用しており、
     `line-rag-bot`（Python / FastAPI / Firestore）とは**作りが根本的に違う**。
     したがって**現行の技術スタックをそのまま使用する**。この理由をオンボーディング資料の Q&A（Q3）へ追記する。
     - ※ 本項目は当初「浮遊街アプリのバックエンドを line-rag-bot へ統合する」と読める指示があったため確認したもの。
       確認の結果、**統合は行わない**ことが確定した（2026-08-16 §9 #31・2026-08-22 の既存決定と整合）。
  2. **`target_role` の正本は浮遊街アプリ側とする**（選択肢B）。（オーナー回答 R4）
     正本 v13 §2 のロール定義を唯一の正とし、`line-rag-bot`（`app/roles.py`）は**追従側**とする。
  3. **マルチテナントは考慮不要**（オーナー回答 R5）。現状のテナントは浮遊街のみであるため、
     ロール設計にあたって他テナントへの影響を考慮しなくてよい。**ただし後述の注意点あり。**
- ⚠️ 回答3（マルチテナント考慮不要）についての技術的補足 —— **「設計上考えなくてよい」と「デプロイ上影響しない」は別**:
  - `line-rag-bot` は**既存クライアントと Cloud Run / Firestore を共有**している
    （`docs/08-浮遊街RAG詳細設計.md` §2-3、リスク R5）。**浮遊街以外のテナントが現に存在する。**
  - したがって「浮遊街のことだけ考えて `app/roles.py` の4値を2値に変更する」と、
    **同じコードで動いている既存クライアントのロール判定も同時に変わる**。
  - **結論**: ロールの*意味づけ*は浮遊街基準で決めてよい（回答3のとおり）。
    しかし*実装*は `feature_flags`（テナント単位）で切り替え、
    **既存テナントの挙動が1ミリも変わらないこと**をリリースゲートとして維持する必要がある。
  - この点は R6（4値→2値統合）の実装時に必ず効くため、下記の新規項目として起票した。


**▼ 回答前の論点（記録として保持）** — 下記は本項目が回答される前の論点・選択肢である。
2026-08-22 の回答（上記）で解決済み。**選択肢B が採用された。**

- 優先度: 中
- 決定済みの方針（2026-08-22、オーナー）: **`line-rag-bot` は浮遊街アプリへ統合しない。リポジトリ・デプロイは独立を維持する。**
  ただしプロダクトとしては「浮遊街プロジェクト」1つとして扱い、**ドキュメント・オンボーディング・ロール定義は束ねる**。
  結合点は `target_role`（`guest` / `member` / `core_member` / `admin`）の4値のみで、これは両システムで完全一致させる。
- 背景: 本方針は 2026-08-16 の §9 #31（API連携ゼロ）を追認・補強するものだが、
  「**なぜ統合しないのか**」の根拠（line-rag-bot がマルチテナント製品であり浮遊街はテナント1件にすぎない、
  技術スタックが別、既に本番稼働中）は現在どのドキュメントにも明文化されていない。
  新規参画メンバーが繰り返し同じ疑問に到達するため、正本化が望ましい。
- 確認したい点:
  1. 記録先は `docs/spec/CONSOLIDATED_DECISIONS.md`（意思決定ログ）でよいか。
     `CLAUDE.md` §2.2 のフォーマット（決定／決定日／理由／根拠プロンプト／反映先）に従って追記する想定。
  2. `CLAUDE.md` §2.3 が要求する**根拠プロンプトのリンク**をどうするか。
     本決定の根拠となった会話は現時点で `AI-Conversations/06_浮遊街アプリ・FELプロジェクト/` に未保存。
     先に会話ログを保存してからリンクする必要がある。
  3. **`target_role` の正本をどちらのリポジトリに置くか。** 現在は両リポジトリに同じ4値が独立して定義されており、
     二重管理の状態。片方だけロールを増やす・名前を変える変更が入ると、
     **内部専用ナレッジがゲストへ漏れる**事故につながる。正本の所在と同期手順を決めておきたい。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md`（§9 #31・#32、§5.7.5）、
  `docs/spec/detailed-design/外部連携設計.md`、`Projects/line-rag-bot/docs/07-ブランチ戦略.md`、
  `Projects/line-rag-bot/app/roles.py`、`docs/オンボーディング資料_新規メンバー向け概要.md`（§3）
- 選択肢:
  - A. `CONSOLIDATED_DECISIONS.md` に追記し、`target_role` の正本は line-rag-bot（`app/roles.py`）とする
  - B. `CONSOLIDATED_DECISIONS.md` に追記し、`target_role` の正本は正本v13（§2 ロール定義）とする
  - C. オンボーディング資料の記載のみで足りるものとし、正本化しない
---

## [2026-08-22] line-rag-bot の検索・フォールバック・エスカレーションフロー（詳細設計の未決項目）

**ステータス**: **未回答 — 設計検討中（実装確認待ち）**  
**優先度**: 高（Phase 1 実装のブロッカー候補。ブランチ 3・4・6 の実装前に決定必須）  
**出典**: [[2026-08-22_line-rag-bot_浮遊街統合設計の考慮事項_OLD|OLD/2026-08-22_line-rag-bot_浮遊街統合設計の考慮事項（旧版・2026-08-22 退避）]]

本セッション（2026-08-22）で抽出された、line-rag-bot の検索・回答フロー周辺の未決事項一覧。  
内容の詳細は上記ドキュメントを参照。以下は実装前の確認チェックリスト。

### A. ナレッジ検索フロー周辺

#### A1. `has_knowledge` フラグの粒度
- **論点**: テナント単位で十分か、カテゴリ単位で分ける必要があるか？
- **背景**: 
  - 現状設計（docs/08）では `feature_flags.structured_knowledge` がテナント全体で ON/OFF
  - ただし Phase 1 初期はレシピマスタのみ（道具は未投入）となりうる
  - ナレッジ種別ごとに信頼度が異なる可能性
- **確認項目**:
  - [ ] レシピ・道具・トラブル症例を独立した toggle にするか
  - [ ] UI（初期メッセージのカテゴリ選択）と連動する必要があるか
- **決定待ち**: 要件定義ヒアリング

#### A2. ナレッジ未整備期（初期データ投入前）の有効期限
- **論点**: PDF マニュアルから半自動抽出した直後は `review_status="draft"` のまま。  
  いつ `reviewed` へ昇格させるか、その間の検索・公開ポリシーは？
- **現状設計**: 
  - docs/08 ブランチ 7「初期データ投入」に「レビュー → 確定」ステップあり
  - ただし実運用ではこのレビュー期間がどのくらい続くか不定
- **確認項目**:
  - [ ] draft ナレッジを検索対象に含めるか除外するか
  - [ ] 期限を決めるか（例：1週間後に自動昇格）
- **決定待ち**: オーナーの初期データ投入スケジュール確認

#### A3. フォールバック（`has_knowledge=False` または検索結果 0件）時の system_prompt
- **論点**: ユーザーへの免責文を含めるべきか
- **現状設計**: docs/08 では temperature だけが書かれ、prompt 内容が未定
- **確認項目**:
  - [ ] 「ナレッジがないため間違っている可能性があります」といった警告文を含めるか
  - [ ] フォールバック時のプロンプトテンプレートの詳細
  - [ ] LLM に渡す context（時刻・ユーザーロール等）に何を含めるか
- **決定待ち**: PM/UX 判断（透明性 vs ユーザー混乱回避）

### B. インデックス・再作成周辺

#### B1. Embedding モデル変更時のインデックス切り替え戦略
- **論点**: `text-embedding-3-small` → `text-embedding-3-large` へアップグレードする際、  
  全ベクトルの再計算と Firestore インデックス置き替えをどの戦略で行うか
- **選択肢**:
  - Blue/Green（旧インデックスと新インデックスを並行）
  - Alias 切り替え（ES 的な仮想インデックス、Firestore では実装形式が異なる）
  - 瞬間的ダウン（検索が一時停止する）
- **確認項目**:
  - [ ] 浮遊街の想定 SLA（応答時間・可用性）
  - [ ] Firestore ネイティブの切り替え実装方式
- **決定待ち**: オーナーの SLA 要件確認

#### B2. Embedding API quota 超過時の対応
- **論点**: 初期データ投入（最大1000+ チャンク）や再インデックス時に Gemini embed API の rate limit に引っかかった場合
- **対応案**:
  - リトライ・バックオフ（何秒待つか）
  - DLQ へ記録して手動再試行
  - 失敗したチャンクだけスキップして先に進む（危険）
- **確認項目**:
  - [ ] 月間の Gemini API quota 見積もり
  - [ ] 初期データ投入時の API 消費ペース
  - [ ] 失敗時の許容度（何件ならスキップしてよいか）
- **決定待ち**: インフラ・コスト側の確認

### C. エスカレーション周辺

#### C1. 高リスク道具検出時の system_prompt 動的切り替え
- **論点**: `safety_level="high"` の道具が含まれた場合、LLM に「資格確認を促す一文を必ず添える」  
  という既存設計（docs/08 ブランチ 4）の詳細ロジック
- **確認項目**:
  - [ ] 検索結果のどの段階でこの判定をするか（マスタ展開時か、prompt 生成時か）
  - [ ] 複数の高リスク道具が混在した場合の prompt 文案
  - [ ] 「資格確認」の具体例（刈払機取扱教育等）を prompt に列挙するか
- **決定待ち**: 道具マスタの実装時に併せて

#### C2. エスカレーション時のチケット化・通知スコープ
- **論点**: 信頼度 < threshold で未回答一覧へ記録する時、同時に誰へどの形で通知するか
- **現状設計**: 
  - docs/08 ブランチ 6 では「管理画面『未回答問い合わせ』タブ」のみ
  - 運営への即時通知（LINE グループ / メール等）についての記載が曖昧
- **確認項目**:
  - [ ] リアルタイム通知が必要か（管理画面定期確認で十分か）
  - [ ] 通知先（LINE グループチャット / メール / 両方）
  - [ ] チケット化（Jira / Slack スレッド / 管理画面内のステータス管理のみ）
  - [ ] SLA（通知後の回応時間目標）
- **決定待ち**: 運営チーム・管理画面 PM の要件確認

### D. UI/UX 周辺

#### D1. 初期メッセージのロール選択パターン最終決定
- **論点**: LINE でユーザーとの初回やりとり時に、ロールを誰がどのタイミングで判定するか
- **選択肢**（詳細は考慮事項ドキュメント参照）:
  - **案 A**: シンプル自由入力（UX シンプル、ロール判定できない）
  - **案 B**: カテゴリ先行選択（ロール判定可能、UX に入力ステップ追加）
  - **案 C**: ハイブリッド（入力後に推定ロール表示 + 修正可能、推奨）
- **確認項目**:
  - [ ] 案 C の実装方法（入力後に `確認ボタン` を出すか / 自動判定して選択肢を表示するか）
  - [ ] 修正機能のフロー設計（一度判定後、切り替え可能にするか）
- **決定待ち**: オーナー / UX 判断

#### D2. エスカレーション案内の初期メッセージ組み込み
- **論点**: 確信度未満の場合、ユーザーへ「運営へ連絡してください」メッセージをどうフォーマットするか
- **確認項目**:
  - [ ] LINE / メール / フォーム等の連絡手段をどこへ書くか
  - [ ] 質問内容を自動でチケット化するか（ユーザーが再度説明する手間を減らすため）
  - [ ] チケット番号を付与して「この質問は #123 で追跡できます」と返すか
- **決定待ち**: カスタマーサポート / 運営 PM の判断

### E. 実装前の確認項目（cross-check）

#### E1. ロール定義の正本確認
- **確認事項**: 
  - [ ] v13 §2 の「アプリ 5値 / LINE 2値 非対称」が line-rag-bot の実装に反映されているか
  - [ ] `app/roles.py` の実装値と v13 §2 の定義が一致しているか
  - [ ] `feature_flags` による切替で既存クライアント（テナント/default）に影響しないか
- **依存**: R6（4値→2値統合）の実装時に必須

#### E2. DuplicateCheck（reply_token TTL 600秒）の実装
- **確認事項**:
  - [ ] `duplicates_{tenant}` コレクション使用か Redis か
  - [ ] Firestore TTL で自動削除するか手動削除するか
  - [ ] reply_token の一意性保証（複数ユーザーの同時質問で衝突しないか）
- **依存**: webhook 実装時

#### E3. データモデル一貫性（Firestore スキーマ）
- **確認事項**:
  - [ ] `recipes_{tenant}` / `tools_{tenant}` / `knowledge_{tenant}` の 3 コレクション設計が実装に反映済みか
  - [ ] `metadata.type`（"recipe" / "tool" / "failure" / "alias"）の値集合が コード化されているか
  - [ ] マスタ変更→インデックス投影の cascade ロジックが実装されているか
- **依存**: 全ブランチ（1-7）の実装確認

---

**本未決事項が全て「回答済み」になるまで、関連ブランチ（3・4・6）の本実装は着手しないこと。**  
**テスト計画（tests/test_webhook_rag_flow.py など）も、この未決事項の決定を待ってから設計すること。**

---

## [2026-08-20] 2026-08-13 レビューの未反映11項目（棚卸しの結果、v1.15.0 で一括反映）
- ステータス: 回答済み（2026-08-20、11/11点）
- 回答:
  1. **§5.11 メディア** → **Phase 1 へ再復帰**。UIは管理者・コアメンバー・街人・**ゲスト**の全ロールから
     「アップロード」メニューで画像・動画を登録できる。**Phase 1 はアップロードのみ**とし、
     **用途・フリーワードによる検索と、検索に合う素材のAI推論提示は Phase 2**（v13 §5.11.7・§5.11.3／§9 #33）。
  2. **通貨表記** → **Uii を主・円を副へ逆転**（`2,400 Uii（¥3,000）`）。計算ロジックは不変、表示順序のみ（§9 #41）。
  3. **カフェ注文の提供ステータス** → `未提供／提供済み` を追加。決済ステータスと独立した2軸（§9 #39）。
  4. **Eumo給付の追跡** → `未送付→送付済→受領確認済` を管理する給付一覧を新設（§9 #35）。
  5. **アプリ内からの宿泊予約** → アカウント作成済みの利用者は直接予約可。Googleフォームは初回来訪者用に維持（§9 #36）。
  6. **オーダーメイドクエスト（AI個人指名）** → **Phase 2「スキルツリー機能」へ統合**（§9 #38）。
  7. **顧客管理のチェックイン中優先表示** → 既定ソートを確定（§9 #43）。
  8. **宿泊枠の残数表示** → `room_assignments` から都度算出。保存カラムは持たない（§9 #37）。
  9. **宿泊予定・履歴のカレンダー表示** → 本人向け（マイログ）と運営向け（宿泊予定カレンダー）の2系統（§9 #37）。
  10. **宿泊形態の価格／カフェメニューの管理画面編集** → `accommodation_rates`・`menu_items` をマスタ化（§9 #40）。
  11. **クエスト承認の二段階化** → `コアメンバー確認済` ステージを新設。最終承認は `admin` のみ（§9 #34）。
  12. **Discord／LINE素材の自動吸い上げ** → **Phase 2**（§9 #42）。
- 優先度: 最高（うち #41 通貨表記は全画面に波及するため、実装着手前の解決が必須だった）
- 背景: 2026-08-20 の再調査により、**2026-08-13 のクライアントレビューは §5.7〜§5.11 の5要件のみが
  v1.10.0 に反映され、同時に提示された11項目が正本にも本ファイルにも起票されないまま失われていた**ことが
  判明した。当時 AI が整理した「v14反映案（7カテゴリ）」は、**v14 が作られないまま消滅**していた。
  特に「メイン表示をUiiにしてサブを円にする」という指示は、**10日間にわたり正本に真逆の記述
  （円を主・Uiiを副）が残り続けていた**。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §9 #33〜#43、
  `docs/spec/CONSOLIDATED_DECISIONS.md` §11、`docs/spec/basic-design/frontend/HTMLモック_v13.md`、
  `docs/spec/basic-design/frontend/UI_UX設計指針.md`、`docs/spec/detailed-design/DB物理設計.md`、
  `docs/spec/detailed-design/画面設計.md`
- **再発防止（運用ルール）**: レビューで受けた要望は、反映する／しないに関わらず**全件を起票**し、
  ①今回反映 ②Phase 2 送り ③却下 ④要確認 のいずれかに振り分けて記録に残すこと。
  改訂履歴には「何を反映したか」だけでなく**「同時に受けたが反映しなかったもの」も書く**
  （詳細: `CONSOLIDATED_DECISIONS.md` §11-3）。

## [2026-08-20] 現場運用マニュアルの作成タイミング
- ステータス: 回答済み（2026-08-20）
- 回答: **要件とモックが固まってから作成する**（保留）。着手条件は次の2点。
  1. v13 v1.15.0 の要件がオーナー承認される
  2. モック（`prototype_v14.html`）が v1.15.0 準拠で更新される
  この2点が揃った時点で `docs/operations/` 配下に作成する。
- 進捗（2026-08-22、オーナー回答 R11）: **モックは 2026-08-23 の MTG で確定予定。**
  - 確定後は、**モック＋要件定義書を入力として Claude が実装する**ため、工期は足りるとの見立て。
  - 着手条件②（モック更新完了）は **8/23 に充足見込み**。充足した時点でマニュアル作成に着手できる。
  - リスク R11 は**「遅延」から「予定どおり進行中」へ格下げ**。ただし完了までクローズしない。
- ⚠️ **監視すべき前提**（崩れたら R1 へ波及する）:
  - **8/23 の MTG でモックが確定しなかった場合**、実装開始日が後ろへ動き、9/10 が直撃を受ける
  - Claude による実装は**レビューとテストの工数を圧縮しない**。`CLAUDE.md` §4.4 が必ずテストを求める
    3領域（認可・金額計算・個人情報）は、生成量が増えるほどレビュー負荷が増える
  - マニュアルは**実装と並走**させること。開発方針①は「操作マニュアルも同時準備・提出」と定めており、
    実装完了後に着手すると納品が遅れる構造は変わっていない
- 優先度: 中（Phase 1 の納品物だが、仕様確定前に作ると全面書き直しになる）
- 背景: 2026-08-13 に『現場運用マニュアル（Phase 1対応版）』が一度作成されたが、**リポジトリに実体がなく
  散逸している**（2026-08-20 の調査で glob 0件を確認）。加えて v1.15.0 で仕様が大幅に変わったため、
  当時の内容は既に陳腐化している（通貨表記・提供ステータス・二段階承認・アプリ内予約がいずれも未反映）。
  仕様が動いている段階でマニュアルを作ると、改訂のたびに全面書き直しになり、かつ
  **「古いマニュアルが現場に残る」事故**を招く。
- 進捗（2026-08-23）: **運営スタッフ向けのドラフトを先行作成した**（オーナー指示）。
  - 成果物: `docs/operations/現場運用マニュアル_運営スタッフ向け.md`、
    `docs/operations/コンシェルジュ登録用ナレッジ原稿_運営向け.md`（登録台帳）
  - **同日、マニュアルの内容を浮遊街コンシェルジュへ50件投入した**（`ops-01`〜`ops-50`／本番 `knowledge` 18→68件）。
    オーナー判断「**プレリリースまではコンシェルジュを運営しか触らない**」を前提に、
    `role_filter` はオフのまま `target_role=member` のみ先に付与。
    **個人情報・原価を入れる必要が出た時点で権限を分ける**方針（2026-08-23 オーナー確定）
  - 出典は正本 v13 v1.15.0 と `画面設計.md`（2026-08-20 版）のみ。**新しい仕様は足していない**
  - ⚠️ **着手条件②（`prototype_v14.html` の v1.15.0 準拠更新）は未充足のまま**である。
    当該ファイルはリポジトリ・ボルトのいずれにも存在せず（2026-08-23 時点で glob 0件）、
    8/23 の MTG での確定を待っている状態。**したがって本ドラフトは現場へ配布できない**
  - アプリ本体が未実装（コード0行）のため、記述は「設計上こう動く」であり実機検証を経ていない。
    実装後に画面ID・ボタン名との突合が必須
  - 作成過程で判明した**資料間の矛盾2点**（①差額の免除操作の権限範囲：`画面設計.md` §4 C9「保留」と
    §6-#2「解消済み・コアメンバー可」が食い違う ②宿泊形態の名称：`画面設計.md` と v13 §5.2.4 に
    旧名称「ゲストハウス」「テント」「コテージ2棟」が残存）は、マニュアル §12 に未決事項として明記した。
    **①は運用に直結するため、実装前にオーナー確認が必要**
  - ステータスは「回答済み（保留）」のまま維持する。**#44 のクローズ条件は着手条件②の充足と現場配布可否の判断**であり、
    ドラフトの存在をもって解消扱いにしない
- 関連ファイル: v13 §8（操作マニュアルの整備・提出）、§9 #44、`docs/operations/`

## [2026-08-20] AI基盤（Claude / ChatGPT）の選定に関する検討記録が残っていない
- ステータス: **回答済み（2026-08-22）**
- 回答: **選択肢A — Claude で対応する。**（オーナー回答 R12）
  - これにより `CONSOLIDATED_DECISIONS.md` §1 の「AI基盤: Claude API / Agent SDK」は
    **「既定路線が残っただけ」ではなく、明示的に選択された確定事項**になった。リスク R12 はクローズ。
  - 注記: 朝会音声の文字起こしのみ **Gemini API へ直接投入**する方針は従来どおり維持（マルチモーダル・話者分離）。
    「AI基盤＝Claude」は**テキスト生成・エージェント処理**を指し、音声処理の Gemini とは競合しない。
  - 注記: `line-rag-bot` 側の RAG 回答生成は **Gemini** のまま（本番稼働中・変更しない）。
    「Claude で対応」は**浮遊街アプリ本体**の AI 基盤を指す。2リポジトリで採用モデルが異なる状態は意図的。
- 優先度: 低（実装のブロッカーではない。ただしコスト前提が経営判断に影響する）
- 背景: 2026-08-13 のレビューで「ChatGPTの有料プランに課金している。ClaudeからChatGPTに変更する場合は」
  というコスト比較の依頼が**複数回**あったが、**その検討があった事実自体が正本にも
  `CONSOLIDATED_DECISIONS.md` にも記録されていない**（2026-08-20 の調査で grep 0件を確認）。
  現行の技術スタックは「Claude API / Agent SDK」で確定している（`CONSOLIDATED_DECISIONS.md` §1）が、
  この確定が「ChatGPTへの切替を検討したうえでの結論」なのか「検討自体が立ち消えたまま既定路線が
  残っただけ」なのかが追跡できない。
- 関連ファイル: `docs/spec/CONSOLIDATED_DECISIONS.md` §1、v13 §9 #45
- 選択肢:
  - A. 現行の Claude 前提を維持する（検討は済んでおり、Claude で確定）
  - B. OpenAI へ切り替える（サブスクの一本化によるコスト削減を優先）
  - C. ハイブリッド：一次回答など軽量な処理のみ安価なモデルへ振り分ける
  - D. 実装時に差し替え可能な抽象化だけしておき、選定は後回しにする

## [2026-08-16] ナレッジ・RAG基盤：pgvector（浮遊街アプリ本体）とFirestore（line-rag-bot）の二重化疑義
- ステータス: ~~回答済み（2026-08-16）~~ → 🔄 **決定を覆した（2026-09-11）：Supabase pgvector へ一本化**

> [!warning] 2026-09-11 に本項の結論は反転した（`CLAUDE.md` §2.4 に従い記録を残す）
> 当時の結論は「**Firestore を単一のナレッジ基盤とし、アプリ本体は pgvector を持たない**」であった。
> **2026-09-11 のオーナー決定により、これを反転し Supabase pgvector へ一本化する**
> （v13 §9 #31 限定改訂・`CONSOLIDATED_DECISIONS.md` §17）。
>
> **反転できた理由**: 本項が pgvector を捨てた根拠は技術的優劣ではなく
> 「**Supabase 無料枠の DB 容量 500MB 制約**下で併存させる利点が薄い」であった。
> **2026-09-08 の「Supabase は有料プランで運用する」決定により、この制約は失効していた。**
> つまり本項は**前提が消えたまま結論だけが残っていた**。
>
> ⚠️ **ただし「二重化を避ける」という本項の問題意識自体は正しく、いまも維持されている。**
> 反転後も**ベクトル空間は1つ**であり、二重管理は発生しない。変わったのは**どちらに寄せるか**だけである。
> なお「**アプリ内 AI チャット UI を持たない**」という同日の追加方針は**変更せず維持**する。
- 回答: 選択肢A（統合型）を採用。line-rag-bot（Firestore）を単一のナレッジ基盤とし、浮遊街アプリ本体は
  独自のpgvectorナレッジ格納（`knowledge_items`/`knowledge_embeddings`）を持たない。さらにオーナーから
  重要な追加方針が示された：**浮遊街アプリ本体はエンドユーザー向けのAIチャットUI（アプリ内コンシェルジュ）
  自体を持たない**。街人・ゲストへのAI案内（FAQ・レシピ・道具・failure_patterns等）は**LINE（line-rag-bot）
  経由に一本化**する。ナレッジの「登録」（書き込み）のみ浮遊街アプリの管理者権限UIから行い、line-rag-bot側の
  REST API（docs/08 §9-5で方針決定済み）へ送信する構成とする。
  - 影響: DB物理設計.mdの`knowledge_items`/`knowledge_embeddings`（pgvector）テーブル提案は不要となり、
    「不採用となった設計案」として参考セクションへ移動済み。画面設計.md（A7削除）・API設計.md（`POST
    /api/ai/ask`削除、`/api/knowledge`系をline-rag-bot APIプロキシへ再定義）・外部連携設計.md・Master
    Index・v13正本（§9 #31）・WBS_Phase1.mdへの横断反映は**2026-08-16中に完了済み**。
  - **✅API連携ゼロが最終確定（2026-08-16オーナー最終判断）**: ナレッジ登録・編集、未回答エスカレーションの
    閲覧・代理回答は全てline-rag-bot Streamlit管理画面で完結。浮遊街アプリ本体からline-rag-botを呼び出す
    REST APIは**一切実装しない**（読み取り専用のエスカレーション一覧APIも含めて不採用）。浮遊街アプリ側は
    管理画面（B9/C5）に「line-rag-bot管理画面を開く」外部リンクを配置するのみ。システム間の認証連携
    （JWT検証・APIキー等）も不要になった（各システムが自身のログイン機構で個別に認証）。
  - **✅`target_role`4値・単一値で確定（2026-08-16オーナー回答）**: `guest`/`member`/`core_member`/
    `admin`の4値。親方衆は`member`権限として扱う。`target_audience`（複数選択、v13 §9 #29）は保留
    （実装がline-rag-bot側Streamlitへ移ったため、浮遊街アプリ側のブロッカーではなくなった）。
- 優先度: 最高（詳細設計（DB物理設計・API設計・画面設計のナレッジ関連部分）の前提そのものが変わりうる）
- 背景: オーナー追加指示によるdetailed-design作成の一環で、`docs/spec/RAGシステム仕様.md`・v13・Master Indexが
  前提とする「pgvector（Supabase内）」のRAG基盤設計と、別プロジェクト`line-rag-bot`側の設計書
  （`C:\Users\user\Documents\MyVault\Myvault\Projects\line-rag-bot\docs\08-浮遊街RAG詳細設計.md`）を
  突き合わせたところ、**line-rag-bot側は浮遊街を1テナントとして扱うFirestoreベースの独自RAG実装であり、
  レシピ・道具マスタ／failure_patterns／エスカレーション機能が2026-08-15〜16時点で既に実装済み**である
  ことが判明した。正本v13・RAGシステム仕様はこのline-rag-bot・Firestore実装の存在を明示的に位置づけて
  おらず、両者が同じ役割（ナレッジ登録・RAG検索・エスカレーション）を別々の技術（pgvector vs Firestore）
  で二重実装している可能性がある。line-rag-bot側docs/08 §9-5には「ナレッジ登録UIは最終的に浮遊街アプリ
  本体の管理者権限UIに統合し、line-rag-bot側にREST APIを切り出す」という2026-08-14の方針決定が既に
  記録されており、これに従うなら浮遊街アプリ本体は独自のpgvectorナレッジ基盤を持たず、line-rag-bot API
  を呼び出すクライアントとして実装すべき、とも読める。一方、v13の記述だけを見ると自前のpgvector実装を
  前提にしているようにも見え、確定的な判断ができなかった。詳細は
  `docs/spec/detailed-design/外部連携設計.md` §1を参照。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.7、`docs/spec/basic-design/ai-rag/RAGシステム仕様.md`（2026-08-16に工程×領域の2軸フォルダへ再配置）、
  `docs/spec/detailed-design/外部連携設計.md`、`docs/spec/detailed-design/DB物理設計.md` §3-3、
  line-rag-bot `docs/08-浮遊街RAG詳細設計.md`（特に§9-5・§1スコープ表）
- 選択肢:
  - A. 統合型：line-rag-botのFirestoreへ一本化し、浮遊街アプリ本体はline-rag-bot APIのクライアントとして実装する（自前のpgvectorナレッジテーブルは不要）
  - B. 並存型：アプリ内チャット（浮遊街アプリ本体・pgvector）とLINEチャットボット（line-rag-bot・Firestore）を意図的に別データとして並存させる（役割分担を明確化した上で）
  - C. 移行型：実態としてline-rag-botの実装が先行しているため、正本側の記述をline-rag-bot前提に書き換えて追認する

## [2026-08-15] 技術スタックは正式に承認されているか
- ステータス: 回答済み（2026-08-15）
- 回答: 正式承認済み（選択肢A）。Next.js(Vercel) / Supabase(PostgreSQL・Auth・Realtime・RLS) / Cloud Storage for Firebase / Claude API・Agent SDK / pgvector / MCP のスタックを確定として自律ループ（fuyuugai-appは仕様整理のみ、実装は別途基盤構築後）が前提にしてよい。テスト基盤（CI/単体・結合テスト方針）は依然未定のため、実装着手前に別途構築タスクが必要。
- 優先度: 最高（実装着手の絶対ブロッカー）
- 背景: `docs/spec/浮遊街アプリ 統合ナレッジベース (Master Index).md` §4、および background の技術検討ログ（2026-07-08〜09の複数回）を通じて「Next.js(Vercel) / Supabase(PostgreSQL・Auth・Realtime・RLS) / Cloud Storage for Firebase / Claude API・Agent SDK / pgvector / MCP」という具体的な技術スタックに実質収斂しており、v13本文もこの構成を前提に設計されている（例: §5.11.2のFirebase/Supabase認可分離設計はこのスタックを前提とした議論）。技術選定自体は事実上決着しているように見える。一方、本タスクの依頼元は「技術スタックも未決定・テスト基盤もないため、いきなりコードを書かせるのは危険」という前提でこのレビュータスクを立てている。両者の認識にギャップがあるため、このスタックが「クライアントに正式提示・承認された決定」なのか「AIとの壁打ちで収束した社内案（未提示）」なのかを明確にする必要がある。またテスト基盤（CI、単体/結合テストの方針）については仕様書のどこにも記述がなく、完全に未定。
- 関連ファイル: `docs/spec/浮遊街アプリ 統合ナレッジベース (Master Index).md` §4、`docs/spec/background/2026-07-08_技術スタックと設計検討項目.md`、`docs/spec/background/2026-07-09_インフラ分離とデータ定義共通化.md`
- 選択肢:
  - A. 上記スタックを正式決定として自律ループが前提にしてよい（テスト基盤は別途最初のタスクとして構築する）
  - B. まだクライアント承認前。技術選定の最終承認を得るまで、自律ループは仕様整理のみを継続する
  - C. 一部は決定済み・一部は未確定（例: DB/フロントはSupabase+Next.jsで確定だが、テスト戦略・CI/CDは白紙、等。項目ごとに切り分けて回答してほしい）

## [2026-08-15] 個人情報方針の大転換（摩擦ゼロ/ニックネームのみ → 氏名・住所を保持）が意図した決定か
- ステータス: 回答済み（2026-08-15）
- 回答: 意図的な決定（選択肢A）。v13の方針（旅館業法対応のため氏名・カナ・住所等を正式に保持）が正しく、「摩擦ゼロ」は初期アイデアであり撤回済み。v13 §5.2にその旨の注記を追記済み。利用規約・プライバシーポリシーへの反映状況（選択肢C）についても、2026-08-16オーナー確認により法務・経営マターとしてクリア済み。
- 優先度: 最高（実装のデータモデル・プライバシーポリシー・利用規約に直結する）
- 背景: background の2026-08-03時点のドラフト（v9/v10相当）では「摩擦ゼロのアカウント設計：厳密な個人情報（本名・クレジットカード等）は持たず、UUMO（eumo）側に処理を委譲する」「アプリ内では原則として保持しない」と明記されており、リスク管理簿にも「個人情報漏洩リスク」への対策として「最初から個人情報を保持しない」という設計思想が書かれていた。一方、正本v13 §5.2・§5.8.1では宿泊法（旅館業法）対応のため氏名・カナ氏名・住所・前泊地等の正式な個人情報を収集・保持する設計になっており、方針が実質的に真逆へ転換している。v13側にはこの転換を明示的に説明する記述が見当たらず（§9の改訂履歴にも「摩擦ゼロ撤回」への言及なし）、意図した決定なのか、単に旅館業法要件を後から追加した結果ノーチェックでなし崩しになったのかが不明。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.2、§5.8.1、`docs/spec/background/2026-08-03_浮遊街アプリ要件定義書 更新.md`、`docs/spec/background/2026-08-03_浮遊街アプリ要件定義書v9作成.md`
- 選択肢:
  - A. v13の方針（旅館業法対応のため氏名・住所を正式に保持）が正しい。「摂擦ゼロ」は初期アイデアであり撤回済み
  - B. 個人情報の保持は最小化したい。旅館業法対応は別の方法（例: 別テーブルで暗号化・アクセス制限を厳格化）で満たしたい
  - C. 利用規約・プライバシーポリシーへの反映状況も確認したい（法務チェック未実施なら要手配）

## [2026-08-15] RAGチャットボット（line-rag-admin）に登録済みの回答内容がv13の決済フローと矛盾していないか
- ステータス: 回答済み（2026-08-15）
- 回答: 確認済み・古い「eumoで送金する」案内は現在の本番ナレッジには残っていない。念のため、line-rag-bot側の設計ドキュメントに「Uii直接決済は不可」の方針を明記し、今後のナレッジ登録時に誤った案内が再混入しないようにする（対応: line-rag-bot docs/08-浮遊街RAG詳細設計.md に追記）。
- 優先度: 高（既にユーザー向けに稼働中の可能性がある機能に、旧仕様の案内が残っているリスク）
- 背景: background 2026-07-20の複数ログで、AIコンシェルジュ（LINE Bot、line-rag-admin管理画面）向けに実際に投入されたQ&Aナレッジのサンプルが作成されており、その中に「アプリ内の『eumoで送金する/受け取る』ボタンからeumoアプリを起動して決済する」という操作手順が含まれている。一方、正本v13 §5.5.1では「Uiiによる直接決済は不可。すべて精算QRを経由して清算する」と明確に確定しており、アプリ内にUii/eumo送金ボタンを置く設計にはなっていない。Master Index記載のURL（line-rag-admin）から見て、このチャットボットは既に稼働している可能性があり、稼働中なら実際のユーザーに古い決済手順を案内し続けていることになる。
- 関連ファイル: `docs/spec/background/2026-07-20_ナレッジ登録方法：PDF vs Q&A.md`、`docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.5.1、Master Index §2-D
- 選択肢:
  - A. line-rag-adminは現在稼働中。登録済みナレッジの棚卸し・修正を最優先タスクとして起票する
  - B. line-rag-adminはまだ稼働していない（プロトタイプのみ）。本実装時にナレッジを作り直せば良い
  - C. 現状の稼働有無をまず確認する必要がある

## [2026-08-15] 街人登録の付与宿泊券枚数「4枚」が§7データ要件のみ「3枚」のまま残置（v13内の更新漏れ）
- ステータス: 回答済み（2026-08-15）
- 回答: 選択肢A（単純な記述更新漏れとして修正）を採用。v13 §7を「付与宿泊券枚数（既定4枚）」に修正済み。実データ・値そのものの変更はなし。
- 優先度: 高（会計・会員特典に直結。データモデル定義の齟齬は実装時のバグに直結する）
- 背景: 正本v13 §5.10.2・§5.10.4・§5.10.5・冒頭サマリは全て「宿泊券4枚（4泊分）＋コインCB5,000」に統一済み（§9 #15で確定）。しかし同じv13 §7「データ要件」の『街人登録申込』エンティティ定義の一文にだけ「付与宿泊券枚数（既定3枚）」という**古い値がそのまま残っている**。実装方針としては`membership_plans`マスタを参照しコードに直書きしないことが明記されているため実害は限定的だが、仕様書の記述自体に矛盾が残っている状態。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.10.2、§7、§9 #11・#15
- 選択肢:
  - A. 単純な記述更新漏れ。§7を「4枚」に修正してよい（実装上は既に4枚で統一されているため実質確認のみ）
  - B. 念のためクライアントに最終数値を再確認してから修正する

## [2026-08-15] Phase 1 スコープの再合意（納期との整合）
- ステータス: 回答済み（2026-08-16）
- 回答: 再合意済み（選択肢A）。現行Phase 1スコープ（§4記載の全項目、2026-08-13レビューで追加された4ブロック含む）で納期内に収まる前提で進めてよい。
- 優先度: 高
- 背景: v13 §9末尾の確認事項表に「Phase 1 スコープの再合意（追加5ブロックと納期の関係）: 未合意」と明記されている。2026-08-13レビューでナレッジ登録・事前登録名寄せ・ロール別UI制御・街人登録導線の4ブロックがPhase 1に追加され、メディアストレージと会員権失効の2ブロックはPhase 2へ再移送されたが、この結果としての「新Phase 1スコープ全体」と当初の納期（2026年7〜8月、既に本日時点で終盤）との整合がクライアントと再合意されていない。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §4、§9末尾の確認事項表
- 選択肢:
  - A. 現行Phase 1スコープ（§4記載の全項目）で納期内に収まる前提で進めてよい
  - B. スコープをさらに絞る必要がある（優先順位を再指定してほしい）
  - C. 納期自体を見直す

## [2026-08-16] 宿泊予約フォーム連携（TO-BE設計）の未確定事項5点（v13 §5.2.3・§9 #30）
- ステータス: 回答済み（2026-08-16、5/5点回答）
- 回答:
  1. 部屋タイプ選択肢の不一致 → **運用中の6種（コテージA/B・アースバッグ・テント・車中泊・ゲストハウス・
     サロン）が正**。Googleフォームの選択肢を4種から6種へ修正する。
  2. 旅館業法必須項目（住所・前泊地・後泊地/行先）の収集方法 → **チェックイン時にアプリ側で収集**する
     （Googleフォームへの項目追加は行わない）。収集の要否（法的な必要性）自体は「法務・経営マターはクリア
     済み」で確認済みだった一方、収集の**手段**は別論点として本セッションで追加確認し、フォーム非改修・
     チェックイン時アプリ収集に決定。実装上は、宿泊予約フォーム連携で自動生成される`pre_registered`予約
     （§5.2.3 TO-BE）に対し、チェックイン処理の中でこれら必須項目の入力を必須化する設計とする。
  3. 「備考欄が空なら自動確定」の判定基準 → **完全な空欄（無入力）の場合のみ自動確定**。「特になし」等の
     定型文言が入力されている場合は空欄扱いにせず「要確認」ステータスとする。
  4. 自動返信メールの発信主体 → **現状のApps Script（ひかる作成）を継続**。アプリ発の通知への統合はしない。
  5. 管理者画面「宿泊予定カレンダー」の表示粒度・UI → **Googleカレンダーと同等の操作感**（月表示中心の
     カレンダーUI、直感的なドラッグ操作感等）を目指す。施設稼働率表示等の詳細仕様は詳細設計時に具体化する。
- 優先度: 高（②は旅館業法対応の必須項目収集に関わり、実装・法令順守のブロッカーになりうる。①③④⑤も§5.2.3の運用開始前に決着が必要）
- 背景: 2026-08-16付けで、v13へ「宿泊予約フォーム連携：業務プロセス分析（AS-IS/TO-BE）」が §5.2.3 として追加され（現時点ではワーキングツリー上の未コミットドラフト。コミット可否は本日付のTASKS.md/LOOP_LOG.mdエントリを参照）、TimeTree連携は不採用・管理者画面に「宿泊予定カレンダー」を新設という方針とあわせて、以下5点が §9 #30 として「要確認」のまま明記されている。
  1. Googleフォームの部屋タイプ選択肢（ゲストハウス／コテージ／車中泊／テント泊の4種）が、運用中の希望部屋タイプ（コテージA/B・アースバッグ・テント・車中泊・ゲストハウス・サロンの6種）と不一致。アースバッグ・サロンは現状フォームから予約できない。
  2. 旅館業法が要求する必須項目（住所・前泊地・後泊地/行先）を、現行Googleフォームが収集していない（氏名・電話番号のみ）。フォームへ項目追加するか、チェックイン時にアプリ側で別途収集するかが未決。
  3. 「備考欄が空なら自動確定」という判定基準の厳密な定義（完全な空文字のみを対象とするか、「特になし」等の定型文言も空扱いにするか）が未決。
  4. 自動返信メールの発信主体（現状のApps Script／ひかる作成を維持するか、アプリ発の通知・確認メールへ統合するか）が未決。
  5. 新設する管理者画面「宿泊予定カレンダー」の表示粒度・UI（カレンダー/リストの優先度、施設稼働率表示の要否等）が未決。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.2.3（新設）・§9 #30（未決事項表）、`docs/spec/CONSOLIDATED_DECISIONS.md` §7
- 選択肢:
  - A. 5点それぞれについて個別に方針を指示してほしい（特に②旅館業法必須項目の収集方法は法令対応のため優先的に確認したい）
  - B. 現場運用で決めてよい項目（①③④⑤）と、法務・コンプライアンス判断が必要な項目（②）を切り分けて、②だけ先に確認したい
  - C. §5.2.3自体（TimeTree不採用の判断を含む）がまだ正式決定ではなく、内容ごと再検討したい

## [2026-08-15] クエストの反復実行・複数人按分・スキル連動報酬はPhase 1で実装するか
- ステータス: 回答済み（2026-08-15）
- 回答: Phase2送り。反復実行・複数人按分・スキル連動報酬のいずれもPhase 1では実装せず、Phase2「スキルツリー機能」と合わせて整理する。Phase 1は「募集人数の範囲内で受注可・報酬均等割り」のシンプル運用。v13 §5.3に注記を追記済み。
- 優先度: 中（Phase1クエスト機能の基本仕様に関わるため、早めの決着が望ましい）
- 背景: background（2026-07-06〜10の複数ログ、2026-08-03のv9/v10ドラフト）では「クエストは反復実行可能」「一つのクエストに複数人で同時参加し貢献度按分」「スキル熟練度に応じた報酬変動（経験者優遇）」が仕様として繰り返し言及されていたが、正本v13 §5.3にはこれらの実装方針が明記されていない。スキルLv・職業システム自体はv13でPhase2「スキルツリー機能」に整理されているため、これに連動する「スキル連動報酬」もPhase2送りと解釈できるが、「反復実行可否」と「複数人按分」はスキルツリーと独立した論点であり、Phase1でも必要になりうる（v13が単に記載を省略しただけの可能性）。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.3、§7（クエスト情報）、`docs/spec/background/2026-07-10_アプリ内経済圏と外部連携の検討.md`
- 選択肢:
  - A. Phase1では「1クエスト=1回きり・1人（または運営が募集人数を決めて複数人同時受注可、按分ロジックはなし＝均等報酬）」で単純化してよい
  - B. 複数人按分ロジック（時間按分/一律/スキル傾斜）をPhase1から実装する必要がある。方式を指定してほしい
  - C. 反復実行の可否を明確にしてほしい（同じクエストに何度も参加してよいか）

## [2026-08-15] 差額繰越（返金・免除運用）の詳細
- ステータス: 回答済み（2026-08-16）
- 回答: ①返金差額の無期限繰越は**Phase1では許可**する（返金3,000円超なら運営から連絡、という推奨運用と
  併用可）。②免除操作の権限は**Phase1はコアメンバーにも許可**する（管理者限定にしない）。③90日滞留後は
  自動免除・督促のいずれも行わず、**滞留フラグを立てるだけ**（運営が任意のタイミングで目視対応する）。
- 優先度: 中
- 背景: v13 §5.6.6・§9 #17で「未収金／未返金は次回チェックイン時に現地精算」という大方針は確定しているが、以下3点が仕様書自身により「要確認」と明記されたまま残っている。①返金差額（運営が会員に返す側）を無期限に繰り越してよいか（会員の不利益になりうる） ②免除操作の権限は管理者のみか、コアメンバーにも許すか ③90日滞留後の扱い（自動免除か督促か）。v13は「返金は3,000円等の閾値を超えたら運営から連絡して速やかに返す」ことを**推奨**として書いているが、確定はしていない。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.6.6、§9 #17
- 選択肢:
  - A. v13の推奨案（返金3,000円超は運営から連絡）をそのまま採用する
  - B. 別の閾値・運用にする（具体的な数値を指定してほしい）

## [2026-08-15] 親方・街人リスト受領後の最終データレビューは完了しているか
- ステータス: 回答済み（2026-08-16）
- 回答: 完了。371件→370名への重複統合ロジック（中野裕士氏の親方兼街人扱い等）を含め、クライアント最終
  承認済み。370名という移行対象数・名寄せ方針で確定（選択肢A）。
- 優先度: 中
- 背景: v13冒頭サマリに「残る確認事項は『親方・街人リストの現物受領』…の3点」とある一方、§9 #12・#26では「2026-08-14 受領完了」「移行対象370名」と既に確定値として扱われている記述もある。受領自体は完了したが、370名という移行対象数・重複統合（中野裕士氏の親方兼街人扱い等）の内容についてクライアント側の最終レビュー・承認が済んでいるかが明記されていない。
- 追記（2026-08-15、v13 §9末尾の「本改訂に伴うクライアント確認事項」表の再読で判明）: 同表内に「親方・街人リストの**現物受領**」という行があり、状態は「✅ **2026-08-14 受領完了**（街びと327件20列／親方衆44件9列）」と明記されている。つまり**「現物受領」というイベント自体はクライアント確認事項として完了扱い**になっていることが確定した。一方、371件→370名への重複統合ロジック（#26）自体はシステム側（開発チーム）の判断として記録されており、**この統合結果をクライアントが最終レビュー・承認したかは、この表・#26いずれの記述からも読み取れない**。論点は「受領完了か」ではなく「統合後の370名という数字・名寄せロジックをクライアントが承認したか」に絞られる。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` 冒頭サマリ、§9 #26
- 選択肢:
  - A. 370名という移行対象数・名寄せ方針でクライアント最終合意済み
  - B. まだクライアントへの最終レビューが必要

## [2026-08-15] RAGベクトル検索基盤: pgvectorで確定か
- ステータス: 回答済み（2026-08-16、上位論点の決定により解消）
- 回答: 本質問自体が前提から変わった。2026-08-16のナレッジ・RAG基盤統合決定（本ファイル冒頭の項目を参照）
  により、浮遊街アプリ本体は**pgvectorを使用しない**方針に転換（line-rag-bot／Firestoreへ統合）。
  Master Index §4の「pgvector（Supabase内） または OpenSearch」の記述は、両方とも不要である旨を
  取り消し線＋「line-rag-bot（Firestore）に統合、アプリ本体は自前のベクトル検索基盤を持たない」注記へ
  2026-08-16中に更新済み。
- 優先度: 低（実装方針自体は既にpgvectorで一貫しているため、確認のみ）
- 背景: Master Index §4では「pgvector（Supabase内） または OpenSearch」と両論併記のまま更新が止まっている。一方、正本v13の §5.7.5・§9では一貫して「pgvector（Supabase）」を前提に設計が進んでいる（Supabase内で完結させる設計思想と整合）。Master Index側の記述が単なる更新漏れなのか、まだ両論検討中なのかが不明。
- 関連ファイル: `docs/spec/浮遊街アプリ 統合ナレッジベース (Master Index).md` §4、`docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §5.7.5
- 選択肢:
  - A. pgvector（Supabase内）で確定。Master Indexの「またはOpenSearch」は更新漏れなので削除してよい
  - B. まだ両論検討中

## [2026-08-15] 直売所の郵送購入（Phase 2）の運用条件
- ステータス: 未回答
- 優先度: 低（Phase 2以降のため実装は今すぐブロックしない）
- 背景: v13 §9 #4で「送料・梱包コストの負担者、生鮮品の品質保証・クレーム対応、発送オペレーションの担当者確保」が未決のまま残っている。Phase 1には影響しないが、Phase 2着手前には確定が必要。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §4（Phase 2）、§9 #4

## [2026-08-15] 30,000円の会計区分・前払式支払手段の該当性（経営マター）
- ステータス: 回答済み（2026-08-16）
- 回答: クリア済み（法務・経営マターとして解消済み、2026-08-16オーナー確認）。システム側は引き続き
  未使用宿泊券残高（発行/使用済/未使用）を集計提供する要件のみで対応する（設計変更なし）。
- 優先度: 低（v13により「経営マター・システム設計では考慮しない」と役割分担は整理済みだが、経営側の最終判断自体は宙に浮いている）
- 背景: v13 §9 #14で「経営側で対応。システム設計では考慮しない」と役割分担は明確化されているが、資金決済法上の届出要否そのものの結論（経営側の判断）はまだ出ていない。システムは未使用宿泊券残高（発行2,048泊／使用済270泊／未使用1,780泊、2026-08-14実測）を集計提供する要件のみ確定。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §8、§9 #14

## [2026-08-15] メタバース連携構想（誰かの家＝マイショップ）は採用するか
- ステータス: 未回答
- 優先度: 低（現行スコープ外だが、方向性次第でデータモデルの拡張性に影響しうる）
- 背景: `docs/spec/background/2026-07-23_メタバース連携とショップ・クエスト構想.md` で「メタバース空間に個人のショップを持ち、そこでクエスト受発注・スキル/推薦の可視化を行う」という構想がブレインストーミングされているが、正本v13にはPhase 1〜3のいずれにも一切記載がない。意図的に不採用としたのか、単に検討が追いついていないだけなのかが不明。
- 関連ファイル: `docs/spec/background/2026-07-23_メタバース連携とショップ・クエスト構想.md`
- 選択肢:
  - A. 不採用（アイデア止まり）。今後のデータモデル設計で考慮不要
  - B. 将来（Phase 3以降）検討対象として残す。拡張しやすいデータモデルにしておいてほしい

## [2026-08-15] v13 §9末尾「本改訂に伴うクライアント確認事項」表に更新漏れの疑い（付与泊数・名寄せキー）
- ステータス: 未回答
- 優先度: 低（v13本文の該当セクション自体は既に正しい値で確定しているため実装への影響は無いが、v13自身の記述に矛盾が残っている）
- 背景: v13 §9末尾の「★ 本改訂に伴うクライアント確認事項（2026-08-13レビュー分）」表に、以下2行が**2026-08-13時点のまま**残っている。
  - 「30,000円の付与泊数（3泊 vs 実データ4泊＋コインCB）」→ 状態「実データと矛盾。§5.10.2の確定値が揺れている」
  - 「名寄せキーの再設計」→ 状態「対応方針は策定済み。§5.8.1の記述修正と招待コード運用の設計が必要」
  しかし同じv13の #15（2026-08-14決定）・#16（2026-08-14決定）行、および改訂履歴v1.12.0（2026-08-15）では、いずれも既に確定・反映済みと明記されている。付与宿泊券枚数の「3枚→4枚」更新漏れ（2026-08-15に別途修正済み）と同種の、**確認事項サマリ表側の更新が本文の決定に追いついていない**というパターン。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` §9「本改訂に伴うクライアント確認事項」表、および同じ§9の #15・#16 行、改訂履歴v1.12.0
- 選択肢:
  - A. 単純な表の更新漏れ。オーナー確認後、正本v13側の当該2行を「決定済み」に修正してよい（このタスクを担当した自律ループの制約上、v13自体の編集は今回見送った）
  - B. 念のため実際にクライアントへの再確認が必要（考えにくいが、#15/#16の決定自体がまだクライアント未提示という可能性もゼロではない）

## [2026-08-15] UI_UX設計指針など、v13が参照する子ノートの実体がこのリポジトリ内に見当たらない
- ステータス: **🟡 一部回答（2026-08-16）** — ~~回答済み（2026-08-16）~~
  - ✅ **回答済み**: 配置方式（工程 × 領域の2軸で分ける）
  - ⚠️ **未回答**: **子ノート5本を取り込むこと自体の是非**（下記 選択肢A／B）。2026-08-16 の追記が
    「**ステータスは「未回答」のまま維持する**」と明記しているとおり、オーナーの最終確認待ち。
    2026-09-08 の突合で、見出しの「回答済み」と本文の記述が食い違っていたため本行を訂正した
- 回答: 単純な`docs/spec/`直下へのフラットなコピーではなく、**工程（要件定義／基本設計／詳細設計／実装／
  単体テスト／結合テスト／運用）× 領域（フロントエンド／バックエンド／インフラ／推論・AI 等）の2軸**で
  フォルダを整理して格納する方針が指示された。これに基づき、5本を以下へ再配置済み（`git mv`で移動、
  履歴は保持）。
  - `docs/spec/requirements/機能要件定義.md`（工程: 要件定義、領域: 横断）
  - `docs/spec/basic-design/frontend/UI_UX設計指針.md`（工程: 基本設計、領域: フロントエンド）
  - `docs/spec/basic-design/backend/ER図_概念データモデル.md`（工程: 基本設計、領域: バックエンド）
  - `docs/spec/basic-design/infra/システムアーキテクチャ.md`（工程: 基本設計、領域: インフラ）
  - `docs/spec/basic-design/ai-rag/RAGシステム仕様.md`（工程: 基本設計、領域: 推論・AI）
  既存の`docs/spec/detailed-design/`（詳細設計フェーズ：DB物理設計・画面設計・API設計・外部連携設計・
  非機能要件詳細の5本）は今回は移動していない（工程＝詳細設計のみで領域別サブフォルダ化はされていない状態）。
  将来的にこの階層も同じ2軸で再整理するかは別途判断事項として残る。v13・Master Indexは`[[wikilink]]`名前
  参照のみで明示パス依存がないため移動の影響を受けず、参照元の明示パス記述（DB物理設計.md・画面設計.md・
  非機能要件詳細.md・本ファイル）は新パスへ更新済み。正式な内容の承認・「回答済み」への格上げは別軸の
  論点だったが、格納方法自体の意思決定が完了したため本項目は解消とする。
- 優先度: 中（正本v13が繰り返し参照している資料の実体不明は、今後の自律ループの矛盾検知精度に影響する）
- 背景: v13は本文中で `[[UI_UX設計指針]]` `[[システムアーキテクチャ]]` `[[ER図_概念データモデル]]` `[[RAGシステム仕様]]` `[[クエストシステム仕様]]` `[[会員データモデル_ユーザーテーブル定義]]` 等の子ノートを頻繁に参照し、「本決定によりUI_UX設計指針のUii決済前提は無効化」のように**子ノート側の記述を上書きする形で決定を記録**している。しかしこれらの子ノート実体は `docs/spec/` 配下（このリポジトリ）には存在せず、Obsidianボルト側にのみあると思われる。自律ループが将来これらを参照できないと、v13が「無効化」と言っている旧記述を確認できず、矛盾検知の精度が落ちる。
- 追記（2026-08-15、v13 §9 改訂履歴の再読で判明）: v13 v1.12.0（2026-08-15）の改訂履歴に「子ノート6本（UI_UX設計指針／システムアーキテクチャ／ER図_概念データモデル／RAGシステム仕様／機能要件定義／Master Index）とHTMLモック_v13を正本へ追随させ、v13以降放置されていた乖離を解消」と明記されていた。つまり**子ノート自体はObsidianボルト側に実在し、2026-08-15時点でv13の内容に同期済み**であることが分かった。「実体が存在しない」のではなく「このリポジトリ（docs/spec/配下）にコピーされていないだけ」という状況。矛盾検知の精度向上のためにコピーが望ましい点は変わらないため、下記の選択肢はそのまま有効。
- 追記（2026-08-16、選択肢A実施）: オーナーのVault（`C:\Users\user\Documents\MyVault\Myvault\Knowledge\浮遊街アプリ\`）から、v13 v1.12.0改訂履歴が挙げる子ノート6本のうち、Master Indexを除く5本（`UI_UX設計指針.md` / `システムアーキテクチャ.md` / `ER図_概念データモデル.md` / `RAGシステム仕様.md` / `機能要件定義.md`）を単純ファイルコピー（解釈・改変なし）で `docs/spec/` 配下へ取り込んだ。Master Indexは元々 `docs/spec/` に存在し既に追跡対象のため対象外。コピーは選択肢Aの性質上、解釈を伴わない可逆な操作（Gitで追跡）と判断し実施したが、**内容の正式な承認・「回答済み」への格上げは行っていない**（オーナー自身がB案＝子ノート不要と判断する余地は残る）。ステータスは「未回答」のまま維持する。
- 関連ファイル: `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md`（各所の `[[...]]` リンク）、`docs/spec/basic-design/frontend/UI_UX設計指針.md`、`docs/spec/basic-design/infra/システムアーキテクチャ.md`、`docs/spec/basic-design/backend/ER図_概念データモデル.md`、`docs/spec/basic-design/ai-rag/RAGシステム仕様.md`、`docs/spec/requirements/機能要件定義.md`（2026-08-16取り込み、同日中に工程×領域の2軸フォルダへ再配置）
- 選択肢:
  - A. 子ノート群もこのリポジトリ（docs/spec/配下）へコピーしてほしい（2026-08-16、5本を実施済み。オーナーの最終確認待ち）
  - B. 子ノートは古い作業メモなので不要。v13の記述のみを正として扱ってよい（この場合、取り込んだ5本の扱いを別途指示してほしい）

---

## [2026-09-21] Phase 1 に必要な4テーブルの DDL が本リポジトリに存在しない（`member_identifiers` / `member_notes` / `member_import_links` / `uii_transactions`）

- ステータス: ~~未回答~~ → ✅ **回答済み・クローズ（2026-09-24・オーナー決定）**
- 優先度: 高（WBS `10-2`（名寄せロジック）・`10-3`（再訪アラート）をブロック。`member_identifiers` は
  v13 §5.8.3 が名寄せの初回紐付けに必須とする連絡先の格納先そのもの）
- 背景: 2026-09-21 に「正本を満たすために足りない DB」を棚卸しした際に判明。
  `DB物理設計.md` §2 の表はこの4つを**テーブルとして列挙している**が、同書に `CREATE TABLE` が無く、
  `supabase/migrations/` にも存在しない。dev Supabase の実 DB にも無い（PostgREST の公開オブジェクトは
  26件で、マイグレーションと完全一致。ドリフトは無い）。
  - `member_identifiers` — §2「名寄せキー（email/phone/line/discord）。検証済み識別子のみ `(kind, value)` 一意」。
    §5.3 が部分ユニーク `uq_identifier_verified ... WHERE is_verified = true` を実体として名指ししている
  - `member_notes` — §2「運営メモ。`core_only`/`admin_only` の可視性制御」。**`0005_rls_policies.sql` は
    コメントで言及するのみ**で、同書自身が「実体は Vault 側 `01_schema.sql` にのみ存在し、
    本リポジトリからは確認できない」と書いている（CLAUDE.md §1.1「正本は本リポジトリのみ」に反する状態）
  - `member_import_links` — §3-14 L997 が「`import_jobs` / `member_import_links` は DDL が本書が初出」と
    書いているが、**実際に DDL があるのは `import_jobs` だけ**である（本改訂で当該行を訂正した）
  - `uii_transactions` — §2「定義のみ先行。Phase1 では移行・稼働しない」。一方 §6-1 #10 は
    「Phase 1 では稼働しないが **RLS は先に張る**」としており、張る対象の表が無い
- なぜ止めたか: 4つとも**個人情報の格納先か認可の対象**である。`member_identifiers` は連絡先（PII-B）、
  `member_notes` は運営メモ（可視性制御あり）、`member_import_links` は取込元ファイル名・照合根拠（PII-B）。
  CLAUDE.md §7.0 は「認可・個人情報の**仕様判断**は必ず先に聞く」と定めており、
  列構成を推測で起こすことはできない。`member_notes.visibility` の値域は §6-9 ⑤ で既に未確定である。
- 選択肢:
  - A. Vault 側 `01_schema.sql` の当該 DDL をオーナーが提示し、それを正として `DB物理設計.md` へ転記したうえで
    マイグレーション化する（**実データは持ち込まない**。スキーマのみ／CLAUDE.md §7.1）
  - B. `DB物理設計.md` §2 の表の記述から DDL を新規に起こし、オーナーがレビューして確定する
  - C. Phase 1 では `member_identifiers` のみ先に作り、残る3つは Phase 2 へ送る
    （`member_notes` は `10-3` 再訪アラート、`member_import_links` は `10-2` 取込監査を落とすことになる）
- 推奨: A（理由: 既存の実体があるなら列構成を二重に決めない。§1.1 の二重管理事故を繰り返さないためにも、
  Vault 側にしか無い状態を解消して本リポジトリへ寄せるのが筋である）

### ✅ 回答（2026-09-24・オーナー確定）

**A（Vault 側 `01_schema.sql` を正として転記）を採用する。**

| テーブル | 結果 | 備考 |
| --- | --- | --- |
| `member_identifiers` | 対応不要 | **2026-09-22 に別経路（`0025_member_identifiers.sql`）で先に実装済み**と判明。Vaultとは列構成が異なる（`kind`の値・`is_primary`/`source`の有無）が、**作り直さず現状維持**する（オーナー確認済み） |
| `member_notes` | ✅ `0034_member_notes.sql` | Vaultの定義をそのまま転記。可視性は`core_only`/`admin_only`のCHECK制約どおり |
| `uii_transactions` | ✅ `0035_uii_transactions.sql` | Vaultの定義をそのまま転記。Phase 1では書き込みアプリコードを作らない（RLS/GRANTのみ先に張る） |
| `member_import_links` | ✅ `0036_member_import_links.sql` | **Vault側にも定義が無かったため、この1件だけ選択肢B（新規起案）**。`import_jobs`（`0024`）と同じ「ポリシー0件＝全拒否」方針を踏襲し、`match_basis`（照合根拠）を自由記述の列として追加した |

⚠️ **Docker（`supabase start`）がこの実行環境に無く、ローカルでの適用検証はできていない。** 次にこのブランチへ触るとき、
またはCIの`db-test`ジョブで`0034`〜`0036`が問題なく適用されることを確認すること。受入テストは未着手（`10-2`・`10-3`側で追加する）。
- 関連ファイル: `docs/spec/detailed-design/DB物理設計.md` §2・§3-14・§6-1、`supabase/migrations/0005_rls_policies.sql`・
  `0025_member_identifiers.sql`・`0034_member_notes.sql`・`0035_uii_transactions.sql`・`0036_member_import_links.sql`

## [2026-09-21] Phase 1 のテーブルが6つ未作成のまま、依存する画面だけが実装されている

- ステータス: 未回答（**オーナー判断は不要かもしれない。着手順の確認のみ**）
- 優先度: 中（着手可能だが、どれから入れるかで開く画面が変わる）
- 背景: 同じ棚卸しで、`DB物理設計.md` に **DDL まで設計済みなのにマイグレーションが無い**表が6つあると判明した。
  いずれも「仕様の追加は無い」状態であり、設計判断ではなく**実装順の問題**である。

  | テーブル | 設計 | これが無いと開かない画面・機能 | WBS |
  | --- | --- | --- | --- |
  | `accommodation_rates` | §3-10 | 宿泊料金の Uii 主・円 副表示、公開予約 `/reserve`、マスタ管理 C13 の宿泊料金 | `3-9` |
  | `reservation_otps` | §3-6③ | 公開予約ページ `/reserve` の本人確認（未ログイン導線そのもの） | `3-5b` |
  | `media_assets` | §3-7 | アップロード A10、完了報告 A4 の Before/After 写真、`menu_items.image_media_id` の FK | `2-1c` |
  | `eumo_grants` | §3-8 | Eumo給付一覧 B10・C12 | `5-5` |
  | `membership_applications` | §3-5 | 街人登録モーダル A8、街人登録申請一覧 C7 | `12-2` |
  | `import_jobs` | §3-14 | 会員データ取込の二重取込防止（v13 §8 の再実行安全性） | `10-2` |

  ⚠️ `reservation_form_submissions`（§3-6 の旧構想）は **§9 #46 で撤回済み**のため作らない。
- なぜ止めたか: 止めてはいない。**どれから着手するかだけ確認したい。**
  2026-09-21 時点で `/upload`・`/staff/eumo` は「何待ちか」を出す画面に留めてあり、
  `/reserve` は未作成である（v13 §5.2.3 が定める未ログインの入口が存在しない状態）。
- 選択肢:
  - A. `reservation_otps` ＋ `accommodation_rates` を先に入れ、**未ログインの入口 `/reserve` を開ける**
  - B. `media_assets` を先に入れ、アップロードと完了報告の写真を開ける（`1-4` の GCS 基盤が別途要る）
  - C. `eumo_grants` を先に入れ、承認済みクエストの給付漏れを塞ぐ
- 推奨: A（理由: 未ログインの入口が無い状態は、**アプリに辿り着けない人が居る**という点で影響範囲が最も広い。
  `media_assets` は WBS `1-4`（GCS・署名付きURL）が未着手のため、表だけ作っても画面は開かない）
- 関連ファイル: `docs/spec/detailed-design/DB物理設計.md` §3-5〜§3-14、`docs/spec/WBS_Phase1.md`

## 使い方

このファイルは、自律実装ループ（毎晩仕様整理タスクを回す仕組み）が仕様の矛盾・未決事項を
新たに発見するたびに追記していく前提のファイルです。

### 追記フォーマット

新しい質問は、このファイルの適切な優先度の位置（先頭に近いほど優先度が高い）に、以下の形式で追記すること。

```markdown
## [YYYY-MM-DD] 質問タイトル
- ステータス: 未回答
- 優先度: 最高 / 高 / 中 / 低
- 背景: なぜこの確認が必要かを、根拠となる仕様書の記述を引用しつつ具体的に書く
- 関連ファイル: 出典となった仕様書・議事録のパス（複数可）
- 選択肢: （分かる場合のみ）A. ... B. ...
```

### ステータスの更新

- プロジェクトオーナーが回答した場合、該当項目の `ステータス: 未回答` を `ステータス: 回答済み（YYYY-MM-DD）` に書き換え、
  回答内容を「- 回答:」の行として追記する。
- 回答済みの項目は削除せず残す（決定の経緯を追跡できるようにするため。正本v13の§9の運用スタイルに準拠）。
- 回答により `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` 側の更新が必要になった場合は、
  そちらの改訂も別途行うこと（本ファイルは仕様書そのものではなく、確認事項の管理台帳）。
