---
title: "詳細設計：外部連携設計（line-rag-bot・eumo等）"
doc_type: 設計
status: "詳細設計ドラフト（要オーナーレビュー・重大な論点あり）"
owner: プロジェクトオーナー
date: "2026-08-16"
updated: 2026-08-22
tags: ["浮遊街アプリ"]
up: "[[浮遊街アプリ 総合要件定義・設計書_v13]]"
---

# 外部連携設計（Phase 1）

> 本書は浮遊街アプリ本体と外部システム（line-rag-bot、eumo等）との連携を設計するドキュメントである。
> **line-rag-bot側の実装コードには一切触れていない**（読解のみ）。line-rag-botは別リポジトリ
> （`C:\Users\user\Documents\MyVault\Myvault\Projects\line-rag-bot`）で開発が進む独立システムであり、
> 本書はあくまで浮遊街アプリ側から見た連携インターフェースの設計に限定する。

---

## 1. ★ 最重要の論点：ナレッジ・RAG基盤の二重化疑義

本書作成のためline-rag-bot側の設計書（`docs/08-浮遊街RAG詳細設計.md`）を精読した結果、
**DB物理設計.md・API設計.mdで設計した「浮遊街アプリ本体のpgvectorベースのナレッジ基盤」と、
「line-rag-bot側の既存Firestoreベースのナレッジ基盤」が、実質的に同じ役割を別々の技術で
二重実装している可能性がある**ことが判明した。これは本サイクルの新規発見であり、
**オーナーの確認が必須の重大な論点**として本書冒頭に記載する。

### 1-1. 判明した事実

| 項目 | 浮遊街アプリ本体（正本v13・RAGシステム仕様が前提とする設計） | line-rag-bot（実際に実装が進んでいる別システム） |
| --- | --- | --- |
| データストア | Supabase（PostgreSQL）＋ pgvector | Firestore（`knowledge_{tenant_id}`等のコレクション） |
| 検索方式 | pgvectorのベクトル類似検索（想定） | 全件取得＋Python側cosine計算（Phase1は意図的にこの方式を維持、§9-1） |
| レシピ・道具・failure_patterns | ER図_概念データモデル.mdで`Recipe`/`Tool`エンティティとして定義 | `recipes_{tenant_id}`/`tools_{tenant_id}`としてFirestoreに**既に実装済み**（2026-08-15〜16時点でブランチ6・7まで完了） |
| エスカレーション | `unanswered_escalations`テーブルを本書DB物理設計.mdで新規提案 | `escalations`コレクションとして**既に実装済み** |
| LINE連携 | v13・RAGシステム仕様は「フロントエンド：LINE Messaging API」と記載し、浮遊街アプリのRAG基盤の一部として位置づけ | line-rag-botが独自にLINE Messaging APIのWebhookを持ち、Firestore上のナレッジで完結した応答を行う（浮遊街アプリ本体を経由しない） |
| ナレッジ登録UI | v13 §5.7・§5.7.7で浮遊街アプリの管理者権限UIとして設計 | line-rag-bot側は現状Streamlit管理画面のみで登録可能。§9-5で「将来的に浮遊街アプリ管理者UIへ統合し、line-rag-bot側にREST APIを切り出す」方針が2026-08-14に決定済みだが**未着手** |

### 1-2. 考えられる解釈（いずれもオーナー確認が必要）

- **解釈A（統合型）**: 浮遊街アプリ本体のAIコンシェルジュ（アプリ内チャット）とLINEチャットボットは**同一のナレッジ基盤を共有すべき**であり、正本の想定どおりline-rag-bot側にREST APIを切り出し（§9-5）、**浮遊街アプリ本体はpgvectorを持たず、line-rag-botのAPIを呼び出すクライアントとして実装する**。この場合、本書DB物理設計.mdで提案した`knowledge_items`/`knowledge_embeddings`（pgvector）テーブルは**不要**になり、代わりにline-rag-bot APIとの連携層のみを実装すればよい。
- **解釈B（並存型）**: アプリ内チャット（浮遊街アプリ本体）とLINEチャットボット（line-rag-bot）は**別のナレッジ基盤を意図的に並存させる**（例: アプリ内は宿泊・会計等の運営系ナレッジ、LINEは来訪者向けFAQ、のように役割分担する）。この場合、本書DB物理設計.mdの提案どおりpgvectorテーブルを維持しつつ、line-rag-botとは別データとして運用する。
- **解釈C（移行型）**: 元々はpgvectorで作る想定だったが、line-rag-botが汎用マルチテナントRAG SaaS的に先行して実装が進んでしまった（line-rag-bot docs/08は「浮遊街」をline-rag-botの1テナントとして扱う設計）ため、**実質的にline-rag-botへ一本化する方向で後から追認する**のが実態に近い。

v13・Master Index・RAGシステム仕様は**いずれもpgvectorを前提に書かれており、line-rag-botの存在・Firestore実装を明示的に位置づけていない**（Master Index §2-Dにline-rag-adminへの言及はあるが、Firestoreベースである点やマルチテナント設計である点には触れていない）。一方、line-rag-bot側は既にPhase1相当の機能（レシピ・道具マスタ、failure_patterns、エスカレーション）を**実装済み**であり、後から作られた本書のDB物理設計（pgvector版）よりも先行している。

> [!success] 確定方針（2026-08-16 オーナー回答）
> **解釈A（統合型）で確定**。line-rag-bot（Firestore）を単一のナレッジ基盤とし、浮遊街アプリ本体は
> 独自のpgvectorナレッジ格納を持たない。さらにオーナーから重要な追加方針が示された：
> **浮遊街アプリ本体はエンドユーザー向けのAIチャットUI（アプリ内コンシェルジュ）自体を持たない**。
> 街人・ゲストへのAI案内（FAQ・レシピ・道具・failure_patterns等）は**LINE（line-rag-bot）経由に一本化**
> する。浮遊街アプリ側が持つのは「ナレッジ登録」（書き込み）UIのみで、管理者権限UIからline-rag-bot側の
> REST API（docs/08 §9-5方針）へ送信する構成とする。DB物理設計.md側の`knowledge_items`/
> `knowledge_embeddings`テーブル提案は不要となったため、同ドキュメント側で参考セクションへ移動済み。
> 詳細は `docs/spec/QUESTIONS.md`（2026-08-16回答）・v13 §9 #31 を参照。
>
> **呼称について（2026-08-16追記）**：本書では技術的な正確性を優先し、システム名・リポジトリ名
> としての`line-rag-bot`表記を維持する。ただし浮遊街アプリの画面設計.md・利用者向け文言では、
> 街人・ゲストがLINEで対話する相手の呼称として**「浮遊街コンシェルジュ」**を用いる（line-rag-bot
> というリポジトリ名・システム名自体は変更しない。ブランド名と技術名の対応関係のみの整理）。

---

## 2. line-rag-bot 連携設計（暫定・解釈Aベース）

### 2-1. 現状（2026-08-16時点、line-rag-bot docs/08 §9-5より）

- ナレッジCRUD用のREST APIは**line-rag-bot側に存在しない**（`/webhook` `/health` `/debug/*` のみ）。
- 登録経路はline-rag-bot単体のStreamlit管理画面のみ。
- 浮遊街アプリ管理者UIとの統合、スクリプト経由登録は「2026-08-14に方針決定済みだが未着手」。
- 着手条件は**「浮遊街アプリ本体の技術スタック・管理者認証方式が固まってから」**（line-rag-bot docs/08 9-5）
  → 技術スタック自体はQUESTIONS.mdで2026-08-15に回答済み（Next.js/Supabase等で正式承認）のため、
  **line-rag-bot側の着手条件は満たされつつある**。ただし、上記§1の二重化疑義が未解消のため、
  API設計を確定する前にオーナーとの整理が必要。

### 2-2. 想定される連携インターフェース（2026-08-16再確定：REST APIは一切実装しない）

ナレッジの「登録・編集」、および「未回答エスカレーション一覧の閲覧・代理回答」は、いずれも
line-rag-bot の Streamlit 管理画面で完結させる。浮遊街アプリ本体からline-rag-botを呼び出す
REST APIは**一切実装しない**（読み取り専用APIも含めて不採用。2026-08-16オーナー最終決定）。

| エンドポイント（line-rag-bot側・既存） | 用途 |
| --- | --- |
| ~~`POST /api/v1/tenants/fuyugai/knowledge`~~ | **不要（2026-08-16確定）**: ナレッジ登録は Streamlit 継続。浮遊街アプリ管理画面（B9/C5）から line-rag-bot Streamlit へのリンク/リダイレクトで対応 |
| ~~`GET /api/v1/tenants/fuyugai/knowledge/search`~~ | **不要**: アプリ内AIチャットUIを持たないため検索APIは不要 |
| ~~`GET /api/v1/tenants/fuyugai/escalations`~~ | **不要（2026-08-16再確定）**: 未回答一覧の閲覧も line-rag-bot Streamlit 管理画面で完結。浮遊街アプリ側でのAPI経由取得・表示は行わない |
| ~~`POST /api/v1/tenants/fuyugai/escalations/{id}/resolve`~~ | **不要**: 代理回答もline-rag-bot Streamlit で実施 |

**結論：浮遊街アプリ本体とline-rag-botの間にAPI連携は存在しない。** 両者をつなぐのは
「浮遊街アプリ管理画面からline-rag-bot Streamlitへのリンク（別タブ/別画面遷移）」のみであり、
認証は各システムがそれぞれ自身のログイン機構（浮遊街アプリ＝Supabase Auth、line-rag-bot＝
Streamlit側の既存認証）で個別に行う。JWT受け渡し等のシステム間連携は不要。

### 2-3. 浮遊街アプリ本体側で必要になる作業（WBS §9-2相当・大幅簡素化）

| 作業 | 概要 | 依存 | 優先度 |
| --- | --- | --- | --- |
| 外-1 | ナレッジ登録・エスカレーション確認への導線（B9/C5）の実装 — 「line-rag-bot管理画面を開く」ボタン（外部リンク、別タブ） | 管理画面UI実装 | 中 |
| ~~外-2~~ | ~~line-rag-bot APIのクライアントラッパー実装~~（`/api/knowledge`） | **不要化（2026-08-16）**: REST API自体を作らないため実装不要 | ~~高~~ |
| ~~外-3~~ | ~~認証方式の確定（JWT検証 or APIキー）~~ | **不要化（2026-08-16）**: システム間API連携がないため、認証連携の設計自体が不要 | ~~中~~ |
| ~~外-4~~ | ~~エスカレーション一覧（C5画面）の実装~~ | **不要化（2026-08-16再確定）**: line-rag-bot Streamlit側で完結。浮遊街アプリ側の実装は外-1のリンクのみで足りる | ~~高~~ |

---

## 3. eumo連携設計（Phase 2スコープ）

Phase 1ではUiiは「円価格換算表示専用」であり、eumoとの直接連携は行わない（v13 §5.5.1、
`Uii経済圏とeumo連携.md`）。Phase2以降に向けた設計メモのみ記載する。

| 項目 | 内容 |
| --- | --- |
| 連携方式 | 非同期の決済ゲートウェイ方式。eumo側を実資金のSSOT（信頼できる唯一の情報源）とし、浮遊街アプリ側は予測残高をキャッシュする「オペレーション帳簿」として振る舞う |
| 同期タイミング | リアルタイム双方向同期は行わない（仕様上困難と判明済み）。非同期同期・自動リトライ・手動同期機能で不整合リスクに対応 |
| データ項目 | 取引ID・送金者ID・受取者ID・金額・有効期限・付与/利用/失効履歴・EUMO同期メタデータ（`uii_transactions.memo`に格納する想定。DB物理設計.md参照） |
| 消費メタデータ | 「何に消費したか」を記録し、地域内経済循環の可視化に利用（Phase2要件） |
| リスク対応 | eumo API制限・不整合時はアプリ側キャッシュ残高を表示しつつ、非同期リトライ。障害はLINE経由で管理者へ即時通知（システムアーキテクチャ.md非機能要件） |

Phase1では実装対象外のため、本書はこれ以上詳細化しない。

---

## 4. 朝会音声処理（Gemini直接連携）

外部連携という位置づけとしては、Gemini APIへの音声直接投入（マルチモーダル）もこのカテゴリに含まれるため
参考記載する。DB物理設計.md `morning_meetings`、API設計.md `POST /api/morning-meetings`と対応。

- 統一メディア基盤（Cloud Storage for Firebase）へアップロード → GCS URIをGeminiへ直接渡す（音声ファイル自体をAPIボディに含めない）。
- 話者分離（Diarization）は不要と判断済み（対面朝会・決定事項の把握が目的のため）。
- 60秒以内のSLA（システムアーキテクチャ.md非機能要件）を満たすため、議事録生成とクエスト候補抽出を**同一のAPI呼び出し**で行う（正本§5.1）。

---

## 5. オーナー確認事項まとめ

| # | 内容 | 重要度 |
| --- | --- | --- |
| ~~1~~ | ~~ナレッジ・RAG基盤をpgvector（浮遊街アプリ本体）とFirestore（line-rag-bot）のどちらに一本化するか~~ | ✅**解消済み（2026-08-16）**：解釈A（line-rag-botへ統合、アプリ内チャットUIなし）で確定 |
| ~~2~~ | ~~line-rag-bot側REST API・認証方式の設計着手をいつ・誰が行うか~~ | ✅**解消済み（2026-08-16再確定）**：**REST APIは一切実装しない**ことが確定。ナレッジ登録・編集・未回答エスカレーションの閲覧・代理回答は全てline-rag-bot既存のStreamlit管理画面で完結させる。浮遊街アプリ側は「line-rag-bot管理画面を開く」外部リンクを実装するのみ。システム間の認証連携（JWT検証・APIキー）自体が不要になった |
| 3 | eumo API連携の具体的な仕様（Phase2着手時に別途） | 低（Phase1スコープ外） |

---

## 改訂履歴

| 日付 | 内容 |
| --- | --- |
| 2026-08-16 | 初版作成。line-rag-bot `docs/08-浮遊街RAG詳細設計.md`を精読し、浮遊街アプリ本体（pgvector前提）と
line-rag-bot（Firestore・実装済み）のナレッジ基盤が二重化している疑義を発見。3つの解釈（統合/並存/移行）を
整理し、最重要のオーナー確認事項として本書冒頭に記載。line-rag-bot連携API（暫定）、eumo連携（Phase2）、
朝会音声のGemini直接連携を設計。line-rag-bot側のコードは一切変更していない（読解のみ）。 |
| 2026-08-16（追記） | オーナー回答により解釈Aで確定。「浮遊街アプリ本体はアプリ内AIチャットUIを持たない」という
追加方針を反映し、§1を暫定判断から確定方針へ書き換え。検索用エンドポイント（`GET .../knowledge/search`）を
不要と明記。line-rag-bot管理画面の将来方向性は「当面Streamlit継続・将来API化」の現行折衷案を維持することを
オーナーに確認済み（§5 #2）。DB物理設計.md・API設計.md・画面設計.md・Master Index・v13正本・WBS_Phase1.mdへの
横断反映は同日中に別途実施。 |
| 2026-08-16（重要変更） | **Streamlit継続方針の確定反映**（2026-08-16オーナー最終判断）。ナレッジ登録・編集は当面line-rag-bot Streamlit で行い、REST API（`POST .../knowledge`）は実装しない決定を反映。§2-2の想定エンドポイント表を「読み取り専用」に再定義し、登録用エンドポイントを削除。§2-3の浮遊街アプリ側作業を簡素化（外-2外-3削除、Streamlit リダイレクト実装のみ）。§5 #2を「解消済み」に変更。API設計.md・画面設計.md・WBS_Phase1.mdの対応する箇所も同時に更新。 |
| 2026-08-16（再確定） | **エスカレーション読み取りAPIも不採用に確定**。当初「`GET .../escalations`のみは読み取り専用APIとして必要」としていたが、オーナー最終判断により**REST APIは一切実装しない**（読み取り専用も含めて不採用）ことが確定。未回答エスカレーションの閲覧・代理回答もline-rag-bot Streamlitで完結させる。§2-2・§2-3を全面改訂し、浮遊街アプリ側の作業を「line-rag-bot管理画面への外部リンク」のみに縮小。API設計.md・画面設計.md・WBS_Phase1.md・CONSOLIDATED_DECISIONS.md・QUESTIONS.mdへも同時反映。 |
| 2026-08-16（呼称整理） | **利用者向け呼称「浮遊街コンシェルジュ」を追記**（§1末尾）。line-rag-botというシステム名・リポジトリ名自体は変更せず、街人・ゲストがLINEで対話する相手のブランド名としてのみ導入。画面設計.md・API設計.mdの利用者/操作者向け文言もあわせて更新。 |
