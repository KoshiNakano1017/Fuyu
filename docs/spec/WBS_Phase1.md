---
title: "WBS_Phase1（浮遊街アプリ Phase 1 作業分解構造）"
doc_type: 計画
status: 運用中
owner: プロジェクトオーナー
date: 2026-08-22
updated: 2026-09-22
tags: ["浮遊街アプリ"]
---

# WBS_Phase1（浮遊街アプリ Phase 1 作業分解構造）

> 本書は `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md`（正本）§4「スコープ」・§5「詳細業務要件」を
> 機能領域ごとの作業パッケージへ分解した計画ドキュメントである。
> QUESTIONS.mdが「未回答」のままの論点に依存する作業パッケージは、独断で仕様を確定させず
> **「オーナー確認待ち（QUESTIONS.md該当項目を参照）」**として明示する。
>
> 作成日: 2026-08-16 / 全面改訂: 2026-09-03 / 参照: v13 §4・§5・§7・§9、`CONSOLIDATED_DECISIONS.md`、`QUESTIONS.md`

---

## ⚠️ サマリー：現在地（2026-09-20 時点）

> [!important] 納期は「9月中」（2026-09-03 オーナー確定）／**残り10日**
> Phase 1 のリリース目標は ~~**2026-09-10**~~ → **2026年9月中（〜2026-09-30）**。
>
> 経緯: 正本 v13 §4 と `QUESTIONS.md`「[2026-08-22] Phase 1 リリース目標」は、
> **「8/31 時点で主要機能が動いていない場合は、スコープ再交渉（経営判断）へエスカレーションすること」**
> を明記しており、2026-09-03 時点でこの条件は発火していた。納期改訂はそれに対するオーナー判断である。
>
> - **残日数: 10日**（2026-09-20 → 09-30）。09-08 版の 22日から12日を消化した
> - **スコープは据え置き**（絞り込みは行わない／2026-08-16 選択肢A を継続）
> - ✅ **9月中に「何を」リリースするのかは確定している**（2026-09-08 オーナー確定）：**61パッケージ全部**。
>   部分リリースは行わない（§19-5 #1）
> - ⚠️ **残10日に対して未着手 47件**。09-08 版が要求していた「2.5パッケージ／日」は
>   **4.7パッケージ／日**へ上がった。§19-5 #4（中間チェックポイントとエスカレーション条件）を再読すること
>
> 判断材料とスコープ仕分け案は §19 に整理した。

### S-0. 2026-09-21 の実装（DB 基盤の一括解消）

> [!important] 止まっていた6パッケージの共通原因を取り除いた
> **`check_ins` が本リポジトリに存在しない**ことが、`3-3`・`3-4`・`3-8`・`6-1`・`8-1`・`8-6` の
> 6件を同時に止めていた。`DB物理設計.md` §3-11 は「Vault 側 `01_schema.sql` で実装済み」と
> 書いていたが、あれは移行用のドラフトであり `supabase/migrations/` へ取り込まれていなかった。
>
> **2026-09-20 オーナー指示**（「WBS 上着手可能なタスクを実装。その際に DB が存在しない場合は
> 実装すること」）により、`QUESTIONS.md`「[2026-09-19] WBS `3-8` の残枠ビューは `check_ins`
> 未作成では組めない」の**選択肢 C** を採って解消した。

| 追加したマイグレーション | 内容 | 主な作業パッケージ |
| --- | --- | --- |
| `0014_check_ins_and_accommodation_types.sql` | `accommodation_types`（6形態・`allocation_mode`）／`check_ins`（予約の正本）／`0006` が送っていた ALTER 2件の回収／**`rooms` の収容枠を確定表（合計66）へ是正** | `3-2`（DB層）・`3-3` |
| `0015_room_availability_view.sql` | `v_room_availability`（日付×形態の残枠。`security_invoker = false`） | `3-8` |
| `0016_membership_plans_and_stay_tickets.sql` | `membership_plans`（4プラン）／`stay_ticket_transactions`／`stay_ticket_balance()` | `3-4` |
| `0017_quest_applications_and_work_logs.sql` | `quest_applications`／`work_logs`／`work_log_reviews`／**承認ガードのトリガー2本** | `5-2`・`5-3`・`5-4`・`5-6` |
| `0018_menu_items.sql` | `menu_items`（正本メニュー31品＋送迎＝32行）／SOLDOUT の列単位 GRANT | `6-3`・`6-4` |
| `0019_orders_and_settlement.sql` | `orders`／`order_items`／`settlement_adjustments`／`meal_reservations` | `6-1`・`3-5c`・`7-2`・`7-3`・`8-3` |
| `0020_morning_meeting_structuring.sql` | `extracted_quest_candidates` 列／`summary_text` への列単位 UPDATE 権限 | `4-2`・`4-3` |

**実装中に見つけて直した欠陥（いずれも放置すると本番で誤動作するもの）:**

1. **`0006` の収容枠が確定表と違った** — キャンプサイト 4（正: 10）／車中泊 2（正: 30）。
   合計 32 対 66。残枠ビューの分母になるため、**予約できる枠を満室と表示していた**
2. **`quests` の列マスク迂回に続く2例目** — `menu_items` に全列 UPDATE を与えると、
   SOLDOUT だけ許すはずのコアメンバーが**単価を書き換えられる**（列単位 GRANT で是正）
3. **最終承認の admin 限定は RLS では守れない** — `USING (is_staff())` だと core_member が
   `approved_by` を書ける。`service_role` の迂回もあるためトリガーで実装した
4. **精算QRに失効列が無かった** — 伝票を編集しても旧QRで元の金額のまま精算できた

**まだ画面が無い。** 本日入ったのは DB 層とドメインロジック（＋単体テスト176件・DBテスト60件）であり、
`8-1`（顧客管理画面）・`6-1` の A5・`5-4` の B5 等の画面は未着手である。
（テストは**単体98件＋DB61件**を追加した。単体は緑、DB は `psql` を要するため CI で走る）

### S-0a2. 2026-09-22（インタラクティブセッション）— 画面が無くて動かなかった層を通した

> [!important] この日入ったのは「作ってあるのに呼ばれていなかったもの」の接続である
> 09-21 までに DB 層とドメインロジックは広く入っていたが、**それを呼ぶ画面や Server Action が
> 無く、実際には1件も動かない**パッケージが並んでいた（朝会の構造化・メディアのアップロード・
> 会計・給付・入館）。本セッションはそこを通した。

| # | 入ったもの | 主な作業パッケージ | PR |
| --- | --- | --- | --- |
| 1 | 朝会の**構造化とクエスト候補の起案**（Server Action ＋ 画面。1回の Gemini 呼び出しで議事録・クエスト候補・ナレッジ候補） | `4-2`・`4-3` | #134 |
| 2 | **アップロード画面**（ファイル選択・用途タグ・進捗・失敗分のみ再試行・Exif 取得）＋ **バケットの CORS** | `14-2`・`14-1` | #135 |
| 3 | **顧客管理画面・伝票編集・精算QR**（差額の自動起票・編集時のQR失効・明細行の論理削除 `0031`） | `8-1`・`8-5`・`7-2`・`7-3` | #136 |
| 4 | **Eumo給付一覧**と、クエスト最終承認での**報酬給付の自動起票** | `5-5`・`5-7` | #137 |
| 5 | **本人の受領報告**（`0032` のガード）と**再訪アラート** | `8-4`・`10-3` | #138 |
| 6 | **ロール表示名の変換表**と**未認証アクセスの E2E 17件** | `2-5`・`1-3` | #139 |
| 7 | **初回来訪キャッシュバックのバッジ・起票**と**宿泊履歴（泊まった部屋）** | `12-4`・`8-6` | #140 |
| 8 | **チェックイン／チェックアウト・キャンセル操作**と、入館時の**キャッシュバック自動起票** | `3-2`・`3-3`・`12-4` | #141 |

**この日に足した DB マイグレーション**: `0031_order_items_void.sql`（明細行の論理削除）・
`0032_eumo_self_receipt.sql`（本人の受領報告を1遷移に限るトリガー）。

**⛔ オーナー判断待ちとして `QUESTIONS.md` へ記録したもの**（止めずに先へ進んだ分）:
①朝会本文（PII-A）をマスキングせず Gemini へ送ってよいか ②朝会のナレッジ候補の恒久的な保存先
③精算QRをゲストが読み取る先（未ログインで金額を見せてよいか）。

**⚠️ 実行環境の未了**（`TASKS.md` に起票）: **dev Supabase が main より11本分古い**
（最新 `0020`）。この日入れた画面は dev では動かない。`supabase db push --include-all` が要る。

### S-0b. 2026-09-21 の棚卸し：まだ足りない DB（画面を実装した結果、残りが確定した）

> [!important] 画面は9枚入ったが、**6つの表が未作成のまま残っている**
> 2026-09-21 に `/orders`・`/me`・`/reservations`・`/staff/orders`・`/staff/quests`・
> `/staff/calendar`・`/admin`・`/admin/master`・`/admin/preview` の9画面を実装した（PR #103）。
> その過程で「DB が無いから作れない画面」が確定したため、`DB物理設計.md` と
> `supabase/migrations/` を突き合わせて棚卸しした。
>
> **dev Supabase にドリフトは無い。** PostgREST が公開するオブジェクトは26件で、
> `supabase/migrations/` の定義と完全に一致している（2026-09-21 実測）。

**① DDL は設計済み・マイグレーションだけが無い（6件）**

2026-09-20 オーナー指示「WBS 上着手可能なタスクを実装。その際に **DB が存在しない場合は実装すること**」
（§S-0）の対象である。仕様の追加は無く、**実装順だけの問題**。

| テーブル | 設計 | これが無いと開かない画面・機能 | パッケージ |
| --- | --- | --- | --- |
| `accommodation_rates` | `DB物理設計.md` §3-10 | 宿泊料金の Uii 主・円 副表示、公開予約 `/reserve`、マスタ管理 C13 の宿泊料金欄 | `3-9`（宿泊料金マスタ） |
| `reservation_otps` | §3-6③ | **公開予約ページ `/reserve` の本人確認**（v13 §5.2.3 が定める未ログインの入口そのもの） | `3-5b`（★ 公開予約ページ `/reserve`） |
| `media_assets` | §3-7 | アップロード A10、完了報告 A4 の Before/After 写真、`menu_items.image_media_id` の FK | `2-1c`（★ `media_assets` スキーマ ＋ RLS） |
| `eumo_grants` | §3-8 | Eumo給付一覧 B10・C12 | `5-5`（Uii支払・XP／貢献バッジ付与） |
| `membership_applications` | §3-5 | 街人登録モーダル A8、街人登録申請一覧 C7 | `12-2`（申請〜決済〜承認フロー） |
| `import_jobs` | §3-14 | 会員データ取込の二重取込防止（v13 §8 の再実行安全性） | `10-2`（名寄せロジック） |

⚠️ `reservation_form_submissions`（§3-6 の旧構想）は **v13 §9 #46 で撤回済み**のため**作らない**。

**② DDL 自体が本リポジトリに無い（4件・オーナー確認待ち）**

`QUESTIONS.md`「[2026-09-21] Phase 1 に必要な4テーブルの DDL が本リポジトリに存在しない」へ起票した。
4件とも**個人情報の格納先か認可の対象**であり、CLAUDE.md §7.0「認可・個人情報の仕様判断は必ず先に聞く」に該当する。

| テーブル | 状態 | 影響 |
| --- | --- | --- |
| `member_identifiers` | §2 の表に載るが DDL 無し | v13 §5.8.3 が名寄せの初回紐付けに必須とする**連絡先の格納先**。`10-2` を止める |
| `member_notes` | 同上。`DB物理設計.md` 自身が「実体は Vault 側 `01_schema.sql` にのみ存在」と明記 | 運営メモ・再訪アラート B6（`10-3`）。CLAUDE.md §1.1 に反する状態でもある |
| `member_import_links` | §3-14 が「DDL は本書が初出」と書くが**実際には無い**（本改訂で訂正） | 取込ジョブと会員行の対応（`10-2` の監査） |
| `uii_transactions` | §2「定義のみ先行」。一方 §6-1 #10 は「Phase 1 では稼働しないが **RLS は先に張る**」 | 張る対象の表が無い |

**③ 影響を受けている画面の現状**

| 画面 | 現状 | 待っているもの |
| --- | --- | --- |
| `/reserve`（未ログインの入口・A11 の公開版） | **未作成** | `reservation_otps` ＋ `accommodation_rates` ＋ 公開予約OTPの送信実装 |
| `/upload`（A10） | 「何待ちか」を出す画面のみ | `media_assets`（`2-1c`）＋ `1-4`（GCS・署名付きURL） |
| `/staff/eumo`（B10） | 同上 | `eumo_grants`（`5-5`） |
| `/admin/master` の宿泊料金欄 | 「編集できません」と表示 | `accommodation_rates`（`3-9`） |

> [!important] 2026-09-22 追記 — 本表は同日中に3列が解消済みへ変わった
> 本表は 2026-09-21 18:00 時点のスナップショットであり、**その日のうちに状況が変わった**
> （履歴として残し、行そのものは書き換えない／CLAUDE.md §2.4）。
>
> - `/reserve`: 18:47（コミット `a5836b4`）に実装済み。`3-5b` の行を参照
> - `/admin/master` の宿泊料金欄: 18:23（コミット `1fceecb`）に `accommodation_rates` と
>   登録UIが入り解消。`3-9` の行を参照
> - `/upload`（A10）: 2026-09-22 09:15（コミット `014dbd1`）に `media_assets`／GCS基盤が
>   入ったが、**アップロード画面（A10）自体は依然未実装**（`14-2` の行を参照）
> - `/staff/eumo`（B10）: `eumo_grants`（`5-5`）は 18:23 に表としては作成済みだが、
>   **画面は依然プレースホルダのまま**（`5-5` の行を参照。画面側のコメントが
>   「テーブルが存在しない」と書いたままなのは誤りである点に注意）

### S-0c. 2026-09-21 開発環境の実機確認（動作確認URLと、設定が未了の箇所）

> [!important] ローカル開発サーバは動く。ただし**実 Supabase 側の設定が追いついておらず、画面の動作確認は途中で止まる**
> 2026-09-21、オーナー指示「開発環境で動作確認しながら進めたい」を受けて実機を起動し、
> dev Supabase（実プロジェクト）と Vercel の設定状態を突合した。
>
> ⚠️ 本節はこの日の**実機確認と起票の記録**である。§S-0b（同日付・別トピック＝画面実装に伴う
> DB棚卸し）とは論点が異なるため節番号を分けた（`S-0c`）。

**動作確認URL（ローカル開発サーバ ／ `npm run dev`）**

| URL | 状態（2026-09-21 実測） | 備考 |
| --- | --- | --- |
| `http://localhost:3000/` | 200 | トップ。未ログインでも表示される |
| `http://localhost:3000/login` | 200 | メールOTPログイン（パスワード欄なし／v13 §5.2.6） |
| `http://localhost:3000/api/health` | 200 `{"ok":true}` | ヘルスチェック |
| `http://localhost:3000/quests` | 307 → `/login` | `middleware.ts` の認可ガードが正しく効いている |
| `http://localhost:3000/admin/invitations` | 307 → `/login` | 同上 |
| `http://localhost:3000/admin/morning-meetings` | 307 → `/login` | 同上 |

> [!warning] **Vercel の URL は存在しない（2026-09-21 時点）**
> `.vercel/`・`vercel.json`・デプロイ用ワークフローのいずれもリポジトリに無く、
> **Preview / Production の URL はまだ発行されていない**（`1-2` の残 20% の実体）。
> 現時点で開けるのは上記のローカルURLだけである。⚠️ **2026-09-22 に `1-7`（Vercel WAF）が
> `vercel firewall` で本番へ投入されており**（§1 `1-7` 行）、CLI 経由の何らかのプロジェクト
> 連携はその時点で成立している。本節・`1-2a` の実装値は**2026-09-21時点の実測**であり、
> `1-2a` が要求する GitHub 連携・Preview/Production・環境変数登録まで揃っているかは
> 別途再確認が要る。

**実機で判明した「設定が未了」の4点**（いずれも新規パッケージとして §1 へ起票し、**2点は同日に解消した**）

| # | 事象（実測値） | 影響 | 起票先 |
| --- | --- | --- | --- |
| 1 | **dev Supabase に `0014`〜`0020` が未適用**。`supabase migration list` の remote 列が `0014`〜`0020` だけ空。`check_ins`・`accommodation_types`・`membership_plans`・`quest_applications`・`menu_items`・`orders`・`v_room_availability` が **PostgREST から見えない**（`PGRST205`） | §S-0 で実装した DB 層・ドメインロジックが**実DBに対しては1行も動かない**。`3-3`・`3-4`・`5-2`・`6-1`・`8-1` 等の画面を作っても動作確認できない | **`1-1c`** ✅ 同日解消 |
| 2 | **`auth.users` が 0件**。`members` は 1件のみ | `signInWithOtp` は `shouldCreateUser: false`（`src/app/login/actions.ts`）のため、**誰もログインできない**。ログイン後の画面はすべて確認不能 | **`1-1d`** ✅ 同日解消 |
| 3 | **dev Supabase の Auth メール設定が未確認**。`supabase/config.toml` の `site_url` は `http://127.0.0.1:3000`（**ローカルスタック用であり、実プロジェクトには効かない**） | Supabase のメールテンプレートが既定のままだと**マジックリンクが飛び、6桁コードが本文に入らない**（`verifyOtp({type:"email"})` が受け取れない）。§16-1a #3 の「送信経路は2系統」の Auth 側がこれに当たる | **`1-1e`** ⚠️ 迂回路あり |
| 4 | **Vercel プロジェクトが未連携**（`.vercel/` 無し・`vercel.json` 無し・`.github/workflows/` に deploy 無し） | オーナー／運営スタッフが**ブラウザから触れる環境が存在しない**。`1-7`（Vercel WAF）も、プロジェクトが無いため設定画面に到達できない | **`1-2a`** ⏳ オーナー作業 |

### S-0d. オーナー作業の手順（`1-1e`・`1-2a`）

> [!note] 2026-09-21 時点で、**こちら側で実行できる分（`1-1c`・`1-1d`）は完了している**
> 残りはいずれも**ブラウザのダッシュボード操作**であり、アクセストークンを持たないエージェントからは実行できない。
> 手順を分散させないため、ここに置く（CLAUDE.md §1.2「新規ドキュメントを作らない原則」）。

**① Supabase Auth の設定（`1-1e`）** — `https://supabase.com/dashboard` → 当該 dev プロジェクト

> [!warning] **順番が固定されている。** Supabase は**カスタム SMTP を設定するまでメールテンプレートの本文（Source）を編集できない**。
> 「テンプレートへ `{{ .Token }}` を入れる」を先に片付けようとしても着手できないので、SMTP → テンプレートの順で進める。

| 順 | 場所 | やること | なぜ |
| --- | --- | --- | --- |
| ✅ 済 | Authentication → Sign In / Providers → Email → **Email OTP Length** | **6** にする | ~~実測で8桁が返った~~ → **2026-09-21 オーナー設定済み。実測6桁・`verifyOtp` 成功**。`LoginForm.tsx` の `maxLength={6}`・v13 §5.2.6 と整合した |
| 1 | Project Settings → Authentication → **SMTP**（テンプレート画面の `Set up SMTP` ボタンも同じ場所） | Resend を設定（`smtp.resend.com` / 465 / user=`resend` / pass=APIキー / 送信元は検証済みドメイン） | 既定SMTPは **2通/時の共用サンドボックス**で、370名が使える送信基盤ではない。§16-1a #3 のとおり**送信経路は2系統**で、ここは Auth 側。公開予約OTP（`3-5b`）は自前実装なので**別に配線が要る** |
| 2 | Authentication → Emails → **Magic link or OTP** → Body を `Source` へ | **`{{ .Token }}` だけにし、サインインリンク（`{{ .ConfirmationURL }}`）は消す**（2026-09-21 オーナー決定 ／ 決定ログ §20-1） | 既定テンプレートは**サインインリンクだけで6桁コードが載らない**。アプリは `verifyOtp({type:"email"})` の**リダイレクトを伴わない**方式（`emailRedirectTo`・`/auth/callback` はコード上に存在しない）。リンクを外せば**③が不要になり、設定ミスという失敗様式そのものが消える**。あわせてメール転送・誤送信による乗っ取り経路が1本減る |
| 3 | Authentication → **URL Configuration** | ~~Site URL と Redirect URLs に開発用オリジンを登録~~ → **②を実施すればログイン経路では不要**。招待（経路B）はその後 `2-1d`（決定ログ §22-1・v13 §9 #67）でコード方式へ寄せたため、こちらも不要化した | `supabase/config.toml` の `site_url` は**ローカルスタック専用で実プロジェクトには効かない**。~~招待（`inviteUserByEmail`）だけは今もリンク方式~~ → **2026-09-22 に `inviteUserByEmail()` 自体を廃止**し、招待もログイン画面の案内メールへ統一済み（§1 `2-1d` 行） |
| 4 | Authentication → **Rate Limits** | OTP の送信レート上限を確認 | §16-2 の F-7（OTP以外のレート制限が未定義）と対で見る |

> SMTP が済むまでは `npm run dev:login-code -- dev-admin@example.invalid` でコードを取り出せる（メールは飛ばない）。

> [!note] ⚠️ プランが `FREE` のままである
> 2026-09-21 時点のダッシュボード表示は **FREE**。2026-09-08 のオーナー決定「Supabase を有料プランへ」が未適用であり、
> §19-2 が解消済みとした無料枠リスク3点（日次バックアップ不可＝非機能 A4 ／ 1週間非アクティブで自動停止 ／ DB 500MB）が**実際にはまだ残っている**。
> `1-6`（pgvector有効化・RAG基盤）は 500MB 制約と直結する。

**② Vercel プロジェクトの作成（`1-2a` ／ 10分）** — `https://vercel.com/new`

1. `KoshiNakano1017/Fuyu` を Import する（Framework は `vercel.json` の `nextjs` が効く）。**公開ドメインは Vercel 既定（`*.vercel.app`）を使う**（2026-09-21 オーナー決定 ／ 決定ログ §20-2）。独自ドメインは当てない — ⚠️ `fuyugai.jp` は **Resend の送信ドメイン**であって、アプリのURLではない
2. Settings → General → **Node.js Version を 20.x** にする（`.nvmrc`・CI と揃える）
3. Settings → Environment Variables へ登録する

   | 変数 | Production | Preview | 備考 |
   | --- | --- | --- | --- |
   | `NEXT_PUBLIC_SUPABASE_URL` | prod（`1-1b`） | dev | 公開値 |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | prod（`1-1b`） | dev | 公開値（RLS前提） |
   | `SUPABASE_SERVICE_ROLE_KEY` | prod（`1-1b`） | dev | **Sensitive で登録**。クライアントへ渡らないこと |
   | `ANTHROPIC_API_KEY` / `GEMINI_API_KEY` | ○ | ○ | `1-5` |
   | `LINE_CHANNEL_*` / `GCP_*` / `GCS_*` | ○ | △ | `1-4`・通知が要る段階で |
   | `SIGNED_URL_*_TTL_MIN` | ○ | ○ | v13 §5.11.2 の決定値 |

4. **Preview がどの Supabase を指すかを決めて明文化する**

   > [!danger] Preview に prod の値を入れない
   > CLAUDE.md §6.2 が E2E について警告しているのと**同じ事故**である。
   > Preview は **dev を指す**か、`1-1b` で作る E2E 専用プロジェクトを指すかの二択で、
   > **prod を指す選択肢は無い**（実名370名のデータに対してレビュー操作が走る）。

5. デプロイ後、`1-7`（Vercel WAF）の設定画面へ到達できるようになる（⚠️ `1-7` は 2026-09-22 に
   `vercel firewall` CLI 経由で既に投入済みのため、**本手順は実際にはこれより前に済んでいる可能性が高い**。
   ダッシュボード側の状態を実機で確認してから着手すること）

### S-1. 全体進捗

| 軸 | 進捗 | 前版（09-08） | 根拠 |
| --- | --- | --- | --- |
| **設計進捗** | **約 80%** | 78% | 正本 v13 が v1.28.0 → **v1.29.0**（§5.2.7 宿泊者名簿の収集経路・§5.10.6 の列非開示と `core_only_reward`）へ進み、`DB物理設計.md` §6-7・`API設計.md` §2-4／§3-5 が追随した |
| **実装進捗** | **約 27%** | 6% | **15パッケージが完了**（`1-1`・`1-3`・`1-5`・`2-1`・`2-1b`・**`2-1d`**・`2-2`・`2-3`・`2-4`・**`2-7`**・`3-1`・`4-1`・`5-1`・`7-1` ＋ **`1-7`**（90%＝実測待ち））。65件平均＝（完了 14×100 ＋ `1-7` 90 ＋ `1-6` 80 ＋ `1-2` 80 ＋ `0-1` 60）÷ 65 ≒ **27%**（**2026-09-22：`2-1d`・`2-7` の新設で分母が 63 → 65 へ増え、同日に両件と `1-7` を実装した**） |
| **総合進捗** | **約 46%** | 35% | 設計 40% ＋ 実装 60% の加重（§S-4 参照） |

**実装対象パッケージ 65件のうち、完了 12件・部分実装 3件（`1-6` 80% ／ `1-2` 80% ／ `0-1` 60%）。残る 50件は 0% である。**

> [!important] 2026-09-22 時点の実測（上の数値は 09-20 集計のままである）
> 本書の進捗セル（設計%・実装%・総合%）を**実際に読み取って平均した値**は次のとおり。
> 母数は「進捗セルを持つ行 75件」であり、上の「63件」とは数え方が違う（§0 の運営基盤や
> 新設パッケージを含むため）。**数え方の統一は次回の全面改訂で行う。**
>
> | 軸 | 実測（2026-09-22） |
> | --- | --- |
> | 設計% の平均 | **84.5%** |
> | 実装% の平均 | **71.7%** |
> | 総合% の平均 | **76.8%** |
> | 実装 100% の行 | **23件** |
> | 実装 0% の行 | **10件** |
内訳の変化: 2026-09-19 に `10-1` を不要化（−1）し `2-1c` を新設（＋1）で 61件のまま。
そのうえで **2026-09-20 のオーナー確定（ベクトル基盤は Phase 1）により `1-6`・`9-1` が対象へ入り 63件**になった（§19-2b）。

### S-2. 実装の現在地（09-08 → 09-20）

> [!note] 前版からの最大の変化 — 「入ったのは足場だけ」は解消した
> 09-08 版は「入ったのは**足場**であり、画面・テーブル・API はいずれも業務機能に到達していない」と
> 記録していた。**この記述はもう正しくない。** 認証（メールOTP・ロールガード）・RLS・部屋台帳・
> 朝会テキスト投入・クエストボードが実コードとして入り、`tests/db/` の DB テスト層で検証されている。
>
> ⚠️ **ただし注文（§6）・会計（`7-2` 以降）・公開予約（`3-5b`）は依然 0%** であり、
> **現場の1日を通しで回せる状態ではない**。§19-5 #4 が想定していた 9/19 の中間チェックポイント
> （チェックイン・注文・会計が通しで動くこと）は**未達のまま通過した**。

| 確認項目 | 09-08 | 現在（09-20） |
| --- | --- | --- |
| tracked ファイル総数 | 126件 | **244件** |
| `.ts` / `.tsx`（`src/` 配下） | 13件・計290行 | **40件・計2,239行** |
| `.sql`（マイグレーション） | **0件**（`.gitkeep` のみ） | **13件**（`0001`〜`0012` ＋ `0100`） |
| テストコード | 単体13ケース ＋ E2E スモーク1件 | **`tests/` 30ファイル・計4,772行**（単体14スイート ＋ `tests/db/` 12スイート ＋ フィクスチャ／ヘルパ）。**E2E は依然スモーク1件**（`1-3` の残作業） |
| CI ジョブ | build-test・E2E が空回りから実働へ | **5ジョブ体制**（`build-and-test` ／ `db-test` ／ `e2e` ／ `secret-scan` ／ `docs-policy`） |
| 「動く成果物」 | `/` と `/api/health` が応答する Next.js アプリ | ＋ **ログイン（メールOTP）・招待発行・朝会テキスト投入・クエストボード・`GET /api/quests`・受注申請APIの認可ゲート** |

**完了した作業パッケージ（12件）**

| # | パッケージ | 完了 | 実体 |
| --- | --- | --- | --- |
| `7-1` | Uii換算ロジック | 2026-09-05 | `src/lib/uii.ts`。単品ごとの `floor(単価×0.8)` を単体テストで固定 |
| `1-1` | Supabaseプロジェクト作成・環境構築 | 2026-09-13 | dev プロジェクト作成・接続確認。**2026-09-20：dev へスキーマを初回適用**（`0001`〜`0011`＋`0100`＋`0101` の13本。PostgREST 公開オブジェクト 0件→13件、`tests/db` 218件が実DBに対して緑／PR #82）。~~⚠️ `0012`（quests 列ロックダウン）・`0013`（vector を extensions スキーマへ）は dev へ未適用~~ → ✅ **2026-09-20：`supabase db push --include-all` で適用済み**。`supabase migration list` で**ローカル15本と dev の履歴が完全一致**（`0001`〜`0013`＋`0100`＋`0101`）。⚠️ 0012・0013 は `0101` より前の連番のため `--include-all` が要る（既定の `db push` は「リモート最新より前に挿す移行」を拒否する） |
| `2-1` | ロール・アカウントステータス スキーマ | 2026-09-14（#29） | `0001`〜`0003`。`member_profiles_private` への PII 分離、権限ガードトリガー |
| `1-3` | テスト基盤 | 2026-09-15 | CI 5ジョブ ＋ `tests/db/helpers/` による合成フィクスチャのローカルスタック |
| `2-1b` | ログイン導線・セッション・ロールガード | 2026-09-16（#39） | メールOTP・`middleware.ts`・`src/lib/auth/`・`0004` |
| `2-2` | Supabase RLSポリシー設計 | 2026-09-16（#35） | `0005_rls_policies.sql` ＋ `tests/db/rls-policies.test.ts` |
| `2-3` | ロール別UI表示制御（フロント） | 2026-09-16 | `src/lib/auth/navigation.ts`・`RoleNav`・`AccessDenied` |
| `3-1` | 部屋台帳・部屋割当 | 2026-09-16（#31） | `0006_rooms_and_assignments.sql` |
| `2-4` | 個人情報収集・保持（宿泊法対応） | 2026-09-19（#58） | `0010_lodging_register_entries.sql`（収集UIは `3-2` へ。v13 §5.2.7） |
| `1-5` | Claude／Gemini 連携基盤 | 2026-09-19（#55） | `src/lib/ai/`（MCP Gateway は Phase 3 へ外した） |
| `5-1` | クエストボード | 2026-09-19（#53） | `0007`／`0008`・`src/lib/quests/`・`GET /api/quests` |
| `4-1` | 朝会テキスト投入UI | 2026-09-19（#23） | `0011_morning_meetings.sql`・`/admin/morning-meetings` |

**部分実装（2件）**

| # | パッケージ | 実装 | 残り |
| --- | --- | ---: | --- |
| `1-2` | Next.js雛形・Vercelデプロイパイプライン | **80%** | ⚠️ **Vercel のデプロイ設定がリポジトリ内に存在せず未検証**。Issue #50 は `needs-spec` のまま滞留している |
| `0-1` | 通貨表示の共通コンポーネント | **60%** | `src/components/ui/Money.tsx` は実在するが、**置き換える画面が揃っていない**ため「全画面をこれ経由に」は未達 |
| `1-6` | pgvector有効化・RAG基盤 | **80%** | `0100` を dev へ適用済み（2026-09-20・PR #82）。残るのは検索時スコープと PII 検出の範囲拡張（§19-2b） |

### S-3. 完了しているもの（§0 運営基盤 ＋ 納品物）

| 完了物 | 完成度 | 備考 |
| --- | --- | --- |
| **コンシェルジュ運用ナレッジ 50件の本番投入**（17-2） | **100%** | **Phase 1 で唯一、本番投入・動作確認まで到達した納品物**（2026-08-23） |
| CLAUDE.md（開発ルール正本）（0-A） | 100% | |
| pre-commit フック（0-B） | 100% | 212行 |
| GitHub Actions CI（0-C） | **95%** | ~~4ジョブ中2つは検査対象0行で空回り~~ → **09-05 の雛形投入により build-test・E2E とも実働**。残＝E2Eシナリオの中身（`1-3`） |
| ドキュメント体系 MECE 再編・SITEMAP（0-E） | **100%** | ~~未コミットによりリンク切れ~~ → **09-03 に解消**（§S-5） |
| 要件定義 8本・ER図・ユースケース図（0-G/0-H） | 100% | |
| 詳細設計 5本（DB物理／画面／API／外部連携／非機能） | 100%（作成） | **09-05 に DB物理設計 +641行**。ただし画面設計はモックより古い（§18-A） |
| HTMLモック `prototype_v15.html`（18-1） | 85% | 33画面中28件を実装。v1.21.0 未追随 |
| 自律開発ループ基盤 `automation/`（0-D） | **95%** | `auto-01`〜`auto-03` の3ワークフローとエージェント定義11本が揃い、**2026-09-08 に参照切れ7か所と frontmatter 未付与7本を修正**（`check_agents.py` 違反なし）。~~エージェント主導への「移行」で②連携層を書き直す~~ → **移行作業は存在しないことがオーナー確定**（§S-5）。残＝**実装ループの試運転**と Environment `gate-low-risk-objection` の作成 |

### S-4. 進捗率の定義

- **設計進捗** ＝ 要件確定（正本v13で確定しているか）40% ＋ 詳細設計（DB物理／API／画面設計でカバーされているか）40% ＋ モック実在（`prototype_v15.html`）20%
- **実装進捗** ＝ 実コード・実環境の到達度。**ドキュメントが書かれたことは実装に数えない**
- **総合** ＝ 設計 × 0.4 ＋ 実装 × 0.6
- §0（プロジェクト運営基盤）と §17（運用ドキュメント）は実装WBSではないため、**全体進捗の分母から除外**する。混ぜると「アプリ実装がほぼ進んでいない」という最重要事実が見えなくなるため

### S-5. 情報の分散状況（09-08 時点で解消したもの・残るもの）

> [!note] 前版の警告「未コミット差分の滞留」は解消済み
> 09-03 版が警告していた 08-24〜09-01 の約2週間分（v13 v1.18.0〜v1.21.0 の +895行、
> `prototype_v15.html`、`docs/operations/` 2本、レビュー突合3本、`.env.example` の誤削除）は、
> **すべてコミット済み**である。SITEMAP・CONSOLIDATED_DECISIONS からのリンク切れも解消した。

**代わりに 09-05〜09-07 は「ブランチへの分散」という形で滞留していた**（本改訂で解消）。

| 分散していたもの | 内容 | 状態 |
| --- | --- | --- |
| `feature/tailwind-and-supabase-wiring` | Tailwind v4 導入・Supabase 配線（09-07・2コミット） | ✅ **本改訂でマージ済み** |
| `docs/role-lock-and-lodging-law` | `DB物理設計.md` +641行（09-06・WIP） | ✅ **本改訂でマージ済み** |
| 作業ツリーの未コミット差分 | 自律開発ループのエージェント方式改訂（09-07） | ✅ **本改訂でコミット済み** |

**残る不整合（要対応）**

| # | 内容 | 影響 |
| --- | --- | --- |
| 1 | **`docs/role-lock-and-lodging-law` は WIP コミット**であり、`DB物理設計.md` には反映されたが 会員データモデル・ER図・正本 v13 への反映が未了だった（コミットメッセージが自ら明記）<br>→ 🟡 **2026-09-13 に2件解消**：`会員データモデル_ユーザーテーブル定義.md`（v1.4.0）と `データモデル図_ER_Diagram.md`（v1.3.0）へ §6-6b の権限ガードトリガーを反映済み。~~**残るは正本 v13 §2／§5.9.3 の1件**~~ → ✅ **2026-09-20 確認：解消済み**。正本 v13 §7「★ 権限変更履歴」に**自己変更の禁止・`CHECK (member_id <> operator_id)`・トリガーによる強制**が記載されている（v1.27.0 ／ 2026-09-12）。残るのは §5.9.3（サーバサイド認可）本文からの相互参照だけで、実装をブロックしない | ✅ `2-1`・`2-4`・`3-1` とも完了済み |
| 2 | ~~**自律ループの実装（09-05）と設計（09-07）が逆方向を向いている**~~ → ✅ **2026-09-08 解消**。オーナー確定：**「移行」という作業は無く、既存の実装にタスクを流すだけ**。実体として不足していたのは **①`auto-02`／`auto-03` の参照切れ7か所**（`automation/prompts/` を指したままで、実行すれば必ず失敗する状態だった）と **②frontmatter 未付与7本**の2点のみで、**いずれも 09-08 に修正済み**（`check_agents.py` 違反なし・11体 ok） | `0-D`。⚠️ 残るのは**実装ループの試運転**と Environment `gate-low-risk-objection` の作成 |
| 3 | `新規メンバー向け_ウェルカムメール案.md`（7.2 KB）が**リポジトリルート直下に untracked のまま** | SITEMAP の9層分類に反する。`docs/` へ移すか破棄の判断が必要（09-03 版から未処理） |
| 4 | **2026-09-20 の作業が作業ツリーに未コミットで滞留**（`0012_quest_reward_column_lockdown.sql` ／ v13 §5.2.7・§5.10.6 ／ `DB物理設計.md` §6-7 ／ `API設計.md` §2-4・§3-5 ／ CI の 0100 退避撤去 ／ `sweep.py` の `OWNER_WAIT`） | `5-1`・`3-2`・`1-6`。**09-05〜09-07 と同じ「ブランチ・作業ツリーへの分散」が再発している**。ブランチ `fix/sweep-owner-wait-capacity` 上で PR まで通すこと |
| 5 | **`QUESTIONS.md` 冒頭の現況表が 2026-09-13 集計のまま**（全56件）。09-14〜09-20 の起票・回答分が未集計 | §16 の逆引き表が件数を二重管理しないよう、集計は `QUESTIONS.md` 側で更新する |
---

## 凡例・前提

### 規模感の目安
- **S**（小）: 画面1〜2枚 or テーブル1〜2個相当。数日規模。
- **M**（中）: 画面数枚＋API複数本、または中規模なワークフロー（承認フロー等）。1〜2週間規模。
- **L**（大）: 複数画面＋複数テーブル＋外部連携を含む。3週間以上、または複数人での分担が前提。

### ステータス区分
- 🟢 **着手可能**: 要件・設計とも確定。実装に入れる。
- 🟡 **要件のみ確定**: 詳細設計またはモックが未整備。設計から着手が必要。
- 🔴 **ブロック中**: QUESTIONS.md の未回答項目により実装着手できない（§16 参照）。
- ⏸ **保留**: 着手条件が未充足。
- ➖ **廃止・不要化・統合**: 取り消し線で残す（CLAUDE.md §2.4）。

### 依存関係の読み方
「依存」列は、原則としてその作業パッケージの**着手前提**を示す。

### 2026-09-03 改訂での構造変更
1. **§0「プロジェクト運営基盤」を新設** — WBS に無かったが実際に完了している作業15件を集約。
2. **旧 §14b を解体し、各パッケージを本来の機能領域へ統合**（3-7〜3-9→§3、5-6→§5、6-4/6-5→§6、8-5→§8、14-1〜14-3→§14）。
3. **採番衝突を解消** — `5-5` が「Uii支払・XP付与」と「Eumo給付追跡」の2件に重複していたため、後者を **`5-7`** へ改番。
4. **`13-2`（アプリ初期操作マニュアル）と `17-1`（現場運用マニュアル）の重複を解消** — `17-1` に一本化。
5. **v13 §9 #50〜#60 の未反映分を反映** — 新規7パッケージ起票・既存9件の記述修正。
6. **§16 逆引き表を全面更新** — 2026-08-20 起票分で更新が止まっており、以降の14件が未掲載だった。

---

## §0. プロジェクト運営基盤（WBS 未起票のまま完了していた作業）

> [!note] 全体進捗の分母から除外する
> これらは実装WBSではなく、プロジェクトを運営するための基盤・成果物である。
> 実装パッケージと混ぜると「アプリ実装が 0%」という事実が薄まるため、進捗は別建てで管理する。

| #   | 作業パッケージ                                                | 完成度             | 証拠                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------ | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0-A | CLAUDE.md（開発ルール正本）の制定・維持                               | **100%**        | 2026-08-18 新設。8/22 §1.5、8/24 §7.1、9/03 §3.3 を追補                                                                                                                                                                              |
| 0-B | pre-commit フック（個人情報ガード・gitleaks・ドキュメント規約）              | **100%**        | `.githooks/pre-commit` 212行                                                                                                                                                                                                  |
| 0-C | GitHub Actions CI（gitleaks／docs-policy／build-test／E2E） | **70%**         | `ci.yml` 129行・`claude-review.yml` 81行。⚠️ build-test と E2E は**検査対象コードが0行のため実質空回り**。実働は gitleaks と docs-policy のみ                                                                                                              |
| 0-D | 自律開発ループ基盤 `automation/`                                | **95%**         | 9/01 足場追加 → 9/03 に `auto-01` 完成 → **9/05 に `auto-02`／`auto-03` とエージェント定義10本が完成** → 9/07 に `risk-classify` 追加・`agents/` へ改名 → **9/08 に参照切れ7か所と frontmatter 未付与7本を修正し `check_agents.py` を全件通過**。残＝実装ループの試運転と Environment 1件の作成 |
| 0-E | ドキュメント体系の9層MECE再編 ＋ `SITEMAP.md`                       | **90%**         | 2026-08-22（35ファイル・+5,134行）。⚠️ §S-5 の未コミットによりリンク切れ                                                                                                                                                                            |
| 0-F | オンボーディング資料（新規メンバー向け）                                   | **100%**        | 1,073行                                                                                                                                                                                                                       |
| 0-G | 要件定義群 `requirements/` 8本の新規作成                          | **100%**        | 2026-08-18                                                                                                                                                                                                                   |
| 0-H | ER図（論理・1,090行）／ユースケース図（458行）／会員データモデル（595行）            | **100%**        | 2026-08-18                                                                                                                                                                                                                   |
| 0-I | RAG基盤_line-rag-bot概要                                   | **100%**        | 527行                                                                                                                                                                                                                         |
| 0-J | クライアントレビュー突合記録3本（v1.15.0／v1.18.0／v1.21.0）              | **100%**（未コミット） | 各17 KB前後                                                                                                                                                                                                                     |
| 0-K | インフラ構成図・データフロー6図・STRIDE脅威モデリング・暗号化/鍵管理設計               | **100%**（未コミット） | 2026-08-28。システムアーキテクチャ +298行、非機能要件詳細 +140行                                                                                                                                                                                   |

> ⚠️ `新規メンバー向け_ウェルカムメール案.md`（7.2 KB）が**リポジトリルート直下に untracked で放置**されている。
> SITEMAP の9層分類に反する配置のため、`docs/` 配下へ移すか破棄の判断が必要。

---

## §0-1. 最優先パッケージ

| #       | 作業パッケージ             | 概要                                                                                                                                                                      | 依存  | 規模  |  設計 |      実装 |      総合 | ステータス                                                                                                                                                                         |
| ------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- | --: | ------: | ------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **0-1** | **通貨表示の共通コンポーネント化** | `2,400 Uii（¥3,000）`（Uii 主・円 副）を返す共通コンポーネントを1箇所に実装し、**全画面をこれ経由に置き換える**。計算ロジック（`floor(単価×0.8)`）は表示層から分離。**2026-09-01：「CB」表記の全廃と単位（コイン/uii）の必須併記も本コンポーネントの責務に含める**（§9 #51） | 1-2 | S   | 85% | **60%** | **70%** | 🟡 **一部実装（2026-09-05）**：`src/components/ui/Money.tsx` が `2,400 Uii（¥3,000）` を返す。~~1-2 が未着手のため着手不能~~ は**解消**。⚠️ **置き換える画面がまだ無い**ため「全画面をこれ経由に」は未達。「CB」表記の全廃・単位の必須併記（§9 #51）も未着手 |

---

## §1. 基盤整備（全機能領域の前提）

| #       | 作業パッケージ                                | 概要                                                                     | 依存  | 規模    |      設計 |      実装 |      総合 | ステータス                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------- | -------------------------------------- | ---------------------------------------------------------------------- | --- | ----- | ------: | ------: | ------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1-1 | Supabaseプロジェクト作成・環境構築 | PostgreSQL・Auth・Realtime・RLS の初期セットアップ、環境変数管理（**dev プロジェクト**。~~dev / prod の2プロジェクト~~ → prod と E2E は `1-1b` へ分離） | なし | S | 90% | 100% | 96% | ✅ **完了（2026-09-13）**：Supabase dev プロジェクトを作成し、`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` を取得。`src/lib/supabase/{client,server,env}.ts` の配線と `supabase/migrations/` の置き場も完了。**2026-09-13 オーナー決定**：E2E 専用プロジェクトと prod プロジェクトは `1-1b` へ切り出した（下流が必要とするのは dev だけで、束ねたままでは `2-1` 以降が prod 作成まで開かないため）。 |
| **1-1b** | **★ E2E 専用 ＋ prod の Supabase プロジェクト**（新規） | E2E 専用プロジェクト（`E2E_SUPABASE_URL` / `E2E_SUPABASE_ANON_KEY` を **GitHub Secrets のみ**へ登録）と、prod プロジェクトの作成。**いずれもブラウザ操作のため自律ループでは完了できない** | 1-1 | S | 100% | 0% | 40% | 🟡 **未着手（オーナー作業）**：E2E は CI のブロッカー。**CLAUDE.md §6.2「E2E は本番 Supabase を絶対に指さない。本番の値を Secrets に入れると実名370名の会員データに対してテストが走る」**。prod はリリース直前でよい（2026-09-05 の dev/prod 2プロジェクト決定を継承）。 |
| **`1-1c`** | **★ dev Supabase へのマイグレーション適用（`0014`〜`0020`）**（新規） | §S-0 で追加した7本を dev プロジェクトへ適用する。`supabase link` → `supabase db push --include-all`（`0100`／`0101` より前の連番を挿すため `--include-all` が要る。`1-1` の 09-20 注記と同じ罠）。適用後に `supabase migration list` の local／remote 一致と、PostgREST 公開オブジェクト数を証拠として残す | 1-1 | S | 100% | **100%** | **100%** | ✅ **完了（2026-09-21）**：`supabase db push --include-all` で7本を dev へ適用。`supabase migration list` が **local 22本 ＝ remote 22本で完全一致**。適用後の実測で `accommodation_types` 6行・`membership_plans` 4行・`menu_items` 32行・`check_ins` 0行・`orders` 0行・`quest_applications` 0行・`v_room_availability` が PostgREST から見えるようになった。~~実測で **remote に `0014`〜`0020` が1本も入っていない**ことを確認（`check_ins`・`accommodation_types`・`membership_plans`・`quest_applications`・`menu_items`・`orders`・`v_room_availability` が `PGRST205`）。**`3-3`・`3-4`・`3-8`・`5-2`〜`5-4`・`6-1`・`6-3`・`7-2`・`8-1`・`8-3` の動作確認を全部止めている**。設計作業は無く、実行するだけ（0.5日未満） |
| **`1-1d`** | **★ 開発用テストユーザーとシードデータの投入**（新規） | dev Supabase に**ロール別のテストユーザー**（`admin`／`core_member`／`member`／`guest`）を作り、`auth.users` と `members.auth_user_id` を束ねる。合わせて `quests`・`menu_items`・`check_ins` の動作確認用データを投入。**実会員データは絶対に使わない**（CLAUDE.md §7.1）。合成フィクスチャは `tests/db/helpers/fixtures.ts` の作りを流用する | 1-1c | S | **100%** | **100%** | **100%** | ✅ **完了（2026-09-21）**：`scripts/dev/seed-dev.mjs`（`npm run seed:dev`／冪等）で **4ロール分の `auth.users` ＋ `members` ＋ `member_profiles_private` ＋ 確認用クエスト2件**を投入。あわせて `scripts/dev/login-code.mjs`（`npm run dev:login-code -- <メール>`）を追加し、**メールが届かなくても6桁コードを取り出せる**ようにした（`1-1e` の迂回路）。実測で `verifyOtp` → `members` の RLS 越し読み出し → `v_quest_board` まで通り、**ゲストには `description` が返らない**ことも確認できた（v13 §5.10.6 が画面で検証可能になった）。~~実測で **`auth.users` が0件・`members` が1件**。`src/app/login/actions.ts` は `shouldCreateUser: false` のため、**ユーザーを先に作らない限り誰もログインできず、ログイン後の画面は1枚も確認できない**。⚠️ 設計 60% ＝ 「どのロールで何件作るか」の受入データ定義が未文書（`画面設計.md` にロール別の確認シナリオが無い） |
| **`1-1e`** | **★ dev Supabase の Auth 設定（メールOTP）**（新規） | 実プロジェクト側ダッシュボードで、① **メールテンプレートに `{{ .Token }}` を入れる**（既定のままだとマジックリンクが飛び、6桁コードが本文に入らない）／② Site URL・Redirect URLs に開発用オリジンを登録／③ OTP 有効期限とレート制限／④ 既定SMTP（2通/時）から **Resend** への切替（§16-1a #3 の Auth 側の配線） | 1-1 | S | **90%** | **25%** | **51%** | 🟡 **2026-09-21 新規（残りはオーナーのブラウザ操作）**。**開発は `1-1d` の `npm run dev:login-code` で迂回できるため、ブロッカーではない**。✅ **完了条件①「OTP 長を6桁へ」は 2026-09-21 にオーナーが設定し、実測で 6桁・`verifyOtp` 成功・RLS 越しの `members` 読み出しまで確認済み**（~~実測で8桁が返り、`LoginForm.tsx` の `maxLength={6}` にコードを貼り切れなかった~~）。**2026-09-21 オーナー決定（決定ログ §20-1）で残条件が3つ → 2つになった**：②テンプレートを **`{{ .Token }}` だけにし、サインインリンクを消す**（⚠️ **Supabase はカスタム SMTP を設定するまでテンプレート本文を編集できない**ため④が先行）／④Resend の SMTP 設定。~~③Site URL・Redirect URLs の登録~~ は**ログイン経路がリダイレクトを伴わないため不要化**。招待（経路B）のリンク問題も、`2-1d`（決定ログ §22-1・v13 §9 #67・2026-09-22）で `inviteUserByEmail()` 自体を廃止して解消済み。`supabase/config.toml` の `site_url = "http://127.0.0.1:3000"` は**ローカルスタック専用で実プロジェクトには効かない**ため、実設定が未確認のまま残っていた。`1-1d` でユーザーを作っても、**ここが既定のままだと6桁コードが届かずログインが完結しない**。⚠️ Resend は `3-5b`（公開予約OTP＝自前実装）と**設定箇所が別**である点に注意（§16-1a #3） |
| 1-2     | **Next.js雛形構築・Vercelデプロイパイプライン**       | プロジェクト雛形、CI連携、環境ごとのデプロイ設定                                              | 1-1 | S     |     70% | **80%** | **76%** | 🟢 ~~未着手~~ → **2026-09-05 に雛形投入**（Next.js 16.3.4 ／ React 19.2.8）。09-07 に Tailwind v4 とモックのデザイントークン移植を追加。`/` と `/api/health` が応答する。⚠️ **残作業＝Vercel のデプロイ設定**（リポジトリ内に設定が存在せず、環境ごとのデプロイは未検証）。**前版の「`package.json` が無いため 0-1 以下すべてが着手不能」というブロックは解消済み**。**2026-09-21：残作業を `1-2a` として切り出した**（Issue #50 が `needs-spec` のまま滞留していたため、着手できる粒度へ割った）                                                                                                                                                                                                                                            |
| **`1-2a`** | **★ Vercel プロジェクト連携とデプロイ環境変数の登録**（新規 ／ `1-2` の残 20% を切り出し） | GitHub リポジトリを Vercel プロジェクトへ接続し、**Preview（ブランチ）／Production の URL を発行**する。`vercel.json`（Node 20・ビルドコマンド・リージョン）をリポジトリへ置く。`NEXT_PUBLIC_SUPABASE_URL`／`NEXT_PUBLIC_SUPABASE_ANON_KEY`／`SUPABASE_SERVICE_ROLE_KEY`／`ANTHROPIC_API_KEY` 等を **Vercel の Environment Variables へ環境別に登録**（Service Role は Production/Preview ともサーバ専用スコープ）。**Preview がどの Supabase を指すか**（dev 固定か Preview 専用か）を決めて明文化する。**公開ドメインは Vercel 既定（`*.vercel.app`）で確定**（2026-09-21 オーナー決定 ／ 決定ログ §20-2） | 1-1 | S | **80%** | **20%** | **44%** | 🟡 **2026-09-21 新規（リポジトリ側は同日に投入済み）** — `vercel.json`（framework=nextjs／リージョン `hnd1`＝東京／`github.silent`）と `.gitignore` への `.vercel/` 追加。残るのは **Vercel 側でのプロジェクト作成・GitHub 接続・環境変数登録**（§S-0d 手順②）。実測（2026-09-21）で **`.vercel/`・`vercel.json`・deploy ワークフローがいずれも不在**＝**ブラウザから触れる環境が1つも無い**ことを確認。⚠️ **2026-09-22 に `1-7`（Vercel WAF）が `vercel firewall` CLI で本番へ投入されており**（本表 `1-7` 行）、その時点で**何らかの形で Vercel プロジェクトは存在していた**はずである。CLI 経由の `vercel link` 等で最小限だけ繋いだ可能性があり、本行が要求する GitHub Import・Preview環境変数登録まで完了しているかは**未確認（要実機再確認）**。数値は 2026-09-21 時点の実測のまま据え置く。`1-2` に「残作業＝Vercel のデプロイ設定」と書かれたまま Issue #50 が `needs-spec` で滞留していたので、**着手できる粒度へ割った**。⚠️ Preview の接続先を決めないと、**プレビュー環境が prod Supabase を指す事故**が起きうる（CLAUDE.md §6.2 と同じ性質の事故） |
| 1-3 | テスト基盤（CI／単体・結合テスト方針）構築 | テストフレームワーク選定、CI（lint/test/build）のパイプライン整備 | なし | M | 100% | 100% | 100% | ✅ **完了（2026-09-15）**：CI は5ジョブ体制（`build-and-test`＝ESLint・型チェック・Jest・ビルド ／ `db-test`＝RLS とガードトリガー ／ `e2e`＝Playwright ／ `secret-scan`＝gitleaks ／ `docs-policy`）。**最後まで欠けていた DB テスト層は、Issue #29（`2-1`）の PR #33 が `supabase/config.toml` と `db-test` ジョブを追加して充足した**（PM が導出した完了条件#22）。`tests/db/helpers/{fixtures,psql}.ts` により、実会員データを参照しない合成フィクスチャで `auth.users` 込みのローカルスタックを回せる（CLAUDE.md §3.2・§7.1）。~~テストコード0件・E2E は `playwright.config.*` 不在で自動スキップ~~ は**解消**（単体13ケース＋E2E スモーク1件）。CI の build-test・E2E ジョブも**空回りから実働へ**変わった。残作業＝**認可・金額計算・個人情報**の3領域の E2E シナリオ（CLAUDE.md §4.4 が必須とする範囲） |
| 1-4     | Cloud Storage for Firebase設定・署名付きURL基盤 | GCSバケット・Cloud Functions・Supabase Edge Functionでの認可判定（Firebase Auth不使用）。**`media_assets` の DDL ＋ RLS は含まない**（2026-09-19 オーナー決定 B ／ `2-1c` の担当） | 1-1, 2-1c | M     |    100% |     70% |     82% | 🟨 **基盤実装（2026-09-22 ／ PR #119）**：署名付きURL発行（V4・PUT10分／GET5分／内部専用2分・`contentType` 固定・`x-goog-content-length-range`）を `src/lib/media/` へ、`POST /api/media/signed-upload-url` と `POST /api/media/{id}/signed-view-url` を Route Handler として実装。Object Finalize の書き戻し（`pending` → `stored`）は Issue #18 ⚠️1 **案A**（経路の疎通まで）に従い `functions/media-object-finalize` へ最小実装し、実処理は `14-1` へ送った。バケット・IAM・ライフサイクル・Cloud Tasks の作成は `scripts/gcp/setup-media-infra.sh`（冪等）。**残るのは GCP コンソール側の実行のみ**（オーナー作業）。上限値は 2026-09-22 オーナー確定＝画像20MB／動画200MB。⚠️ **2026-09-19：依存に `2-1c` を追加**（Issue #18 の推奨 B を採用。完了条件が `media_assets` への `pending` レコード先行作成を含むため、テーブル本体を作る `2-1c` が先行する）。🟢 着手可能（v13 §5.11.2・§9 #9）。**2026-09-19 オーナー決定 B**：`media_assets` のスキーマ ＋ RLS は `2-1c` へ切り出し、本パッケージは GCS バケット・Edge Function・Cloud Functions 経路に専念する。完了条件①「`media_assets` へ `pending` を先行作成」は `2-1c` の完了が前提。**着手順の後退はリスクとして扱わない**（オーナー：どの順番でも進むため） |
| 1-5 | Claude API／Gemini API連携基盤 | APIキー管理と、Claude／Gemini のテキスト呼び出し基盤（`src/lib/ai/`）。~~朝会音声のマルチモーダル入力~~・~~MCP Gateway~~ は**本パッケージの範囲外** | 1-1 | M | 60% | 100% | 84% | ✅ 完了（#55 ／ 2026-09-19）。**2026-09-19 オーナー決定2件で範囲を確定**：①**MCP Gateway は正本 §4 のとおり Phase 3 へ**（`QUESTIONS.md` [2026-09-19] WBS `1-5`：A を採用）／②**朝会音声のマルチモーダル入力は落とす**（B を採用。v13 §9 #63「アプリは録音データを扱わず、入力は文字起こし済みテキストのみ」と整合） |
| **1-6** | **pgvector有効化・RAG基盤**（**2026-09-11 復活**） | Supabase 内で pgvector を有効化し、アプリと浮遊街コンシェルジュが**同一のベクトル空間** `public.knowledge_chunks` を共有する。PII は同一DB内に閉じ、**マスキング後のテキストだけをベクトル化**する | 2-1 | M | 85% | **80%** | **82%** | 🟡 ~~➖ 不要化（2026-08-16、v13 §9 #31）~~ → **復活（2026-09-11 オーナー決定 ／ v13 §9 #31 限定改訂・v1.26.0 ／ `CONSOLIDATED_DECISIONS.md` §17）**。pgvector を捨てた根拠は**無料枠 500MB 制約**であり、2026-09-08 の有料プラン決定で失効していた。✅ **2026-09-20：Phase 1 に含めることがオーナー確定**し、同日 `0100_rag_pgvector_knowledge_chunks.sql`（424行）を **dev Supabase へ適用**（PR #82）。CI でも退避ステップを撤去し、`tests/db/rag-pgvector.test.ts`（受入32件）で毎回検証される。適用で判明した欠陥は `0013_vector_extension_schema.sql`（vector を extensions スキーマへ）と `0101_revoke_service_role_excess_privileges.sql`（service_role の余剰権限を剥がす）で是正済み。残作業は `CONSOLIDATED_DECISIONS.md` §17-6 のうち**検索時スコープ**と **PII 検出の範囲拡張**（`line-rag-bot` 側の移行は別リポジトリ） |
| **1-7** | **★ ネットワーク層の防御（Vercel WAF 設定）**        | IPブロック・カスタムルール・レート制限を Vercel Firewall で設定。**R1 `/api/public/*` 60req/分/IP ／ R2 `/admin/*` 120req/分/IP ／ R3 `/admin/*` 国外ブロック**（値は `非機能要件詳細.md` §2-6a④ ／ 2026-09-22 確定）。日次送信数の監視（同 §2-6a①）も本パッケージ | 1-2 | **S** | **100%** | **90%** | **94%** | ✅ **投入済み（2026-09-22）**：`vercel firewall` で3本を本番へ公開した（`firewall status` ＝ Enabled・3 rules）。**R1 `/api/public/*` 60req/60s/IP・超過で5分 deny**（確定値どおり）／**R3 `/admin/*` を `geo country ≠ JP` で deny**（同）／**R2 は `/admin/*` の log ルールで代替**。⚠️ **確定値のうち R2（120req/分/IP）だけは投入できなかった**：**Hobby プランではレート制限ルールを1本しか作れない**（2本目は `Rate limiting is not available for this plan (401)`。2026-09-22 実測）。唯一の枠は「必ず Vercel を通る」`/api/public/*`（R1）へ割り当てた。⚠️ **残 10% ＝ 日次送信数の監視（§2-6a①）と、超過リクエストが実際に弾かれることの実測**（同節 warning「設定しただけで信用しない」）。⚠️ **R3 は国外から `/admin/*` を落とす**。海外・VPN から管理画面を開く必要が出たら `vercel firewall rules remove "R3 管理画面の国外ブロック"` → `vercel firewall publish` で即外せる。旧記述→ 🟡 **2026-09-03 新規（F-9）**。設計文書に **WAF/FW が0件**であることが判明。**基本機能は全プラン無料・設定のみで再デプロイ不要**（0.5日）。⚠️ ただし `API設計.md` §1 原則①により**トラフィックの一部は設計上 Vercel を迂回する**ため、これ**だけ**では塞げない（→ 2-2 が本命）。~~採否は `QUESTIONS.md` 未回答~~ → **2026-09-05 オーナー回答で「3前線」構成が確定**（Vercel WAF ／ Supabase 側 ／ アプリ層）。設計 40% → **80%**。✅ **2026-09-22：F-7（レート制限の具体値）が確定し、設計 80% → 100% になった**（`非機能要件詳細.md` §2-6a ／ v13 §9 F-7）。**設定するだけの状態**である                                                                                                                                                                                                |

---

## §2. 認証・アカウント基盤（§2, §5.2, §5.9）

| **2-1c** | **★ `media_assets` のスキーマ ＋ RLS**（新規） | メディア資産テーブルの DDL と RLS（投稿者本人 ＋ staff は全件／一般会員・ゲストは公開のもののみ）。`DB物理設計.md` §3-7（L444-480）と §6（L1960・L2011）に定義済みで、仕様の追加を伴わない | 2-1 | S | 100% | 100% | 100% | 🟨 **実装済み・PR #119 レビュー中（2026-09-22）**：`supabase/migrations/0027_media_assets.sql`（DDL ／ RLS ／ GRANT）と受入テスト40件。列構成は **2026-09-22 オーナー決定 C（正本 §7 の全面採用）** で確定（`QUESTIONS.md` の該当論点はクローズ）。`0017`・`0018` が申し送っていた FK 3本（Before/After 写真・商品画像）もここで接続した。マージで ✅ へ。旧記述→ ~~🟢 着手可能（2026-09-19 新規）~~：Issue #18 の調査で、`1-4`（署名付きURL基盤）の完了条件が `media_assets` への `pending` レコード先行作成を含むのに、**そのテーブルを作る担当が WBS 上に存在しない**ことが判明した（`2-1` はロール・アカウントステータスのみ、`14-1` は `1-4` に依存する側、`14-6` は `ai_tags[]` 列のみ）。**2026-09-19 オーナー決定で推奨 B を採用**し、独立パッケージとして切り出す。⚠️ 同テーブルは `DB物理設計.md` §6 で **PII-B**（`geo_location`＝Exif 位置情報・`ai_caption`）に分類されるため**高リスク**。RLS は認可の設計であり、`2-2` と一貫して `2-x` 系に置く |
| #       | 作業パッケージ                               | 概要                                                                                                                                              | 依存       | 規模  |       設計 |  実装 |      総合 | ステータス                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --- | -------: | --: | ------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2-1 | ロール・アカウントステータス スキーマ設計 | `role`（admin/core_member/member/guest/custom）、`member_type`（認可には不使用）、`account_status`、**`member_profiles_private` への個人情報分離**、`auth.users` 結合カラム | 1-1 | S | **100%** | 100% | 100% | ✅ 完了（#29 ／ 2026-09-14）。**2026-09-05〜06 に設計が出揃った**：①**A案＝PII別テーブル分離**（`full_name`／`full_name_kana`／`address`／`hometown`／`birth_ym` を `member_profiles_private` へ）。②アプリ本登録で**メールアドレス必須**。③`auth.users` 結合カラムと「未登録／街人・アプリ未登録／街人・アプリ登録済」の状態区分。④**自己の `role` を誰も変更できない BEFORE UPDATE トリガー**（`DB物理設計.md` §6-6b）。~~⚠️ `会員データモデル_ユーザーテーブル定義.md`・ER図への反映が未了~~ → ✅ **2026-09-13 反映完了**（会員データモデル §5.2a に列ごとの可否8列・`app.operator_id` の申告義務・ブートストラップ、ER図に設計ポイントの警告と `MEMBER_ROLE_CHANGES` のエンティティ説明を追加）。⚠️ **正本 v13 への反映は依然として未了**（§S-5 の残1） |
| **2-1b** | **★ ログイン導線・セッション・ロールガード**（新規） | Supabase Auth の**メールOTP**（6桁コード／送信基盤 Resend）によるログイン画面、`middleware.ts` でのセッション更新、サーバサイドのロールガード。`members.auth_user_id` と `auth.users` の結合、`pre_registered` → `active` の遷移。**★ 運営が個別にアカウント作成リンクを送る経路B**（105名向け／§5.2.6）を含む | 2-1 | M | 100% | 100% | 100% | ✅ **完了（#39 ／ 2026-09-16）**：メールOTP のログイン画面、`middleware.ts` のセッション更新、サーバサイドのロールガード、アクセス制限画面（§5.9.4）、経路B（招待リンク・監査台帳）を実装。ガードトリガーは**改訂していない**（決定B＝システム操作者を申告する形で通した）。。方式は v13 **§5.2.6** で確定（メールOTP ／ **新規に導入する基盤は無い**：公開予約ページが既にメールOTP・送信基盤 Resend は 2026-09-10 決着・LINE は API 連携ゼロで採れない・認可は Supabase 一元化）。**★ 2026-09-15 追加**：メールを持たない105名（親方衆44名を全員含む）向けに、運営が個別にアカウント作成リンクを送る**経路B**を含める。⚠️ 経路Bは**対面の本人確認を伴わない**ため、送信の `admin`/`core_member` 限定・監査記録・有効期限・`active` 会員への送信禁止が**必須要件**。⚠️ **DOM 非表示だけで権限を隠さない**（§5.9.3 サーバサイド認可の併置は必須） |
| **2-1c** | **★ `media_assets` スキーマ ＋ RLS**（新規） | v13 §5.11.2・§7 の `media_assets`（メディア基盤のメタデータの正）を DB へ写像する。DDL は `DB物理設計.md` §3-7、RLS ／ GRANT は §6（L1960・L2011）。公開範囲は**投稿者本人 ＋ staff は全件／一般会員・ゲストは `visibility='公開'` のみ**。用途タグ必須制約（`ck_media_purpose_tags_required`）・論理削除（`deleted_at`）・`ai_processing_status='pending'` の既定値を含む。**PII-B**（`geo_location`＝Exif 位置情報・`ai_caption`） | 2-1 | S | 100% | 100% | 100% | 🟨 **実装済み・PR #119 レビュー中（2026-09-22）**：`supabase/migrations/0027_media_assets.sql`（DDL ／ RLS ／ GRANT）と受入テスト40件。列構成は **2026-09-22 オーナー決定 C（正本 §7 の全面採用）** で確定（`QUESTIONS.md` の該当論点はクローズ）。`0017`・`0018` が申し送っていた FK 3本（Before/After 写真・商品画像）もここで接続した。マージで ✅ へ。旧記述→ ~~🟢 着手可能（2026-09-19 新設）~~：`QUESTIONS.md`「[2026-09-15] `media_assets` テーブル（DDL ＋ RLS）を作る作業パッケージが WBS 上に存在しない」の**オーナー決定 B** により新設（`1-4` からの切り出し）。`1-4`・`14-1`・`14-6` の依存元。**仕様の追加は無い**（DDL は §3-7、RLS は §6 に定義済み）ため設計 100%。⚠️ RLS は `2-2` のテンプレート（列単位 GRANT・`anon` 権限ゼロ）に合わせる。⚠️ `linked_work_log_id` の外部キーは **`work_logs` が未作成のため後続の ALTER へ送る**（`3-1` が `check_ins` の FK を `3-2` へ送ったのと同じ扱い。マイグレーションにコメントで明記する）。`linked_quest_id` は `0007_quests_schema.sql` で `quests` が実在するため本パッケージで張れる |
| 2-2 | Supabase RLSポリシー設計 | テーブル単位のRow Level Security。DOM非表示だけでなくサーバサイド認可を必須とする（§5.9） | 2-1 | M | **85%** | 100% | 94% | ✅ **完了（#35 ／ 2026-09-16）**：DDL が実在する4テーブル（`members`・`member_profiles_private`・`member_role_changes`・`member_invitations`）のRLS ポリシーと GRANT を `0005` で実装。**列単位 GRANT が本体**（§6-2② danger：`role` を列リストから外すことで自力昇格を防ぐ）。`anon` は `ALTER DEFAULT PRIVILEGES` で今後のテーブルにも自動適用。。あわせて `anon` 権限を原則ゼロにする GRANT/REVOKE も本パッケージの範囲（DDL 本体は `DB物理設計.md` §6）。<br>🚨 **2026-09-03 追記（F-9）**：**本パッケージは全 WBS 中で最も重要**。ネットワーク層の防御が存在せず（`WAF`/`FW` は設計文書に0件）、Supabase の IP制限は HTTPS API に効かず、`anon` キーは公開情報であるため、**インターネットと会員370名の個人情報の間に立っているのは RLS 1枚のみ**。ここに1行でも緩みがあれば、それがそのまま全世界からアクセス可能な穴になる。`CLAUDE.md` §4.4 の「認可はテスト必須」を厳格に適用すること |
| 2-3 | ロール別UI表示制御（フロント） | タブ・メニューの動的表示（DOM非描画）、ロール切替プレビューの非表示化、アクセス制限画面 | 2-1, 2-2 | M | **80%** | 100% | 92% | ✅ **完了（2026-09-16）**：v13 §5.9.1 の表示マトリクスを `src/lib/auth/navigation.ts` に**唯一の出所**として持ち、ナビ描画（§5.9.2 DOM ごと非描画）とサーバサイド認可（§5.9.3）の両方がこれを参照する（WBS `2-6` の「ナビ定義と表を同一ロール定義から生成」を先取り）。。他者向け表示は **`nickname`／未設定時は会員番号**（`full_name` へのフォールバックは**禁止**）。~~⚠️ **残ブロック＝親方兼街人をどちらの会員番号で表示するか**が未回答~~ → ✅ **解消（2026-09-10：街人番号で表示する）**。✅ **2026-09-22：#58（ロール表示名）も決着**（**改称しない**）。残るは §9 #54（ナビ要件＝`2-6`）と、表示名の変換表の設置（`2-5`）、`nickname` 必須化（`2-7`）である |
| 2-4 | 個人情報収集・保持（宿泊法対応） | 氏名・カナ・住所・前泊地等の収集・保持。プライバシーポリシー・利用規約側の反映 | 2-1 | S | **85%** | 100% | 94% | ✅ 完了（#58 ／ 2026-09-19）。⚠️ カラムレベル暗号化の採否は未決だが**非ブロッカー**（「Phase 1 不採用」提案であり、A承認なら実装差分ゼロ）。✅ **2026-09-20：宿泊者名簿の収集経路が確定した** — テーブル（`lodging_register_entries`・3年保存・カスケード削除の対象外／`DB物理設計.md` §3-13）に加え、**収集は現地チェックイン時の店員タブレット操作（`POST /api/checkins/{id}/lodging-register`・画面ID A1）**と定まり、**実装は `3-2` の範囲**になった（v13 §5.2.7 新設・v1.29.0）。公開予約API・アプリ内予約のリクエストには追加しない |
| **2-5** | **★ 利用者向けロール表示名（変換表の設置）**（新規） | ~~表示名テーブルを1箇所に持ちUI・通知文言をこれ経由に。§5.9.4 を「運営業務専用です」＋現在ロール／必要権限の表示へ~~ → **呼称は据え置くため（#58 決着）、残る作業は「内部識別子 → 表示名」の変換表を1箇所に置き、UI をこれ経由にすることだけ**。§5.9.4 の文言は v1.21.0 で反映済み。RLS注記は本番非表示。内部識別子は不変 | 2-3 | S | **100%** | **100%** | **100%** | ✅ **実装完了（2026-09-22）**：`src/lib/auth/role-labels.ts` を唯一の出所にし、`AccessDenied` と ロール切替プレビュー（C11）がこれを参照する形へ変えた。**同じ表が2箇所に重複していた**（両画面が自前の `Record<Role, string>` を持っていた）のを解消し、`guard.ts` の「必要な権限」の文言も同じ表から組み立てる。★ 呼称は #58 の決着どおり**据え置き**（管理者／コアメンバー）。変換表を置くのは、**いま一致していることと今後も一致し続けることは別**だからである。試験は「表が1箇所であること」を実ファイルの走査で固定した（`tests/role-labels.test.ts`）。旧記述→  🟢 ~~🔴 **ブロック中**：#58 の最終ラベルが未確定~~ → **着手可能（2026-09-22）**：**#58 が決着した（改称しない＝「管理者」「コアメンバー」のまま）。** ⚠️ **それでも変換表は置く** — いま内部呼称と表示名が一致していても、変換を経由する構造が無ければ **Phase 2 で改称する判断になったときに全画面を触ることになる**（v13 §2・§9 #58）                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **2-6** | **★ 管理系メニューの到達性（ナビ情報設計）**（新規）         | 常時ナビ7項目上限、運営専用は「⚙️ 管理」へグルーピング、**横スクロール依存禁止**、ナビ定義と §5.9.1 表を同一ロール定義から生成                                                                         | 2-3      | S〜M |      75% |  **90%** |     **84%** | 🟨 **穴を塞いだ（2026-09-21）**：`AREAS`（`src/lib/auth/navigation.ts`）が宣言する11経路のうち10経路が404だった状態を解消（コミット `c81a9f8`）。`src/app/admin/preview/page.tsx`（C11）が `AREAS`／`visibleAreasFor()` を直接読む到達性プレビュー表を実装。⚠️ 実装できない2経路（`/staff/eumo`・`/upload`）は理由表示の暫定画面のまま（`5-5`・`14-1`／`2-1c` 待ち） |
| **2-1d** | **★ 招待（経路B）のコード方式化**（新規） | `admin.auth.admin.inviteUserByEmail()`（リンク方式）を**やめる**。招待メールには**ログイン画面の案内だけ**を載せ、会員は**通常のメールOTP（6桁コード）**でログインする。`auth.users` 生成後、**招待台帳の宛先と一致することを確認したうえで** `members.auth_user_id` と結合する。`member_invitations` の台帳・有効期限・`active` への送信禁止・監査記録は**そのまま維持** | 2-1b | S | **100%** | **100%** | **100%** | ✅ **完了（2026-09-22）**：`inviteUserByEmail()` を廃止し、①台帳へ記録 → ②**ログイン画面の案内メール**（`src/lib/mail/resend.ts` ＝ アプリ側の HTTP API）の2段にした。続く手順3〜5はログイン側で成立させる：**招待台帳に未消費・期限内の行があるアドレスにだけ `auth.users` の新規作成を許す**（`requestLoginCode` の `shouldCreateUser`）→ `bindAuthUserToMember()` が台帳の宛先と突合して結合。**リンクは全経路から消えた**（案内メールに載る URL はログイン画面だけ）。受入テスト：招待11件（リンク・トークンを本文へ載せないことを含む）＋ ログインのゲート5件。⚠️ **`RESEND_API_KEY` が未設定のため実際の送信はまだできない**（未設定時は `mail_not_configured` を返し、**台帳の行は残す**ので会員が自分でログインを始めれば成立する）。鍵の登録は `1-1e` と同じオーナー作業 |
| **2-7** | **★ ニックネームの必須化 ＆ 親方会員番号の採番**（新規） | 本登録時に `nickname` を**必須入力**にする。既存370名（未設定）には**次回ログイン時に設定を求める**。設定されるまでの間は**会員番号で表示**する（`full_name` へのフォールバックは**禁止**）。あわせて**親方会員番号の採番**を実装する（**一意であればよく、桁数・起点・接頭辞は実装側の裁量**。`legacy_member_no` の `10xxx`／`20xxx` と識別できることだけが条件） | 2-1, 2-3 | S | **100%** | **100%** | **100%** | ✅ **完了（2026-09-22）**：`0029_oyakata_member_no_and_nickname.sql` で **`members.oyakata_member_no`**（`OYA-` ＋3桁・採番 `next_oyakata_member_no()`・起点45＝1〜44 は移行の予約枠）を新設し、`v_member_public.display_name` を **nickname → 街人番号 → 親方会員番号 → `member_id` 先頭8文字**の4段へ差し替えた。ニックネームの必須化は**アプリ層に置いた**（`src/lib/members/nickname.ts` の入力検査を唯一の出所にし、既存370名向けに**ログイン直後の設定画面 `/nickname`** を置いた）。DB 側は**空白だけの値を拒否する CHECK 1本**まで — ⚠️ 初版は「`imported_from IS NULL`（アプリから作る会員）は必須」の CHECK も入れていたが、**`imported_from` を「アプリ経由で作られたか」の代理にすると、サーバ側で表示名を持たない会員行を正当に作る経路（k匿名コホートの試験等）を巻き添えで落とす**ことが CI で判明したため外した（決定どおり**強制はしない**。「あとで設定する」を残し、未設定の間は会員番号で表示）。★ **採番は専用列**にした：オーナー決定⑤は方式を問わないが、2026-09-10 決定「両方の番号を持つ会員は街人番号で表示」＋ v13 §9 #26（親方兼街人は1レコード）より、**1行が2つの番号を同時に持てないと決定が実装不能**になるため、接頭辞1列方式は採れない。★ 仕様に明文が無かった1点として **`街人#`/`親方#`/`ゲスト#` で始まるニックネームを拒否**した（他人の会員番号を自分の表示名に掲げられるため）。受入テスト：DB 12件 ＋ 単体10件（初版の CHECK を外した経緯も試験で固定した）。⚠️ **親方衆44名への番号投入は `10-1`**、**本登録フォームは `12-1`** の範囲 |

---

## §3. 予約・チェックイン・宿泊管理（§5.2, §5.6.9, §5.8.5）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 3-1 | 部屋台帳・部屋割当（`rooms`/`room_assignments`） | 部屋マスタ（`capacity`保持）、割当履歴管理、部屋移動対応。**2026-08-25：`room_name_snapshot`（割当時点の部屋名）の追加を検討**（#50・要確認） | 1-1 | M | 85% | 100% | 94% | ✅ **完了（#31 ／ 2026-09-16）**：`rooms`・`room_assignments` のスキーマ・制約・RLS・GRANT・初期8行を `0006` で実装。スコープは**DB層まで**（オーナー決定）。`check_ins` を参照する外部キーと会員向けポリシー2件は `3-2` へ送った（後続が ALTER で足せる形にしてある）。。モックに部屋割当・部屋移動モーダル実在 |
| 3-2 | チェックイン／チェックアウト操作（QR） | 現地QR読み取り or 画面タップでの状態切替、AIウェルカムメッセージ。**2026-08-29：街人照合・初回キャッシュバックの3バッジ表示を追加**（§5.10.8）。**2026-09-20：旅館業法の宿泊者名簿の収集を追加**（`POST /api/checkins/{id}/lodging-register`・画面ID A1・既存会員は `member_profiles_private` の現在値を初期表示しスナップショット保存／v13 §5.2.7） | 2-1, 3-1 | M | **85%** | **85%** | **85%** | 🟨 **チェックイン／チェックアウト操作を実装（2026-09-22）**：`/staff/checkins`（画面ID A1）。当日の到着・滞在中・本日退館を1枚に出し、`pre_registered`／`confirmed` → `staying` → `checked_out` の遷移を操作できる。★ **遷移の向きは純関数（`src/lib/lodging/checkin-ops.ts` ／ 試験15件）で縛る** — DB の CHECK は値域しか縛らないため、「退館済みからの再開」を止める場所が他に無い（再開すると通算来訪回数が1回に潰れ、初回来訪の判定が狂う）。★ **書き込みは `eq("status", expected)` 付き**で、別端末が先に操作していたら何も起きない。★ **初回来訪キャッシュバックの自動起票（v13 §5.10.8 ②）をここへ接続**した。判定は顧客管理画面の手動起票と同じ関数を通る（条件を2箇所に書かない）。起票に失敗してもチェックインは巻き戻さない（入館を給付の都合で止めない）。★ 板に**宿泊者名簿の登録有無**を出す（v13 §5.2.7 の義務を入館の流れで拾えるように）。⚠️ **残＝QR 導線**（本人特定のQRは発行・読み取り・失効の基盤が要る）と同伴者の登録UI。旧記述→  🟨 **宿泊者名簿UIまで実装（PR #122 ／ 2026-09-22）**：`POST /api/checkins/{id}/lodging-register`（`src/app/api/checkins/[id]/lodging-register/route.ts`）・プレフィル/確定ロジック（`src/lib/lodging/register.ts`）・店員向け画面（`src/app/admin/checkins/[id]/lodging-register/`）を実装した。既存会員は `member_profiles_private` と紐づく `check_ins` 予約からプレフィルするが、**「氏名・カナを本人へ提示し確認した」申告が無ければ確定できない**（アプリ層・サーバ層の二重検証／v13 §5.2.7「本人の自己申告のみでは確定させない」）。`0028` で `lodging_register_entries.checkin_id` → `check_ins` の FK も回収した（`0010` からの先送り分）。⚠️ 同伴者（`is_representative=false`）の登録・訂正UIは対象外（代表者のみ）。⚠️ checkin_id を特定する一覧・QR導線がまだ無いため、現状は checkin_id が既知である前提で URL を直接開く運用。**残るのは状態切替UI・QR読み取り・AIウェルカムメッセージ・3バッジ**であり、下記の未回答論点はそこにかかる。バッジ部分は 12-4 と一体で、画面設計に未反映。⚠️ 完了条件と `12-4` との範囲の切れ目（A/B/C）は **`QUESTIONS.md` [2026-09-19] WBS `3-2` が未回答**。名簿収集は A/B/C のいずれでも `3-2` に残る |
| 3-3 | 予約キャンセル・ノーショー処理 | 顧客管理画面からの論理削除、部屋割当の解放 | 3-1 | S | 90% | **95%** | **93%** | ✅ **画面まで実装（2026-09-22）**：チェックイン板（`/staff/checkins`）にキャンセル／ノーショーの操作を置いた。種別（会員都合／ノーショー／運営都合）と理由が**両方必須**（v13 §5.2.2）。★ **入館前の予約にだけ出す** — 途中退去は「退館」であってキャンセルではない。キャンセルにすると滞在の記録が通算来訪回数・宿泊履歴（§5.6.8）から抜け落ちる。処理の本体（`check_ins` の論理削除 ＋ 部屋の解放を必ず一緒に行う）は2026-09-21 実装の `cancelStay()` がそのまま担う。旧記述→ ~~🟨 サーバ層まで実装（2026-09-21）~~：`0014` が `check_ins` にキャンセル4列＋`chk_check_ins_cancel_complete`（理由なし取消を DDL で禁止）を入れ、`src/lib/lodging/cancellation.ts` が**論理削除と部屋割当の解放を必ず対で行う**。残るのは顧客管理画面のモーダル（`8-1` と同時に作る） |
| 3-4 | 宿泊券管理・消費/付与ロジック | `stay_ticket_transactions`、チェックアウト時消費、`membership_plans`マスタ参照（値の直書き禁止） | 2-1, 3-2 | M | 85% | **55%** | **67%** | 🟨 **DB層まで実装（2026-09-21）**：`0016` が `membership_plans`（4プラン）＋`stay_ticket_transactions`＋`stay_ticket_balance()` を入れた。★ **残高カラムを持たずイベントソーシング**（v13 §5.8.5）。`uq_stay_tx_consume_per_checkin` で二重消費を DDL で禁止。残るはチェックアウト操作からの消費呼び出し（`3-2` の画面側） |
| ~~3-5~~ | ~~宿泊予約フォーム連携（AS-IS→TO-BE）~~ | ~~Googleフォーム送信をトリガーに`pre_registered`予約を自動生成~~ | ― | ― | ― | ― | ― | ➖ **廃止（2026-08-23／v13 §9 #46）**：Webhook・署名検証・べき等性・再送台帳・Apps Script改修が**すべて不要化**。3-5b へ差し替え |
| **3-5b** | **★ 公開予約ページ `/reserve`** | **ログイン不要**で予約が完結するWebページ。日程 → 宿泊形態 → 人数 → 連絡先＋**メールOTP** → 交通手段（送迎は課金）→ **カフェ事前予約注文**（任意）→ 同意 → 備考。**2026-09-01：「街人ですか？」の自己申告を再導入**（料金・権限判定には一切使わず、チェックイン時の照合の参考フラグ限定） | 3-1, 3-8, 3-9, 6-4 | **L** | 85% | **75%** | **79%** | 🟨 **主要フローを実装（2026-09-21）**：`src/app/reserve/page.tsx`（コミット `a5836b4`）。日程・宿泊形態・人数・連絡先＋メールOTP（`0022_reservation_otps.sql`・`src/lib/reservations/otp.ts`）・交通手段・同意・備考の一連を実装し、料金は `accommodation_rates`（`0021`）からサーバが引く（フォームから受け取らない）。名寄せキーは `member_identifiers`（`0025`）。OTP 試行制限（`OTP_MAX_ATTEMPTS`）は実装済み。⚠️ **Turnstile・一般的なレート制限は未実装**（コード内に該当箇所なし。総当たり・spam送信への防御が残課題） |
| **3-5c** | **★ カフェ事前予約注文（`meal_reservations`）** | 滞在日別・朝/昼/夜の事前予約（**任意**）。提供操作時に `orders` へ変換。管理画面に**日別食数サマリー**（仕込み数量） | 3-5b, 6-4, 6-5 | M | 75% | **45%** | **57%** | 🟨 **DB層まで実装（2026-09-21）**：`0019` の `meal_reservations`。★ **予約時点では `orders` を作らない**（「予約済・未来訪」という第3の状態が会計×提供の2軸に混ざるため／v13 §5.4.1b）。日別食数サマリーは `ix_meal_res_date`（未キャンセル・未変換のみ）で引く。残るは予約画面と変換トリガ（`6-5`） |
| 3-6 | 管理者画面「宿泊予定カレンダー」 | TimeTree代替。確定予約の日付・部屋タイプ・人数の一覧化。**日別食数サマリーを併記** | 3-1, 3-5b, 3-5c | M | 80% | **85%** | **83%** | 🟨 **実装済み（2026-09-21）**：`src/app/staff/calendar/page.tsx`（コミット `03ffa2a`）。月切替・`v_room_availability` 参照・人数表示・「要確認」フラグを実装。実名は出さずニックネーム／会員番号のみ（CLAUDE.md §7.1）。⚠️ **日別食数サマリーは未確認**（`3-5c` の変換トリガ待ち）。⚠️ 最後の1枠を取る同時競合は防げていない（DB側の排他が無いため。取りこぼしは本画面で気づける設計） |
| 3-7 | アプリ内からの宿泊予約 | 日程 → 宿泊形態タブ → 残枠表示 → 人数 → 備考 → 確認。既知情報は自動補完 | 3-5b | **S（縮小）** | 85% | **35%** | **55%** | 🟨 **共通基盤は実装済み・差分は未着手（2026-09-21）**：`src/app/reservations/page.tsx`（コミット `03ffa2a`）で本人の宿泊予定・履歴一覧＋予約フォーム（`ReservationForm`）を実装。日程・宿泊形態・残枠・人数・備考の基本フローは動く。⚠️ **本タスク本来のスコープ（ログイン時の自動補完・会員料金・宿泊券充当）はコードに未確認**（`ReservationForm.tsx`・`reservations/`・`lodging/` 配下に該当ロジックなし） |
| 3-8 | 宿泊枠の残数算出＋カレンダー | `v_room_availability` ビュー、本人向けカレンダー（A12）、運営向けカレンダー（C10）。**算出元は `check_ins`（形態単位）**、`allocation_mode`（人数枠型／棟貸型）の2モード分岐 | 3-1 | M | **95%** | **90%** | **92%** | 🟨 **カレンダー2画面まで実装（2026-09-21）**：ビュー（`0014`・`0015`・`src/lib/lodging/availability.ts`）に加え、運営向けカレンダー（C10 ／ `3-6` の `/staff/calendar`）と本人向け予定・履歴（A12 ／ `3-7` の `/reservations`）がいずれも実装済み。⚠️ **副次的に `0006` の収容枠の誤りを是正**（キャンプサイト 4→10 ／ 車中泊 2→30 ＝ 合計66。従来は分母が32で**予約できる枠を満室表示**していた）。残るのは同時アクセス時の排他制御（`3-6` の note 参照） |
| 3-9 | 宿泊料金マスタ | `accommodation_rates`（宿泊形態 × 会員区分 × 適用期間）とその管理画面（C13） | 1-2 | S | **85%** | **70%** | **76%** | 🟨 **#55 完全解消・実装済み（2026-09-22）**：`0021_accommodation_rates.sql` を追加。`room_type` を CHECK ではなく `accommodation_types` への外部キーにして旧名称の混入を構造的に防いだ（コミット `1fceecb`）。料金選択ロジック（`src/lib/lodging/rates.ts`。現行行を優先しない）と管理画面（`/admin/master` の宿泊料金登録・一覧）も実装済み。初期行は入れていない（v13 §5.4.2②「価格を直書きしない」。管理者がC13から入力）。✅ **正本側の旧名称残存（§4・§5.2.4・§5.2.5・§7）も解消**（`QUESTIONS.md` [2026-08-26] 選択肢Aを採用し一括置換／v13 v1.29.2）。実装・ドキュメントとも #55 は解消済み |
| **3-10** | **★ 滞在中の宿泊形態・部屋・日程・人数の変更**（新規） | 変更後の残枠を再判定し満室なら拒否。宿泊費は**泊単位でその夜の形態の単価**を積む。理由入力必須＋編集履歴。`rooms.type` を書き換えない実装ガード | 3-1, 3-8, 3-9, 8-1 | **M** | 30% | 0% | 12% | 🟡 **要件のみ**（v13 §5.6.9・#50／2026-08-25 新設）。詳細設計・モックとも未整備 |

---

## §4. 朝会録音・議事録自動生成 ＆ クエスト自動起案（§5.1）

| #   | 作業パッケージ                                                   | 概要                                                                                     | 依存                             | 規模  |  設計 |  実装 |  総合 | ステータス                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------ | --- | --: | --: | --: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 4-1 | ~~朝会録音UI~~ → **朝会テキスト投入UI** | ~~管理者PC/タブレットからの録音開始・終了、音声ファイルの自動送信~~ → **外部ソフトで文字起こし済みのテキストを、アプリのUI画面へコピー＆ペーストで投入する** | ~~1-4~~ ~~なし~~ **2-1, 2-1b** | S | 85% | 100% | 94% | ✅ 完了（#23 ／ 2026-09-19）。**2026-09-15 追記**：`2-1`（スキーマ）は #29／PR #33 で完了したが、Issue #23 の計画フェーズが「`src/app/` にログイン導線・`middleware.ts`・ロールガードが無い」と再度停止した。完了条件4・5（`member`/`guest` のサーバサイド拒否・議事録の他ロールからの遮断）は**ログイン済みであることが前提**のため、`2-1b` を依存に追加した。**2026-09-13 オーナー決定**：完了条件4・5（サーバサイド認可・PII遮断）は `public.members` を要するが未作成のため、依存に `2-1` を戻した（自律ループ Issue #23 の計画フェーズが検出）。**2026-09-05 オーナー決定でスコープ変更**：アプリは音声を扱わない。録音・自動送信・GCS 経由の受け渡しは**すべて不要化**した。⚠️ 依存から `1-4`（署名付きURL基盤）が外れ**着手条件が軽くなった**。モック②は録音前提のため要改訂 |
| 4-2 | ~~Gemini連携（文字起こし＋議事録＋クエスト候補抽出）~~ → **議事録の構造化 ＋ クエスト候補起案** | ~~音声を統一メディア基盤（GCS）経由でGeminiへ直接投入（マルチモーダル、話者分離は不要）~~ → **投入済みテキストから構造化議事録とクエスト候補を生成する** | ~~1-4, 1-5, 4-1~~ **1-5, 4-1** | M   | **80%** |  **85%** | **83%** | 🟨 **画面まで実装（2026-09-22）**：`src/app/admin/morning-meetings/structuring-actions.ts`（Server Action）＋ `src/components/morning-meetings/MinutesBoard.tsx`（投入済み議事録の一覧・「AIで構造化する」・進捗表示）。保存は `summary_text` と `extracted_quest_candidates` の**1回の UPDATE**（片方だけ成功した議事録を作らない）。ナレッジ候補は保存先が `1-6`／`9-1` 待ちのため `summary_text` 末尾へ `■ナレッジ候補（未登録）` として残す（`QUESTIONS.md`「[2026-09-22] 朝会の「ナレッジ候補」の恒久的な保存先」）。⚠️ **残＝非機能 P3 の「バックグラウンド処理」**（Cloud Tasks への載せ替え。GCP キュー作成がオーナー作業のため同期実行で先行した＝`QUESTIONS.md`「[2026-09-20] WBS `4-2` …の完了条件」の選択肢 C）。⛔ **PII-A 本文の外部送信のマスキング要否はオーナー判断待ち**（画面に「本文はそのままAIへ送られます」と明示済み）。旧記述→ ~~🟨 生成ロジック実装（2026-09-21）~~：`src/lib/morning-meetings/structure.ts`。★ 議事録（決定／共有／注意）・クエスト候補・ナレッジ候補を **1回の呼び出しで同時生成**（v13 §5.7.4 ② が別呼び出しを禁じている。P3 の60秒を圧迫するため）。モデルは **Gemini**（朝会のみ Gemini 継続）。`0020` が `summary_text` への UPDATE 権限と `extracted_quest_candidates` 列を追加。`TextAiClient` を引数で受け取る形にしてテストから実 API を叩かない。残るは Server Action と進捗表示。旧記述→ ~~非同期ジョブ実行基盤が未決~~ → **2026-09-10 に解決**（Cloud Tasks ＋ Cloud Scheduler）。ステータス欄の追随漏れを 2026-09-19 に修正。**2026-09-05 スコープ変更**：音声マルチモーダルを不要化したため、Gemini を音声基盤として使う前提は消えた（朝会以外の用途は Claude）。⚠️ **非機能 P3 の起点も「録音終了から60秒以内」→「テキスト投入から60秒以内」へ要改訂**（v13 §8・未反映）                                                                                                                                                                                                                             |
| 4-3 | クエスト候補プレビュー・ワンタップ起案                                       | 抽出候補の補正・承認、クエストボードへの即時反映                                                               | 4-2, 5-1                       | S   | 85% |  **95%** | **91%** | ✅ **実装完了（2026-09-22）**：補正フォーム画面（`src/components/morning-meetings/QuestCandidateCard.tsx`）と公開・却下の Server Action。★ 公開は **`quests` への INSERT が先・候補の `published` 化が後**（逆にすると「公開済みと記録されているのにボードに無い候補」が残る）。二重起案は候補の状態が `pending` でなければ拒否。報酬額の欄は**空のまま公開でき**、空なら `reward_uii` は未設定のままボードに載る（AI の推定額を初期値にしない）。旧記述→ ~~🟨 ロジック実装（2026-09-21）~~：`src/lib/morning-meetings/quest-candidates.ts`（補正・却下・`quests` 行への写し）。★ `guest_allowed` は候補から**導出せず常に false**（v13 §5.10.6）、報酬額も**AIに決めさせない**。候補の保持先は `0020` が足した `extracted_quest_candidates`。残るは補正フォーム画面                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## §5. クエスト管理・受注承認・完了報告フロー（§5.3）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 5-1 | クエストボード（一覧・集約表示） | 手動登録＋朝会自動抽出クエストの統合表示、`guest_allowed`フラグによるゲスト開放判定 | 2-1 | M | 90% | 100% | 96% | ✅ 完了（#53 ／ 2026-09-19）。表示規則は①未設定時＝会員番号・②親方兼街人＝**街人番号**（2026-09-10 回答）で**決着済み**。⚠️ **2026-09-20 追補（作業ツリーに未コミット）**：`quests` への `GRANT SELECT` が全列に与えられており、**PostgREST 直アクセスで報酬額・指示内容の列マスクを迂回できていた**。`0012_quest_reward_column_lockdown.sql` で列単位GRANTへ是正し、`v_quest_board` を `security_invoker = false` へ変更。あわせて**コア・管理者限定の `core_only_reward` フラグを新設**した（v13 §5.10.6・`CONSOLIDATED_DECISIONS.md` §2 ／ 2026-09-20 オーナー決定） |
| 5-2 | 受注申請・運営審査・実行指示 | 「申請中」ステータス管理、運営による審査・指示 | 5-1 | S | 90% | **65%** | **75%** | 🟨 **サーバ層まで実装（2026-09-21）**：`0017` が `quest_applications`（正本 §7 に合わせ **`指示済み` を追加**。派生設計の CHECK には無かった）＋実行指示3列（いつ・どこで・何を）＋遷移ガードのトリガーを入れた。**資格ゲート（`required_certification`）を `canApplyToQuest()` に条件1つで追加**（判定点を増やさない）。範囲の切れ目 A/B/C はどれでも本 DDL は変わらない。残るは指示出しモーダル |
| 5-3 | 完了報告（Before/After写真・作業時間） | 添付機能（1-4のストレージ基盤に依存） | 1-4, 5-2 | S | 90% | **95%** | **93%** | ✅ **画面まで実装（2026-09-22）**：作業報告（画面ID A4 ／ `/reports`）。自分の受注を「報告できるもの」と「それ以外」に分け、**作業前・作業後の写真つき**で完了報告を出せる。★ 写真は**先にストレージへ送って `media_id` だけを提出する**（v13 §5.11.2 不可侵ルール1）。Server Action へファイルを載せると、避けたかった「アプリのサーバが実体を受ける」形になる。`1-4`・`14-2` で入れた送信処理を `uploadFileToStorage()` として共有した（画面ごとに書かない）。★ **写真の必須はアプリ層が担保する**（`0017` の申し送りどおり。DDL を NOT NULL へ締めると移行期の写真なし報告が更新できなくなる）。判定は `work-log-report.ts`（試験14件）。★ **申請中のままでは報告できない** — 指示の前に作業が終わっている状態を作らない（§5.3-3）。★ 差戻し理由を報告一覧に必ず出す（`0017` は再提出を別行として積むため、理由が見えないと同じ形でもう一度出すことになる）。旧記述→ ~~🟨 DB層まで実装（2026-09-21）~~：`0017` の `work_logs`（差戻し後の再提出を**別行として積む**）。写真列は `media_assets` 未作成のため **FK を張らず値だけ持つ**（`0006` の `place_id` と同じ作法。ALTER 文をコメントで残した）。**NOT NULL にしていない** — 縛ると `1-4` 完了まで `5-3` が死ぬため、「写真必須」はアプリ層で担保する |
| 5-4 | 日次承認／差戻しサイクル | 1日おきの運営承認画面 | 5-3 | S | 90% | **55%** | **69%** | 🟨 **判定ロジック実装（2026-09-21）**：`src/lib/quests/review.ts`＋`0017` のトリガー。差戻しは**理由必須を DDL で強制**（`ck_worklog_rejection_reason`。空白文字も弾く）。作業待ち行列は `ix_worklog_pending` で引く。残るは承認ダッシュボード画面（B5） |
| 5-5 | Uii支払・XP／貢献バッジ付与 | 承認完了時のUii額表示、Eumo送付用リンク発行 | 5-4, 7-1 | S | 80% | **85%** | **83%** | ✅ **実装完了（2026-09-22）**：**最終承認と同時に Eumo 給付（`quest_reward`）を自動起票する**（`src/app/staff/quests/actions.ts`）。起票を運営の別操作にすると「承認したのに報酬の依頼が立っていない報告」が積み上がる。★ **コアメンバー確認済の段階では起こさない** — 最終承認を admin に限った意味が消えるため（v13 §5.3.2）。★ **報酬額が未設定のクエストでは起票せず、画面にその旨を返す**（朝会から自動起案した候補は報酬未設定でボードに載る）。二重起票は**作業報告単位**で防ぐ（差戻し後の再提出は別の報告であり、別の給付になる）。旧記述→ ~~🟨 DB層のみ実装（2026-09-21）~~ | 🟨 **DB層のみ実装（2026-09-21）**：`0023_eumo_grants.sql`（`grant_type`／`purpose` 列を含む・コミット `1fceecb`）。⚠️ `src/app/staff/eumo/page.tsx` は依然プレースホルダで、その文言は「`eumo_grants` テーブルが存在しない」と書いているが**これは現在誤り**（テーブルは存在する）。UI・送付/受領操作のロジックは未着手 |
| 5-6 | クエスト承認の二段階化 | `コアメンバー確認済` ステージ新設。確認者・承認者を別カラムで保持。**最終承認は admin のみ**（~~RLSで制限~~ → **トリガーで制限**） | 5-2, 2-3 | M | **90%** | **70%** | **78%** | 🟨 **ガードまで実装（2026-09-21）**：`0017` の `work_logs_guard_approval()`。⚠️ **RLS では守れない**ことが判明 — `UPDATE ... USING (is_staff())` だと core_member が `approved_by` を書けてしまい、列単位 GRANT でも防げない（core と admin は同じ `authenticated` ロール）。`service_role` の迂回も塞ぐ必要があるためトリガーで実装した。確認スキップは DB 側で `review_skipped` を自動記録。`work_log_reviews` は**本人に見せない**（評価コメント） |
| **5-7** | **Eumo給付の送付・受領追跡**（旧 5-5・**採番衝突のため改番**） | `eumo_grants` と給付一覧画面（B10 / C12）。送付操作と受領確認操作を分離。**2026-09-01 拡張：`grant_type`（quest_reward/first_visit_cashback/registration_cashback/manual）・`purpose` を追加、手動起票を許可、受領確認を「本人の受領報告」でも可能に**（→ 8-4 マイログにも表示） | 5-4, 3-2, 12-4, 8-4 | **M→L** | **80%** | **80%** | **80%** | 🟨 **一覧・送付・受領追跡まで実装（2026-09-22）**：`/staff/eumo`（画面ID B10）を `NotYetAvailable` から実画面へ。発行依頼／発行済み・未受領／送付失敗／受領済みを**状態ごとに分けて**出す（運営の仕事が状態ごとに違うため。混ぜると両方が滞る）。操作は送付記録・受領確認・送付失敗・手動起票の4本（`src/lib/eumo/`）。★ **呼称（発行依頼／発行済み・未受領／受領済み）は画面だけで読み替える** — 保存値を用語に合わせて作り直すと既存行と突き合わせられない。★ **送付から14日を超えた未受領を行の見た目で警告**する（v13 §5.3.1）。★ 過去発行分は**個別の手動起票**（#59 論点1 ／ 一括インポートは作らない）。⚠️ 残＝本人向け表示（`8-4` マイログの未受領表示）。旧記述→ ~~🟢 ブロック完全解除（2026-09-22）~~：**#59 が全面決着**。論点1（過去発行分の取り込み）＝個別手動起票（`eumo_grants.source='manual'`）。論点2（既存街人の受取済み判定）＝街人リストの列による一括判定は作らず、§5.10.8③の「要確認」フォールバックへ一本化（`QUESTIONS.md` [2026-09-01]）。`0023_eumo_grants.sql`（`grant_type`／`purpose` 含む）は実装済み（2026-09-21）。残るは給付一覧画面（B10/C12）と送付・受領操作UI |
| **5-8** | **★ 買い物リスト（ほしいものリスト）の登録・一覧・ステータス管理**（新規 ／ v13 §5.12.1・§5.12.2） | `shopping_list_items` と一覧画面（C14）＋**ダッシュボード（A2／C1）からの登録モーダル**。**必須は品名のみ**。登録時の**重複検知**（未購入品との部分一致）と**相乗り（`requesters[]`）**。ステータスは `希望 → 買う → クエスト化済 → 購入済` ／ `見送り`（**見送りは理由必須**） | 2-1, 2-3 | S | **90%** | **80%** | **84%** | 🟨 **実装済み（2026-09-22）**：`0030_shopping_list_items.sql`（DDL ＋ RLS ＋ ガードトリガー ＋ 相乗り RPC。マイグレーション連番は main 合流時に `0028`→`0030` へ付け替え。`0028`は`lodging_register_check_in_fk`、`0029`は`oyakata_member_no_and_nickname`が使用済み）、`src/lib/shopping/{status,duplicate,fetch-items}.ts`、`/shopping`（A13・C14 を1画面に統合）。**ゲストの登録不可はナビ・Server Action・RLS/トリガーの3箇所**に置いた。残り：①**DB 受入テスト（16件）が未実行** — ローカルに psql が無く CI 実行分 ②**ダッシュボード（A2 ホーム／C1）からのクイックアクション**は A2・C1 の実装待ち（`13-1`） |
| **5-9** | **★ 買い物リストからの買い出しクエスト化 ＋ 立替実費の記録**（新規 ／ v13 §5.12.3・§5.12.4） | 複数品目を選択 →**買い出しクエストを1件起案**（`quests.origin_type = 'shopping_list'`・`execution_mode = remote`。**既存の `chk_quests_origin_type` を張り直す**必要がある）。**品目1つ＝1クエストにしない**。承認完了で紐づく品目を一括 `購入済`、**部分購入は残りを `買う` へ戻す**。**立替実費は扱わない**（`advance_*` カラムを作らない。証憑が要る場合は完了報告の After 写真にレシートを添付） | 5-8, 5-1, 5-6 | S | **90%** | **75%** | **81%** | 🟨 **実装済み（2026-09-22）**：`src/lib/shopping/quest-draft.ts` ＋ `/shopping` の起案フォーム。**複数品目 → クエスト1件**（`origin_type = 'shopping_list'`・`execution_mode = 'remote'`）。`chk_quests_origin_type` を `0030`（旧 `0028`）で張り直した。**立替実費は扱わない**（#65③ 決着で金額の器が丸ごと落ちた）。残り：①**クエスト INSERT と品目 UPDATE が非トランザクション**（PostgREST の2往復。後段が落ちても品目は `買う` のまま残り再実行できる側へ倒してある）②**承認完了の検知による一括『購入済』の自動化は未実装**（現状は運営の手動操作。`5-4` の承認画面と接続する） |

> [!important] 5-8・5-9 の Phase 1 入りに伴い、分母は **61 → 63** になる（2026-09-22 オーナー確定）
> 2026-09-08 オーナー確定の「Phase 1 は **61パッケージ全部**をリリースする」は本2件の起票前の数字である。
> **v13 §9 #65① の決着により、両パッケージは Phase 1 に入る**ため、以後の完了判定は **63パッケージ**で行う。
> - 分母が2件増えるため、**全体進捗率は見かけ上わずかに下がる**（作業が増えたのだから正しい挙動である）
> - §S-1 の全体進捗表の再計算は、次回の実態突合（§S サマリーの更新）とあわせて行う

---

## §6. 注文管理・店舗オペレーション（§5.4）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 6-1 | セルフ注文（スマホ） | チェックイン中ユーザー限定の注文導線 | 3-2 | S | 90% | **90%** | **90%** | 🟨 **A5画面まで実装（2026-09-21）**：`src/app/orders/page.tsx`（コミット `fa72766`）。チェックイン中限定の注文導線・メニュー一覧・数量入力を実装。`0019` の `orders`／`order_items`＋`src/lib/orders/totals.ts`。★ `orders_insert_self` は「**本人名義かつ自分が `staying` のとき**」だけ通す（他人の `checkin_id` への付け替えを塞ぐ）。明細は**注文時点の単価のコピー**で `menu_items` へ FK を張らない |
| 6-2 | 店員タブレット代理注文 | チェックイン者限定選択＋管理画面補正機能 | 3-2, 6-1 | S | 90% | **85%** | **87%** | 🟨 **実装済み（2026-09-21）**：`src/app/staff/orders/page.tsx`（コミット `fa72766`）の代理注文セクション。チェックイン中のユーザーのみを選択肢に表示し、`checkin_id`／`member_id` の組で誤請求を防止 |
| 6-3 | 売り切れ（SOLDOUT）トグル | 店舗端末からの即時切替 | 6-1 | S | 90% | **90%** | **90%** | 🟨 **B1トグルUIまで実装（2026-09-21）**：`src/app/staff/orders/page.tsx`（コミット `fa72766`）の売り切れセクション。`0018` の `menu_items.is_sold_out`＋**列単位 GRANT**（RLS は列を絞れないため、全列 UPDATE を与えると**コアメンバーが単価を書き換えられる**）。単価等の編集はサーバ側の管理 API 経由（API設計 §2-5b） |
| 6-4 | カフェメニューマスタ | `menu_items` とその管理画面（C13）。**Uii価格は保存せず都度算出**。`is_pre_orderable` / `meal_slot` を保持。カテゴリ「送迎・オプション」に送迎（1,900円＝1,520Uii・片道）を登録（専用マスタは作らない） | 1-2 | S | 85% | **90%** | **88%** | 🟨 **C13画面まで実装（2026-09-21）**：`src/app/admin/master/page.tsx`（コミット `c81a9f8`）にメニュー登録・一覧セクションを実装。`0018` が正本メニュー31品＋送迎＝**初期32行**を投入。Uii 列は作らない（`toUii()` で都度算出）。⚠️ **朝スロットの商品が正本メニューに無い**ため事前予約の朝は0件（`QUESTIONS.md` [2026-09-20] に起票・未解消） |
| 6-5 | 提供ステータス | `orders.serving_status`、B1カンバンの列追加、客側への Realtime 反映。**Phase 1 は伝票単位**（明細行単位の部分提供は扱わない）。**事前予約（`meal_reservations`）→ 伝票への変換トリガも本パッケージの責務**。会計ステータスとは独立した2軸とし単一カラムに統合しないこと | 6-1, 3-5c | S | 60% | **45%** | **51%** | 🟨 **カンバン・マイログ表示まで実装（2026-09-21）**：`src/lib/serving-status.ts` が表示文言を実装で確定した（保存値「未提供」→ 本人向け「調理中」。CLAUDE.md §1.1 により正本を採用）。`staff/orders` の `OrderKanban`（B1）、`me`・`admin` の表示に反映済み（コミット `fa72766`／`c81a9f8`）。⚠️ **`QUESTIONS.md` [2026-09-03] 論点1（画面設計.md L319 の表記訂正）は依然未回答**（実装は止まっていないが派生文書の訂正は残る）。⚠️ **Realtime反映・注文完了画面（`/orders`）自体への表示・`meal_reservations` 変換トリガは未確認**（`/orders` のソースに提供ステータス表示なし） |

---

## §7. Uii機能・経済圏会計ロジック（Phase 1：会計管理限定）（§5.5）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 7-1 | Uii換算ロジック | `floor(単価(円)×0.8)`の単品ごと丸め・合算ルール実装 | なし | S | 95% | **100%** | **98%** | ✅ **実装完了（2026-09-05）**：`src/lib/uii.ts`。単品ごとの丸めと「合計に0.8を掛けると単品ごとの和とずれる」ことを単体7ケースで固定した。**Phase 1 で最初に完了した実装パッケージ** |
| 7-2 | 伝票管理（明細編集・注文者付替） | 顧客管理画面からの編集、遡及修正時の差額明示 | 6-1, 8-1 | M | 90% | **85%** | **87%** | 🟨 **画面まで実装（2026-09-22）**：顧客詳細（`/admin/customers/[memberId]`）の伝票エディタ（`src/components/customers/SlipEditor.tsx`）＋ Server Action（数量・単価の編集／明細行の取消／注文者付替／伝票取消／決済ステータスの手動切替）。判定は `src/lib/orders/slip-edit.ts`（純関数・試験17件）。★ **精算済み伝票の編集は差額を `settlement_adjustments` へ「未処理」で自動起票**（v13 §5.6.5-3）、**編集すると生きている精算QRを自動失効**（§5.6.3-5）。★ `0031_order_items_void.sql` を追加 — `0019` には**明細行を消す手段が無く**（DELETE の GRANT なし・`quantity > 0`）、v13 §5.6.2 の「品目の削除」を表現できなかった。論理削除＋理由必須で回収した。⚠️ **残＝手動調整行（まかない補助・割引）と編集履歴ログ**。物理設計が無く `QUESTIONS.md`「[2026-09-20] 伝票の編集履歴ログ・精算グループ・手動調整行の物理設計が存在しない」でオーナー判断待ち。旧記述→ ~~🟢 着手可能。モックに伝票エディタ・遡及修正モーダル実在~~ |
| 7-3 | 精算QR発行・失効基盤 | 即時・後払い両対応、Uii直接決済は不可（すべて精算QR経由） | 7-2 | M | **85%** | **80%** | **82%** | 🟨 **発行・失効・消し込みまで実装（2026-09-22）**：`src/lib/billing/settlement-qr.ts`（256bit乱数 ／ sha256 で保存 ／ TTL 24時間 ／ 単回使用＝2026-09-10 A案）と`src/lib/orders/slip-store.ts` の発行・消し込み。★ **平文は発行時に1度だけ画面へ返し、保存しない**（見失ったら再発行＝旧QRは失効する）。★ 判定は**トークン一致を先に見る** — 失効・期限切れを先に答えると、当たっているかどうかが応答から読める。⚠️ **残＝ゲストが読み取る先（公開の精算ページ）**。未ログインで金額を見せる設計になるため `QUESTIONS.md`「[2026-09-22] 精算QRの読み取り先」でオーナー判断待ち。旧記述→ ~~🟨 列だけ先行実装（2026-09-21）~~：`0019` の `orders` に QR 5列（ハッシュ保存・TTL・単回使用）を初版 DDL として入れた。★ 派生設計に無かった **`settlement_qr_revoked_at` を追加**（v13 §5.6.3-5・§7「トークン失効フラグ」。無いと**伝票を編集しても旧QRで元の金額のまま精算できる**窓が開く）。発行・検証ロジックは未着手。~~QRトークンの保護強化が未決~~ → **A案で確定**（256bit乱数／ハッシュ保存／TTL 24h／単回使用）。**マイグレーション未作成の段階で確定したため、スキーマ変更ではなく初版DDLとして入る**。設計は `DB物理設計.md` §3-2・`非機能要件詳細.md` §2-7 に反映済み |

---

## §8. 顧客管理画面・会計調整（§5.6）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 8-1 | 顧客サマリー・注文履歴編集画面 | 明細編集・単価上書き・キャンセル・手動調整、会計済み伝票の遡及修正 | 2-2, 6-1 | L | **90%** | **70%** | **78%** | 🟨 **一覧・詳細まで実装（2026-09-22）**：`/admin/customers`（一覧 ／ 滞在中セクション ／ 検索）と `/admin/customers/[memberId]`（顧客サマリー ／ 注文履歴 ／ 未処理差額）。★ **実名を出さない** — 表示名は `v_member_public` のニックネーム／会員番号だけを使い、`member_profiles_private`（PII-A）へは触れない。宿泊法の申告項目は `2-4`・`3-2` の担当である。★ 未会計額は**保存済みの Uii を合算**して表示する（円から換算し直すと単品ごとの切り捨てとずれる／v13 §5.5）。⚠️ **残＝宿泊タブ（§5.6.8）・宿泊形態の変更（§5.6.9 ＝ `3-10`）・手動調整行**。旧記述→ ~~🟢 ブロック解除（2026-09-05）~~|
| 8-2 | 未会計額表示（Uii主・円副） | 全画面共通の表示ルール。**2026-08-20：円主→Uii主へ方針逆転**（§9 #41） | 8-1, 0-1 | S | 85% | **70%** | **76%** | 🟨 **2画面に実装済み（2026-09-21）**：`src/app/me/page.tsx`・`src/app/admin/page.tsx`（コミット `fa72766`／`c81a9f8`）が `Money` コンポーネント経由で未会計額を表示。⚠️ **8-1（顧客サマリー画面）は未着手のまま**のため、「全画面共通」のうち運営の顧客詳細側は未反映 |
| 8-3 | 差額の次回繰越・免除運用 | `settlement_adjustments`、再訪アラート連動、90日滞留アラート | 8-1 | M | **90%** | **65%** | **75%** | 🟨 **本人向け表示まで実装（2026-09-21）**：`0019` の `settlement_adjustments`＋`src/lib/billing/adjustment.ts`。`src/app/me/page.tsx`（コミット `fa72766`）が本人への未処理差額の常時表示（v13 §5.6.6）を実装。免除は **コアメンバーも可**（`is_staff()`）。90日滞留は `is_stale` の**旗を立てるだけ**。★ 返金の Uii は**絶対値で丸めてから符号を戻す**。⚠️ **運営側の免除操作UI・再訪アラート連動は未確認** |
| 8-4 | マイログ（本人の注文・決済・クエスト履歴閲覧） | ユーザー向け個人ログ画面、未処理差額の常時表示。**2026-09-01：未受領のEumo給付も本画面に表示**（5-7 連動） | 7-2, 8-3, 5-7 | S | 85% | **100%** | **94%** | ✅ **実装完了（2026-09-22）**：未処理差額・未会計・注文履歴に加えて、**未受領のUii給付**（`5-7` 連動）を表示し、**本人の受領報告**を可能にした（v13 §5.3.1 の 2026-08-29 拡張）。★ 本人に開ける経路は `0032_eumo_self_receipt.sql` の**トリガー**で「送付済 → 受領確認済」1遷移に限る — RLS は列を絞れないため、本人 UPDATE を開けた時点で `amount_uii` も書き換えられるようになる。★ 受領報告のボタンは**送付済のものにだけ**出す（送った記録が無いのに受領済みの行を作らない）。旧記述→ ~~🟨 実装済み（2026-09-21）~~：`src/app/me/page.tsx`（コミット `fa72766`）。未会計合計・未処理差額・提供ステータス付き注文履歴を実装。⚠️ **未受領のEumo給付表示は未確認**（`5-7` がブロック中のため `eumo_grants` 連携は未着手の可能性） |
| 8-5 | 顧客一覧のソート順 | チェックイン中を最上部に固定する既定ソート＋セクション見出し | 8-1 | S | 90% | **100%** | **96%** | ✅ **実装完了（2026-09-22）**：`src/lib/customers/sort.ts`（純関数・試験11件）。①滞在中を最上部（内部はチェックイン日の**古い順**＝退去が近い人が上）→ ②未処理差額あり → ③未会計額の大きい順 → ④最終来訪日の新しい順。セクション見出し（🟢 滞在中（◯名））と件数も出す。検索は滞在状態に関わらず全件が対象。⚠️ **電話番号での検索は実装しない** — `member_profiles_private`（PII-A）を一覧へ運ぶことになるため（仕様の3項目のうち氏名・ニックネームのみ）。旧記述→ ~~🟢 着手可能（§9 #43）。モックに実装済み~~ |
| **8-6** | **★ 宿泊履歴（泊まった部屋）の表示**（新規） | 顧客管理「宿泊」タブに滞在単位の履歴。部屋移動は潰さず時系列展開、未割当は「部屋未割当」明示、「通算◯泊／よく使う部屋」集計 | 8-1, 3-1 | S | 35% | **90%** | **68%** | 🟨 **実装済み（2026-09-22）**：顧客詳細（C11）の「宿泊」節。`src/lib/customers/stay-history.ts`（純関数・試験11件）。★ **部屋移動のあった滞在を1行に潰さず**、割当履歴を時系列で展開する（§5.6.8）。忘れ物・クレームの追跡は日付と部屋の対応が正確でなければ役に立たないため。★ **部屋名は `room_name_snapshot`（割当時点の名前）**を読む。`rooms` の現在名を引くと、改名した瞬間に去年の履歴まで新しい名前で表示される（§5.6.8 の [!warning]）。★ **部屋未割当を空欄にしない**（記録漏れか、割り当てずに運用したかが区別できなくなる）。★ 予約段階・キャンセルは**通算泊数に数えないが行としては残す**（顧客の履歴であるため）。⚠️ **残＝宿泊費（Uii 主・円 副）の表示**。滞在ごとの料金算定は `3-9`（宿泊料金マスタ）と`3-10`（滞在中の形態変更で泊単位に単価が変わる）に依存するため、確定してから足す。設計は要件のみのまま（詳細設計・モックは未整備）。旧記述→  🟡 **要件のみ**（v13 §5.6.8・#50／2026-08-25 新設） |

---

## §9. FAQ・暗黙知AIコンシェルジュ ＆ ナレッジ登録画面（§5.7）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| **9-1** | **アプリ内の横断セマンティック検索**（旧「RAG検索基盤（pgvector）」・**2026-09-11 復活**） | クエスト起票・評価のための**内部検索**。**エンドユーザー向け AI チャット UI は持たない**（`CONSOLIDATED_DECISIONS.md` §17-7 で維持） | 1-6 | M | 60% | 0% | 24% | 🟡 ~~➖ 不要化（2026-08-16、§9 #31）~~ → **復活（2026-09-11）**：基盤側は `1-6` に一本化し、本パッケージはその上で動く検索機能を指す。✅ **Phase 1 に含めることが 2026-09-20 にオーナー確定**（§19-2b）。基盤（`0100`）は同日 dev へ適用済みで、残るのは**検索時スコープ**（LINE 経路の個人紐付き除外）と**PII 検出の範囲拡張**（`CONSOLIDATED_DECISIONS.md` §17-6） |
| 9-2 | **line-rag-bot管理画面への導線（外部リンクのみ）** | 管理画面B9/C5に「line-rag-bot管理画面を開く」ボタンを配置し、別タブでStreamlitを開く | 2-1 | S | 100% | 100% | 100% | ✅ 完了（#94 ／ 2026-09-20）。モックに `openConcierge()` が実装済み。システム間の認証連携も不要 |
| ~~9-3~~ | ~~タイプ別構造化入力への拡張~~ | ― | ― | ― | ― | ― | ― | ➖ 不要化：ナレッジ登録・編集は line-rag-bot Streamlit 側で完結 |
| ~~9-4~~ | ~~未回答エスカレーション一覧の管理画面表示~~ | ― | ― | ― | ― | ― | ― | ➖ 不要化：エスカレーションの閲覧・代理回答も line-rag-bot 側で完結 |
| ~~9-5~~ | ~~line-rag-bot連携API（読み取り専用）~~ | ― | ― | ― | ― | ― | ― | ➖ Phase 1 では不要（下記の方針変更に注意） |

> [!warning] 「API連携を一切実装しない」は撤回されている（2026-08-29／v13 v1.19.0）
> 本節はこれまで「浮遊街アプリと line-rag-bot の間に API 連携は一切存在しない」という 2026-08-16 の
> 最終確定に基づいて全パッケージを不要化していた。しかし **v1.19.0 で #31 の当該部分が撤回され、
> 「API連携は Phase 2 以降で実施する」へ変更**されている。
>
> **Phase 1 のスコープは変わらない**（9-2 の外部リンクのみ）が、「恒久的に不要」ではなく
> 「Phase 2 へ繰り延べ」が正しい。§15 のスコープ外表にも Phase 2 項目として計上した。

---

## §10. 既存街人の事前データ登録・名寄せ ＆ 宿泊日数管理（§5.8）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| ~~10-1~~ | ~~一括インポートパイプライン~~ | 親方・街人リスト（Excel/CSV）から`pre_registered`アカウント生成 | ― | ― | ― | ― | ― | ➖ **不要化（2026-09-19 オーナー決定）**：**会員データの投入は Vault 側で行うため、システムの機能としては追加しない。** 正本 v13 §9 #32④「C6会員データ一括インポート：実装不要（初期移行はVault側スキーマで完了済み。以後はA1オンボーディング経由が前提）」を**生かす**判断。⚠️ 正本 v13 は §4 L312・§5.8.2・§6 L2127・§7 L2249 の4か所で「作る」側に書かれており、**§9 #32④ と自己矛盾している**（Issue #57 の調査で判明）。**正本側の追随が必要**。旧ステータス: ~~🟢 着手可能（370名で最終承認済み）~~ |
| 10-2 | 名寄せロジック | `contact_info`優先、次いで氏名+誕生年月。照合キーのない105名は次回チェックイン時に現地入力 | 2-1 | M | 85% | 0% | 34% | ⚠️ **2026-09-19：依存を 10-1 から `2-1` へ付け替え**（10-1 が Phase 1 の対象外になったため。会員データは Vault 側で投入済みの状態から始まるので、前提は `members` テーブルの存在に変わる。**この付け替えが妥当かはオーナー確認が要る**）。🟢 着手可能（§9 #2・#16）。3-5b の メールOTP により「予約時点での名寄せ」も可能になった。⚠️ **2026-09-05 決定**：開発段階は**自作フィクスチャのみ**を使い、実データでの名寄せ検証は**セキュリティ試験合格後・本番環境でのみ**行う |
| 10-3 | 再訪アラート | 街人ステータス・残チケット・運営メモの即時表示 | 10-2, 3-2 | S | 80% | **85%** | **83%** | 🟨 **実装済み（2026-09-22）**：`src/lib/customers/revisit.ts`（純関数・試験8件）＋ `RevisitAlerts`。**未処理の差額を持つ会員が滞在中**なら、店員タブレット（B1）と管理ダッシュボード（C1）の**先頭**に出す。差額は「次回来訪時に現地で精算する」前提で繰り越されており（v13 §5.6.6）、回収・返金の機会は本人が滞在している間しかない。★ **返金（マイナス）も出す** — 追加請求だけ出すと、運営が返すべき差額が現地で返されない（§5.6.6 の [!warning]）。★ 並びは**金額の絶対値**の大きい順（金額順にすると返金が末尾へ沈む）。⚠️ 残＝**運営メモの表示**（`member_notes` の DDL が本リポジトリに無い ／ Issue #112）。旧記述→ ~~🟢 着手可能。モックでは顧客一覧のインライン警告として実装。**2026-09-05：A案採用によりレスポンスマスキングの未決が解消**（運営メモ・個人情報の表示範囲が確定した） |
| 10-4 | 宿泊日数手動調整ログ | 運営による増減操作と調整履歴の記録（理由入力必須） | 2-1 | S | 55% | 0% | 22% | ⚠️ **2026-09-19：依存を 10-1 から `2-1` へ付け替え**（10-1 が Phase 1 の対象外になったため。会員データは Vault 側で投入済みの状態から始まるので、前提は `members` テーブルの存在に変わる。**この付け替えが妥当かはオーナー確認が要る**）。🟡 画面設計 C8 に定義はあるが**モック未実装**。管理者のみ操作可・監査ログ必須の業務のため、モックでフロー検証してから実装に入ること |

---

## §11. ロール別UI表示制御・アクセス制御

→ 2-2・2-3・2-5・2-6 に統合済み（§2 参照）。

---

## §12. ゲスト → 街人（会員）アップグレード導線（§5.10）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 12-1 | ロック中クエストの施錠表示・登録モーダル | `🔒 街人登録で解放`表示から2段階モーダル起動 | 5-1 | S | 90% | 0% | 36% | 🟢 着手可能。モックに4ステップモーダル実在。**2026-09-05 決定：アカウント作成（アプリ本登録）時にメールアドレスを必須登録**する（OTP による所有確認と、`contact_info` 第1優先の名寄せ照合キーを同一メールで揃えるため） |
| 12-2 | 申請〜決済〜承認フロー | アプリ内で決済情報を入力させず、管理者が承認。**2026-08-25：Phase 1 は3経路**（①精算QR〈eumo・円〉②**現金** ③**Uii〈eumo〉QR**）、**カードは Phase 2**（加盟店審査に通常2〜4週間かかるため。⚠️ **納期が9月中へ改訂されたことで前提が変わっている** — §19-4 参照）。`payment_method`/`paid_at`/`received_by` を追加。**現金選択時はQRを発行させない** | 7-3, 12-1 | M | **85%** | 0% | 34% | 🟢 **ブロック解除（2026-09-10）**：~~QRトークン保護（#11）未決~~ → **A案で確定**（入金QRは TTL 7日・`qr_consumed_at` で単回使用）。⚠️ **残る課題**: 画面設計 C7「街人登録申請一覧」が**モック未実装**（モック自身が「本来は管理画面から行う」と注記）。18-2 の対応表でも 🔴 判定 |
| 12-3 | 特典付与処理（宿泊券4枚＋キャッシュバック） | 承認時のロール昇格＋特典付与。**2026-09-01 決着：「CB」表記を全廃し単位はコイン（uii）。`membership_plans` を2プラン化**（40,000円＝10,000 uii ／ 30,000円＝5,000 uii）。**本導線＝30,000円プランは 5,000 uii**。金額はマスタ参照・直書き禁止 | 3-4, 12-2, 0-1 | S | 75% | 0% | 30% | 🟡 §9 #51 で決着。**`membership_plans` の2プラン化が DB物理設計に反映済みか未確認** |
| **12-4** | **★ 初回来訪キャッシュバックの判定・自動起票**（新規） | チェックイン時に「街人／ゲスト」「初回／再訪（通算◯回目）」「未発行／依頼済み／受領済み」の**3バッジ**を表示。初回×街人で `eumo_grants` へ `first_visit_cashback`・`未送付` を自動起票。**初回判定は都度算出（保存カラム不可）**。二重付与ガード必須 | 3-2, 5-7, 10-2, 10-3 | **M** | **85%** | **80%** | **82%** | 🟨 **判定・バッジ・起票まで実装（2026-09-22）**：`judgeFirstVisitCashback()`（`src/lib/eumo/grants.ts` ／ 試験7件）＋ 顧客詳細の `CashbackPanel`。**3バッジ（立場／初回・再訪 通算◯回目／発行状態）を必ず並べる** — 1つでも欠けると判断できない（「初回」だけ見て起票すると、登録キャッシュバック済みの人へ二重に送る）。★ **初回判定は都度算出**（保存カラムを持たない）。数えるのは実際に滞在に入った記録だけで、予約やキャンセルは数えない（数えると初回が永久に来ない）。★ **移行由来の街人は一律「要確認」**（#59 論点2 の決着）。操作は塞がず、額を運営が入れて起票できる形にして判断だけを人へ渡す（塞ぐと正当な未受領者へ永久に送れない）。⚠️ **残＝チェックイン確定時の自動起票**（`3-2` のチェックイン操作へ組み込む）。現状は顧客管理画面からの手動起票である。旧記述→ ~~🟢 ブロック完全解除（2026-09-22）~~：**#59 が全面決着**し、退避設計だった§5.10.8③の「要確認」フォールバックが**正式な運用として確定**した。判定ロジックは単純化される：`eumo_grants` に該当会員の起票が**既にあれば**二重付与ガードで自動起票をスキップ、**無ければ**街人リストの列を読んで推測せず一律「要確認」を出して運営判断に回す（街人リストからの一括判定ロジックは作らない）。3バッジ表示・自動起票とも未実装 |

---

## §13. 管理者ダッシュボード

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 13-1 | 「今日の浮遊街サマリー」ダッシュボード | PC/タブレット最適化WebUI | 各機能領域一式 | M | 75% | **90%** | **84%** | 🟨 **実装済み（2026-09-21）**：`src/app/admin/page.tsx`（コミット `c81a9f8`）。本日の滞在・未提供・未会計・審査待ちの4サマリーカードと各画面への導線、本日の残枠一覧、コンシェルジュ管理画面リンクを実装。**数えるだけで判断はしない**（閾値判断は各画面側に残す） |
| ~~13-2~~ | ~~アプリ初期操作マニュアル~~ | ― | ― | ― | ― | ― | ― | ➖ **17-1 と重複のため統合（2026-09-03）**。§17 を正とする |

---

## §14. メディアライブラリ（画面設計 A10）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 設計 | 実装 | 総合 | ステータス |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 14-1 | メディアアップロード基盤 | Cloud Storage for Firebase への署名付きURLアップロード（動画・写真、**全ロール＝ゲスト含む**）。Object Finalize 起点 | 1-4 | S | 80% | **70%** | **74%** | 🟨 **クライアント側まで実装（2026-09-22）**：ブラウザ → ストレージへの直接 PUT（`src/components/media/UploadForm.tsx` ／ `XMLHttpRequest` で進捗を取る。`fetch` はリクエスト側の進捗を取れない）と、署名ヘッダの取り回し（`src/lib/media/upload-client.ts`）。★ **バケットの CORS を `scripts/gcp/setup-media-infra.sh` へ追加した** — 無いと preflight が拒否され、**署名も IAM も正しいのにアップロードだけが失敗する**（原因の読めない失敗様式）。残るは Object Finalize の実処理（`pending` → `stored` の書き戻し・サムネイル）とオーナー作業の GCP 実行。旧記述→ ~~🟢 **着手可能**：~~非同期ジョブ実行基盤が未決~~ → **2026-09-10 に解決**（Cloud Tasks ＋ Cloud Scheduler ／ §16-1）。ステータス欄の追随漏れを 2026-09-19 に修正 |
| 14-2 | アップロード画面（A10） | ファイル選択／撮影 → **用途タグ入力（最低1つ必須）** → 進捗表示 → 失敗分のみ再試行。Exifから撮影日時・位置を自動取得 | 14-1 | S | 85% | **100%** | **94%** | ✅ **実装完了（2026-09-22）**：`src/app/upload/page.tsx` ＋ `src/components/media/UploadForm.tsx`。①ファイル選択／その場で撮影 → ②用途タグ（プリセット ＋ 自由入力・最低1つ必須）→ ③進捗表示（**バイト数で重み付け**。件数で割ると 200MB の動画と 2MB の写真で「50%」の意味が変わる）→ ④**失敗分のみ再試行**。Exif から撮影日時・位置を自動取得（`src/lib/media/exif.ts` ／ JPEG の APP1 のみ。HEIC・MOV は別容器のため読まず、**推測値で埋めない**）。★ 送信は1件ずつ順番（並列だと現場のモバイル回線を食い合う）。★ 撮影メタデータは**クライアント由来**のため `normalizeCaptureMetadata()` が形だけ検証し、読めない値は 400 にせず捨てる（補助情報でアップロードを止めない）。旧記述→ ~~🟢 着手可能（**Phase 1 はここまで**／v13 §5.11.7）。モック⑦実在~~ |
| 14-3 | メディアの運営措置（非表示化・削除） | 管理者・コアメンバーが他者の投稿を論理削除できる操作。**アップロード開放と同時に必要** | 14-2, 2-3 | S | 60% | 0% | 24% | 🟡 v13 §5.11.7 の警告：**公開機能だけを出さないこと** |
| **14-6** | **★ `media_assets.ai_tags[]` カラムの先行用意**（新規） | **Phase 1 はカラムのみ**。Phase 2 開始時に Phase 1 蓄積分を一括バックフィルする前提でスキーマを確定 | 2-1c | S | 60% | 100% | 84% | ✅ 完了（#120 ／ 2026-09-22）。**2026-09-19：依存を ~~`14-1`~~ → `2-1c` へ付け替え**（決定 B の派生。列を足す前提はテーブルの存在であり、アップロード経路の完成ではないため。`14-1` の完了を待つ必要が無くなる） |
| ~~14-4~~ | ~~AI用途分類・キャプション自動生成~~ | ― | ― | ― | ― | ― | ― | ➖ **Phase 2 へ移送**（§9 #33）。⚠️ **2026-08-29：エンジンを Gemini → Claude へ変更、AIタグは「アップロード時の事前計算」方式へ**（#57）。§15 参照 |
| ~~14-5~~ | ~~メディアライブラリ画面（用途別ソート）~~ | ― | ― | ― | ― | ― | ― | ➖ **Phase 2 へ移送**（§9 #33）。検索は4モード（用途タグ／フリーワード／特徴／曖昧・雰囲気）へ拡張（#57） |

> [!note] 14-4 / 14-5 を Phase 2 へ送った理由（2026-08-20）
> 検索・AI推薦は**素材が貯まっていることが前提**であり、Phase 1 でアップロードだけでも稼働させて
> おかないと Phase 2 に入った時点で母数がゼロになる。Phase 1 で価値を出すには
> **アップロードと用途タグの蓄積で十分**である（v13 §5.11.3）。

---

## §15. Phase 1 スコープ外（参考・Phase 2 以降）

| 項目 | Phase | 備考 |
| --- | --- | --- |
| **メディアの検索・AI推論提示・キャプション自動生成** | Phase 2 | **2026-08-29 更新（#57）**：AIタグは**アップロード時の事前計算**、検索時は照合のみ。タグは人間編集可（履歴記録）、語彙はフォークソノミーで育成。検索は**4モード**。**エンジンは Claude**（Gemini から変更）。Phase 1 は `ai_tags[]` カラムのみ用意（14-6）し、Phase 2 開始時に一括バックフィル |
| **line-rag-bot との API 連携** | **Phase 2** | **2026-08-29 追加（v1.19.0）**：#31 の「API連携を一切実装しない」を撤回し「Phase 2 以降で実施」へ変更。Phase 1 は 9-2（外部リンクのみ） |
| **★ アプリのデータを浮遊街コンシェルジュのナレッジ空間へ投入** | **Phase 2** | **2026-09-03 追加（#61・オーナー決定）**：クエスト完了報告の失敗知見・朝会議事録の決定事項・レシピ／作業手順を `line-rag-bot` のベクトル空間へ投入する。`line-rag-bot` 側に受け皿が既存（`IMPLEMENTATION_STATUS.md` 残タスク **E2/E3**・既知の制限 #3）。⚠️ **着手前に3件の未決**：①投入方式（**A. 取込キュー方式が推奨**／B. 直接書きは `KnowledgeIndexer` の「唯一のライター」不変条件を壊す）②**個人情報を弾く機械的ガード**（**これが無いまま着手すると個人情報が LINE へ露出する**。`target_role` はロールが自己申告のためガードにならない）③同一GCPプロジェクトか |
| **★ ベクトル検索の `find_nearest` 移行**（`line-rag-bot` 側） | **Phase 2**（前倒し検討中） | **2026-09-03 追加（#61）**：現行は全件 `stream()`＋Python 総当りコサイン。**費用はむしろ下がる**（月 $1.1 → $0.014 概算）／**工数 約3〜4人日**。**距離しきい値で R16（`RAG_MIN_SIMILARITY=0.7`）を DB 側で強制でき、equality 事前フィルタで R13（カテゴリルーティング）も解ける**。⚠️ Firestore エミュレータの `find_nearest` 対応は**未確認**（CI がエミュレータ前提のため着手前に30分で検証すること）。上記ナレッジ投入の**先行条件**。前倒し可否は `QUESTIONS.md` で未回答 |
| **物販・直売所（外部EC送客）** | Phase 2 | **2026-08-25 更新（#53）**：アプリ内に円決済を作らず**外部ECへリンク送客**。`menu_items.ec_url`。EC購入は未会計に計上しない。⚠️ **リンク先ECが既存か新規かは未回答**。新規構築なら本アプリの工数外の別タスク |
| **LINE／Discord の自動吸い上げ** | Phase 2 | **2026-09-01 拡張（#60）**：素材（画像）に加え**チャット履歴（テキスト）**も対象へ。Firestore格納→構造化・クエスト案生成・関心度可視化・災害版。⚠️ スコープ・同意取得・分担は未回答 |
| **オーダーメイドクエスト（AI個人指名）** | Phase 2 | 朝会議事録＋現場稼働状況＋個人スキルボードをAIが統合解析。**スキルツリー機能と統合**（単独では前倒ししない／§9 #38） |
| **クレジットカード決済** | **Phase 2**（要再検討） | **2026-08-25（#52）**：加盟店審査に通常2〜4週間かかり、~~9/10~~ に対して審査落ち時の代替時間が残らないため。⚠️ **2026-09-03 の納期改訂（9月中）で前提が変わった** — 9/30 までなら審査期間の下限は収まる。Phase 1 復帰の可否はオーナー判断（§19-4 参照） |
| **Uii保有残高の表示／eumoアプリからの残額参照** | Phase 2 | #22（2026-08-14）で「前倒し不要」と明示的に決定済み |
| 会員権の有効期間・期限表示・一斉失効 | Phase 2 | 最初の満了が2027/4/30のためPhase1稼働期間中は非発生。**ただし`memberships.expires_on`等のスキーマ自体はPhase1で用意** |
| Uii高度機能（個人Wallet・自動失効・EUMO直接連携） | Phase 2 | Phase1のUiiは会計管理特化（§7参照） |
| クエストの反復実行・複数人按分・スキル連動報酬 | Phase 2 | スキルツリー機能と合わせて整理（決定済み） |
| スキルツリー機能／P2Pマーケット／メタバース連携 | Phase 2/3 | メタバース連携はQUESTIONS.md未回答（採否自体が未定） |
| **★ MCP Gateway（テナント間ルーティング中継層）** | **Phase 3** | **2026-09-19 オーナー決定**：~~WBS `1-5` に含める~~ → **Phase 3 へ送る**。用途は「分離された各テナント（浮遊街・FEL・将来の他コミュニティ）のデータ・AIログをセキュアかつ疎結合にルーティングする中継層」（`basic-design/infra/システムアーキテクチャ.md` L1175-1177）であり、**正本 v13 §4 が「FEL連携強化・マルチテナント化」を Phase 3 と定めている**（L380・§2 L78 同旨）以上、その部品である本機能も Phase 3 が正しい。⚠️ **正本 v13 に `MCP` の記述は0件**で、定義は上記の派生文書1か所のみ。Phase 3 着手時に正本へ昇格させること |
| **★ 買い物リストの拡張（予算管理・定期購入・在庫連動・外部EC振り分け）** | **Phase 2** | **2026-09-22 更新（v13 §5.12.5 ／ §9 #65 決着）**：最小構成（登録・一覧・クエスト化）は `5-8`・`5-9` として **Phase 1** で実装する。~~立替実費のアプリ内精算~~ は **❌ 実装しない**（精算も記録もしない／§5.12.4）。Phase 2 に残るのは**予算管理・定期購入・在庫連動・外部EC振り分け**。LINE・Discord の会話からの買い物候補の自動抽出は #60（チャット履歴吸い上げ）へ統合 |
| IoT同期・FEL連携強化・マルチテナント化 | Phase 3 | 将来検討 |

---

## §16. QUESTIONS.md ブロッカー逆引き表（**2026-09-20 更新**）

> [!success] **2026-09-10：真のブロッカーが 3件 → 0件になった**
> 同日のオーナー決定3件（**非同期ジョブ＝Cloud Tasks＋Cloud Scheduler** ／ **公開予約OTP＝Resend** ／
> **QRトークン＝A案**）により、**実装着手を止めている仕様上の論点は無くなった**。
> あわせて半ブロッカーの「ニックネーム未設定会員の他者向け表示」も**街人番号で決着**し、4件 → 3件。
>
> 課題管理表（`QUESTIONS.md`）の現況: **同ファイル冒頭の現況表を参照**（2026-09-13 集計・全56件）。
> ⚠️ **09-14〜09-20 の起票・回答分が未集計**であり、件数を本表で二重管理しない（§S-5 の残件5）。
>
> ✅ **§16-1a の設計書反映4件は完了済み**（2026-09-20 時点）。**いま設計書を読んで実装に入ってよい。**

> [!important] （履歴）2026-09-05 の回答で構図が変わった
> 前版（09-03）が「真のブロッカー3件」の筆頭に挙げていた**ロール別レスポンスマスキングは解決済み**であり、
> これにより **`2-2`・`2-3`・`2-4`・`8-1`・`8-4`・`10-3` の6パッケージが一斉にブロック解除**された。
> 一方で 09-05 に**新しいブロッカーが2件起票**された（公開予約OTPの送信基盤／無料枠と非機能要件）。
>
> **2026-09-08 のオーナー決定で、そのうち1件が解消した** — **Supabase を有料プランへ変更**したことで
> 「無料枠で満たせない非機能要件の扱い」が不要になり、**真のブロッカーは 4件 → 3件**となった。
>
> 課題管理表（`QUESTIONS.md`）の現況: **全55件（回答済み・クローズ 31 ／ 部分回答 4 ／ 未回答 20）**
> — 前版の「全45件（回答済み27／未回答17／クローズ1）」から**10件増えた**
> （09-05 起票 5件／09-08 の情報統合で起票 2件／前版の計上漏れ 3件）。

### 16-1. 真のブロッカー（~~3件~~ → **0件**）— **2026-09-10 に全て解消した**

> [!success] **仕様上のブロッカーは残っていない**
> 2026-09-10 のオーナー決定3件により、実装着手を止めている論点は**ゼロ**になった。
> **以後、実装が進まない理由は「決まっていないから」ではなく「着手していないから」である。**
>
> ⚠️ **ただし決定は設計書へ未反映**（§16-1a）。反映前に実装へ入ると、
> 設計書を読んだエージェント／人間が**古い前提（`pg_cron` 等）で実装する**。

| QUESTIONS.md 項目 | 起票日 | 優先度 | ブロックする作業パッケージ | なぜ後回しにできないか |
| --- | --- | --- | --- | --- |
| ~~**非同期ジョブの実行基盤選定**（Cloud Functions / pg_cron / Cloud Scheduler）~~ → ✅ **2026-09-10 解決** | 2026-08-28 | ~~高~~ | ~~1-1, 1-4, 1-5, 3-5b, 4-2, 5-7, 8-3, 14-1~~ → **8件ブロック解除** | **Cloud Tasks ＋ Cloud Scheduler で確定**（GCP 全面統一・**`pg_cron` 不使用**）。イベント起点2本＝Cloud Tasks／定時5本＝Cloud Scheduler。⚠️ **「キューイングサービス」だけでは7本中5本を担えない**（キューは定時起動の仕組みではない）ことが確認過程で判明し、Scheduler を併用する形に確定した |
| ~~**決済QR・入金QRトークンの保護強化**（乱数要件・TTL・単回使用）~~ → ✅ **2026-09-10 解決** | 2026-08-28 | ~~高~~ | ~~7-3, 12-2~~ → **ブロック解除** | **A案で確定**（256bit乱数／平文を保存しない／TTL 精算24h・入金7日／単回使用）。**「後付けはマイグレーション修正が必要」という前提は、`supabase/migrations/` にテーブルが1つも無い現時点では未発生**だったため、初版DDLとして書き込めた（後付けコストゼロ）。オーナーが判断を Claude へ委任 |
| ~~**★ 公開予約OTPの送信基盤**（Supabase 既定SMTP の **2通/時** 上限）~~ → ✅ **2026-09-10 解決** | **2026-09-05** | ~~**高**~~ | ~~3-5b, 3-5c~~ → **ブロック解除** | **Resend で確定**。⚠️ **起票時の前提が誤っていたことも判明** — 公開予約OTPは**自前実装**（`reservation_otps`）で Supabase Auth を経由しないため、**既定SMTP の 2通/時 はそもそも掛かっていなかった**。真の問題は「アプリからのメール送信手段が未選定」。**送信経路は2系統**（アプリ自前＝HTTP API／Supabase Auth＝SMTP設定）であり、**ダッシュボード設定だけでは公開予約は動かない** |

### 16-1a. 決定の設計書への反映（**4件 ／ 3件は 2026-09-10・1件は 2026-09-13 完了**）

決定と設計書が食い違ったまま実装に入ると、設計書を読んだ実装者（人間・エージェントとも）が
**決定と違うもの**を作る。**4件とも反映済み**。

| # | 反映先 | 直した内容 | 状態 |
| --- | --- | --- | :---: |
| 1 | `basic-design/infra/システムアーキテクチャ.md`「非同期ジョブ一覧と実行基盤」 | 3レーン案（`pg_cron` 前提・★提案）→ **Cloud Tasks ＋ Cloud Scheduler の2レーンへ改訂し「確定」へ格上げ**。`pg_cron` 不採用を取り消し線で記録し、**pg_cron を捨てたことで生じる追加設計3点**（Secret Manager／N-2 との衝突／Blaze 従量課金）を新設。あわせて「日次エクスポートは実行基盤の未決に依存する」節も**解消済みへ更新** | ✅ |
| 2 | `detailed-design/非機能要件詳細.md` §7-4 | 受容済みリスクの根拠を差し替え。**§7-4a を新設**し、旧根拠（経路が存在しない）が #61 決定で成立しないこと、新根拠が**チャネル判定による到達制御**であること、**実装時の必須条件4点**（`target_role` を見ない／単一チョークポイント／再スキャン／`find_nearest` 先行）を明記 | ✅ |
| 3 | `detailed-design/API設計.md` §2-2b | **メール送信経路が2系統**であることと Resend の配線先（自前＝HTTP API／Auth＝SMTP）を警告ボックスで明記。**「ダッシュボード設定だけでは公開予約は動かない」**ことと、起票時の前提が誤りだった経緯も記録 | ✅ |
| **4** | **`basic-design/backend/会員データモデル_ユーザーテーブル定義.md` §5.2 / §5.2a / §5.9（v1.4.0）**<br>**`データモデル図_ER_Diagram.md`（v1.3.0）** | **2026-09-05 オーナー決定「自分自身の権限は誰も変更できない」（`DB物理設計.md` §6-6b）を上流2本へ反映**（**2026-09-13**）。①会員データモデル §5.2a の可否表を4列 → **8列へ拡張**（集計キャッシュ3列・`member_id`・採番系・自由更新列を追加）し、`member_type`／`account_status` の他者変更を ~~`admin` のみ~~ → **staff** へ訂正（§6-6b① と食い違っていた）。②**`app.operator_id` / `app.change_reason` の申告義務**を実装者向けに明記（`service_role` 経由では `auth.uid()` が NULL のため、申告が無いと権限列 UPDATE は拒否される）。③**ブートストラップ**（初期 `admin` は `INSERT` で作る／`admin` 0人時の復旧は DB 管理コンソール）を追記。④ER図に**「RLS と列単位 GRANT だけでは自分を admin にするのを止められない」警告**と **`MEMBER_ROLE_CHANGES` のエンティティ説明**を新設し、リレーション表へ 1.2.0 で漏れていた2行を追加。<br>⚠️ **反映作業中に §6-6b① の記載誤りを1件検出**：`oyakata_member_no` という**本リポジトリのどこにも列定義が無い**カラムが挙がっている。上流には実在する列だけを反映し、**要確認として明示**した。なお「親方は別系統の採番を持つ（番号空間は分離）」自体は 2026-09-05 の生きた決定であり、現状は **`legacy_member_no` の `OYA-` 接頭辞**で実現している。**専用列を持たせるか接頭辞で足りるかが未決**で、専用列を作るなら §5.2・§6.2・§6-6b① を同時に直す必要がある | ✅ |

> [!success] 4件目の**正本 v13 への反映も完了している**（2026-09-20 確認）
> ~~v13 §2・§5.9.3 は「認可は `role` のみで判定する」までしか書いておらず、自己変更の禁止と
> ガードトリガーの存在に触れていない~~ → 正本 v13 §7「★ 権限変更履歴」に
> **自己変更の禁止・`CHECK (member_id <> operator_id)`・トリガーによる強制**が記載済み（v1.27.0 ／ 2026-09-12）。
> 残るのは §5.9.3（サーバサイド認可）本文からの相互参照のみで、実装をブロックしない。

### 16-2. 半ブロッカー（~~4件~~ → ~~3件~~ → **2件**）— 退避策はあるが放置すると事故る

> [!note] 2026-09-11 の**ベクトル基盤一本化で1件が論点ごと消滅**した（下表の #61）。

| QUESTIONS.md 項目 | 起票日 | 優先度 | ブロックする作業パッケージ | 退避策 |
| --- | --- | --- | --- | --- |
| ~~**★ ニックネーム未設定会員の他者向け表示**~~ → ✅ **2026-09-10 表示規則が完成** | **2026-09-05** | 中 | ~~5-1, 2-3~~ → **ブロック解除** | ①未設定時は**会員番号**（09-05 確定）②**親方兼街人は「街人番号」**（**09-10 確定**）。⚠️ **提案していた「親方優先」は不採用**。両方の番号を持つ会員は常に街人番号を用い、`member_type` は表示番号の選択に影響しない。**残る②`nickname` 必須化・③設定依頼運用・⑤親方番号の採番方式は表示規則をブロックしない** |
| **EUMO発行記録の取得元・既発行分の突合基準**（#59） | 2026-09-01 | 高 | **5-7, 12-4, 12-3, 3-2, 10-1/10-2** | §5.10.8③ の「疑わしい会員は自動起票せず『要確認』表示→運営判断」で**手動フォールバック前提なら実装は進む**。ただし自動起票の完全稼働は不可。誤ると二重付与／付与漏れ |
| ~~**宿泊形態の旧名称が正本に残存**（#55）~~ → ✅ **2026-09-22 解消** | 2026-08-26 | ~~中~~ → **解消** | ~~**3-9, 3-8, 3-5b/3-5c, 3-7**~~ → **ブロック解除** | 正本 §4・§5.2.4・§5.2.5・§7 を §5.4.2 確定表へ一括置換（v13 v1.29.2）。値の変更は伴わない |
| ~~**アプリデータのナレッジ空間投入方式**（#61）~~ → ⛔ **論点消滅（2026-09-11）** | 2026-09-03 | ~~高~~ | ~~**17-2**~~ → **`1-6`／`9-1` へ移行** | **単一ベクトル空間化により app → bot の移送自体が消え、投入方式 A〜C は検討不要になった**（`CONSOLIDATED_DECISIONS.md` §17-4）。**PII ガードの原則（ロールではなくチャネルで判定）は維持**され、実装場所が Firestore 側アプリコードから**単一DB内の RLS／ビュー**へ移る。<br>~~（起票時点の記録）方針・PIIガードとも回答済み（09-10）／投入方式A〜Cは未回答**。ガードは **出力導線での channel ガード**（投入は止めず、**LINE 経路で `pii_flag` を無条件除外**）に確定。⚠️ **実装時の必須条件**: 判定軸は**ロールではなくチャネル**（`target_role` を見た時点で、非推奨として退けた案b になる）。⚠️ **新たな先行条件**: 現行は全件スキャン＋アプリ側計算のためフィルタがコード頼みになる。**`find_nearest` 移行を先に行う**と DB 層強制の事前フィルタにでき、実装漏れリスクが構造的に消える~~ |

### 16-3. 非ブロッカー（回答が無くても実装を進められる／~~12件~~ → **11件**）

| QUESTIONS.md 項目 | 起票日 | 優先度 | 影響 |
| --- | --- | --- | --- |
| ~~9/10~~ **9月中**のリリースにクレカ決済が無くても運用できるか（#52関連） | 2026-08-26 | 高→**中** | 実装は非ブロック。⚠️ **2026-09-03 の納期改訂で重みが下がった** — 9/30 までなら加盟店審査の下限（2週間）は収まるため、Phase 1 復帰も選択肢になる（§19-4）。⚠️ **残22日となり、この窓は 09-08 時点でほぼ閉じた** |
| 物販（Phase 2）のリンク先ECは既存か新規か（#53） | 2026-08-26 | 高 | Phase 2 の見積り・体制。新規構築なら別タスク立ち上げが必要 |
| line-rag-bot の検索・フォールバック・エスカレーションフロー（A1〜E3） | 2026-08-22 | 高 | **浮遊街アプリ側は非ブロッカー**（API連携ゼロのため）。line-rag-bot 側の真のブロッカー |
| 写真AI判定の残る未決（責任範囲・コスト上限） | 2026-08-29 | 高 | line-rag-bot 側・Phase 2。**アプリ側の変更は不要**（#56 A案確定） |
| **★ 監査ログ A6「AI対話履歴」を GCS へどこから書き出すか** | **2026-09-05** | 中 | **新規**。監査ログの GCS 保管自体は決定済み。書き出し元の確定は実装時でよい（ジョブ基盤の決定に従属） |
| ~~Firestore `find_nearest` への移行時期（前倒しの可否）~~ → ⛔ **論点消滅（2026-09-11）** | 2026-09-03 | ~~高~~ | **Firestore を検索基盤として使わなくなった**ため（`CONSOLIDATED_DECISIONS.md` §17-4）。削減分 3〜4人日は §19-2b の差し引きに計上済み |
| 提供ステータス表示文言の仕様内不整合 | 2026-09-03 | 中 | 6-5。正本「調理中」が自動的に勝つ（CLAUDE.md §1.1）。**ただしマイログにも表示するかは 8-4 のスコープを増減させる** |
| 利用者向けロール表示名の最終ラベル（#58） | 2026-09-01 | 中 | 2-5（表示文言のみ）。内部実装は非ブロック |
| LINE／Discord チャット履歴の吸い上げスコープ（#60） | 2026-09-01 | 中 | Phase 2 のため Phase 1 は非ブロック |
| 個人情報カラムのカラムレベル暗号化の採否 | 2026-08-28 | 中 | 2-4。「Phase 1 不採用」提案でありA承認なら実装差分ゼロ |
| RAG統合の未決事項（残13件） ／ 監査ログ10年経過後のアーカイブ方針 | 2026-08-22 / 08-28 | ―／低 | 主に line-rag-bot 側。アプリ側は 9-2 に限定的。アーカイブ方針は法務確認の可能性のみ |
| 直売所の郵送購入の運用条件 ／ メタバース連携の採否 ／ v13 §9末尾「クライアント確認事項」表の更新漏れ | 2026-08-15 | 低 | Phase 1 スコープ外・表記のみ |

### 16-4. 解決済み（記録として保持）

**2026-09-05 に解決したもの（本改訂での最大の変化）**

| 項目 | 解除された作業パッケージ |
| --- | --- |
| **ロール別レスポンスマスキング → A案（PII別テーブル分離）で確定** | **2-2 ／ 2-3（一部） ／ 2-4 ／ 8-1 ／ 8-4 ／ 10-3** の6件 |
| **ネットワーク層の防御（F-9） → 「3前線」構成で確定** | **1-7**（設計 40% → 80%） |
| **朝会文字起こしテキストの投入経路 → アプリUIへのコピー＆ペーストで確定** | **4-1 ／ 4-2**（スコープ自体が縮小。`1-4` 依存も解消） |

**2026-09-08 に解決したもの**

| 項目 | 解除された作業パッケージ |
| --- | --- |
| **無料枠で満たせない非機能要件の扱い → Supabase を有料プランへ変更** | **1-1**（自動停止・DB 500MB・プロジェクト数上限2 の制約が消滅。非機能 A4 も充足）。**真のブロッカーから外れた** |
| **自律開発ループの「移行」→ 移行作業として扱わず、既存の実装にタスクを流す** | **0-D**（工数の食い合いが解消）。実体は参照切れ7か所と frontmatter 未付与7本で、**いずれも 09-08 に修正済み** |
| **Phase 1 で何をリリースするか → 61パッケージ全部** | §19-5 #1 が解消し、**完了判定が可能になった** |

**それ以前に解決したもの**

技術スタック承認／個人情報方針の大転換／宿泊券4枚／差額繰越の詳細／親方・街人リストの最終承認（370名）／
pgvector 不採用（line-rag-bot へ統合）／30,000円の会計区分／宿泊予約フォーム連携5点／
2026-08-13レビュー未反映11項目／テスト基盤のCI構成（**WBS 1-3 のブロック解除**）／AI基盤 Claude 確定／
収容枠と部屋タイプの確定（合計66名）／キャッシュバックの単位＝コイン（uii）／子ノートの実体

---

## §17. 運用ドキュメント（Phase 1 納品物）

| # | 作業パッケージ | 概要 | 依存 | 規模 | 完成度 | ステータス |
| --- | --- | --- | --- | --- | ---: | --- |
| 17-1 | **現場運用マニュアル（運営スタッフ向け ＋ 利用者向け）** | 2本セット。旧 13-2 を統合 | モック確定・実装完了 | M | **50%** | ⏸ **保留（ただし実物は存在する）**。運営向けは **493行・v0.1.2・全13章＋38小節すべて本文あり・TODO/空節ゼロ**。⚠️ **冒頭に「現場ではまだ配らないでください」の警告**（実画面と未突合）。**利用者向けは未着手**。§12 に未決8件 |
| **17-2** | **★ コンシェルジュ運用ナレッジの整備・本番投入**（新規起票・**完了扱い**） | `ops-01`〜`ops-50` の50件を本番 `default` テナントへ投入（18件→68件）。検索動作確認6問の類似度実測 0.759〜0.846（閾値0.55を全件クリア） | — | M | **90%** | ✅ **2026-08-23 完了**。**Phase 1 で唯一「本番投入・動作確認まで到達した納品物」**。残: `role_filter` 有効化（オーナー判断で現状は全件公開）・利用者向けナレッジ・実装後の突合 |

> [!warning] 17-1 の着手条件が存在しないファイルを指している
> 着手条件②は「`prototype_v14.html` の更新完了」だが、**`prototype_v14.html` はリポジトリ・ボルトのいずれにも存在しない**。
> 実在するのは `prototype_v15.html`（2026-08-26 更新＝マニュアルより3日新しい）。
> **着手条件を `prototype_v15.html` への突合へ付け替える必要がある。**

---

## §18. モック・設計書の維持

| # | 作業パッケージ | 概要 | 規模 | 完成度 | ステータス |
| --- | --- | --- | --- | ---: | --- |
| **18-1** | **★ HTMLモック `prototype_v15.html` の維持・レビュー反映**（新規起票） | 4,096行（HTML 2,383行＋Alpine.js 約1,450行）。**12画面＋モーダル/ドロワー15本**。クエストの状態機械（`open→申請中→指示済み→報告済み→コアメンバー確認済→承認完了`）、Uii自動換算、残枠都度算出、精算QR自動失効、メールOTP検証まで動作 | M | **85%** | 🟡 **v1.21.0 が未追随**（CONSOLIDATED §14-2 が明記）。`fetch`/`localStorage` は0件でインメモリのみ＝バックエンド結合は未着手 |
| **18-2** | **★ 画面設計.md のリバース更新 ＋ 画面ID対応表の作成**（新規起票） | 設計書（2026-08-22・33画面）がモック（2026-08-26）より古く、~~12件~~ → **20件がモック先行で設計書に未記載**。さらに設計書の `A1〜C13` 採番とモックの `①〜⑫` 採番が**別体系で対応表が存在しない** | S〜M | **90%** | 🟢 **2026-09-10 実施**。`画面設計.md` に **§2-4 画面ID対応表**（採番3体系の対応・1:多の写像3か所・全33画面の実測判定）と **§2-5 A11 の食い違い明細**を新設。§2 の「状態」列が**両方向に陳腐化**（「要改修」5件は解消済み／「新規」8件は実装済み）していたことを明記し、現行判定を §2-4 へ移した。§18-A の実装済み28件のうち**3件が過大計上**（B9・B11・A3/A6）であることも検出。⚠️ 残10%＝**§4 の構成メモ9本のモック追随**（特に C10 が「ドラフト・参考記述のみ」のまま） |

### 18-A. モックと設計書の突合結果

> [!important] 2026-09-10 全面更新（WBS 18-2 実施）
> `画面設計.md` §2-4 に**画面ID対応表**を新設し、全33画面をモック実測値と突合した。
> **突合の正本は `画面設計.md` §2-4** であり、以下は件数サマリーのみを持つ（二重管理禁止・CLAUDE.md §1.1）。

| 区分 | 件数 | 内訳 |
| --- | --- | --- |
| ✅ 一致 | **12** | A1, A4, A5, B1, B2, B3, B5, B8, C1, C3, C5, C13 |
| ⚠️ 双方にあるが食い違う | **14** | A3, A6, A8, A9, A10, A11, A12, B4, B6, B7, B10, B11, C2, C9, C11, C12 のうち実装差のあるもの |
| 🔴 設計のみ（モック未実装・部分） | **6** | **A2** ホーム（専用画面なし・3分散）／**B9** コンシェルジュ導線（**core_member から到達不能**）／**C4** データ管理CRUD（部分）／**C7** 街人登録申請一覧（未実装）／**C8** 宿泊日数の手動増減（未実装）／**C12** 月次給付総額サマリー（部分） |
| 🔵 モックのみ（設計書に未記載） | **20** | ~~12~~ → 実測20件。内訳は `画面設計.md` §2-4-3 |

**前版（2026-09-03）からの訂正3件** — いずれも「実装済み28件」の数え方が実態より甘かった。

| # | 前版の記述 | 訂正 |
| --- | --- | --- |
| 1 | 実装済みに **B9** を含む | ❌ **誤り。**導線ボタンは⑫ダッシュボード（`roles:['admin']`）にしかなく、**B9 の対象ロール `core_member` から到達できない**。C5 は充足するが B9 は未充足 |
| 2 | 実装済みに **B11** を含む | ⚠️ **配置が不一致。**残枠表示は⑥⑪⑫に存在するが、設計の指定位置「B4（＝⑧顧客管理）に内包」には無い |
| 3 | 実装済みに **A3 / A6** を含む | ⚠️ **要改修が未消化。**A3 は `guest_allowed` がモックの `quests[]` にフィールドごと無く個別施錠表示が未実装。A6 は差額繰越・運営修正バッジが無い |

> ⚠️ **C7・C8 はいずれも管理者しか操作できず監査ログが要る業務**。モックでフロー未検証のまま実装に入るリスクが高い。
>
> ⚠️ ~~**A11（宿泊予約）の宿泊形態名の不一致は C7・C8 より波及範囲が広い** — 予約・料金マスタ・残枠・カレンダー・ダッシュボードの**5画面**に及ぶ。`QUESTIONS.md` #55（旧名称の残存）を解消しないままマスタを組むと誤った初期値が生成される（§16-2）。~~ → ✅ **2026-09-22 解消**（§16-2）。明細は `画面設計.md` §2-5。

---

## §19. 9月中リリースに向けた実装順序とスコープ仕分け（2026-09-08 改訂）

> [!important] 仕分け案は提案であり決定ではない
> 納期「9月中」はオーナー確定事項だが、**Tier の割り当ては提案**である。
> 正本 v13 §4 は「スコープは据え置き（絞り込みは行わない）」のままであり、本節はそれを覆すものではない。
> Tier B/C は「削る」提案ではなく、**実装着手の順序**として読むこと。

### 19-1. 前提として動かせない事実

| 事実 | 内容 |
| --- | --- |
| **残日数** | **10日**（2026-09-20 → 09-30）。~~7日~~ → 27日 → ~~22日~~ → **10日** |
| 実装済みコード | ~~0行・`package.json` すら存在しない~~ → ~~13ファイル・290行の足場のみ~~ → **`.ts`/`.tsx` 40ファイル・2,239行 ＋ マイグレーション13本**。**認証・RLS・部屋台帳・朝会投入・クエストボードは動く**が、**注文・会計・公開予約は0行** |
| 実装対象パッケージ | **61件**（うち L規模 2件・M規模 20件）。**完了12件／部分実装2件／残り47件が 0%**（2026-09-20） |
| 設計の到達度 | **平均80%**。モックは 33画面中28件が動く状態で実在 → **残るのは主に実装工数**であり、仕様検討からのやり直しではない |
| 直近12日（09-08〜09-20）の工数の向き先 | **認可基盤に集中した**（`2-1`・`2-1b`・`2-2`・`2-3` ＋ `1-1`・`1-3`）。CLAUDE.md §4.4 が必須とする3領域のうち**認可は実装とテストが揃った**が、**金額計算は `7-1` のみ・個人情報は収集UIが未実装**である |
| ~~直近5日（09-03〜09-08）の工数の向き先~~ | **初めてアプリ実装へ向いた**（Next.js 雛形・テスト基盤・Supabase 配線・Uii換算）。ただし**同時に自律ループの方式変更（09-07）も走っており、工数は二分されている** |
| 8/22 時点の前提 | 「モックが 8/23 に確定し、**モック＋要件定義書を入力として Claude が実装する**ため工期は足りる」 |
| その前提の現況 | モックは確定済み。実装も**09-05 に開始された**。⚠️ ただし**5日間で進んだのは足場だけ**であり、**残22日で56パッケージ**という比率は 09-03 時点より厳しい |

> [!danger] 09-03 版との最大の差 — 「着手できない」から「間に合うのか」へ論点が移った
> 前版のボトルネックは **`package.json` が無く物理的に着手不能**なことだった。これは解消した。
> いま残っているのは**純粋な実装スループットの問題**であり、**残22日・56パッケージ**は
> 1日あたり2.5パッケージのペースを要求する。**§19-5 #1「何をリリースするのかの定義」が
> 未回答のままでは、このペースが達成不能かどうかすら判定できない。**

### 19-2. 残**20日**で最初に潰すべきもの（順序が重要 ／ 2026-09-10 時点の記録）

> [!success] **「回答待ち」は消えた。ここからは実装スループットだけの勝負になる**
> §16-1 の真のブロッカーは **2026-09-10 に全て解消**した（3件 → 0件）。
> 前版がここに並べていた「決定を取る」タスクは**すべて消化済み**である。
> **残り20日・56パッケージ**に対して、以後の遅れは**意思決定ではなく着手の問題**として扱うこと。
> （2026-09-20 追記: この節は 09-10 時点の記録として残す。**現時点の順序は §19-2c** を見ること）
>
> ✅ **決定の設計書への反映（§16-1a の3件）も同日中に完了した。**
> 設計書と決定の食い違いは解消済みで、**いま設計書を読んで実装に入って問題ない状態**にある。

| 順 | やること | 理由 | 09-08 版からの変化 |
| --- | --- | --- | --- |
| ~~1~~ | ~~**§16-1a の設計書反映3件**（システムアーキテクチャ／非機能§7-4／API設計§2-2b）~~ | ~~決定と設計書が食い違っている状態で実装に入らない~~ | ✅ **2026-09-10 完了**（3件とも反映済み。§16-1a 参照） |
| ~~1~~ | ~~**「公開予約OTPの送信基盤」の決定**~~ | ~~`3-5b`（Phase 1 最大の L規模）を直接ブロック~~ | ✅ **2026-09-10 解決**（Resend）。⚠️ **起票時の前提が誤りだったことも判明**（公開予約OTPは自前実装で既定SMTPを経由しない）。**アプリ側への送信SDK配線が別途必要**な点は `3-5b` の作業に含めること |
| ~~2~~ | ~~**「非同期ジョブの実行基盤」の選定**~~ | ~~6ジョブ＋監査ログ日次エクスポートに波及~~ | ✅ **2026-09-10 解決**（Cloud Tasks ＋ Cloud Scheduler・`pg_cron` 不使用）。⚠️ **派生作業**: pg_cron を捨てたため定時3本が DB へネットワーク接続する。**Secret Manager への資格情報配置**と、**N-2（IP許可リスト）を採るなら VPC コネクタ＋Cloud NAT で静的IP**が要る |
| ~~3~~ | ~~**「QRトークンの保護強化」の決定**~~ | ~~DBスキーマ変更を伴うため、`7-3` 実装後の後付けはマイグレーション修正が必要~~ | ✅ **2026-09-10 解決**（A案。マイグレーション未作成の段階で確定したため後付けコストゼロ。`7-3`・`12-2` がブロック解除） |
| ~~4~~ | ~~**「無料枠で満たせない非機能要件の扱い」の決定**~~ | ~~`1-1` に直結~~ | ✅ **2026-09-08 解決**（Supabase を有料プランへ変更） |
| ~~—~~ | ~~「ロール別レスポンスマスキング」の方針決定~~ | ~~Tier A の 2-2・8-1 を直接ブロック~~ | ✅ **2026-09-05 解決**（A案）。前版の「順1」が消えた |
| ~~**5**~~ | ~~**`1-1` Supabase プロジェクトの実作成**~~ | ~~配線コードは入ったが**プロジェクトが無い**ため、`2-1` 以降のスキーマ実装が一切進まない。**現在の最大の物理的ボトルネック**~~ → **✅ 2026-09-13 完了**。プロジェクト作成・接続確認まで済み、**`2-1` 以降のスキーマ実装が物理的に可能になった** | ✅ **解消** |
| **6** | **`18-2` 画面設計.md のリバース更新 ＋ 画面ID対応表** | 設計書とモックで採番体系が違い対応表が無い。**このまま実装に入ると「どの画面が A5 か」を都度推測する事故が起きる** | → 据え置き |
| ~~—~~ | ~~§S-5 の未コミット差分の処理~~ | ~~2週間分の成果が履歴に入っていない~~ | ✅ **2026-09-03 解決** |
| ~~—~~ | ~~`1-2` Next.js雛形の作成~~ | ~~ここが無い限り 0-1 以下すべてが着手不能~~ | ✅ **2026-09-05 解決** |

### 19-2b. ⚠️ 2026-09-11 のベクトル基盤一本化による工期への影響（残19日）

**2026-09-11 の決定（v13 §9 #31 限定改訂）は、差し引きで作業を増やす。**
残19日・実装0%の56パッケージという状況に対し、正直に記録する。

| | 内容 | 規模 |
| --- | --- | ---: |
| ➖ **不要になった** | `find_nearest` 移行（Firestore の KNN 化） | **-3〜4人日** |
| ➖ **不要になった** | ナレッジ移送キューの実装（方式A/B/C） | -（未着手のため実質ゼロ） |
| ➕ **新規発生** | `line-rag-bot` の検索基盤移行（`firestore_knowledge_store.py` 194行 ＋ `indexer.py` 238行 ≒ **430行**＋テスト） | **+3〜5人日** |
| ➕ **新規発生** | 本番ナレッジ**68件の移行**（`default` テナント。⚠️ **浮遊街以外の18件が同居**のため切り分けが要る） | +1〜2人日 |
| ➕ **新規発生** | **Cloud Run → Supabase の接続経路**（Secret Manager・**Supavisor**・N-2 を採るなら静的IP） | +1〜2人日 |
| ➕ **新規発生** | ~~案 `0100` の適用（前提テーブル作成後）~~ → ✅ **2026-09-20 完了**（PR #82）。＋**検索時スコープ**（LINE経路の個人紐付き除外）の実装は残 | ~~+2〜3人日~~ → **+1〜2人日** |
| ➕ **新規発生** | PII 検出の範囲拡張（現状は**既知氏名のみ**。住所・電話・メール・金額・`member_notes` が未カバー） | +1〜2人日 |

> [!warning] **差し引き +5〜10人日の増加**（`find_nearest` の削減を織り込んでも）
> **これは Phase 1 の 61パッケージとは別枠の作業**であり、`line-rag-bot` リポジトリ側にも跨る。
> **9/19 の中間チェックポイント**（チェックイン・注文・会計が通しで動くこと）は**アプリ本体の話**であり、
> 本作業はそこに寄与しない。
>
> ✅ **決着（2026-09-20 オーナー確定）**: **Phase 1 に入れる。**
>   同日 `0100` を dev Supabase へ適用済み（PR #82）。`.github/workflows/ci.yml` の退避ステップも撤去し、
>   CI のローカルスタックで `0100` が毎回適用・検証される状態になった。
>   残るのは検索時スコープと PII 検出の範囲拡張であり、`0100` の適用自体はもう論点ではない。
>   → `QUESTIONS.md`「[2026-09-20] ベクトル基盤（WBS `1-6`／`9-1`）を Phase 1 に入れるか、Phase 2 へ回すか」は
>   **本決定により回答済みでクローズ**した（起票と同日に決着）。
>
> ~~⚠️ **判断が要る点**: この移行を **Phase 1 の 9/30 までに入れるのか、Phase 2 へ回すのか**が未確定。~~
> - **Phase 2 へ回す場合**: Phase 1 は現行どおり「アプリはベクトル基盤を持たず、bot は Firestore のまま」で走り、
>   9/30 後に移行する。**アプリ本体の実装に集中でき、納期リスクは増えない**
> - **Phase 1 に入れる場合**: クエスト起票・評価でナレッジを使う機能が 9月中に入るが、
>   **56パッケージが 0% の現状では、コア業務（チェックイン・注文・会計）を圧迫する**
>
> ~~**正本 v13 §4 は「61パッケージ全部をリリース」（2026-09-08 確定）であり、本作業はその 61 に含まれていない。**~~
> → **2026-09-20 の決着により、`1-6`（pgvector有効化・RAG基盤）・`9-1`（アプリ内の横断セマンティック検索）は
> Phase 1 の対象に入った。実装対象は 61件 → 63件**（§S-1）。`line-rag-bot` 側の移行は引き続き別リポジトリの作業である。

### 19-2c. 残**10日**で最初に潰すべきもの（2026-09-20 ／ 提案）

> [!important] 認可基盤は揃った。**残っているのは「現場の1日」を通す経路である**
> 09-13〜09-20 の12日で完了した12件は**認証・認可・スキーマに偏っている**。
> 一方 **`3-2` チェックイン以降（注文 → 会計 → 顧客管理）は1行も書かれていない**。
> Tier A（§19-3）のうち未着手は **`3-2`・`6-1`〜`6-4`・`7-2`・`8-1`・`8-2`・`8-4`・`8-5`・`5-2`〜`5-4`・`2-6`** である。

| 順 | やること | 理由 |
| --- | --- | --- |
| **0** | **`1-1c`・`1-1d`・`1-1e`・`1-2a`（開発環境の設定）** | **2026-09-21 追加 → 同日 `1-1c`・`1-1d` 完了。** 実機確認で、**dev Supabase に `0014`〜`0020` が未適用・`auth.users` が0件・Vercel プロジェクト未連携**であることが判明した（§S-0c）。前2件を解消した結果、**順1以降の画面を作りながら実DBで動作確認できる状態になった**。残る `1-1e`・`1-2a` はブラウザ操作（§S-0d）で、**順1以降をブロックしない** |
| **1** | **`3-2` チェックイン／チェックアウト** | 注文（`6-1`＝チェックイン中ユーザー限定）・会計・再訪アラートがすべてここに依存する。**宿泊者名簿の収集（v13 §5.2.7）も 2026-09-20 に本パッケージへ確定**しており、法定記録の入口でもある。⚠️ 完了条件は `QUESTIONS.md` [2026-09-19] WBS `3-2` が未回答（A/B/C）だが、**A（状態切替＋名簿に限定）なら回答を待たずに着手できる** |
| **2** | **`6-1`〜`6-4` 注文・メニューマスタ** | 現場が毎日使う唯一の高頻度機能。設計90%・モック実在で、**残るのは実装工数だけ** |
| **3** | **`7-2` 伝票管理 → `8-1` 顧客サマリー** | 会計の中核。`8-2`・`8-4`・`8-5` は `8-1` の上に薄く載る |
| **4** | **`5-2`〜`5-4` 受注申請・完了報告・日次承認** | `5-1` が完了し、受注申請APIの**認可ゲートだけは先に入っている**（`src/lib/quests/application-gate.ts`）。その関数に条件を1つ足す形で `5-2` に入れる |
| **5** | **`2-6` 管理系メニューの到達性** | `2-3` が完了したため、ナビ定義の置き場は既にある（`src/lib/auth/navigation.ts`） |
| 6 | **`9-1` の検索時スコープ・PII 検出の範囲拡張** | ~~Phase 1 に入れるか Phase 2 へ回すかが未確定~~ → ✅ **2026-09-20 に Phase 1 で確定**（§19-2b）。基盤（`1-6`）は dev 適用済みで、残るのはこの2点。⚠️ ただし**上記1〜5（現場の1日を通す経路）より後**に置くこと |

> [!warning] 残10日で 47パッケージは、実績ペースでは収まらない
> 09-13〜09-20 の実績は **12日で12件**（≒1件／日）。この実績を残10日へ当てると **10件**であり、
> **47件との差は 37件**になる。§19-5 #4 の「新しいエスカレーション条件を設定するか」は、
> **9/30 を待たずに判断が要る論点**として残っている。

### 19-3. 仕分け案（Tier A/B/C）

**Tier A｜これが無いと現場が一切回らない（最小の運用可能セット・約20パッケージ）**

| 段 | パッケージ |
| --- | --- |
| 基盤 | 🟡 `1-2` Next.js雛形（**80%**・Vercel 設定が未検証） → ✅ `1-1` Supabase（**2026-09-13 完了**） → ✅ `1-3` テスト基盤（**2026-09-15 完了**。E2E シナリオの中身は残作業） |
| 認可 | ✅ `2-1` ロール（09-14） → ✅ `2-2` RLS（09-16） → ✅ `2-1b` ログイン導線（09-16） |
| 全画面前提 | 🟡 `0-1` 通貨表示の共通コンポーネント（**60%**・置き換える画面待ち） |
| 宿泊 | ✅ `3-1` 部屋台帳（09-16） → ⬜ **`3-2` チェックイン（未着手・§19-2c の順1）** |
| 注文 | ⬜ **`6-1`〜`6-4` すべて未着手**（セルフ注文 → 代理注文 → SOLDOUT → メニューマスタ） |
| 会計 | ✅ `7-1` Uii換算（**完了**） → `7-2` 伝票管理 |
| 顧客管理 | `8-1` 顧客サマリー ✅**ブロック解除（09-05）** → `8-2` 未会計額 → `8-4` マイログ → `8-5` ソート順 |
| クエスト | ✅ `5-1` ボード（09-19・**ニックネーム表示の半ブロックは 09-10 に解除済み**） → ⬜ `5-2` 受注申請 → ⬜ `5-3` 完了報告 → ⬜ `5-4` 日次承認 |
| ナビ | ✅ `2-3` ロール別UI表示制御（09-16・**親方兼街人＝街人番号で 09-10 に決着**） → ⬜ `2-6` 管理系メニュー到達性 |

- 設計進捗は平均85%以上でモックも動いており、**残るのは純粋な実装工数**
- ✅ **2026-09-05 に `2-2`／`8-1` のブロックが解除された**。前版の「19-2 順1」は消化済みで、Tier A の完走を阻む認可側の障害は無くなった
- ~~⚠️ 代わりに **`1-1` の Supabase プロジェクト未作成**が Tier A 全体を止めている（`2-1` 以降のスキーマ実装が着手できない）~~
- ✅ **2026-09-13：`1-1` 完了により Tier A の物理的ブロックは解消**。あわせて 2026-09-12 に `QUESTIONS.md` の会員スキーマ反映漏れも決着したため、**`2-1` は起票3条件を満たす**（残るは完了条件を人が書くこと）
- ~~⚠️ `5-1`／`2-3` は**ニックネーム未設定会員の表示規則**が未確定のため、表示部分のみ半ブロック~~ → ✅ **2026-09-10 に決着し、両パッケージとも 09-16／09-19 に完了**
- ⚠️ **2026-09-20 現在、Tier A の未着手は `3-2`・`6-1`〜`6-4`・`7-2`・`8-1`・`8-2`・`8-4`・`8-5`・`5-2`〜`5-4`・`2-6`**（§19-2c）

**Tier B｜残22日で射程に入るかは微妙。現行運用での代替が効く（納期リスクの調整弁）**

| パッケージ | 代替手段（間に合わない場合） |
| --- | --- |
| `3-5b` 公開予約ページ（**L規模・Phase 1 最大**） | **Googleフォームがまだ稼働している**（公式サイトの「Reservation」リンク差し替えは運営側作業のため、切り替えなければ現行フォームが生き続ける）。**7日では不可能だったが27日なら射程に入る** |
| `3-7`／`3-8`／`3-9` アプリ内予約・残枠算出・料金マスタ | 3-5b と一体。~~`3-9` は #55（旧名称残存）の解消が先~~ → **2026-09-22 解消済み・着手可能** |
| `3-3` 予約キャンセル・ノーショー ／ `3-6` 宿泊予定カレンダー | Googleカレンダー／TimeTree の現行運用を継続 |
| `3-5c` カフェ事前予約 ／ `6-5` 提供ステータス | 口頭・紙運用を継続。`6-5` は表示文言の不整合を先に決着 |
| `5-5`／`5-6` Uii支払・二段階承認 | 運営の目視承認を継続 |
| `7-3` 精算QR ／ `12-1`〜`12-3` 街人登録導線 | 現金・既存 eumo QR 運用を継続（**`7-3` は QRトークン保護の決定待ち**） |
| `10-1`／`10-2` 一括インポート・名寄せ | 370名の移行は初回チェックイン時の現地入力で段階的に吸収可能 |
| `5-7` Eumo給付追跡 | メール運用の継続（**#59 未決のため、自動化を急ぐとむしろ二重付与リスク**） |
| `9-2` コンシェルジュ導線 | **規模S・ボタン1つ**。設計100%なので最後に差し込める |
| `17-1` 運用マニュアル | 運営向けドラフト493行が既にあり、実機突合後に配布すればよい |

**Tier C｜9月中に間に合わなくても運用への支障が小さい**

`4-1`〜`4-3` 朝会議事録AI ／ `14-1`〜`14-3`・`14-6` メディア ／ `13-1` ダッシュボード ／
`3-10` 滞在中の変更 ／ `8-6` 宿泊履歴表示 ／ `12-4` 初回CB自動起票 ／ `2-5` ロール表示名 ／
`10-3` 再訪アラート ／ `10-4` 宿泊日数手動調整 ／ `8-3` 差額繰越

### 19-4. 納期改訂で状況が変わった論点

| 論点 | 9/10 時点の判断 | 9月中（〜9/30）での再検討 |
| --- | --- | --- |
| **クレジットカード決済（#52）** | **Phase 2 送り**。理由は「加盟店審査に通常2〜4週間かかり、9/10 に対して審査落ち時の代替時間が残らない」 | ⚠️ **前提が変わった**。9/30 までなら**審査期間の下限（2週間）は収まる**。ただし審査落ち時の代替時間は依然ほぼ無いため、**Phase 2 据え置きが妥当と思われるが、判断はオーナーマター**。あわせて「クレカ無しで運用できるか（未回答）」の重みも下がる |
| `3-5b` 公開予約ページ（L規模） | Tier B（現行フォームで代替） | **Tier A に近い扱いへ格上げ可能**。27日あれば実装が射程に入り、Googleフォーム廃止（#46）の意図を9月中に実現できる |
| 「何をリリースするのか」の定義 | 未定義のまま | **依然として未定義**。61パッケージ全部か Tier A のみかが決まらないと**完了判定ができない**（QUESTIONS.md が自ら記録している論点） |

### 19-5. 残る確認事項

1. ~~**9月中に「何を」リリースするのかの定義**~~ → ✅ **回答済み（2026-09-08）：61パッケージ全部をリリースする。** 部分リリースは行わない。§19-3 の Tier A/B/C は**着手順序**であってスコープの仕分けではないことが確定した。⚠️ ただし**残22日で56パッケージ**という比率は変わらず、スコープ据え置きの決定は工期の逼迫を解消しない
2. **クレカ決済の Phase 1 復帰を検討するか**（19-4 参照）
3. ~~**正本 v13 §4・QUESTIONS.md・CONSOLIDATED_DECISIONS.md §1 への納期改訂の反映**~~ → ✅ **反映済み**（v13 v1.22.0 ／ `QUESTIONS.md` [2026-08-22] Phase 1 リリース目標 ／ `CONSOLIDATED_DECISIONS.md` §1）。<br>⚠️ **代わりに 2026-09-05 決定の下流反映が滞っている** — `WBS_Phase1.md`（本書・本改訂で対応）以外に、**v13 §5.1・§8（朝会のテキスト化・非機能P3の起点）と §5.10（メール必須）が未反映**。`API設計.md` のエラーコード共通封筒、`システムアーキテクチャ.md` の監査ログ保管構成・課金モデルの節も「新設予定」のまま
4. **新しいエスカレーション条件を設定するか**（旧条件「8/31 時点で主要機能が動いていない場合」は発火済み・09-03 改訂で対応済み）。⚠️ **残22日となった現在、中間チェックポイントは未設定のまま**。「9/15 時点で Tier A の基盤3件（`1-1`・`1-2`・`1-3`）が揃っていなければ再交渉」のような**具体的な発火条件を置くことを推奨する**
5. 品質を削る選択肢は取れない（CLAUDE.md §4.4 が**認可・金額計算・個人情報**の3領域のテストを必須としているため）
6. ~~**★ 自律開発ループの方式変更（09-07）を、残22日の工期の中でいつ実施するか**~~
   → ✅ **回答済み（2026-09-08 オーナー確定）：「移行」という作業は存在しない。**
   エージェント定義11本・`auto-01`〜`auto-03`・承認ゲートはすでに揃っており、**やることはタスクを流すことだけ**。
   09-07 の決定は作り直しではなく**方式の再定義**だった。したがってアプリ実装と工数を食い合う懸念は消えた。
   ⚠️ ただし調査の過程で「流すだけ」を実際に阻んでいた不具合が2件見つかり、**同日修正した**：
   **①`auto-02`／`auto-03` の参照切れ7か所**（`automation/prompts/` を指したままで、実行すれば必ず失敗する状態）／
   **②frontmatter 未付与7本**（`tools:`／`settings:` が無く、設計 §7.2 の権限制限が効いていなかった）。
   現況は `check_agents.py` 違反なし（11体すべて ok）。**残るのは実装ループの試運転と Environment の作成のみ**。

---

## 改訂履歴

| 日付                          | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-16                  | 初版作成。v13 §4・§5・§6・§7、QUESTIONS.md、CONSOLIDATED_DECISIONS.mdを基に、Phase1スコープを14機能領域・約45作業パッケージへ分解。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-16（追記）              | オーナー回答（ナレッジ・RAG基盤をline-rag-botへ統合、v13 §9 #31）を反映。1-6・9-1（pgvector関連）を不要化。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-16（重要変更）            | Streamlit継続方針の最終確定を反映。9-2を「導線のみ」に縮小、9-5を「読み取り専用API」に縮小。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-16（再確定）             | API連携ゼロが最終確定。9-2を外部リンクのみに再縮小、9-3・9-4・9-5を不要化。`target_role`は4値単一値で確定。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-16（オーナー指示反映）        | §14「メディアライブラリ」を新設（Phase2からPhase1へ前倒し）。14-1〜14-3を追加。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-20（v13 v1.15.0 反映）  | 2026-08-13 レビュー未反映分の一括反映（§9 #33〜#45）。§14 メディアを2段に分割（AI部分はPhase 2へ移送）。§14b を新設し 0-1・3-7〜3-9・5-5・5-6・6-4・6-5・8-5 の9パッケージを追加。17-1 を保留として起票。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **2026-08-23**              | **§9 #46〜#49 を反映**：Googleフォーム廃止（3-5 廃止 → 3-5b 公開予約ページ `/reserve` 新設・規模L）、3-5c カフェ事前予約を新設、3-8 の残枠算出元を `check_ins` へ変更、6-4 に送迎を追加、3-6 のブロック解除。※本行は 2026-09-03 の改訂時に補記（当時、履歴行の追加が漏れていた）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **2026-09-03（全面改訂）**        | **要件定義書・課題管理表・作業履歴・成果物実物との全面突合を実施し、以下を改訂。**<br>**① 構造**: §0「プロジェクト運営基盤」を新設（WBS未起票のまま完了していた作業11件を集約）／旧§14bを解体し各機能領域へ統合／**`5-5` の採番衝突を解消（Eumo給付を `5-7` へ改番）**／**`13-2` と `17-1` の重複を `17-1` に一本化**／§17・§18・§19 を新設。<br>**② 進捗率**: 全パッケージに「設計％／実装％／総合％」の3列を追加（定義は §S-4）。**全体＝設計77% / 実装1% / 総合31%**。<br>**③ v13 §9 #50〜#60 の未反映分（11件）を反映**: 新規7パッケージ（**2-5** ロール表示名・**2-6** ナビ到達性・**3-10** 滞在中の変更・**8-6** 宿泊履歴表示・**12-4** 初回CB自動起票・**14-6** `ai_tags[]`・**17-2** ナレッジ投入）を起票。既存9件（0-1・3-1・3-5b・3-9・5-7・6-5・12-2・12-3・14系）の記述を最新決定へ修正。<br>**④ 鮮度ズレの是正**: `1-3` テスト基盤（8/22 ブロック解除済・CI実装済 40%）／`10-1` 一括インポート（8/16 ブロック解除済／本文と§16の自己矛盾を解消）／`8-3` 差額繰越（ブロック解除済）／**§9 の「API連携を一切実装しない」が v1.19.0 で撤回され「Phase 2 以降で実施」へ変更されている点を明記**／`17-1` の着手条件が存在しない `prototype_v14.html` を指している点を警告。<br>**⑤ §16 逆引き表を全面更新**: 2026-08-20 起票分で更新が止まり **2026-08-23 以降の14件が未掲載**だった。**真のブロッカー3件（非同期ジョブ基盤・QRトークン保護・レスポンスマスキング）／半ブロッカー2件／非ブロッカー12件**に再分類。<br>**⑥ §18-A**: モック（4,096行・12画面・モーダル15本）と画面設計.md（33画面）の突合を実施。**モック実装済み28/33（85%）、設計のみ5件、モック先行で設計書未記載12件**。両者の採番体系が異なり対応表が無いため **`18-2` を新規起票**。<br>**⑦ §19 新設**: 8/31 のエスカレーション条件が発火済みかつ実施記録が無いことを明記し、Tier A/B/C のスコープ仕分け案を判断材料として提示（**決定ではない**）。<br>**⑧ §S-5**: 8/24〜9/01 の約2週間分（v13 v1.18.0〜v1.21.0 の +895行、モック、運用マニュアル、レビュー突合3本）が未コミットで滞留し、public リポジトリ上でリンク切れになっている点を警告。                                                                                                                                             |
| **2026-09-03（納期改訂）**        | **Phase 1 のリリース目標を ~~2026-09-10~~ → 「2026年9月中（〜09-30）」へ改訂**（オーナー確定）。**残日数 7日 → 27日**。8/31 のエスカレーション条件発火に対するオーナー判断として位置づけ、§0 冒頭に経緯を記録。**スコープは据え置き**（2026-08-16 選択肢A を継続）。<br>これに伴い §19 を全面改稿: **§19-2「納期27日で最初に潰すべきもの」を新設**（真のブロッカー3件 → 未コミット差分の処理 → 画面ID対応表 → Next.js雛形 の6ステップ順序）／Tier A を約20パッケージへ具体化（クエスト §5・ナビ 2-3/2-6 を追加）／**`3-5b` 公開予約ページ（L規模）を「7日では不可能だが27日なら射程」として Tier B 内で格上げ**／**§19-4「納期改訂で状況が変わった論点」を新設**。<br>⚠️ **クレジットカード決済（#52）の前提が変化**: Phase 2 送りの理由は「加盟店審査 2〜4週間が 9/10 に間に合わない」だったが、**9/30 までなら審査期間の下限は収まる**。§12-2・§15・§16-3 に再検討要の注記を追加（Phase 1 復帰の可否はオーナー判断）。<br>⚠️ **未反映**: 正本 v13 §4、`QUESTIONS.md`、`CONSOLIDATED_DECISIONS.md` §1 は依然 9/10 のまま。**本書のみの改訂では正本との不整合が残る**（CLAUDE.md §1.1）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **2026-09-08（情報統合改訂）**      | **09-05〜09-07 にブランチ・作業ツリーへ分散していた情報を統合し、突合のうえ改訂。**<br>**① 実装の現在地を書き換え**: 前版の「実装0行・`package.json` 不在・着手不能」は**解消済み**。Next.js 雛形（`1-2` 80%）・テスト基盤（`1-3` 70%・単体13＋E2E1）・Supabase 配線（`1-1` 30%）・`Money` コンポーネント（`0-1` 60%）・**Uii換算の完了（`7-1` 100%）**を反映。**全体＝設計78% / 実装6% / 総合35%**（前版 77/1/31）。着手済み5件・完了1件・残56件が0%。<br>**② 2026-09-05 オーナー決定10件を反映**（前版時点では1件も下流へ流れていなかった）: **A案＝PII別テーブル分離の採用により `2-2`・`2-3`・`2-4`・`8-1`・`8-4`・`10-3` の6件を一斉にブロック解除**／`1-1` を dev/staging/prod → **dev/prod ＋無料枠前提**へ／`4-1`・`4-2` を**音声録音から文字起こしテキスト投入へスコープ変更**（`1-4` 依存も解消）／`2-1` にメール必須・`auth.users` 結合・自己role変更禁止トリガーを追加／`12-1` にメール必須／`10-1`・`10-2` の実データ投入を**セキュリティ試験合格後へ後ろ倒し**／`1-7` の F-9 を**3前線構成で確定**（設計40→80%）。<br>**③ §16 逆引き表を全面更新**: 課題管理表の現況を **全53件（回答済み30／部分回答3／未回答20）** へ更新（前版は全45件）。**真のブロッカーは3件→4件**（レスポンスマスキングが解決した一方、**公開予約OTPの送信基盤**と**無料枠と非機能要件**が新規発生）。半ブロッカーは2件→4件。<br>**④ §19 を残22日基準へ改訂**: 論点が「着手できない」から「**間に合うのか**」へ移った点を明記。最優先を ~~レスポンスマスキング~~ → **公開予約OTPの送信基盤**へ差し替え、`1-1` の Supabase プロジェクト未作成が**現在の最大のボトルネック**であることを追記。中間チェックポイントが未設定である点を §19-5 #4 に明示。<br>**⑤ §S-5 を「未コミット差分の滞留」から「情報の分散状況」へ改題**: 前版の警告は解消済み。代わりに 09-05〜09-07 の**ブランチ分散2本と未コミット1件を本改訂でマージ・コミット**した。**残る不整合3件**（role-lock の WIP が会員データモデル・ER図へ未反映／自律ループの実装と設計が逆方向／ウェルカムメール案の untracked 放置）を明記。                                                                                                                                                                           |
| **2026-09-08（オーナー決定3件の反映）** | **同日のオーナー決定を反映。**<br>**① Supabase を有料プランへ**（2026-09-05 の「全面無料枠」を一部撤回。**Vercel Hobby は据え置き**）。無料枠の受容リスク3点（日次バックアップ不可＝非機能 A4／1週間非アクティブで自動停止／DB 500MB・プロジェクト数上限2）が**いずれも解消**し、**真のブロッカーが 4件 → 3件**になった（`1-1`・§16-1・§19-2 を更新）。<br>**② 自律開発ループは「移行」として扱わない**。エージェント定義11本と `auto-01`〜`auto-03` は揃っており、**タスクを流すだけ**。⚠️ ただし「流すだけ」を実際に阻んでいた不具合2件を同日修正した — **`auto-02`／`auto-03` の参照切れ7か所**（`automation/prompts/` を指したままで**実行すれば必ず失敗する状態**だった）と、**frontmatter 未付与7本**（設計 §7.2 の権限制限が効いていなかった）。`check_agents.py` は違反なし（11体 ok）。`0-D` を 70%/85% → **95%** へ。§S-5 の残2を解消。<br>**③ Phase 1 は 61パッケージ全部をリリースする**（部分リリースはしない）。§19-3 の Tier A/B/C は**着手順序**でありスコープの仕分けではないことが確定。§19-5 #1 が解消し**完了判定が可能になった**。⚠️ 残22日で56パッケージという比率は変わらない。<br>課題管理表の現況: **全55件（回答済み 31 ／ 部分回答 4 ／ 未回答 20）**。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **2026-09-13（権限ガードの上流反映）**  | **`DB物理設計.md` §6-6b（自己の `role` を誰も変更できない BEFORE UPDATE トリガー）を上流設計書2本へ反映**。09-08 版が §S-5 の残1・`2-1` の注記として抱えていた「WIP コミットが会員データモデル・ER図へ未反映」を**2/3 解消**した。<br>**① `会員データモデル_ユーザーテーブル定義.md` v1.3.0 → v1.4.0**: §5.2a の可否表を4列 → **8列**へ拡張し、**`member_type`／`account_status` の他者変更が ~~`admin` のみ~~ → `staff` である**という §6-6b① との食い違いを訂正。`app.operator_id` / `app.change_reason` の申告義務、初期 `admin` のブートストラップ、集計キャッシュ3列の専用トリガーを追記。§5.2 の列定義と §5.9 も追随。<br>**② `データモデル図_ER_Diagram.md` v1.2.0 → v1.3.0**: 1.2.0 は `MEMBER_ROLE_CHANGES` の**エンティティ図だけ**を入れており、**それを強制する仕組みの説明が無かった**。設計ポイント §1 に「**`service_role` は `BYPASSRLS` で RLS も列単位 GRANT も迂回する。必ず発火する関門はトリガーだけ**」の警告を新設し、エンティティ説明・リレーション表（1.2.0 で漏れていた2行）を補完。<br>**③ §16-1a**: 3件 → **4件**へ。あわせて §16-1 からコピーされたまま残っていた**残骸2行を削除**（表の体裁が壊れていた）。<br>**④ 反映作業中に検出した記載誤り1件**: §6-6b① が挙げる **`oyakata_member_no` は本リポジトリのどこにも列定義が無い**。鵜呑みにせず実在する列だけを反映し、会員データモデル §5.2a に**要確認**として残した（「親方は別系統の採番」という決定自体は有効で、現状は `legacy_member_no` の `OYA-` 接頭辞で実現。専用列の要否が未決）。<br>**⑤ 反映内容を §6-6b と突合し直し、6件の取り違えを是正**: `CHECK (member_id <> operator_id)` を防御の本体のように書いていた箇所（**本体は `BEFORE UPDATE` トリガー**であり CHECK は二重の表明）、`AUDIT_LOGS` との正本関係を**決着済みのように書いていた**箇所（源泉は**要確認**と明記している）、「§6.2 の唯一の例外」という**§6.2 と矛盾する**ブートストラップの記述、「アプリが INSERT しないのは RLS で全拒否だから」という**`service_role` の `BYPASSRLS` と矛盾する**説明、落としていた要確認（本人からの退会導線）、`ON DELETE RESTRICT`・`CHECK` 3本・索引の欠落。<br>⚠️ **残: 正本 v13 §2／§5.9.3 への反映**（自己変更の禁止とトリガーの存在に未言及）。 |
| **2026-09-20（実態への追随）**             | **09-13〜09-20 の実装・決定と突合し、本書の現在地を書き換えた。**<br>**① §S サマリーを全面更新**: 残日数 22日 → **10日**、実装進捗 6% → **約22%**（完了 12件／部分 2件／未着手 47件）、tracked 126件 → 244件・`.ts`/`.tsx` 13件 → 40件・マイグレーション 0件 → 13本。「入ったのは足場だけ」という 09-08 版の記述を撤回した。<br>**② `1-6`／`9-1` を復活**（2026-09-11 のベクトル基盤反転。本書は §19-2b にしか反映しておらず、§1・§9 の表が「不要化」のまま残っていた）。切り分けは**同日のオーナー確定で Phase 1** となり（PR #82）、実装対象は **61件 → 63件**になった。<br>**③ `1-5` の範囲を確定**（MCP Gateway → Phase 3、朝会音声のマルチモーダル入力を除外／2026-09-19 オーナー決定2件）。<br>**④ `2-4`／`3-2` に宿泊者名簿の収集経路を反映**（v13 §5.2.7 新設・2026-09-20 オーナー決定。収集UIの実装は `3-2` の範囲）。<br>**⑤ `5-1` に列単位GRANT 是正と `core_only_reward` 新設を追記**（2026-09-20 オーナー決定）。<br>**⑥ §16 を更新**: #61（ナレッジ投入方式）と `find_nearest` 移行を**論点消滅**へ、16-1a の v13 反映残り1件を**解消済み**へ。<br>**⑦ §19-2c を新設**: 残10日の着手順序（`3-2` → 注文 → 会計 → クエスト残り）と、実績ペース（12日で12件）から見た 47件との差を明記した。 |
| **2026-09-19（オーナー決定2件の反映）** | **同日のオーナー決定2件を反映。**<br>**① `media_assets` のスキーマ ＋ RLS を `2-1c` として新設**（`QUESTIONS.md` [2026-09-15] 起票分 ／ 決定 **B**）。`1-4` から `media_assets` の DDL ＋ RLS を外し、依存を ~~`1-1`~~ → **`1-1, 2-1c`** へ。`14-6`（`ai_tags[]` 列の先行用意）の依存も ~~`14-1`~~ → **`2-1c`** へ付け替えた（列を足す前提はテーブルの存在であり、アップロード経路の完成ではないため）。**オーナー補足：着手順の後退はリスクとして起票しない**（どの順番でも進むため）。これにより **Issue #18／#49（`1-4`）の停止理由のうち仕様側の1件が解消**した（残るのは Issue 本文の完了条件が雛形のままである点）。<br>**② `core_member` の「自拠点のみ」は放念**（決定 **A**）。現状、拠点を複数化する想定が無いため `members` への `place_id` 追加は行わず、`rooms`／`room_assignments` は現行の `is_staff()` テンプレート（`core_member` は全拠点）で確定させる。正本 v13 §6 の「コアメンバーは自拠点」の記述は将来分として残し、撤回しない。`3-1` とその依存先7パッケージへの影響は無い。 |
| **2026-09-21（開発環境の実機確認）** | **オーナー指示「開発環境で動作確認しながら進めたい」を受けて実機を起動し、dev Supabase・Vercel の設定状態と突合した（§S-0c を新設。同日付の §S-0b とは別トピックのため節番号を分けた）。**<br>**① 動作確認URLを確定**: ローカル `http://localhost:3000`（`/`・`/login`・`/api/health` が 200、`/quests`・`/admin/*` は `/login` へ 307＝認可ガードが正常）。**Vercel の Preview / Production URL は未発行**。<br>**② 設定が未了の4点を新規パッケージとして起票**: `1-1c`（dev Supabase に `0014`〜`0020` が未適用。7テーブル／1ビューが `PGRST205`）・`1-1d`（`auth.users` 0件でログイン不能）・`1-1e`（Auth メールテンプレートに `{{ .Token }}` が無いと6桁コードが届かない）・`1-2a`（Vercel プロジェクト未連携。`1-2` の残20%を切り出し）。<br>**③ 同日中に `1-1c`・`1-1d` を実行して完了**: `supabase db push --include-all` で dev のマイグレーションを **local/remote 22本一致**へ、`npm run seed:dev`（`scripts/dev/seed-dev.mjs`）で **4ロール分のテストアカウント ＋ 確認用クエスト2件**を投入。`npm run dev:login-code`（`scripts/dev/login-code.mjs`）により**メール未設定でもログインできる迂回路**を用意し、`verifyOtp` → RLS 越しの `members` 読み出し → `v_quest_board`（**ゲストには `description` が返らない**）まで実機で確認した。<br>**④ `1-2a` のリポジトリ側を投入**: `vercel.json`（framework=nextjs ／ `hnd1` ／ `github.silent`）と `.gitignore` への `.vercel/` 追加。<br>**⑤ 新たに判明した欠陥1件**: dev の Auth が **8桁 OTP を返す**一方、`LoginForm.tsx` は `maxLength={6}`・v13 §5.2.6 も6桁。**Auth 側を6桁へ揃える**ことを `1-1e` の完了条件に追加した。<br>**⑥ §19-2c に「順0」を追加**、オーナーのブラウザ作業の手順を **§S-0d** に集約。 |
| **2026-09-22（メディア基盤の実装反映）** | **WBS `2-1c`（`media_assets` スキーマ ＋ RLS）と `1-4`（署名付きURL基盤）の実装を反映した（PR #119）。**<br>**① 仕様側のブロッカー2件が決着**: `QUESTIONS.md`「[2026-09-20] `media_assets` の列構成が…食い違う」＝**C（正本 v13 §7 の全面採用）**、「[2026-09-19] アップロード署名付きURLのファイルサイズ上限の具体値」＝**A（画像 20MB ／ 動画 200MB）**。いずれも 2026-09-22 オーナー確定。これにより `2-1c` の設計 100% が実装へ進み、Issue #85・#18 の停止理由が解消した。<br>**② `2-1c` を 実装 0% → 100%**: `supabase/migrations/0027_media_assets.sql`（DDL ／ RLS ／ GRANT ／ 受入テスト40件）。`0017`・`0018` が「本表を作る作業パッケージが足すこと」と申し送っていた **FK 3本**（`work_logs` の Before/After 写真・`menu_items` の商品画像）も接続した。<br>**③ `1-4` を 実装 0% → 70%**: 署名付きURL発行（V4）・`/api/media/*` の Route Handler・Object Finalize の最小関数・GCP 構築スクリプトまで。**残りは GCP コンソール側の実行のみ**（`scripts/gcp/setup-media-infra.sh` をオーナーが実行する）。Cloud Functions の範囲は Issue #18 ⚠️1 **案A**（経路の疎通まで）に従い、実処理は `14-1` へ送った。<br>**④ 連番の付け替え**: `main` に `0021_accommodation_rates.sql` が入ったため `0021` → **`0027`** へ（CI の連番衝突検出が指摘）。<br>反映先: v13 §5.11.5・改訂履歴 v1.29.1、`DB物理設計.md` §3-7、`API設計.md` §2-10、`CONSOLIDATED_DECISIONS.md` §9、`QUESTIONS.md`。 |
| **2026-09-22（買い物リストの起票）** | **オーナー指示「買い物リスト追加、クエストにする。ダッシュボードからほしいものリストみたいに登録」を反映。**<br>**① 正本 v13 §5.12 を新設**（v1.30.0）し、それを **`5-8`（登録・一覧・ステータス管理）**・**`5-9`（買い出しクエスト化 ＋ 立替実費の記録）** の2パッケージへ分解した。いずれも **S規模**・**⏸ 保留**（Phase の置き場所が **v13 §9 #65** のオーナー判断待ち）。<br>**② 61パッケージの分母は動かしていない** — Phase 1 と確定した時点で 63 へ改める。先に分母だけ動かすと全体進捗が実態より低く見えるため。<br>**③ §15 に Phase 2 拡張分を追記**（立替実費のアプリ内精算・予算管理・定期購入・在庫連動・外部EC振り分け）。 |
| **2026-09-22（#65 決着・着手可能へ）** | **同日のオーナー確定3件を反映。**<br>**① `5-8`・`5-9` を ⏸保留 → 🟢着手可能**（最小構成を Phase 1 で実装する）。**分母は 61 → 63**。<br>**② 登録できるのは管理者・コアメンバー・街人の3ロール**（ゲストは閲覧のみ・サーバサイドでも拒否）。<br>**③ 立替実費はアプリで扱わない** — 精算経路も記録項目も作らないことが決まり、`5-9` から金額の器の設計が丸ごと落ちた。§15 の Phase 2 行から「立替実費のアプリ内精算」を削除。 |
| **2026-09-22（買い物リストの実装）** | **`5-8`・`5-9` を実装し 🟨 実装済みへ更新。**<br>**① `0030_shopping_list_items.sql`**（main 合流時に `0028`→`0030` へ連番を付け替え） — DDL ＋ RLS ＋ ガードトリガー ＋ 相乗り RPC。`quests.origin_type` に `shopping_list` を追加（CHECK の張り直し）。<br>**② アプリ層** — `src/lib/shopping/{status,duplicate,quest-draft,fetch-items}.ts` と `/shopping`（画面設計 A13・C14 を1画面に統合）。<br>**③ 検証** — 単体28件が緑。lint・typecheck も緑。**DB 受入16件はローカルに psql が無く CI 実行分**。<br>**④ 残り** — ダッシュボード（A2／C1）からのクイックアクションは `13-1` 待ち。承認完了 → 一括『購入済』の自動化は `5-4` の承認画面と接続する。 |
| **2026-09-22（画面層の一括接続）** | **オーナー指示「WBS 上進められる箇所はすべて実装。高リスクの判断が必要な場合のみ判断をあおぐ」を受けたインタラクティブセッションの反映（§S-0a2 を新設）。**<br>**① 09-21 までに入っていた DB 層・ドメインロジックのうち、呼ぶ画面が無く動かなかったものを接続**：`4-2`・`4-3`（朝会の構造化とクエスト起案）／`14-2`・`14-1`（アップロード画面と CORS）／`8-1`・`8-5`・`7-2`・`7-3`（顧客管理・伝票編集・精算QR）／`5-5`・`5-7`（Eumo給付一覧と自動起票）／`8-4`・`10-3`（本人の受領報告・再訪アラート）／`2-5`・`1-3`（ロール表示名・未認証 E2E 17件）／`12-4`・`8-6`（初回キャッシュバック・宿泊履歴）／`3-2`・`3-3`（チェックイン／チェックアウト・キャンセル）。<br>**② マイグレーション2本**：`0031`（明細行の論理削除）・`0032`（本人の受領報告のガード）。<br>**③ オーナー判断待ち3件を `QUESTIONS.md` へ記録**（朝会本文のマスキング要否／ナレッジ候補の保存先／精算QRの読み取り先）。止めずに先へ進めた範囲と待っている範囲を各論点へ明記した。<br>**④ 進捗の実測を併記**（進捗セルを持つ75行の平均：設計 84.5% ／ 実装 71.7% ／ 総合 76.8%）。⚠️ 本書の「63件」基準の集計は 09-20 のままであり、**数え方の統一は次回の全面改訂で行う**。 |
