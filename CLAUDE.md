# 浮遊街アプリ 開発ルール（AIエージェント／人間 共通の正本）

Claude Code（Claudian経由を含む）がこのリポジトリで作業する際に自動で読み込まれる文脈であり、
同時に人間の開発者にとっての開発ルール正本でもある。**ルールを書く場所はこのファイル1箇所のみ。**

- 最終更新: 2026-09-20
- 対象リポジトリ: `Projects/fuyuugai-app`（Obsidianボルト内）
- リモート: https://github.com/KoshiNakano1017/Fuyu — **⚠️ public（全世界に公開）**

> ## ⚠️ このリポジトリは公開されています
>
> push した内容は**誰でも閲覧・クローンでき、検索エンジンにも載る**。
> 一度 push した情報は、後から削除・private 化しても取り消せない（既にクローン・
> キャッシュされた分は残る）。したがって §3 の機密情報ルールは
> **「コミット前に気づけなければ手遅れ」**という前提で運用すること。
>
> 特に次を絶対にコミットしない:
> - 会員の個人情報（氏名・住所・連絡先）— `Knowledge/浮遊街アプリ/会員データ/` は持ち込み禁止
> - 実データを含む移行スクリプト — `migration/03_seed_members.sql` は実名370件を含む
> - APIキー・トークン・`.env`
>
> 新しくクライアント情報を追加する際は、**公開してよい内容かを都度判断する**。
> 判断がつかない場合はコミットせず、オーナーに確認する。

---

## 0. プロジェクト概要

1,000人限定の通い型自給自足コミュニティ「浮遊街」の運営支援アプリ。
クエスト制・地域通貨「Uii」・暗黙知の形式知化（RAG）が三本柱。

| レイヤー | 技術 |
| --- | --- |
| フロントエンド | Next.js（Vercel） / Tailwind CSS |
| バックエンド・DB・認証 | Supabase（PostgreSQL / Auth / Realtime / RLS） |
| メディアストレージ | Cloud Storage for Firebase（署名付きURL方式・Firebase Auth不採用） |
| AI基盤 | Claude API / Agent SDK、Gemini API。**アプリは録音データを扱わず、朝会の入力は文字起こし済みテキストのみ**（v13 §9 #63 ／ 2026-09-05 決定） |
| RAG/ベクトル検索 | ~~`line-rag-bot`（Firestore）へ統合。本体は独自ベクトル基盤を持たない~~ → **2026-09-11 確定：Supabase pgvector へ一本化**。アプリと浮遊街コンシェルジュが同一のベクトル空間 `public.knowledge_chunks` を共有する（v13 §9 #31 限定改訂・v1.26.0 ／ `CONSOLIDATED_DECISIONS.md` §17） |
| 通知 | LINE Messaging API |

詳細は §1 の正本ドキュメントを参照。

---

## 1. ドキュメント運用ルール（最重要）

### 1.1 正本（Single Source of Truth）

**仕様の正本はこのリポジトリの `docs/spec/` 配下のみ。** ボルト内の他の場所（`Knowledge/浮遊街アプリ/` 等）に
仕様書を置かない。過去に二重管理が発生し、旧版が新版として参照される事故が起きた。

| 種別 | 置き場所 | 役割 |
| --- | --- | --- |
| **正本仕様書** | `docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` | 全ての判断の最終根拠。矛盾時はこれが勝つ |
| 索引 | `docs/spec/浮遊街アプリ 統合ナレッジベース (Master Index).md` | 章立てと子ノートへの入口 |
| 確定方針の要約 | `docs/spec/CONSOLIDATED_DECISIONS.md` | 「仕様の現在地」の索引。§2 の意思決定ログを兼ねる |
| 未決事項 | `QUESTIONS.md` | 人間への確認待ち。**未回答の論点を勝手に確定扱いしない** |
| 要件定義 | `docs/spec/requirements/` | |
| 基本設計 | `docs/spec/basic-design/{frontend,backend,infra,ai-rag}/` | |
| 詳細設計 | `docs/spec/detailed-design/` | |
| 背景・経緯 | `docs/spec/background/` | AI会話ログのコピー。**読むだけ・編集しない** |
| 旧版 | `docs/spec/OLD/` | §1.3 参照 |

### 1.2 新規ドキュメントを作らない原則

**既存ファイルへの追記・改訂を第一選択とする。** 新規ファイル作成は以下を全て満たす場合のみ:

1. 既存のどのファイルにも論理的に収まらない（まず `docs/spec/` 全体を検索して確認する）
2. 追記すると当該ファイルが読めない長さになる（目安: 追記後に2,000行超）
3. Master Index に登録先の節がある、または新設する節が明確

満たさない場合の代替:

- 一時的な調査メモ・作業ログ → `LOOP_LOG.md` に追記
- 未確定の論点 → `QUESTIONS.md` に追記
- 決まった方針 → `CONSOLIDATED_DECISIONS.md` の該当節を更新

**禁止**: `〜_new.md` `〜_final.md` `〜_改訂版.md` `〜のまとめ.md` `〜のコピー.md` のような
「同じ主題の別ファイル」を作ること。改訂は同一ファイルのバージョンを上げて行う。

`_v<数字>` の扱いには注意する。本プロジェクトでは **`_v13` のようなサフィックスは
「準拠する仕様の版」を示す正規の命名**であり（正本 `浮遊街アプリ 総合要件定義・設計書_v13.md`、
`HTMLモック_v13.md` がこの形）、一律禁止ではない。違反になるのは
**同じ主題のファイルが既に存在するのに別版を新規作成した場合**（例: `機能要件定義.md` があるのに
`機能要件定義_v2.md` を追加する）。pre-commit フックもこの条件で判定する。

新規作成した場合は必ず Master Index に登録し、親ノートから1リンク以内で到達できる状態にする。
どこからもリンクされない孤児ノートを残さない。

### 1.3 旧版は OLD へ

ファイルを版として更新する場合、**旧版を削除せず `OLD/` サブディレクトリへ移動する**。

```
docs/spec/OLD/2026-08-18_旧ファイル名_OLD.md   # OLDへ移した日付を先頭に付ける
```

- 移動時、ファイル冒頭に「このファイルは旧版。現行版は `<パス>`」という1行の注記を追加する
- Obsidianのウィキリンク衝突を避けるため、`OLD/` へ移す際はファイル名末尾に `_OLD` を付ける
  （リンク `[[機能要件定義]]` が現行版に一意に解決されるようにするため）
- `OLD/` 配下は参照専用。ここを根拠に実装判断をしてはならない

### 1.4 改訂履歴

正本（v13）を改訂したら、末尾の改訂履歴表に「版数 / 日付 / 変更内容 / 根拠プロンプトへのリンク」を1行追加する。
版数はセマンティックに扱う（`v1.14.0` = 節の新設、`v1.14.1` = 誤記修正）。

### 1.5 ドキュメント標準（2026-08-22 制定）

新規参画メンバーが増えるため、**「どこに何があるか」を属人的な記憶に依存させない**ことを目的に標準化した。

#### 1.5.1 8層の分類（MECE）

すべてのドキュメントは、役割によって次の**9層のどれか1つ**に属する。重複させない。

| 層 | 役割 | 置き場所 |
| --- | --- | --- |
| **A. 入口** | 案内・索引 | `docs/SITEMAP.md`、`docs/オンボーディング資料_*.md`、`docs/spec/*(Master Index)*` |
| **B. 規範** | ルールと正本 | `CLAUDE.md`、`docs/spec/浮遊街アプリ 総合要件定義・設計書_v13.md` |
| **C. 要件** | What（何を作る） | `docs/spec/requirements/` |
| **D. 設計** | How（どう作る） | `docs/spec/basic-design/`、`docs/spec/detailed-design/`、**`docs/design/`（HTMLモックの実体／2026-08-24 新設）** |
| **E. 計画・進捗** | When / Who | `docs/spec/WBS_Phase1.md`、`TASKS.md`、`LOOP_LOG.md` |
| **F. 決定・未決** | Why | `docs/spec/CONSOLIDATED_DECISIONS.md`、`QUESTIONS.md` |
| **G. 一次資料** | 出典（**読むだけ**） | `docs/spec/background/` |
| **H. アーカイブ** | 廃棄済み（**参照専用**） | `docs/spec/OLD/` |
| **I. 運用** | 現場の手順書（**Phase 1 の納品物**） | `docs/operations/`（2026-08-23 新設 ／ v13 §9 #44） |

**新しいドキュメントの置き場所に迷ったら、まずこの表のどの層かを決める。**
どの層にも収まらないなら、それは新規作成すべきでない可能性が高い（§1.2 を再読すること）。

#### 1.5.2 frontmatter の標準スキーマ

`docs/` 配下の Markdown は、次の frontmatter を持つ（`background/` を除く）。

```yaml
---
title: ドキュメントのタイトル
description: 1行の要約（任意）
doc_type: 索引 | 入口 | 規範 | 要件 | 設計 | 計画 | 決定 | 一次資料 | アーカイブ
status: 運用中 | 正本（Current） | ドラフト | 廃止
owner: プロジェクトオーナー
date: 2026-08-22        # 作成日
updated: 2026-08-22     # 最終更新日
supersedes: OLD/...     # 旧版を置き換えた場合のみ
tags: ["浮遊街アプリ", ...]
---
```

- **`status: 廃止`** を付けたドキュメントは、実装判断の根拠にしてはならない
- `updated` は**内容を変えたら必ず更新する**

#### 1.5.3 リンクの書き分け

**リポジトリは GitHub でも Obsidian でも Google Drive でも読まれる。** 用途でリンク形式を使い分ける。

| 用途 | 形式 | 理由 |
| --- | --- | --- |
| **ボルト内のドキュメント間** | `[[ウィキリンク]]` | Obsidian での回遊性。GitHub では素のテキストになるが許容 |
| **`docs/SITEMAP.md` の中** | **GitHub の絶対URL** | Google Drive へ配布するため。**wikilink・Mermaid を使わない** |
| **`line-rag-bot/docs/` の中** | 相対パスの Markdown リンク | 既存の規約を維持（GitHub 上で完結して読める） |

#### 1.5.4 サイトマップの保守

**ドキュメントを追加・移動・廃止したら、`docs/SITEMAP.md` を必ず更新する。**

- Google Drive へ配布するのは **`SITEMAP.md` 1枚だけ**。実体は GitHub 側を正とする
- 実ドキュメントを Drive へミラーしない（版がズレて「どちらが正か分からない」状態を生むため）
- `docs/spec/OLD/` 配下はサイトマップに載せない

---

## 2. 意思決定と根拠プロンプトの記録

**重要な意思決定は、その根拠になったAIとの会話へのリンクを必ず残す。**
「なぜそう決めたか」を後から追跡できない決定は、決定として扱わない。

### 2.1 記録先

`docs/spec/CONSOLIDATED_DECISIONS.md` の該当節。新しいファイルは作らない。

### 2.2 記録フォーマット

```markdown
- **決定**: RAGのベクトル検索基盤を line-rag-bot（Firestore）へ統合する
  - 決定日: 2026-08-16 / 決定者: プロジェクトオーナー
  - 理由: Supabase無料枠のDB容量500MB制約下でpgvectorを併存させる利点が薄いため
  - 根拠プロンプト: [[2026-07-09_インフラ分離とデータ定義共通化]] / [[2026-08-03_浮遊街アプリ要件定義書v9作成]]
  - 反映先: v13 §9 #31、basic-design/infra/システムアーキテクチャ.md
```

### 2.3 根拠プロンプトのリンク先

会話ログの原本はボルト側 `AI-Conversations/06_浮遊街アプリ・FELプロジェクト/` にある。
リポジトリ内では同内容が `docs/spec/background/` にコピーされている。

- Obsidianで読むことを前提に、**ウィキリンク `[[2026-08-03_浮遊街アプリ要件定義書v9作成]]` の形式で書く**
- 会話ログが未取り込みの場合は、先に `AI-Conversations/` へ保存してからリンクする
- URLしか無い場合（Claude.ai の共有リンク等）は URL とタイトルの両方を書く。リンク切れに備えて要旨も1〜2行残す

### 2.4 決定を覆すとき

既存の決定を変更する場合、元の行を消さずに取り消し線（`~~旧方針~~ → **新方針（YYYY-MM-DD確定）**`）で上書きし、
撤回の根拠プロンプトを併記する。「いつ・なぜ方針が変わったか」が消えることを防ぐ。

---

## 3. 機密情報の取り扱い

### 3.1 絶対にコミットしないもの

- APIキー・トークン・シークレット（Supabase service_role key、Claude API key、LINE channel secret、GCPサービスアカウントJSON 等）
- `.env` および `.env.*`（`.env.example` を除く）
- **会員の個人情報**（氏名・カナ・住所・電話番号・メールアドレス・前泊地）
  - 浮遊街は宿泊法対応で実名・住所を保持するため、会員リストは特に厳格に扱う
  - 🚫 **`Knowledge/浮遊街アプリ/会員データ/` 配下はリポジトリへ持ち込み禁止**（2026-08-22 オーナー確定）
    - 対象は**ボルト側の実データ**（街びと327件・親方衆44件＝統合後370名）。実名・住所・電話番号を含む
    - **コピー・移動・参照のいずれも禁止**。「一時的に置いて後で消す」も不可（`git rm --cached` では履歴に残り、
      public リポジトリでは**一度 push した時点で取り消し不能**）
    - `migration/03_seed_members.sql` のような**実データ由来のシードも同様に禁止**。
      投入は開発者のローカル環境で、リポジトリ外のファイルから直接行う
    - スキーマ・件数・統計値（「370名」「105名が名寄せ不能」等）は個人が特定されないため**記載可**
- 本番DBのダンプ・実データを含むCSV/SQL
- クライアント固有の非公開情報（契約金額・個人名を含む議事録の生ログ）

### 3.2 実装ルール

- 設定値はコードに直書きせず、環境変数経由で読む。必要な変数は `.env.example` にキー名と説明だけを列挙する
- CI/CD の秘匿値は GitHub Actions の Repository Secrets に置く。ワークフローYAMLに値を書かない
- ログ出力に個人情報・トークンを含めない。デバッグ時もマスクする（`user_id` は可、`email` は不可）
- テストデータは実在の会員データを使わず、フィクスチャを自作する
- Supabase の `service_role` キーはサーバサイド（Edge Function / Server Actions）でのみ使用し、クライアントへ渡さない

### 3.3 自動検査

コミット時とCIの両方で秘密情報スキャン（gitleaks）が走る。検出されたコミットは通らない。
**誤ってコミットしてしまった場合、`git rm --cached` だけでは履歴に残るため、必ず該当キーをローテーション（無効化・再発行）する。**

---

## 4. コーディング規約（リーダブルコード準拠）

『リーダブルコード』の原則を採用する。**コードは書く時間より読まれる時間のほうが長い**という前提で書く。

### 4.1 命名

| 原則 | 悪い例 | 良い例 |
| --- | --- | --- |
| 明確な単語を選ぶ | `getUser()` | `fetchUserFromSupabase()` / `findUserById()` |
| 汎用的すぎる名前を避ける | `tmp`, `data`, `retval`, `info` | `pendingQuestApplications` |
| 単位・書式を名前に含める | `expiry`, `size` | `expiryDurationSec`, `fileSizeMb`, `signedUrlTtlMin` |
| 危険・重要な属性を付ける | `password` | `plaintextPassword` / `hashedPassword` |
| スコープの広さと名前の長さを比例させる | グローバルに `i` | ループ内のみ `i` は可、モジュール公開は説明的に |
| 真偽値は is/has/can/should | `verified`, `guest` | `isVerified`, `hasCertification`, `canAcceptQuest` |

ドメイン用語はブレさせず、正本の日本語用語に対応する英語を固定する。

| 日本語（正本） | コード上の英語 |
| --- | --- |
| クエスト | `quest` |
| 受注申請 | `questApplication` |
| 完了報告 | `completionReport` |
| 親方 / 街人 / ゲスト | `member_type`: `oyakata` / `machibito` / `guest` |
| 会員種別（立場） | `member_type` ※**認可には使わない** |
| 権限ロール | `role`: `admin` / `core_member` / `member` / `guest` / `custom` |
| 地域通貨 Uii | `uii` |
| 宿泊券 | `stayTicket` |

同じ概念に2つの名前を与えない。新しいドメイン語を導入する時はこの表に追記する。

### 4.2 コードの構造

- **1関数1責務**。「〜と〜をする」と説明したくなったら分割する
- 関数の引数は3個までを目安。それ以上はオブジェクトにまとめて名前付き引数にする
- ネストを浅く保つ。ガード節で早期リターンし、`else` を減らす
- 説明変数・要約変数を使い、複雑な式に名前を付ける

  ```ts
  // 悪い
  if (user.role === 'admin' || (user.role === 'core_member' && quest.guest_allowed)) { ... }
  // 良い
  const canReviewQuest = user.role === 'admin' || (user.role === 'core_member' && quest.guest_allowed);
  if (canReviewQuest) { ... }
  ```

- 巨大なファイルを作らない。目安として1ファイル300行、1関数50行を超えたら分割を検討する
- 「無関係の下位問題」（日付整形・ID生成等）は汎用ユーティリティへ切り出す

### 4.3 コメント

- **コードから読み取れることをコメントに書かない**（`// ユーザーを取得する` のような同語反復は削除）
- 書くべきは **「なぜそうしたか」**。特に「一見不自然だが意図がある」箇所には必ず理由を書く

  ```ts
  // 署名付きURLのTTLを2分に絞る。内部専用ナレッジはURL流出時の被害が大きいため（v13 §5.11.2）
  const INTERNAL_KNOWLEDGE_URL_TTL_MIN = 2;
  ```

- 仕様に由来する定数・分岐には、正本の節番号（`v13 §5.11.2` 等）を根拠として書く
- `TODO:` は放置せず `QUESTIONS.md` か `TASKS.md` へ転記する

### 4.4 テスト

- テスト名は「何をテストしているか」が読める文にする: `ゲストはguest_allowed=falseのクエストを受注できない`
- 1テスト1アサーションを目安に、失敗時に原因が一意に分かるようにする
- 認可・金額計算・個人情報の取り扱いに関わるロジックは必ずテストを書く

### 4.5 言語別

- **TypeScript**: `strict: true`。`any` 禁止（やむを得ない場合は `// eslint-disable-next-line` と理由を併記）。フォーマットは Prettier、静的解析は ESLint に委ねる
- **SQL**: マイグレーションは `supabase/migrations/` に連番で追加。既存マイグレーションを書き換えない
- **Python**（スクリプト類）: PEP 8、フォーマットは ruff

---

## 5. Git / ブランチ / コミット

### 5.1 ブランチ

- `main` が常にデプロイ可能な状態を保つ
- 作業は短命の `feature/*` `fix/*` `docs/*` ブランチを切り、PR経由で `main` へマージする
- `main` への直接pushはしない。クライアント別・環境別のブランチは作らない

### 5.2 コミットメッセージ

Conventional Commits に従う。**日本語の本文で「なぜ」を書く。**

```
<type>: <変更内容の要約>

<なぜこの変更が必要か / 背景>
根拠: [[会話ログ名]] または v13 §番号
```

type: `feat` / `fix` / `docs` / `test` / `refactor` / `chore` / `ci` / `perf`

- 1コミット1論点。無関係な変更を混ぜない（過去に「宿泊予約フォーム連携」と「ナレッジ登録UI拡張」が
  同一バージョンアップに束ねられ、レビュー不能になった事例がある）
- 仕様書の改訂とコード実装は別コミットに分ける

### 5.3 コミット前の必須確認

1. `git diff --staged` を全文読み、意図しない変更が混ざっていないか確認する
2. 秘密情報・個人情報が含まれていないか確認する
3. pre-commit フックがパスすること（§6.1）

---

## 6. CI/CD

**二段構え**: 手元は軽量・高速なチェックで止めず、重い検証はCIに任せる。

### 6.1 コミット時（pre-commit フック / ローカル）

`.githooks/pre-commit` が以下を実行する。数秒で終わるものだけを置く。

| 検査 | 内容 |
| --- | --- |
| 個人情報・機密ガード | 会員リスト・`.env`・鍵ファイル等の混入をパス名で検出してブロック |
| 秘密情報スキャン | gitleaks（未導入時は組み込みのパターン検査にフォールバック） |
| ドキュメント規約 | `_v2` `_new` `_final` 等の禁止ファイル名を検出（§1.2） |
| Lint / 型チェック | 変更のあった TS ファイルのみ ESLint + `tsc --noEmit` |
| 単体テスト | 変更範囲に関連するテストのみ |

セットアップ（クローン後に1回だけ実行）:

```bash
git config core.hooksPath .githooks
```

緊急時のみ `git commit --no-verify` で回避できるが、**回避した理由を次のコミットメッセージに書く**。

### 6.2 push / PR 時（GitHub Actions）

| ワークフロー | トリガ | 内容 |
| --- | --- | --- |
| `.github/workflows/ci.yml` | **全ブランチへの push** / PR | gitleaks（全履歴）、ドキュメント規約、ESLint、`tsc --noEmit`、単体テスト＋カバレッジ、ビルド、**E2E（Playwright）** |
| `.github/workflows/claude-review.yml` | PR / `@claude` コメント | Claude によるコードレビュー。本ファイルのルール（§3 機密・§4 リーダブルコード）への適合を観点に指摘する |
| **`line-rag-bot` の `.github/workflows/ci.yml`** | **全ブランチへの push** / PR | gitleaks（全履歴）、**Pytest（Firestore エミュレータ起動込み）** |

- CI が赤い PR はマージしない
- Claude レビューの指摘は「対応する」か「対応しない理由を返信する」かのどちらかを必ず行い、無視しない

> [!note] なぜ push トリガが「全ブランチ」なのか（2026-08-22 / R2 オーナー回答）
> オーナー回答は「**CI をコミットしたときに走らせる**」だった。
> しかし §5.1 で `main` への直接 push を禁止しているため、`branches: [main]` のままでは
> **feature ブランチのコミットは PR を作るまで一度も CI にかからない**。
> そこで push トリガを全ブランチへ広げ、「push した全コミットが検証される」状態にした。
>
> 一方、**ローカルの `git commit` 時点で E2E まで回すことはしない**。
> Playwright はブラウザ起動を伴い数分かかるため、コミットのたびに実行すると開発が止まる。
> §6.1／§6.2 の二段構え（手元は軽量・CI は重量）は維持する。

> [!warning] E2E は本番 Supabase を絶対に指さない
> E2E ジョブは `E2E_SUPABASE_URL` / `E2E_SUPABASE_ANON_KEY`（テスト用プロジェクト）のみを使う。
> 本番の値を Secrets に入れると、**実名370名の会員データに対してテストが走る**（§3.1 違反）。

### 6.3 デプロイ

- `main` へのマージで Vercel の Production デプロイが走る（Vercel の GitHub 連携）
- PR ごとに Vercel Preview が生成される。UI 変更のレビューは Preview URL で行う
- Supabase のマイグレーションは自動適用しない。手動で内容を確認してから適用する（データ破壊を防ぐため）

### 6.4 必要な Secrets（GitHub リポジトリ設定）

| 名前 | 用途 |
| --- | --- |
| `ANTHROPIC_API_KEY` | Claude コードレビュー |
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ビルド・テスト |

値そのものを Issue・PR・ドキュメント・会話ログに貼らない。

**public リポジトリ特有の注意:**

- **fork からの PR には Secrets が渡らない**（GitHub の仕様）。外部コントリビュータの PR では
  Claude レビューが `ANTHROPIC_API_KEY` を取得できず失敗する。同一リポジトリ内のブランチから
  PR を出す運用（§5.1）を守れば問題にならない
- **Actions のログも公開される**。ワークフローで `echo` した値は誰でも読めるため、
  デバッグ目的でも秘匿値を出力しない
- gitleaks は**コミット済みの履歴も検出対象**にする。過去分に検出があれば、
  該当キーを必ずローテーションする（§3.3）

---

## 7. AIエージェントへの追加指示

- 作業前に `CONSOLIDATED_DECISIONS.md` と `QUESTIONS.md` を読む。**未回答の論点を勝手に確定事項として扱わない**
- 仕様に矛盾・不明点を見つけたら、推測で実装せず `QUESTIONS.md` へ起票して `BLOCKED` にする
- `docs/spec/background/` は一次資料。読むだけで編集しない
- タスクが大きい場合は `TASKS.md` で分割する（**着手件数の上限は撤廃。2026-09-19 オーナー確定** — §7.0 と同じ理由。件数制限そのものがオーナー不在時の停止要因になっていた）
- ファイルを新規作成する前に §1.2 を再読し、既存ファイルへの追記で済まないか検討する

### 7.0 確認を求めず進める（2026-09-19 オーナー確定）

> [!important] オーナーを待つこと自体がコストである
> **「念のため確認」で止まらない。** 高リスクでない作業は、聞かずに実行して結果を報告する。
> 2026-09-16〜19 に、この原則を自律ループには実装しながら**エージェント自身が守らず**、
> 「push しますか？」で3日間ループを停止させた。同じことを繰り返さない。

| 区分 | 扱い |
| --- | --- |
| **聞かずに実行する** | ブランチへの commit ／ push ／ PR 作成 ／ ドキュメントの追随漏れ修正 ／ 調査・確認 ／ 取り消せる変更全般 |
| **実行してから報告する** | CI が緑な PR のマージ ／ リファクタ ／ テスト追加 |
| **必ず先に聞く** | §7.1 に触れる操作 ／ 認可・金額・個人情報の**仕様判断** ／ 本番データに影響する操作 ／ `docs/spec/` の**仕様変更**（追随漏れの修正は含まない） ／ 破壊的 git 操作（force push・reset --hard） |

判断基準は**取り消せるかどうか**である。ブランチ上の変更・PR・コメントはすべて取り消せる。
迷ったら「取り消すのに何分かかるか」を考え、**数分で戻せるなら聞かずにやる**。

**複数の論点で確認が要る場合も、作業全体を止めない。** 確認が要る部分だけを保留し、
依存しない部分は進めたうえで、保留分をまとめて1回で報告する
（自律ループの §3.4 仮決定・枯渇レポートと同じ考え方）。

### 7.0.1 派生文書を修正したら正本へ必ず反映する（2026-09-20 オーナー確定）

> [!important] 派生文書だけ直して正本を放置しない
> 詳細設計・基本設計・DBスキーマ等の**派生文書を修正したら、その内容を正本
> （v13 ／ `CONSOLIDATED_DECISIONS.md`）へ同じ作業の中で必ず反映する。** 「あとで正本に反映する」
> を TODO として残したまま終わらせない。反映漏れは §1.1 が警告する二重管理事故の再発そのものである。
>
> 2026-09-19〜20 に、正本と派生文書（`DB物理設計.md`・`API設計.md`）が食い違ったまま
> `QUESTIONS.md` に「派生文書側の訂正要否はオーナー判断待ち」という形で塩漬けになっていた事例が
> 見つかった。原因は「`docs/spec/` は自律ループから編集しない」という**自律ループ向けの制約**を、
> オーナーと対話するインタラクティブなセッション（Claudian 等）にも一律で適用していたことにある。

- **反映方向はどちらでもよい。** 派生文書側が誤っていれば正本に合わせて派生文書を直す
  （§7.0 表の「ドキュメントの追随漏れ修正」＝聞かずに実行する、に該当）。逆に**実装上の都合で
  正本の仕様そのものを書き換える判断になる場合**は、§7.0 の基準にそのまま従う：
  **高リスクでなければ聞かずに実行し、結果を報告する。高リスクの場合のみ、実行前にオーナーへ確認する。**
  高リスクの目安は §7.0 表と同じ（認可・金額・個人情報の**仕様判断**、本番データへの影響等）。
- 本条は**オーナーと対話するセッション（Claudian・インタラクティブな Claude Code 実行）に適用する。**
  無人で走る自律ループ（`auto-01`〜`auto-03`）が `docs/spec/` を編集しない制約（設計 §8.2 の
  仕様書ガード）は変更しない。オーナー不在を前提に安全側へ倒す自律ループの制約と、オーナーが対話の
  中で直接指示を出せるセッションの権限は、実行主体が異なる以上、同じ強さである必要がない。
- 反映が既存の意思決定の言い換え・整合性の是正で済む場合は §2 の記録フォーマットに従い
  `CONSOLIDATED_DECISIONS.md` へ追記する。反映が新たな仕様判断に相当する場合も同様に記録したうえで、
  正本本体（v13）の該当節を改訂し、§1.4 のとおり改訂履歴に1行追加する。

### 7.0.2 WBS で着手可能になった仕事は自動で起票し、承認待ちで他を止めない（2026-09-20 オーナー確定）

> [!important] 承認待ちはオーナーの待ち行列であり、エージェントの実装枠ではない
> **高リスク1件の承認待ちで、低・中リスクの自動進行が止まってはならない。**
> 2026-09-19〜20 に、スイープが「着手可能 **12件**」を正しく認識しながら
> **払い出し0件**を続けた。`auto:review`（ゲート3でオーナーを待つ状態）を
> 実装枠として数えており、承認待ち2件＋起動待ち1件で上限3枠が埋まっていたため。

| 段階 | 扱い | 実体 |
| --- | --- | --- |
| **起票** | WBS で 🟢着手可能 かつ依存が解けた作業パッケージを、**30分ごとに自動で Issue 化する** | `automation/scripts/sweep.py` の `dispense()`（`auto-05-sweep.yml`） |
| **完了条件** | 起票支援エージェントが正本から導出して記入する（**オーナーの記入を待たない**） | `wbs-to-issue.yml` の `draft`（既定 true） |
| **リスク判定** | `auto-01-plan` が §4.1 の区分で判定する | 判定はエージェント。機械的キーワードで二重判定しない |
| **高リスク** | **従来どおりゲート3（マージ前の承認1回）で止める** | `auto-03-review-merge.yml` |
| **中・低リスク** | 起票から実装・コミット・マージ・WBS更新まで**確認なしで進む** | `auto-01`〜`auto-03` |

- **承認待ちの Issue は払い出しの上限を消費しない**（`sweep.py` の `OWNER_WAIT`）。
  承認待ちには上限が無い（オーナーが見るまで何日でも続く）ため、実装枠に数えると
  ループ全体がオーナーの応答速度に律速される
- 作業中に**派生タスクが生まれたら `WBS_Phase1.md` へ追記する**。追記した行が
  着手可能になれば、次回のスイープが自動で拾う
- **「WBS に 🟢着手可能 と書いてあるのに Issue が無い」状態を放置しない。**
  起票されない場合は上限・依存・重複判定のどれで落ちているかをスイープのログで確認する

### 7.1 🚫 会員データの持ち込み禁止（2026-08-22 オーナー確定 ／ 違反時は即中止）

**`Knowledge/浮遊街アプリ/会員データ/` 配下は、AIエージェントの操作対象から完全に除外する。**
本リポジトリは **public** であり、実名370名分の個人情報を含むため、事故が起きた時点で回復不能になる。

エージェントが**やってはいけないこと**:

| 禁止操作 | 具体例 |
| --- | --- |
| **リポジトリ配下へのコピー・移動** | `cp`／`mv`／`Write` で `Projects/fuyuugai-app/` 配下へ複製する |
| **内容の読み込みと転記** | `Read`／`cat` した氏名・住所・電話番号を、仕様書・テストデータ・コミットメッセージ・**チャットの応答本文**へ書き出す |
| **実データからのフィクスチャ生成** | 実在の会員行を加工・匿名化してテストデータにする（**匿名化したつもりでも禁止**） |
| **`git add` / ステージング** | パスガードに引っかかった際に `--no-verify` で押し通す |

エージェントが**代わりにやること**:

- テストデータは**実データを一切参照せず**フィクスチャを自作する（§3.2）
- 会員データの**構造**を知る必要があるときは、実ファイルではなく
  `docs/spec/basic-design/backend/会員データモデル_ユーザーテーブル定義.md`（スキーマ定義）を読む
- 件数・分布などの**統計値**が必要なときは、既に仕様書に記載済みの数値（370名・105名等）を引用する
- ボルト側の実データを読む必要が生じたら、**自己判断で読まずオーナーへ確認する**

> [!danger] 判断に迷ったら
> 「これは会員データに当たるか？」で迷った時点で**触らない**。
> pre-commit のパスガード（`.githooks/pre-commit`）と CI の gitleaks は**日本語の氏名・住所を検出できない**。
> 最後の防波堤は自動検査ではなくエージェント自身の判断である。
